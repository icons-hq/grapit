{
  "name": "@grabit/edge-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir=dist --env=\"\"",
    "dev": "wrangler dev",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "types": "wrangler types --env=\"\" --include-runtime=false"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260823.1",
    "typescript": "~5.9.0",
    "vitest": "^3.2.0"
  }
}
