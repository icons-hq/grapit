---
phase: quick-260507-n8l
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/e2e/i18n-smoke.spec.ts
  - apps/web/e2e/toss-payment.spec.ts
  - apps/web/components/booking/terms-agreement.tsx
  - apps/web/app/booking/[performanceId]/confirm/page.tsx
  - apps/web/hooks/__tests__/booking-disabled-runtime.test.tsx
autonomous: true
requirements:
  - FLAG-01
  - FLAG-02
  - I18N-01
  - I18N-02
  - COMP-01
---

# Quick Task 260507-n8l: PR #33 CI `check` failure recovery

<objective>
GitHub Actions run `25482488465`의 `check` job 실패를 로컬에서 같은 순서로 재현하고, 실제 회귀만 수정한 뒤 CI와 동등한 검증으로 PR #33을 다시 green 상태로 만든다.
</objective>

<context>
@.github/workflows/ci.yml
@.planning/phases/23-launch-foundation/23-19-SUMMARY.md
@apps/web/messages/ko.json
@apps/web/e2e/i18n-smoke.spec.ts
@apps/web/e2e/toss-payment.spec.ts
@apps/web/app/booking/[performanceId]/confirm/page.tsx
@apps/web/components/booking/terms-agreement.tsx
</context>

<current_evidence>
- `gh pr checks 33 --watch=false` shows only `check` failing.
- `gh run view 25482488465 --job 74769802095 --log-failed` shows two Playwright failures:
  1. `apps/web/e2e/i18n-smoke.spec.ts:94` expects Korean home heading `HOT 공연`, but the shipped Phase 23 visible-copy contract is `apps/web/messages/ko.json -> home.hot = "HOT 이벤트"` and `23-19-SUMMARY.md` records the event-oriented copy rollout.
  2. `apps/web/e2e/toss-payment.spec.ts:206` times out waiting for visible button text `결제하기` after clicking `getByLabel('전체 동의')`; the current confirm CTA only reaches `결제하기` when `agreed === true`, so the break is either the agreement interaction itself or the E2E assumption about it.
</current_evidence>

<tasks>

<task type="auto">
  <name>Task 1: GitHub Actions failure evidence 수집 및 로컬 재현</name>
  <files>none</files>
  <action>
  `gh pr checks 33 --watch=false`와 `gh run view 25482488465 --job 74769802095 --log-failed`로 CI 실패를 다시 확인한 뒤, `.github/workflows/ci.yml`과 같은 순서로 로컬 환경을 올린다. Postgres test DB에 migration/seed를 적용하고, `pnpm --filter @grabit/api build` 후 `node apps/api/dist/main.js`로 API를 `:8080`에 띄운다. Web server는 `apps/web/playwright.config.ts`에 맡겨 `pnpm --filter @grabit/web exec playwright test`가 직접 올리게 한다. 전체 suite 전에 아래 두 개만 먼저 재현한다.

  - `pnpm --filter @grabit/web exec playwright test e2e/i18n-smoke.spec.ts --project=chromium --reporter=line`
  - `pnpm --filter @grabit/web exec playwright test e2e/toss-payment.spec.ts --project=chromium --grep "lock ownership: prepare 409" --reporter=line`

  Confirm flow failure가 즉시 설명되지 않으면 Playwright trace/error context를 열어 `전체 동의` click 이후 checkbox state, CTA accessible name, disabled state가 어떻게 변하는지 확인한 다음 수정에 들어간다.
  </action>
  <verify>
    <automated>`gh run view 25482488465 --job 74769802095 --log-failed` 출력과 동일한 두 실패가 로컬 focused Playwright 재현에서도 확인된다.</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: i18n smoke contract drift와 confirm agreement 회귀 수정</name>
  <files>apps/web/e2e/i18n-smoke.spec.ts, apps/web/e2e/toss-payment.spec.ts, apps/web/components/booking/terms-agreement.tsx, apps/web/app/booking/[performanceId]/confirm/page.tsx, apps/web/hooks/__tests__/booking-disabled-runtime.test.tsx</files>
  <behavior>
    - Korean home smoke는 shipped visible-copy contract를 검사해야 하며, `HOT 공연` 같은 stale literal에 묶이면 안 된다.
    - Confirm page는 required agreement를 유지한 채 `전체 동의` 이후 CTA가 실제 브라우저에서 `결제하기`로 전이되어야 한다.
    - Lock ownership 409 시 Toss confirm은 호출되지 않고, 서버 메시지와 recovery CTA가 그대로 보여야 한다.
  </behavior>
  <action>
  먼저 i18n smoke를 shipped contract에 맞춘다. `apps/web/e2e/i18n-smoke.spec.ts`의 Korean `homeCopy`는 `apps/web/messages/ko.json`과 `23-19-SUMMARY.md`를 source of truth로 삼아 정렬하고, Phase 23가 의도한 event-oriented copy를 test가 다시 `공연`으로 되돌리지 못하게 한다. 실제 UI contract가 잘못됐다는 증거가 나오지 않는 한 `messages/ko.json`을 과거 literal로 되돌리지 않는다.

  Confirm failure는 evidence-driven으로 고친다. Task 1 trace에서 `getByLabel('전체 동의')`가 실제 checkbox state를 바꾸지 못한다면 `apps/web/components/booking/terms-agreement.tsx`와/or `apps/web/app/booking/[performanceId]/confirm/page.tsx`를 수정해 real browser와 Playwright 모두에서 `agreed=true` 전이가 일어나도록 만든다. 반대로 UI state는 정상인데 selector/대기 조건만 stale이라면 `apps/web/e2e/toss-payment.spec.ts`를 고쳐 실제 accessible control을 클릭하고 CTA text가 `결제하기`가 될 때까지 기다린 뒤 진행한다. 어느 경우든 Phase 23 booking consent gate는 유지해야 한다: agreement 없이 결제 요청을 우회시키지 말고, lock-failure 409가 Toss confirm을 막는 현재 contract를 약화시키지 않는다.

  수정과 함께 focused regression coverage를 추가한다. 최소한 `apps/web/hooks/__tests__/booking-disabled-runtime.test.tsx` 또는 동등한 focused test에서 agreement -> CTA text transition과 409 recovery contract를 명시적으로 검증해 broad E2E만으로 이 동작을 보지 않게 한다.
  </action>
  <verify>
    <automated>`pnpm --filter @grabit/web test -- booking-disabled-runtime.test.tsx && pnpm --filter @grabit/web exec playwright test e2e/i18n-smoke.spec.ts --project=chromium --reporter=line && pnpm --filter @grabit/web exec playwright test e2e/toss-payment.spec.ts --project=chromium --grep "lock ownership: prepare 409" --reporter=line`</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: CI 동등 검증으로 PR 재승인 준비</name>
  <files>none</files>
  <action>
  Focused fix가 끝나면 CI와 같은 방향으로 다시 검증한다. 최소 `pnpm typecheck`, `pnpm test`, `pnpm --filter @grabit/web test:e2e`를 로컬 API/web 조합 위에서 재실행해 Playwright 전체가 green인지 확인한다. 실행 중 장기 프로세스는 readiness를 확인한 뒤 종료하고, 마지막에 어떤 명령이 pass했는지 quick summary에 남긴다. 로컬이 green이면 이후 push/PR 갱신 후 `gh pr checks 33 --watch=false`로 GitHub side 상태를 다시 확인한다.
  </action>
  <verify>
    <automated>`pnpm typecheck && pnpm test && pnpm --filter @grabit/web test:e2e`</automated>
  </verify>
</task>

</tasks>
