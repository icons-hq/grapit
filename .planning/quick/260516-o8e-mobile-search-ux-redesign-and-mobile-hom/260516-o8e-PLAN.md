---
phase: quick-260516-o8e-mobile-search-ux-redesign-and-mobile-hom
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/search/page.tsx
  - apps/web/app/search/__tests__/search-i18n.test.tsx
  - apps/web/hooks/use-performances.ts
  - apps/web/hooks/__tests__/use-performances.test.tsx
  - apps/web/components/home/banner-carousel.tsx
  - apps/web/components/home/__tests__/banner-carousel.test.tsx
  - apps/web/components/skeletons/banner-skeleton.tsx
  - apps/web/components/__tests__/skeleton-variants.test.tsx
  - apps/web/components/admin/banner-manager.tsx
  - apps/web/app/admin/banners/page.tsx
  - apps/web/components/admin/__tests__/banner-manager.test.tsx
autonomous: true
requirements:
  - QUICK-260516-O8E
must_haves:
  truths:
    - "Mobile users opening /search see a minimal real search input and submit affordance before any results exist."
    - "Submitting a non-empty search from /search navigates to /search?q=<encoded query> and resets stale pagination."
    - "Mobile home renders only banners targeted to mobile or all; desktop renders only banners targeted to desktop or all."
    - "The mobile home banner slot matches the 1290 x 600 asset ratio so mobile-specific banners are not cropped by the old fixed 188px slot."
    - "Admin users can create and edit desktop/mobile/all banners without losing placement, deviceTarget, status, schedule, or sort metadata."
  artifacts:
    - path: "apps/web/app/search/page.tsx"
      provides: "Minimal /search input UI and submit navigation"
    - path: "apps/web/hooks/use-performances.ts"
      provides: "Viewport-aware filtering of home banners using existing Banner.deviceTarget"
    - path: "apps/web/components/home/banner-carousel.tsx"
      provides: "Mobile banner ratio and carousel rendering for filtered banners"
    - path: "apps/web/components/admin/banner-manager.tsx"
      provides: "Device-aware admin upload guidance and form submission"
    - path: "apps/web/app/admin/banners/page.tsx"
      provides: "Edit flow that preserves expanded banner fields"
  key_links:
    - from: "apps/web/app/search/page.tsx"
      to: "/search?q=..."
      via: "router navigation from submit handler"
      pattern: "URLSearchParams.*q"
    - from: "apps/web/hooks/use-performances.ts"
      to: "Banner.deviceTarget"
      via: "client-side viewport target filter"
      pattern: "deviceTarget.*(mobile|desktop|all)"
    - from: "apps/web/app/admin/banners/page.tsx"
      to: "apps/web/components/admin/banner-manager.tsx"
      via: "BannerForm initialData and submit payload"
      pattern: "deviceTarget: banner.deviceTarget"
---

<objective>
Implement the quick mobile UX fix for search and home banners.

Purpose: close two production mobile dead ends without changing the banner schema or adding a new banner model.
Output: one focused implementation where `/search` is usable on mobile, home banners respect existing `deviceTarget`, and admin banner creation/editing supports separate mobile and desktop assets.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260516-o8e-mobile-search-ux-redesign-and-mobile-hom/260516-o8e-CONTEXT.md
@apps/web/app/search/page.tsx
@apps/web/components/home/banner-carousel.tsx
@apps/web/components/skeletons/banner-skeleton.tsx
@apps/web/components/admin/banner-manager.tsx
@apps/web/app/admin/banners/page.tsx
@apps/web/hooks/use-search.ts
@apps/web/hooks/use-performances.ts
@apps/web/hooks/use-admin.ts
@packages/shared/src/types/performance.types.ts
@packages/shared/src/schemas/performance.schema.ts

<locked_decisions>
- D-01: Search UX uses the minimal input style selected in 260516-o8e-CONTEXT. Do not replace this with a large hero, modal, or broad search redesign.
- D-02: Mobile banner support uses the existing `deviceTarget` model (`all`, `desktop`, `mobile`) with desktop/mobile/all filtering. Do not add a new banner table, column, or image-pair model unless existing code proves the current model cannot persist the required fields.
</locked_decisions>

<interfaces>
- `BannerDeviceTarget` already exists as `all | desktop | mobile` in `packages/shared/src/types/performance.types.ts`.
- `Banner` already includes `imageUrl`, `linkUrl`, `placement`, `deviceTarget`, `status`, `startsAt`, `endsAt`, `sortOrder`, and `isActive`.
- `createBannerSchema` already accepts `deviceTarget`, `placement`, schedule fields, `status`, `sortOrder`, and `isActive`.
- `AdminService.createBanner` and `AdminService.updateBanner` already persist `deviceTarget`; API/schema changes are not expected for this quick task.
- `useSearch()` is enabled only when `q.length > 0`, so the no-query `/search` state can render the input without triggering a search request.
</interfaces>
</context>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Mobile `/search` currently has no input and must become usable from the search tab itself. | Covered by Task 1 |
| GOAL | Mobile home banner is cropped by the fixed 188px slot and needs mobile asset support. | Covered by Task 2 and Task 3 |
| REQ | QUICK-260516-O8E quick task, no ROADMAP requirement update per user constraint. | Covered by this single plan |
| RESEARCH | No research phase per user constraint. | Excluded, not missing |
| CONTEXT D-01 | Minimal input style for search UX. | Covered by Task 1 |
| CONTEXT D-02 | Use existing `deviceTarget` model with desktop/mobile/all filtering. | Covered by Task 2 and Task 3 |

Deferred Ideas: none in 260516-o8e-CONTEXT.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add minimal mobile search input on /search</name>
  <files>apps/web/app/search/page.tsx, apps/web/app/search/__tests__/search-i18n.test.tsx</files>
  <behavior>
    - Test 1: with no `q`, `/search` renders a visible input using existing localized search copy and a submit button, not only the dead-end icon prompt.
    - Test 2: typing `girl rules` and submitting calls router navigation with `q=girl+rules` or equivalent URL-encoded query and removes any stale `page` param.
    - Test 3: with `q=girl`, the result heading, filters, ended toggle, and result grid still render as before.
  </behavior>
  <action>Update `SearchPage` per D-01. Keep the current Grabit tone and compact layout: add a minimal form near the top of the page using the existing `copy.nav.searchPlaceholder`, `copy.search.promptTitle`, or equivalent visible-copy strings. Use controlled input state seeded from the current `q`; on submit, trim whitespace, ignore empty input, set `q`, delete `page`, and navigate with `router.push` or `router.replace` to the current pathname plus encoded params. Keep genre and ended params only when they are already present. Preserve the existing results view, genre chips, ended toggle, pagination, and `useSearch()` contract. Do not introduce a new search library, modal, global command palette, or broad visual redesign.</action>
  <verify>
    <automated>pnpm --filter @grabit/web test -- app/search/__tests__/search-i18n.test.tsx</automated>
  </verify>
  <done>No-query `/search` has a usable input and submit affordance; non-empty submit navigates to an encoded `q` URL; existing query result behavior remains green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Filter public home banners by viewport device target</name>
  <files>apps/web/hooks/use-performances.ts, apps/web/hooks/__tests__/use-performances.test.tsx, apps/web/components/home/banner-carousel.tsx, apps/web/components/home/__tests__/banner-carousel.test.tsx, apps/web/components/skeletons/banner-skeleton.tsx, apps/web/components/__tests__/skeleton-variants.test.tsx</files>
  <behavior>
    - Test 1: when `matchMedia('(min-width: 768px)')` is false, `useHomeBanners()` returns only `deviceTarget: "mobile"` and `"all"` banners.
    - Test 2: when the viewport is desktop, `useHomeBanners()` returns only `deviceTarget: "desktop"` and `"all"` banners.
    - Test 3: `BannerCarousel` and `BannerSkeleton` use the new mobile 1290 x 600 ratio while keeping the existing desktop height.
  </behavior>
  <action>Implement D-02 on the public home path without changing the API/schema. Add a small viewport target helper inside `use-performances.ts` that resolves `mobile` below the `md` breakpoint and `desktop` at `md` and above, using `window.matchMedia('(min-width: 768px)')` with a change listener. `useHomeBanners()` should fetch the existing `/api/v1/home/banners` endpoint, include the resolved target in the query key, and return only banners whose `deviceTarget` is `all` or the resolved target. While the target is unresolved during hydration, keep the hook in a loading state rather than rendering unfiltered banners. Update `BannerCarousel` mobile dimensions from the fixed `h-[188px]` slot to the 1290 x 600 asset ratio, and update `BannerSkeleton` to match. Keep desktop behavior visually stable at `md:h-[400px]`. Do not add a new `mobileImageUrl`, new DB column, or backend route for this quick task.</action>
  <verify>
    <automated>pnpm --filter @grabit/web test -- hooks/__tests__/use-performances.test.tsx components/home/__tests__/banner-carousel.test.tsx components/__tests__/skeleton-variants.test.tsx</automated>
  </verify>
  <done>Mobile home data excludes desktop-only banners, desktop home data excludes mobile-only banners, `all` remains shared fallback, and the mobile carousel/skeleton no longer use the cropped 188px slot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preserve and clarify admin mobile banner upload fields</name>
  <files>apps/web/components/admin/banner-manager.tsx, apps/web/app/admin/banners/page.tsx, apps/web/components/admin/__tests__/banner-manager.test.tsx</files>
  <behavior>
    - Test 1: `BannerForm` initialized with a mobile or desktop banner shows the matching device target, placement, status, schedule, and sort values.
    - Test 2: selecting `모바일` updates upload guidance to mention the 1290 x 600 mobile banner recommendation.
    - Test 3: editing an existing banner submits the preserved `placement`, `deviceTarget`, `status`, `startsAt`, `endsAt`, `sortOrder`, and `isActive` values unless the admin changes them.
  </behavior>
  <action>Complete D-02 in the admin UI using the existing fields. In `app/admin/banners/page.tsx`, pass every expanded banner field into `BannerForm.initialData` when editing; the current partial edit data must not reset `placement`, `deviceTarget`, `status`, `startsAt`, or `endsAt` to defaults. In `BannerForm`, make the upload guidance and preview ratio device-aware: mobile should clearly recommend `1290 x 600px`, desktop/all can keep the existing desktop/16:9 guidance. Add concise device/placement/status metadata to the banner list card so operators can distinguish desktop and mobile banners. Preserve the current upload flow through `usePresignedUpload`, the 5MB limit, accepted jpg/png/webp types, and the existing `CreateBannerInput` payload shape.</action>
  <verify>
    <automated>pnpm --filter @grabit/web test -- components/admin/__tests__/banner-manager.test.tsx</automated>
  </verify>
  <done>Admin create and edit flows can register separate mobile and desktop banners through `deviceTarget`, and editing a mobile banner no longer silently converts it back to `all` or loses schedule/status metadata.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> `/search` route params | User-controlled query text enters URL state and the search API only after encoding. |
| Browser viewport -> banner filtering | Client viewport state controls which already-public banners render. |
| Admin browser -> banner mutation API | Admin-controlled banner metadata and upload URLs cross into protected API and R2/local upload paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260516-o8e-01 | T | `apps/web/app/search/page.tsx` | mitigate | Build search navigation with `URLSearchParams`; trim and encode `q`; never inject query text as HTML. |
| T-260516-o8e-02 | I | `apps/web/hooks/use-performances.ts` | accept | Banner metadata is already public from `/api/v1/home/banners`; filtering changes visibility by viewport but does not expose private data. |
| T-260516-o8e-03 | E | `apps/web/components/admin/banner-manager.tsx` | mitigate | Keep existing admin API/hooks and guards unchanged; do not add public mutation paths or bypass `banner.manage`. |
| T-260516-o8e-04 | D | `apps/web/components/admin/banner-manager.tsx` | mitigate | Preserve existing 5MB image size limit and accepted MIME types; do not expand upload limits in this quick task. |
</threat_model>

<verification>
Run the focused gates:

<automated>pnpm --filter @grabit/web test -- app/search/__tests__/search-i18n.test.tsx hooks/__tests__/use-performances.test.tsx components/home/__tests__/banner-carousel.test.tsx components/__tests__/skeleton-variants.test.tsx components/admin/__tests__/banner-manager.test.tsx</automated>
<automated>pnpm --filter @grabit/web typecheck</automated>
<automated>pnpm --filter @grabit/api test -- src/modules/admin/admin.service.spec.ts src/modules/performance/performance.service.spec.ts</automated>

Optional visual check after automated gates: run the web dev server, open `/search` and `/` at a mobile viewport around 390 x 844, confirm the search input is visible and a mobile-targeted banner is not cropped.
</verification>

<success_criteria>
- `/search` empty state is no longer a dead end on mobile.
- Home mobile carousel renders mobile/all banners only and uses the mobile banner ratio.
- Home desktop carousel renders desktop/all banners only.
- Admin banner create/edit flows preserve `deviceTarget` and clearly guide mobile banner uploads.
- No database schema, banner model, or backend route expansion is introduced.
</success_criteria>

<output>
After completion, create `.planning/quick/260516-o8e-mobile-search-ux-redesign-and-mobile-hom/260516-o8e-SUMMARY.md`.
</output>
