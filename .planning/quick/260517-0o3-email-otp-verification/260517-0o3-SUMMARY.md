---
status: completed
created_at: 2026-05-17
quick_id: 260517-0o3
slug: email-otp-verification
---

# Email OTP Verification Summary

## Completed
- Replaced new email verification issuance with 6-digit numeric codes.
- Stored email codes as HMAC hashes keyed by email and purpose, not raw codes.
- Updated email templates and localized copy to show the 6-digit code instead of a magic-link CTA.
- Updated the email verification page to accept a 6-digit code, verify it, handle invalid/expired/throttled states, and keep legacy token links as fallback.
- Added rate limiting to the email verification submit endpoint because 6-digit codes have a smaller brute-force search space than magic links.
- Kept login behavior: unverified users can log in and are routed to the email verification page.
- Redirected users to the locale-aware home page immediately after email verification succeeds.

## Verification
- `pnpm --filter @grabit/api test -- src/modules/auth/auth.service.spec.ts src/modules/auth/auth.controller.spec.ts src/modules/auth/email/email.service.spec.ts src/modules/auth/email/templates/email-verification.copy.spec.ts`
- `pnpm --filter @grabit/web test -- components/auth/__tests__/auth-email-verification.test.tsx`
- `pnpm --filter @grabit/shared test -- src/i18n/launch-copy-keys.test.ts`
- `pnpm --filter @grabit/api typecheck`
- `pnpm --filter @grabit/web typecheck`
- `pnpm --filter @grabit/shared typecheck`
- `git diff --check`
- Re-ran web email verification tests, web typecheck, and `git diff --check` after the home redirect change.
- Browser smoke on isolated local servers:
  - login with an unverified email user routed to `/auth/verify-email?email=...`
  - 6-digit input rendered with confirm disabled until complete
  - wrong code showed `인증번호가 일치하지 않습니다`
  - correct code showed `이메일 인증이 완료되었습니다.`
  - DB confirmed `is_email_verified=true` and verification token consumed
