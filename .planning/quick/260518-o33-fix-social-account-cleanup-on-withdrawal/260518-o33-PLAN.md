---
quick_id: 260518-o33
slug: fix-social-account-cleanup-on-withdrawal
status: planned
created: 2026-05-18T08:20:30.399Z
---

# Quick Task 260518-o33: Fix social account cleanup on withdrawal

## Goal

탈퇴된 계정의 Naver social account 연결이 남아 같은 Naver identity가 로그인도 재가입도 못 하는 production 장애를 해결한다.

## Tasks

1. Update withdrawal code paths
   - Files: `apps/api/src/modules/user/user.service.ts`, `apps/api/src/modules/admin/admin-user.service.ts`
   - Action: self/admin withdrawal transaction에서 `social_accounts` rows를 user id 기준으로 삭제한다.
   - Verify: withdrawal unit tests assert social link cleanup happens inside the transaction.
   - Done: future withdrawals no longer leave stale provider links.

2. Backfill existing production data
   - Files: `apps/api/src/database/migrations/0022_cleanup_withdrawn_social_accounts.sql`, migration journal
   - Action: 기존 `account_status='withdrawn'` users에 연결된 `social_accounts` rows를 삭제하는 idempotent migration을 추가한다.
   - Verify: migration SQL is safe to rerun and deploy workflow runs migrations before Cloud Run deploy.
   - Done: current affected Naver account can enter new social registration flow after deployment.

3. Validate and ship
   - Files: affected tests and deployment state
   - Action: targeted tests, push, CI/CD deploy, production DB/live OAuth smoke.
   - Verify: affected user no longer has stale `social_accounts` row and live Naver start endpoint remains healthy.
   - Done: production works for Naver re-signup after withdrawal.
