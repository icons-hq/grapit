---
status: complete
quick_id: 260518-l47
slug: db
verified_at: 2026-05-18
method: browser
---

# Quick Task 260518-l47 UAT

## Scope

회원 본인 탈퇴, 어드민 회원 탈퇴 처리, 어드민 DB 완전 삭제 차단 표시를 Chromium browser에서 검증했다.

## Browser Scenarios

### PASS - MyPage self withdrawal

- Opened `/mypage?tab=settings` in Playwright Chromium with mocked authenticated member session.
- Confirmed the `회원 탈퇴` button is disabled until `회원 탈퇴 확인` is checked.
- Submitted reason `서비스 이용 종료`.
- Verified browser request payload to `POST /api/v1/users/me/withdrawal`:
  - `reason: "서비스 이용 종료"`
  - `confirmed: true`
- Verified final browser URL remains `/auth?withdrawn=1` after auth state is cleared.

### PASS - Admin withdrawal

- Opened `/admin/users` in Playwright Chromium with mocked admin session.
- Confirmed `탈퇴 처리` is disabled until a reason and `회원 탈퇴 처리 확인` are provided.
- Submitted reason `운영 정책 위반으로 탈퇴 처리합니다.`
- Verified browser request payload to `POST /api/v1/admin/users/user-fan-1/withdrawal`:
  - `reason: "운영 정책 위반으로 탈퇴 처리합니다."`
  - `confirmed: true`
- Verified the user detail state becomes withdrawn so hard delete can be reviewed.

### PASS - Admin hard-delete blocker display

- Confirmed `DB에서 완전 삭제` is disabled until the user is withdrawn, a reason is entered, and `회원 DB 완전 삭제 확인` is checked.
- Submitted reason `탈퇴 계정 데이터 정리`.
- Verified browser request payload to `POST /api/v1/admin/users/user-fan-1/hard-delete`:
  - `reason: "탈퇴 계정 데이터 정리"`
  - `confirmed: true`
- Verified 409 blocker body is rendered as:
  - `삭제 차단: 예매 이력 2건, 관리자 감사 로그 1건`

## Issues Found And Fixed

- The first browser run showed self-withdrawal payload succeeded but final URL was `/auth`, because `AuthGuard` redirected after auth state clear and overwrote `/auth?withdrawn=1`.
- The browser hard-delete path exposed that `apiClient` did not preserve structured error response data, so blocker categories from real fetch responses could be lost.

Both issues were fixed before final verification.

## Verification Commands

- `pnpm --filter @grabit/web typecheck`
- `pnpm --filter @grabit/web test -- --run components/auth/__tests__/profile-form.test.tsx components/admin/__tests__/admin-user-management.test.tsx lib/__tests__/api-client.test.ts`
- `pnpm --filter @grabit/web test:e2e -- mypage-withdrawal.spec.ts admin-users.spec.ts`

## Result

PASS. Browser UAT for account withdrawal and admin lifecycle management is complete.
