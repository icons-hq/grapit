---
phase: 23-launch-foundation
plan: "19"
type: execute
wave: gap-closure-canary
depends_on:
  - "23-18"
  - "23-UAT"
files_modified:
  - packages/shared/src/i18n/launch-copy-keys.ts
  - packages/shared/src/i18n/launch-copy-keys.test.ts
  - packages/shared/src/schemas/performance.schema.ts
  - packages/shared/src/types/performance.types.ts
  - apps/web/messages/ko.json
  - apps/web/messages/en.json
  - apps/web/messages/th.json
  - apps/web/messages/zh-CN.json
  - apps/web/messages/zh-TW.json
  - apps/web/lib/i18n/visible-copy.ts
  - apps/web/lib/i18n/visible-copy.test.ts
  - apps/web/hooks/use-performances.ts
  - apps/web/hooks/use-search.ts
  - apps/web/components/i18n/locale-switcher.tsx
  - apps/web/components/layout/gnb.tsx
  - apps/web/components/layout/mobile-menu.tsx
  - apps/web/app/page.tsx
  - apps/web/components/home/hot-section.tsx
  - apps/web/components/home/new-section.tsx
  - apps/web/components/home/genre-grid.tsx
  - apps/web/app/search/page.tsx
  - apps/web/components/performance/performance-card.tsx
  - apps/web/components/performance/performance-grid.tsx
  - apps/web/components/performance/status-badge.tsx
  - apps/web/app/performance/[id]/page.tsx
  - apps/web/components/booking/booking-page.tsx
  - apps/web/components/auth/auth-launch-copy.ts
  - apps/web/components/auth/login-form.tsx
  - apps/web/components/auth/signup-form.tsx
  - apps/web/components/auth/signup-step1.tsx
  - apps/web/components/auth/signup-step2.tsx
  - apps/web/components/auth/signup-step3.tsx
  - apps/web/app/__tests__/home-i18n.test.tsx
  - apps/web/app/search/__tests__/search-i18n.test.tsx
  - apps/web/app/auth/__tests__/auth-page-i18n.test.tsx
  - apps/web/app/performance/[id]/__tests__/performance-detail-translation-label.test.tsx
  - apps/web/hooks/__tests__/booking-disabled-runtime.test.tsx
  - apps/web/e2e/i18n-smoke.spec.ts
  - apps/api/src/database/seed.mjs
  - apps/api/src/modules/performance/performance.controller.ts
  - apps/api/src/modules/performance/performance.service.ts
  - apps/api/src/modules/performance/performance.service.spec.ts
  - apps/api/src/modules/search/search.service.ts
  - apps/api/src/modules/search/search.service.spec.ts
  - .planning/phases/23-launch-foundation/23-UAT.md
autonomous: true
gap_closure: true
requirements:
  - FLAG-02
  - I18N-01
  - I18N-02
  - TRANS-01
  - TRANS-02
  - AUTH-01
must_haves:
  truths:
    - "The desktop GNB and mobile menu expose a visible locale switcher, mark the active locale, and preserve locale prefixes when navigating through shell links and search."
    - "Public/auth/home/search/performance/booking-disabled visible copy is sourced from five-locale message files instead of Korean-only literals in the target surfaces."
    - "Locale-prefixed routes for `/en`, `/th`, `/zh-CN`, and `/zh-TW` serve home, auth, search, performance detail, and booking-disabled pages without not-found or `/ko/...` rewrites."
    - "A stable seeded performance UUID exposes showtime, price tiers, seat map, and reviewed machine-translated content so Phase 26 canary smoke does not depend on invalid `test-performance` fixture data."
    - "Published machine-translated performance fields are returned to public web consumers with `automaticTranslationLabel=true`, while legal/manual content remains excluded from this overlay."
    - "A dedicated Playwright smoke covers Phase 23 i18n canary prerequisites without relying on admin login or live SMS state."
  artifacts:
    - path: "apps/web/e2e/i18n-smoke.spec.ts"
      provides: "Canary-entry smoke for locale routing, visible copy, translated performance detail, and booking-disabled UI"
    - path: "apps/api/src/database/seed.mjs"
      provides: "Stable Phase 23 i18n smoke fixture with UUID, showtime, price tiers, seat map, and published translations"
    - path: "apps/api/src/modules/performance/performance.service.ts"
      provides: "Locale-aware public performance content overlay from published translation drafts"
    - path: "apps/web/messages/en.json"
      provides: "Representative five-locale visible copy namespace; all other launch message files mirror the same keys"
    - path: ".planning/phases/23-launch-foundation/23-UAT.md"
      provides: "Updated evidence record that separates closed i18n gaps from any true Phase 26 canary blockers"
  key_links:
    - from: "apps/web/hooks/use-performances.ts"
      to: "apps/api/src/modules/performance/performance.controller.ts"
      via: "`locale` query param"
      pattern: "locale"
    - from: "apps/api/src/modules/performance/performance.service.ts"
      to: "apps/api/src/database/schema/translation-drafts.ts"
      via: "published translation overlay"
      pattern: "automaticTranslationLabel"
    - from: "apps/web/e2e/i18n-smoke.spec.ts"
      to: "apps/api/src/database/seed.mjs"
      via: "PHASE23_I18N_SMOKE_PERFORMANCE_ID"
      pattern: "00000000-0000-4000-8000-000000000023"
    - from: "apps/web/components/layout/gnb.tsx"
      to: "apps/web/components/i18n/locale-switcher.tsx"
      via: "visible GNB locale switcher"
      pattern: "LocaleSwitcher"
---

<objective>
Close the remaining Phase 23 i18n gap before Phase 26 canary.

Purpose: turn the existing locale foundation into a canary-ready user-visible experience by filling GNB locale switcher behavior, public/auth/home/search/performance/booking-disabled copy, locale-prefixed route smoke, translated performance content exposure, and E2E coverage.

Output: one focused remediation pass that leaves Phase 24-25 feature scope untouched and gives Phase 26 a stable, repeatable i18n smoke gate.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
@.planning/phases/23-launch-foundation/23-CONTEXT.md
@.planning/phases/23-launch-foundation/23-UAT.md
@.planning/phases/23-launch-foundation/23-VERIFICATION.md
</execution_context>

<context>
@.planning/phases/23-launch-foundation/23-CONTEXT.md
@.planning/phases/23-launch-foundation/23-RESEARCH.md
@.planning/phases/23-launch-foundation/23-PATTERNS.md
@.planning/phases/23-launch-foundation/23-UI-SPEC.md
@.planning/phases/23-launch-foundation/23-UAT.md
@packages/shared/src/constants/locales.ts
@packages/shared/src/i18n/launch-copy-keys.ts
@apps/web/messages/ko.json
@apps/web/messages/en.json
@apps/web/i18n/routing.ts
@apps/web/proxy.ts
@apps/web/components/i18n/locale-switcher.tsx
@apps/web/components/layout/gnb.tsx
@apps/web/components/layout/mobile-menu.tsx
@apps/web/app/page.tsx
@apps/web/app/search/page.tsx
@apps/web/app/performance/[id]/page.tsx
@apps/web/components/booking/booking-page.tsx
@apps/api/src/modules/performance/performance.service.ts
@apps/api/src/modules/performance/performance.controller.ts
@apps/api/src/database/seed.mjs

<gap_diagnosis>
Plan 23-18 closed the flat-route middleware blocker, but the Phase 26 canary entry surface is still incomplete:
- many public/auth/home/search/performance shell strings are Korean-only literals even when `useLocale()` resolves `en`, `th`, `zh-CN`, or `zh-TW`;
- navigation generated by GNB/mobile/search/card links drops the visible foreign locale prefix in several user paths;
- the public performance API does not overlay reviewed published translations into event data, so the web label component can only prove mocked metadata;
- UAT references `/performance/test-performance` and `/booking/test-performance`, but `performances.id` is UUID, so this invalid fixture cannot be canary evidence;
- current Playwright E2E suites are polluted by admin login DB state and live SMS rate limits, so Phase 23 needs a narrow i18n smoke independent of those flows.
</gap_diagnosis>

<canary_fixture_contract>
Use this stable local/dev fixture ID for Phase 23 i18n smoke:

```text
PHASE23_I18N_SMOKE_PERFORMANCE_ID=00000000-0000-4000-8000-000000000023
```

The fixture must be a real `performances.id` UUID, not the old string `test-performance`. It must include:
- one selling performance, title `2026 걸룰스 팬미팅`;
- at least one showtime;
- price tiers `SVIP석`, `VIP석`, `R석`;
- a seat map;
- published `translation_drafts` for `title`, `description`, and `salesInfo` in `en`, `th`, `zh-CN`, and `zh-TW`;
- no legal content translation rows.
</canary_fixture_contract>

<minimum_visible_copy_contract>
All five `apps/web/messages/{locale}.json` files must contain these namespaces and keys. Exact copy can be refined by execution, but the first execution pass must use these values or equivalent localized text with the same intent.

```json
{
  "nav": {
    "searchPlaceholder": "공연명, 아티스트를 검색하세요",
    "searchAriaLabel": "공연 검색",
    "clearSearch": "검색어 지우기",
    "moreGenres": "더보기",
    "loginSignup": "로그인 / 회원가입",
    "mypage": "마이페이지",
    "logout": "로그아웃",
    "language": "언어",
    "category": "카테고리"
  },
  "home": {
    "hot": "HOT 공연",
    "newOpen": "신규 오픈",
    "genreShortcuts": "장르별 바로가기",
    "more": "더보기",
    "empty": "공연을 검색하거나 장르별로 탐색해보세요."
  },
  "search": {
    "promptTitle": "공연을 검색하세요",
    "promptBody": "공연명, 아티스트를 검색하세요",
    "resultTitle": "'{query}' 검색 결과",
    "totalCount": "총 {count}건",
    "includeEnded": "판매종료 공연 포함",
    "loadError": "공연 정보를 불러오지 못했습니다. 잠시 후 다시 시도해주세요.",
    "retry": "다시 시도",
    "emptyHeading": "검색 결과가 없습니다",
    "emptyBody": "다른 키워드로 검색하거나 장르별 공연을 둘러보세요"
  },
  "performance": {
    "posterAltSuffix": "포스터",
    "detailTab": "상세정보",
    "salesTab": "판매정보",
    "noDetail": "상세 정보가 없습니다",
    "refundTitle": "취소/환불 안내",
    "refundItems": [
      "예매 후 취소 시 취소수수료가 발생할 수 있습니다.",
      "공연일 기준 취소 시점에 따라 수수료율이 달라집니다.",
      "자세한 내용은 예매 시 확인해주세요."
    ],
    "loadError": "공연 정보를 불러오지 못했습니다. 잠시 후 다시 시도해주세요.",
    "retry": "다시 시도",
    "bookCta": "예매하기"
  },
  "booking": {
    "disabled": "예매는 5월말 오픈 예정입니다"
  },
  "auth": {
    "tabs": {
      "login": "로그인",
      "signup": "회원가입"
    },
    "form": {
      "email": "이메일",
      "password": "비밀번호",
      "emailPlaceholder": "이메일을 입력해주세요",
      "passwordPlaceholder": "비밀번호를 입력해주세요",
      "loginButton": "로그인",
      "loginLoading": "로그인 중...",
      "forgotPassword": "비밀번호 찾기",
      "separator": "또는",
      "signupComplete": "회원가입이 완료되었습니다. 이메일 인증을 진행해주세요.",
      "under14Blocked": "만 14세 미만은 가입할 수 없습니다",
      "temporaryError": "일시적인 오류가 발생했습니다. 잠시 후 다시 시도해주세요."
    }
  }
}
```
</minimum_visible_copy_contract>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand the launch copy manifest for canary-visible surfaces</name>
  <files>packages/shared/src/i18n/launch-copy-keys.ts, packages/shared/src/i18n/launch-copy-keys.test.ts, apps/web/messages/ko.json, apps/web/messages/en.json, apps/web/messages/th.json, apps/web/messages/zh-CN.json, apps/web/messages/zh-TW.json, apps/web/lib/i18n/visible-copy.ts, apps/web/lib/i18n/visible-copy.test.ts</files>
  <read_first>packages/shared/src/i18n/launch-copy-keys.ts, packages/shared/src/i18n/launch-copy-keys.test.ts, apps/web/messages/ko.json, apps/web/messages/en.json, apps/web/messages/th.json, apps/web/messages/zh-CN.json, apps/web/messages/zh-TW.json, apps/web/components/auth/auth-launch-copy.ts, .planning/phases/23-launch-foundation/23-UI-SPEC.md</read_first>
  <action>
    Add a canary-visible copy contract for `nav`, `home`, `search`, `performance`, `booking.disabled`, and `auth.tabs/form`.

    Required implementation details:
    - Extend `LAUNCH_COPY_NAMESPACES` with `nav`, `home`, `search`, `performance`, `booking`, and `auth.form`.
    - Extend `LAUNCH_COPY_KEYS` so every launch locale requires the keys listed in `<minimum_visible_copy_contract>`.
    - Preserve the existing `auth.emailVerification`, `auth.otp`, `auth.errors`, and `sms.otp` namespaces.
    - Add `apps/web/lib/i18n/visible-copy.ts` that exports `getVisibleCopy(locale)` and validates locale through `isSupportedLocale`; unsupported input must fall back to `ko`.
    - Fill all five message files with the same key shape. Korean must keep the current Korean copy. English/Thai/Simplified Chinese/Traditional Chinese must provide real localized text rather than empty placeholders.
    - Keep `booking.disabled` exactly present in all five files.
  </action>
  <verify>
    <automated>pnpm --filter @grabit/shared test -- launch-copy-keys.test.ts</automated>
    <automated>pnpm --filter @grabit/web test -- visible-copy.test.ts</automated>
    <automated>node -e "const fs=require('fs'); const locales=['ko','en','th','zh-CN','zh-TW']; for (const l of locales) { const m=JSON.parse(fs.readFileSync('apps/web/messages/'+l+'.json','utf8')); for (const p of ['nav.searchPlaceholder','home.hot','search.promptTitle','performance.bookCta','booking.disabled','auth.tabs.login','auth.form.email']) { let v=m; for (const k of p.split('.')) v=v?.[k]; if (!v) throw new Error(l+' missing '+p); } }"</automated>
  </verify>
  <acceptance_criteria>
    - `LAUNCH_COPY_NAMESPACES` contains `nav`, `home`, `search`, `performance`, `booking`, and `auth.form`.
    - `apps/web/messages/{ko,en,th,zh-CN,zh-TW}.json` all contain `nav.searchPlaceholder`, `home.hot`, `search.promptTitle`, `performance.bookCta`, `booking.disabled`, `auth.tabs.login`, and `auth.form.email`.
    - `getVisibleCopy('xx')` returns Korean copy.
    - Existing auth/email/OTP launch copy tests still pass.
  </acceptance_criteria>
  <done>Visible-copy coverage is explicit, testable, and no longer limited to auth/OTP plus booking-disabled strings.</done>
</task>

<task type="auto">
  <name>Task 2: Wire locale-aware visible copy and prefix-preserving navigation into public/auth surfaces</name>
  <files>apps/web/hooks/use-performances.ts, apps/web/hooks/use-search.ts, apps/web/components/i18n/locale-switcher.tsx, apps/web/components/layout/gnb.tsx, apps/web/components/layout/mobile-menu.tsx, apps/web/app/page.tsx, apps/web/components/home/hot-section.tsx, apps/web/components/home/new-section.tsx, apps/web/components/home/genre-grid.tsx, apps/web/app/search/page.tsx, apps/web/components/performance/performance-card.tsx, apps/web/components/performance/performance-grid.tsx, apps/web/components/performance/status-badge.tsx, apps/web/app/performance/[id]/page.tsx, apps/web/components/booking/booking-page.tsx, apps/web/components/auth/auth-launch-copy.ts, apps/web/components/auth/login-form.tsx, apps/web/components/auth/signup-form.tsx, apps/web/components/auth/signup-step1.tsx, apps/web/components/auth/signup-step2.tsx, apps/web/components/auth/signup-step3.tsx, apps/web/app/__tests__/home-i18n.test.tsx, apps/web/app/search/__tests__/search-i18n.test.tsx, apps/web/app/auth/__tests__/auth-page-i18n.test.tsx, apps/web/hooks/__tests__/booking-disabled-runtime.test.tsx</files>
  <read_first>apps/web/lib/i18n/visible-copy.ts, apps/web/i18n/routing.ts, apps/web/components/i18n/locale-switcher.tsx, apps/web/components/layout/gnb.tsx, apps/web/components/layout/mobile-menu.tsx, apps/web/app/page.tsx, apps/web/app/search/page.tsx, apps/web/app/performance/[id]/page.tsx, apps/web/components/booking/booking-page.tsx, apps/web/components/auth/auth-launch-copy.ts, apps/web/components/auth/login-form.tsx, apps/web/components/auth/signup-form.tsx, apps/web/components/performance/performance-card.tsx, apps/web/components/performance/status-badge.tsx, apps/web/hooks/use-performances.ts, apps/web/hooks/use-search.ts</read_first>
  <action>
    Replace Korean-only UI literals on the canary-visible surfaces with locale-resolved copy and make generated app links preserve the active foreign prefix.

    Required behavior:
    - `GNB` and `MobileMenu` use `getVisibleCopy(useLocale())` for search placeholder, search aria label, clear-search aria label, login/signup, mypage, logout, language, category, and more-genres labels.
    - GNB search submit preserves locale: `/en` plus query becomes `/en/search?q=...`; Korean stays `/search?q=...`.
    - Genre, home more, performance card, auth, mypage, and booking links use a shared localized href helper or `getLocalizedPathname()` so foreign locale users stay under `/en`, `/th`, `/zh-CN`, or `/zh-TW`.
    - `HomePage`, `HotSection`, `NewSection`, and `GenreGrid` use localized home/nav copy.
    - `SearchPage` uses localized prompt, result count, ended toggle, retry, and empty-state copy.
    - `PerformanceDetailPage`, `PerformanceCard`, `PerformanceGrid`, and `StatusBadge` use localized static UI copy for tabs, retry, no-detail, refund fallback, booking CTA, poster alt suffix, empty states, and status aria label.
    - `BookingPage` keeps `bookingDisabledMessage` but all disabled toasts/statuses use the active locale and must not trigger lock/proceed handlers.
    - Auth page tabs, login form labels/placeholders/buttons/separator/forgot-password, signup completion toast, under-14 block message, and temporary error fallback come from locale-aware auth copy.
  </action>
  <verify>
    <automated>pnpm --filter @grabit/web test -- gnb-locale.test.tsx layout-shell-locale.test.tsx home-i18n.test.tsx search-i18n.test.tsx auth-page-i18n.test.tsx booking-disabled-runtime.test.tsx performance-detail-formatting.test.tsx performance-detail-translation-label.test.tsx</automated>
    <automated>pnpm --filter @grabit/web typecheck</automated>
    <automated>! rg -n "\"HOT 공연\"|\"신규 오픈\"|\"장르별 바로가기\"|\"공연을 검색하세요\"|\"공연 정보를 불러오지 못했습니다\"|\"예매하기\"|\"로그인 중\\.\\.\\.\"|\"회원가입이 완료되었습니다\"" apps/web/app apps/web/components apps/web/hooks -g '!**/__tests__/**' -g '!**/*.test.tsx' -g '!**/*.test.ts'</automated>
  </verify>
  <acceptance_criteria>
    - Target web surfaces import `getVisibleCopy` or an equivalent locale-aware copy helper.
    - Foreign-locale search, genre, performance, auth, and booking links preserve the active locale prefix.
    - GNB locale switcher is visible in desktop tests and active locale is marked with `aria-current`.
    - The forbidden Korean literal grep returns no matches outside tests/message fixtures for the listed target strings.
    - Booking-disabled UI tests prove disabled seat/proceed handlers do not fire and the visible disabled message changes by locale.
  </acceptance_criteria>
  <done>Canary-visible web copy and navigation behavior match the five-locale routing contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose reviewed translated performance content through public APIs and seed a stable smoke fixture</name>
  <files>packages/shared/src/schemas/performance.schema.ts, packages/shared/src/types/performance.types.ts, apps/api/src/database/seed.mjs, apps/api/src/modules/performance/performance.controller.ts, apps/api/src/modules/performance/performance.service.ts, apps/api/src/modules/performance/performance.service.spec.ts, apps/api/src/modules/search/search.service.ts, apps/api/src/modules/search/search.service.spec.ts, apps/web/hooks/use-performances.ts, apps/web/hooks/use-search.ts, apps/web/app/performance/[id]/__tests__/performance-detail-translation-label.test.tsx</files>
  <read_first>apps/api/src/database/seed.mjs, apps/api/src/modules/performance/performance.controller.ts, apps/api/src/modules/performance/performance.service.ts, apps/api/src/modules/search/search.service.ts, apps/api/src/database/schema/translation-sources.ts, apps/api/src/database/schema/translation-drafts.ts, apps/api/src/database/schema/performances.ts, packages/shared/src/schemas/performance.schema.ts, packages/shared/src/types/performance.types.ts, apps/web/hooks/use-performances.ts, apps/web/hooks/use-search.ts, apps/web/app/performance/[id]/page.tsx</read_first>
  <action>
    Implement a locale-aware public performance overlay using existing `translation_sources` and `translation_drafts` tables. Do not add a migration.

    Required API behavior:
    - Add optional `locale` to performance list/search query schemas, constrained to `ko`, `en`, `th`, `zh-CN`, `zh-TW`.
    - `PerformanceController.getPerformance()` validates `:id` as UUID or otherwise returns a controlled 400/404 response. Invalid string IDs such as `test-performance` must not produce HTTP 500.
    - `PerformanceService.findById(id, locale)` overlays `title`, `description`, and `salesInfo` from published translation drafts when `locale !== 'ko'` and matching rows exist for `entityType='performance'`, `entityId=id`, and `field in ('title','description','salesInfo')`.
    - `findByGenre`, `getHotPerformances`, `getNewPerformances`, and `SearchService.search` accept locale and overlay published `title` for card/list rows when available.
    - If any public field is overlaid from a published draft, return `automaticTranslationLabel: true` and `translatedBy: 'machine_reviewed'` on that performance/detail/card object.
    - Legal content types remain untouched. Do not read from `legal_content` and do not create legal translation rows.

    Required seed behavior:
    - Add `PHASE23_I18N_SMOKE_PERFORMANCE_ID = '00000000-0000-4000-8000-000000000023'`.
    - Insert the Girl Rules fanmeet performance with that fixed UUID.
    - Insert one showtime, three price tiers, cast, and the existing Donghae seat map for this fixture.
    - Insert translation sources for `title`, `description`, and `salesInfo` with SHA-256 `content_hash`.
    - Insert published drafts for `en`, `th`, `zh-CN`, and `zh-TW` for those three fields with `published_at=now()`.
  </action>
  <verify>
    <automated>pnpm --filter @grabit/api test -- performance.service.spec.ts search.service.spec.ts</automated>
    <automated>pnpm --filter @grabit/api typecheck</automated>
    <automated>pnpm --filter @grabit/web test -- performance-detail-translation-label.test.tsx</automated>
    <automated>rg -n "00000000-0000-4000-8000-000000000023|PHASE23_I18N_SMOKE_PERFORMANCE_ID|automaticTranslationLabel|machine_reviewed" apps/api/src apps/web/hooks packages/shared/src</automated>
  </verify>
  <acceptance_criteria>
    - API tests prove `findById(PHASE23_I18N_SMOKE_PERFORMANCE_ID, 'en')` returns English title/description/salesInfo and `automaticTranslationLabel: true`.
    - API tests prove Korean detail remains canonical Korean and does not set `automaticTranslationLabel`.
    - API tests prove list/search/home cards return translated titles for foreign locale when published translations exist.
    - API tests prove invalid ID `test-performance` is handled as 400/404 rather than 500.
    - Seed script contains the stable UUID and published non-legal translation drafts for all four foreign locales.
  </acceptance_criteria>
  <done>Phase 26 canary can verify real translated performance content using a deterministic local/dev fixture.</done>
</task>

<task type="auto">
  <name>Task 4: Add an i18n canary smoke E2E and update Phase 23 UAT evidence</name>
  <files>apps/web/e2e/i18n-smoke.spec.ts, .planning/phases/23-launch-foundation/23-UAT.md</files>
  <read_first>apps/web/playwright.config.ts, apps/web/e2e/social-login.spec.ts, apps/web/e2e/signup-sms.spec.ts, apps/web/e2e/admin-dashboard.spec.ts, apps/web/e2e/fixtures/booking-store.ts, apps/api/src/database/seed.mjs, .planning/phases/23-launch-foundation/23-UAT.md, .planning/phases/23-launch-foundation/23-VERIFICATION.md</read_first>
  <action>
    Add a dedicated Playwright smoke that can run before Phase 26 canary without admin login or live SMS.

    Required test design:
    - New file: `apps/web/e2e/i18n-smoke.spec.ts`.
    - Use `PHASE23_I18N_SMOKE_PERFORMANCE_ID` from env, defaulting to `00000000-0000-4000-8000-000000000023`.
    - Fail on browser console `error` and hydration mismatch messages.
    - Check default Korean routes: `/`, `/auth`, `/search?q=걸룰스`, `/performance/{id}`, `/booking/{id}`.
    - Check foreign routes for `en`, `th`, `zh-CN`, and `zh-TW`: `/{locale}`, `/{locale}/auth`, `/{locale}/search?q=girl`, `/{locale}/performance/{id}`, and `/{locale}/booking/{id}`.
    - Assert none of those pages render the not-found page.
    - Assert GNB locale switcher is visible and marks the active locale.
    - Assert visible copy changes by locale on home/search/auth/performance/booking-disabled surfaces. Minimum English assertions: `HOT`, `Search shows or artists`, `Login`, `Ticket booking opens in late May`, and `Reviewed machine translation`.
    - Assert `/api/runtime-flags` returns `bookingEnabled:false` during disabled canary smoke.
    - Assert booking-disabled page does not issue lock, reservation prepare, or Toss payment requests. Use Playwright request listeners or route interception for `/api/v1/bookings/**`, `/api/v1/reservations/**`, and Toss SDK endpoints.
    - Assert route headers do not include `x-middleware-rewrite: /ko` for Korean paths and foreign visible URL prefixes remain unchanged after navigation.

    After the smoke passes, update `23-UAT.md`:
    - replace `test-performance` seed-prerequisite wording with the stable UUID fixture contract;
    - mark i18n visible-copy, translated performance exposure, booking-disabled UI, and route behavior as resolved when the smoke passes;
    - leave admin login and live SMS E2E issues classified as outside this i18n smoke, not Phase 23 i18n blockers.
  </action>
  <verify>
    <automated>pnpm --filter @grabit/web exec playwright test e2e/i18n-smoke.spec.ts --reporter=line</automated>
    <automated>pnpm --filter @grabit/shared test -- launch-copy-keys.test.ts && pnpm --filter @grabit/api test -- performance.service.spec.ts search.service.spec.ts && pnpm --filter @grabit/web test -- visible-copy.test.ts home-i18n.test.tsx search-i18n.test.tsx auth-page-i18n.test.tsx booking-disabled-runtime.test.tsx performance-detail-translation-label.test.tsx</automated>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/e2e/i18n-smoke.spec.ts` exists and references `PHASE23_I18N_SMOKE_PERFORMANCE_ID`.
    - The smoke covers all five locales and all five canary-visible route groups: home, auth, search, performance detail, booking-disabled.
    - The smoke does not import `loginAsTestUser` and does not navigate to admin pages.
    - `23-UAT.md` records the smoke command, stable fixture ID, pass/fail result, and any remaining non-i18n E2E blockers.
  </acceptance_criteria>
  <done>Phase 23 has a repeatable i18n smoke gate suitable for Phase 26 canary precheck.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL/query locale -> API content overlay | Public clients can request any locale query value. |
| Translation draft tables -> public performance response | Reviewed translation rows become user-visible event content. |
| Browser route prefix -> flat App Router rewrite | Public locale prefixes are mapped to flat internal routes by `apps/web/proxy.ts`. |
| Seed fixture -> E2E/canary evidence | Local/dev deterministic fixture must not be confused with production proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-19-01 | Tampering | `locale` query params | Medium | mitigate | Validate every locale with `isSupportedLocale`; unsupported locales fall back to `ko` or return schema validation error. |
| T-23-19-02 | Information disclosure | Translation overlay | Medium | mitigate | Overlay only `performance` entity fields `title`, `description`, and `salesInfo`; legal content tables and legal content types are never queried. |
| T-23-19-03 | Spoofing | Automatic translation label | Medium | mitigate | Set `automaticTranslationLabel=true` only when at least one field came from a published draft; tests cover Korean no-label and foreign label cases. |
| T-23-19-04 | Repudiation | Canary smoke evidence | Low | mitigate | Record exact Playwright command, fixture ID, and timestamp in `23-UAT.md`; keep admin/SMS blockers classified separately. |
| T-23-19-05 | Denial of service | E2E smoke route interception | Low | accept | The smoke runs in local/dev Playwright only and must not alter production traffic. |
</threat_model>

<verification>
Execute in this order:
1. `pnpm --filter @grabit/shared test -- launch-copy-keys.test.ts`
2. `pnpm --filter @grabit/api test -- performance.service.spec.ts search.service.spec.ts`
3. `pnpm --filter @grabit/web test -- visible-copy.test.ts home-i18n.test.tsx search-i18n.test.tsx auth-page-i18n.test.tsx booking-disabled-runtime.test.tsx performance-detail-translation-label.test.tsx gnb-locale.test.tsx layout-shell-locale.test.tsx`
4. `pnpm typecheck`
5. Start API with seeded DB and web dev server, then run `pnpm --filter @grabit/web exec playwright test e2e/i18n-smoke.spec.ts --reporter=line`

Do not mark this plan complete if the dedicated i18n smoke cannot prove home/auth/search/performance/booking-disabled route behavior in all five locales. If admin login or live SMS E2E still fails, keep those as separate non-i18n blockers and do not let them obscure this smoke result.
</verification>

<success_criteria>
- Five-locale public/auth/home/search/performance/booking-disabled visible copy is present and tested.
- Desktop GNB and mobile menu locale switchers are visible, active-state marked, and prefix-preserving.
- Public performance APIs expose reviewed machine-translated content with `automaticTranslationLabel=true` for foreign locales.
- The invalid `test-performance` fixture no longer blocks Phase 23 i18n UAT; the stable UUID fixture provides showtime, price tiers, seat map, and published translations.
- A dedicated i18n Playwright smoke passes independently from admin login and SMS E2E state.
- `23-UAT.md` clearly records closed i18n gaps and any remaining Phase 26 canary prerequisites.
</success_criteria>

<output>
After completion, create `.planning/phases/23-launch-foundation/23-19-SUMMARY.md`.
</output>
