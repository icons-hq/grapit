---
phase: 23-launch-foundation
plan: 10
type: execute
wave: 3
depends_on:
  - 23-01
  - 23-02
files_modified:
  - apps/web/lib/i18n/format.ts
  - apps/web/lib/i18n/format.test.ts
  - apps/web/components/i18n/kst-time.tsx
  - apps/web/components/i18n/currency-display.tsx
  - apps/web/components/i18n/locale-switcher.tsx
  - apps/web/components/i18n/locale-suggestion.tsx
  - apps/web/components/i18n/__tests__/format-components.test.tsx
  - apps/web/components/ui/phone-input.tsx
  - apps/web/components/ui/__tests__/phone-input-i18n.test.tsx
  - packages/shared/src/i18n/launch-copy-keys.ts
  - packages/shared/src/i18n/launch-copy-keys.test.ts
  - packages/shared/src/index.ts
  - apps/api/src/modules/user/user.controller.ts
  - apps/api/src/modules/user/user.service.ts
  - apps/api/src/modules/user/user.repository.ts
autonomous: true
requirements:
  - I18N-02
must_haves:
  truths:
    - "Event-critical time displays include a KST anchor and may also show localized local time."
    - "Prices show KRW source amount plus estimated localized conversion with a disclaimer."
    - "PhoneInput labels support `ko`, `en`, `th`, `zh-CN`, and `zh-TW` without removing unsupported country search."
    - "Shared launch copy key manifest requires auth/email/OTP/SMS namespaces for exactly `ko`, `en`, `th`, `zh-CN`, and `zh-TW`."
    - "Locale switcher and suggestion UI make language choice explicit and never auto-redirect."
    - "Anonymous locale preference uses cookie; logged-in preference persists to `users.preferred_locale` with URL-first precedence."
  artifacts:
    - path: "apps/web/lib/i18n/format.ts"
      provides: "KST/local time and KRW/local currency formatting helpers"
    - path: "apps/web/components/ui/phone-input.tsx"
      provides: "Launch-locale PhoneInput labels"
    - path: "packages/shared/src/i18n/launch-copy-keys.ts"
      provides: "Cross-app required auth/email/OTP/SMS localized copy key manifest"
    - path: "apps/web/components/i18n/locale-switcher.tsx"
      provides: "Explicit locale switch UI"
    - path: "apps/api/src/modules/user/user.service.ts"
      provides: "Logged-in preferred locale persistence"
  key_links:
    - from: "apps/web/components/i18n/kst-time.tsx"
      to: "apps/web/lib/i18n/format.ts"
      via: "formatEventTimeWithKstAnchor"
      pattern: "formatEventTimeWithKstAnchor"
    - from: "apps/web/components/i18n/currency-display.tsx"
      to: "apps/web/lib/i18n/format.ts"
      via: "formatKrwWithEstimate"
      pattern: "formatKrwWithEstimate"
    - from: "packages/shared/src/index.ts"
      to: "packages/shared/src/i18n/launch-copy-keys.ts"
      via: "barrel export"
      pattern: "launch-copy-keys|launchCopyKeys"
    - from: "apps/api/src/modules/user/user.service.ts"
      to: "apps/api/src/database/schema/users.ts"
      via: "preferredLocale persistence"
      pattern: "preferredLocale"
---

<objective>
I18N-02의 locale-sensitive interaction foundation을 routing plan에서 분리해 구현한다.

Purpose: D-07/D-08 요구인 KST/KRW anchor, localized conversion disclaimer, PhoneInput label localization, user locale persistence를 작고 검증 가능한 plan으로 닫는다.
Output: formatting helpers/components/tests, locale switch/suggestion UI, localized PhoneInput, preferred locale API persistence.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-launch-foundation/23-CONTEXT.md
@.planning/phases/23-launch-foundation/23-RESEARCH.md
@.planning/phases/23-launch-foundation/23-UI-SPEC.md
@packages/shared/src/constants/locales.ts
@packages/shared/src/types/i18n.types.ts
@apps/web/components/ui/phone-input.tsx
@apps/web/components/auth/phone-verification.tsx
@apps/web/components/layout/network-banner.tsx
@apps/web/components/ui/dropdown-menu.tsx
@apps/api/src/modules/user/user.controller.ts
@apps/api/src/modules/user/user.service.ts
@apps/api/src/modules/user/user.repository.ts

<interfaces>
D-07 formatting contract:
```text
Event-critical time: 2026.07.04 18:00 KST plus optional localized local time.
Currency: KRW source price plus estimated conversion and exchange-rate disclaimer.
```

D-06 locale precedence:
```text
url > explicit-switch > user-profile > cookie > ko
```

I18N-02 launch copy manifest:
```text
locales: ko, en, th, zh-CN, zh-TW
namespaces: auth.emailVerification, auth.otp, auth.errors, sms.otp
required behavior: every namespace key must be present for all five launch locales; tests fail on missing or extra locale keys
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement KST/KRW formatting helpers and components</name>
  <files>apps/web/lib/i18n/format.ts, apps/web/lib/i18n/format.test.ts, apps/web/components/i18n/kst-time.tsx, apps/web/components/i18n/currency-display.tsx, apps/web/components/i18n/__tests__/format-components.test.tsx</files>
  <read_first>packages/shared/src/constants/locales.ts, apps/web/app/fonts.ts, apps/web/components/ui/badge.tsx, .planning/phases/23-launch-foundation/23-UI-SPEC.md</read_first>
  <behavior>
    - `formatEventTimeWithKstAnchor` always includes `KST` for event/booking-critical times.
    - Localized secondary time is generated with `Intl.DateTimeFormat` for `ko`, `en`, `th`, `zh-CN`, and `zh-TW`.
    - `formatKrwWithEstimate` always shows KRW source amount, estimated local amount, and disclaimer text.
    - Formatting uses native `Intl` and does not add `date-fns` unless native APIs cannot satisfy tests.
  </behavior>
  <action>Create `apps/web/lib/i18n/format.ts` exporting `formatEventTimeWithKstAnchor(input, locale, options)` and `formatKrwWithEstimate(krwAmount, locale, exchangeRate)`. Create small presentational components `KstTime` and `CurrencyDisplay` that use the helpers and UI-SPEC warning/informational copy. Use KRW as canonical source and make conversion explicitly estimated with wording equivalent to `exchange rate may change`.</action>
  <verify>
    <automated>pnpm --filter @grabit/web test -- format.test.ts format-components.test.tsx && grep -R "KST" apps/web/lib/i18n apps/web/components/i18n && grep -R "exchange rate may change\\|환율" apps/web/lib/i18n apps/web/components/i18n</automated>
  </verify>
  <acceptance_criteria>Tests cover all five locales, KST anchor, KRW source amount, estimated conversion, and disclaimer text.</acceptance_criteria>
  <done>D-07 time/currency formatting is implemented with automated tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Localize PhoneInput labels and define auth/OTP/SMS copy key manifest</name>
  <files>apps/web/components/ui/phone-input.tsx, apps/web/components/ui/__tests__/phone-input-i18n.test.tsx, packages/shared/src/i18n/launch-copy-keys.ts, packages/shared/src/i18n/launch-copy-keys.test.ts, packages/shared/src/index.ts</files>
  <read_first>apps/web/components/ui/phone-input.tsx, apps/web/components/auth/phone-verification.tsx, packages/shared/src/constants/locales.ts, .planning/phases/23-launch-foundation/23-UI-SPEC.md</read_first>
  <behavior>
    - PhoneInput label set changes for `ko`, `en`, `th`, `zh-CN`, `zh-TW`.
    - Unsupported countries remain searchable/selectable.
    - Long Thai and Chinese labels do not remove existing min-height or popover behavior.
    - Shared copy manifest lists required `auth.emailVerification`, `auth.otp`, `auth.errors`, and `sms.otp` keys for exactly `ko`, `en`, `th`, `zh-CN`, and `zh-TW`.
  </behavior>
  <action>Update `PhoneInput` to accept optional `locale` prop and load `react-phone-number-input/locale/{ko,en,th,zh,zh-Hant}.json` or local labels as needed. Preserve existing shadcn/Radix popover, flags, min-height, and country search behavior. Create `packages/shared/src/i18n/launch-copy-keys.ts` exporting `LAUNCH_COPY_LOCALES = ['ko','en','th','zh-CN','zh-TW']` and required keys for `auth.emailVerification`, `auth.otp`, `auth.errors`, and `sms.otp`. Export it from `packages/shared/src/index.ts` so Phase 23 API templates and web messages can align on the same manifest as execution progresses. Tests should render representative PhoneInput labels for all five launch locales, assert an unsupported country remains discoverable, and assert every launch copy namespace has exactly the five locale keys with no missing locale.</action>
  <verify>
    <automated>pnpm --filter @grabit/web test -- phone-input-i18n.test.tsx && pnpm --filter @grabit/shared test -- launch-copy-keys.test.ts && grep -R "locale.*PhoneInput\\|PhoneInput.*locale" apps/web/components/ui/phone-input.tsx && grep -R "zh-TW" packages/shared/src/i18n/launch-copy-keys.ts</automated>
  </verify>
  <acceptance_criteria>`phone-input-i18n.test.tsx` covers five locales and unsupported country search; `launch-copy-keys.test.ts` asserts required auth/email/OTP/SMS copy keys for exactly `ko`, `en`, `th`, `zh-CN`, and `zh-TW`; existing PhoneInput API remains backward-compatible when `locale` is omitted.</acceptance_criteria>
  <done>D-08/I18N-02 launch-market PhoneInput localization and shared localized copy key contract are wired without narrowing country selection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add explicit locale switch/suggestion UI and persist logged-in locale preference</name>
  <files>apps/web/components/i18n/locale-switcher.tsx, apps/web/components/i18n/locale-suggestion.tsx, apps/api/src/modules/user/user.controller.ts, apps/api/src/modules/user/user.service.ts, apps/api/src/modules/user/user.repository.ts</files>
  <read_first>apps/web/components/layout/network-banner.tsx, apps/web/components/ui/dropdown-menu.tsx, apps/web/components/ui/sonner.tsx, apps/api/src/modules/user/user.controller.ts, apps/api/src/modules/user/user.service.ts, apps/api/src/modules/user/user.repository.ts, apps/api/src/database/schema/users.ts, packages/shared/src/constants/locales.ts</read_first>
  <behavior>
    - Locale switcher shows active locale with `aria-current="true"`.
    - Suggestion copy appears once and dismissal persists for session.
    - Logged-in locale preference writes to `users.preferred_locale` using shared supported-locale validation.
    - Anonymous preference remains cookie-only and is not written through this API.
    - Conflict precedence is documented and tested as `url > explicit-switch > user-profile > cookie > ko`.
  </behavior>
  <action>Create `LocaleSwitcher` using shadcn `DropdownMenu` or `Select`, labels from UI-SPEC, `aria-current="true"`, and explicit click navigation only. Create `LocaleSuggestion` banner/toast with UI-SPEC copy and session dismissal; it may set cookie/session state but must never redirect automatically. Add user profile endpoint support for `preferredLocale` using shared `isSupportedLocale`. Update service/repository to read and write `users.preferred_locale` added by Plan 23-02. Add or extend user service/controller tests if existing files contain profile update tests; otherwise use API typecheck plus focused assertions in the nearest user spec. This task owns explicit switch persistence only; route resolution remains in Plan 23-04.</action>
  <verify>
    <automated>(pnpm --filter @grabit/api test -- user.service.spec.ts user.controller.spec.ts || pnpm --filter @grabit/api typecheck) && grep -R "preferredLocale" apps/api/src/modules/user && grep -R "aria-current" apps/web/components/i18n/locale-switcher.tsx && grep -R "다른 언어로 볼까요" apps/web/components/i18n/locale-suggestion.tsx</automated>
  </verify>
  <acceptance_criteria>`LocaleSwitcher` uses `aria-current`; `LocaleSuggestion` contains Korean suggestion copy and no redirect call; `user.service.ts` and/or `user.repository.ts` reads/writes `preferredLocale`; unsupported locale input is rejected; D-06 precedence string appears in service test or implementation comment.</acceptance_criteria>
  <done>Explicit locale choice is visible and logged-in locale preference persists without changing anonymous cookie behavior.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| locale/currency input -> UI formatting | Locale and exchange-rate inputs affect user-visible pricing and event time interpretation. |
| user profile update -> stored locale | Client-provided locale preference is persisted to user profile. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering | `formatKrwWithEstimate` | mitigate | Always show KRW source amount and estimated-conversion disclaimer; never replace canonical KRW. |
| T-23-02 | Repudiation | event time display | mitigate | Event-critical displays always include explicit `KST` anchor. |
| T-23-03 | Spoofing | locale preference API | mitigate | Validate against shared supported locale enum before storing. |
| T-23-04 | Information disclosure | PhoneInput | mitigate | Do not log phone input or country selection; preserve existing no-PII behavior. |
| T-23-05 | Denial of service | localized labels | mitigate | Tests cover long labels and preserve existing control dimensions/search behavior. |
| T-23-06 | Tampering | exchange rate | accept | Live FX source is Phase 24/payment scope; this plan labels conversion as estimated and disclaimer-bound. |
</threat_model>

<verification>
Run web formatting/PhoneInput tests, API user tests or typecheck fallback, and greps for KST/KRW/disclaimer/preferredLocale.
</verification>

<success_criteria>
I18N-02 has concrete KST/KRW/local formatting, localized PhoneInput, and locale preference persistence with automated verification.
</success_criteria>

<output>
After completion, create `.planning/phases/23-launch-foundation/23-10-SUMMARY.md`.
</output>
