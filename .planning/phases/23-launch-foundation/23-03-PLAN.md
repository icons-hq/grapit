---
phase: 23-launch-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - apps/api/src/modules/feature-flags/feature-flags.module.ts
  - apps/api/src/modules/feature-flags/feature-flags.service.ts
  - apps/api/src/modules/feature-flags/feature-flags.service.spec.ts
  - apps/api/src/app.module.ts
  - apps/api/src/modules/booking/booking.service.ts
  - apps/api/src/modules/booking/__tests__/booking.service.spec.ts
  - apps/api/src/modules/reservation/reservation.service.ts
  - apps/api/src/modules/reservation/reservation.service.spec.ts
  - apps/api/src/modules/payment/toss-payments.client.ts
  - docs/runbooks/phase23-canary-rollback.md
autonomous: true
requirements:
  - FLAG-01
  - FLAG-02
must_haves:
  truths:
    - "`BOOKING_ENABLED=false` blocks direct API booking mutations before Redis, DB transaction, or Toss side effects."
    - "Flag parsing is runtime API-side and does not depend on `NEXT_PUBLIC_BOOKING_ENABLED`."
    - "Canary rollback policy blocks PASS if auth/session, booking-disabled API behavior, Korean root URL, or locale routing smoke fails."
  artifacts:
    - path: "apps/api/src/modules/feature-flags/feature-flags.service.ts"
      provides: "Runtime API feature flag source"
    - path: "apps/api/src/modules/booking/booking.service.ts"
      provides: "Seat lock hard gate"
    - path: "apps/api/src/modules/reservation/reservation.service.ts"
      provides: "Reservation prepare and payment confirm hard gates"
    - path: "apps/api/src/modules/payment/toss-payments.client.ts"
      provides: "Payment-side effect boundary discovery and explicit no-request-path assertion"
  key_links:
    - from: "apps/api/src/modules/booking/booking.service.ts"
      to: "apps/api/src/modules/feature-flags/feature-flags.service.ts"
      via: "FeatureFlagsService injection"
      pattern: "FeatureFlagsService"
    - from: "apps/api/src/modules/reservation/reservation.service.ts"
      to: "apps/api/src/modules/payment/toss-payments.client.ts"
      via: "confirm gate before Toss confirm"
      pattern: "bookingEnabled"
---

<objective>
`BOOKING_ENABLED=false`를 UI 설명이 아니라 API hard gate로 만든다.

Purpose: 광고 오픈 상태에서 직접 API 호출로 좌석 lock, pending reservation, payment confirm/request가 생성되는 사고를 막는다.
Output: API feature flag module, booking/reservation/payment gates, rollback runbook, focused tests.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-launch-foundation/23-CONTEXT.md
@.planning/phases/23-launch-foundation/23-VALIDATION.md
@.planning/phases/23-launch-foundation/23-PATTERNS.md
@packages/shared/src/flags.ts
@apps/api/src/modules/booking/booking.service.ts
@apps/api/src/modules/reservation/reservation.service.ts
@apps/api/src/modules/payment/toss-payments.client.ts
@apps/api/src/app.module.ts

<interfaces>
Use the shared contract from Plan 23-01:
```typescript
readFeatureFlags(process.env).bookingEnabled
```
Gate paths before these side effects:
```typescript
BookingService.lockSeat -> Redis SET NX / Lua lock
ReservationService.prepareReservation -> db.transaction insert pending reservation
ReservationService.confirmAndCreateReservation -> acquire confirm lock / Toss confirmPayment
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add API runtime feature flag service</name>
  <files>apps/api/src/modules/feature-flags/feature-flags.module.ts, apps/api/src/modules/feature-flags/feature-flags.service.ts, apps/api/src/modules/feature-flags/feature-flags.service.spec.ts, apps/api/src/app.module.ts</files>
  <read_first>packages/shared/src/flags.ts, apps/api/src/app.module.ts, apps/api/src/modules/user/user.module.ts, apps/api/src/modules/payment/payment.service.ts</read_first>
  <behavior>
    - `FeatureFlagsService.getFlags().bookingEnabled` reads runtime env through shared `readFeatureFlags`.
    - Missing `BOOKING_ENABLED` returns false.
    - The service never reads `NEXT_PUBLIC_BOOKING_ENABLED`.
  </behavior>
  <action>Create `FeatureFlagsModule` and `FeatureFlagsService` under `apps/api/src/modules/feature-flags`. Inject `ConfigService` or read `process.env` through a narrow method, then call shared `readFeatureFlags`. Register `FeatureFlagsModule` in `AppModule` and export the service. Tests cover true/false/default parsing and absence of `NEXT_PUBLIC_BOOKING_ENABLED`.</action>
  <verify>
    <automated>pnpm --filter @grabit/api test -- feature-flags.service.spec.ts && pnpm --filter @grabit/api typecheck && ! grep -R "NEXT_PUBLIC_BOOKING_ENABLED" apps/api/src/modules/feature-flags</automated>
  </verify>
  <acceptance_criteria>`feature-flags.service.ts` imports `readFeatureFlags`; `feature-flags.service.spec.ts` asserts default false; grep for `NEXT_PUBLIC_BOOKING_ENABLED` in feature-flags module returns no matches.</acceptance_criteria>
  <done>API has one runtime source for booking flag decisions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate booking and payment creation paths before side effects</name>
  <files>apps/api/src/modules/booking/booking.service.ts, apps/api/src/modules/booking/__tests__/booking.service.spec.ts, apps/api/src/modules/reservation/reservation.service.ts, apps/api/src/modules/reservation/reservation.service.spec.ts, apps/api/src/modules/payment/toss-payments.client.ts</files>
  <read_first>apps/api/src/modules/booking/booking.service.ts, apps/api/src/modules/booking/__tests__/booking.service.spec.ts, apps/api/src/modules/reservation/reservation.service.ts, apps/api/src/modules/reservation/reservation.service.spec.ts, apps/api/src/modules/payment/toss-payments.client.ts, .planning/phases/23-launch-foundation/23-CONTEXT.md</read_first>
  <behavior>
    - Disabled booking rejects `lockSeat` before Redis lock mutation.
    - Disabled booking rejects `prepareReservation` before DB transaction and pending reservation creation.
    - Disabled booking rejects `confirmAndCreateReservation` before confirm lock and Toss `confirmPayment`.
    - Executor discovers whether an API-side payment request creation path exists; if it exists, it is gated before side effects, and if it does not exist, the absence is asserted in tests/evidence.
  </behavior>
  <action>Inject `FeatureFlagsService` into `BookingService` and `ReservationService`. Add `assertBookingEnabled()` with user-facing Korean error `예매는 5월말 오픈 예정입니다` and HTTP 403/ForbiddenException. Call it at the top of `lockSeat`, `prepareReservation`, and `confirmAndCreateReservation` before Redis/Toss/transaction side effects. Before coding, run `rg -n "requestPayment|createPayment|paymentRequest|payments/request|confirmPayment" apps/api/src/modules/payment apps/api/src/modules/reservation apps/api/src/modules/booking` to locate every API-side payment creation/confirm path. If any request-creation method exists besides `confirmPayment`, gate it through the same service assertion before external/client side effects. If no API-side request creation path exists, add an explicit unit test or assertion named `does not expose API-side payment request creation while booking disabled` in the nearest reservation/payment spec so the absence is recorded instead of assumed.</action>
  <verify>
    <automated>pnpm --filter @grabit/api test -- booking.service.spec.ts reservation.service.spec.ts && rg -n "requestPayment|createPayment|paymentRequest|payments/request|confirmPayment" apps/api/src/modules/payment apps/api/src/modules/reservation apps/api/src/modules/booking && grep -R "payment request creation" apps/api/src/modules/reservation apps/api/src/modules/payment apps/api/src/modules/booking && grep -R "예매는 5월말 오픈 예정입니다" apps/api/src/modules/booking apps/api/src/modules/reservation</automated>
  </verify>
  <acceptance_criteria>Tests assert disabled `lockSeat` does not call Redis lock methods; disabled `prepareReservation` does not call `db.transaction`; disabled `confirmAndCreateReservation` does not call `tossClient.confirmPayment` or acquire confirm lock; payment request creation is either gated or explicitly asserted absent by test/evidence.</acceptance_criteria>
  <done>Direct API booking and payment creation is blocked when `BOOKING_ENABLED=false`.</done>
</task>

<task type="auto">
  <name>Task 3: Document strict canary rollback gate</name>
  <files>docs/runbooks/phase23-canary-rollback.md</files>
  <read_first>.planning/phases/23-launch-foundation/23-CONTEXT.md, .planning/STATE.md, docs/v2.0-fanmeet-milestone-spec.md</read_first>
  <action>Create `docs/runbooks/phase23-canary-rollback.md` with smoke checks for auth/session refresh, booking-disabled API behavior, Korean root URL preservation, and locale routing. Include exact rollback rule per D-02: if any smoke fails, rollback immediately and do not mark Phase 23 PASS. Preserve Phase 22 `ACCEPTED_RISK` caveats as not-PASS evidence.</action>
  <verify>
    <automated>grep -R "auth/session" docs/runbooks/phase23-canary-rollback.md && grep -R "booking-disabled API" docs/runbooks/phase23-canary-rollback.md && grep -R "Korean root URL" docs/runbooks/phase23-canary-rollback.md && grep -R "do not mark Phase 23 as PASS" docs/runbooks/phase23-canary-rollback.md</automated>
  </verify>
  <acceptance_criteria>Runbook contains the four D-02 smoke categories and the exact phrase `do not mark Phase 23 as PASS`.</acceptance_criteria>
  <done>FLAG-01 canary rollback behavior is executable and traceable.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| public client -> booking API | Attackers can call booking endpoints directly while UI is disabled. |
| Cloud Run env -> API runtime | Runtime feature flags control creation of scarce booking/payment resources. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Elevation of privilege | `BookingService`, `ReservationService` | mitigate | Server-side flag guard before Redis, DB transaction, confirm lock, and Toss calls. |
| T-23-02 | Tampering | canary routing smoke | mitigate | Rollback runbook includes Korean root and locale routing checks. |
| T-23-03 | Repudiation | payment/legal side effects | accept | Legal translation is handled in Plan 05; this plan blocks payment side effects only. |
| T-23-04 | Spoofing | auth/session canary | mitigate | Runbook requires auth/session smoke before PASS. |
| T-23-05 | Repudiation | booking-disabled evidence | mitigate | Tests prove no mutation side effects when disabled. |
| T-23-06 | Information disclosure | logs/runbook | mitigate | Runbook instructs redacted evidence, no raw tokens/PII. |
</threat_model>

<verification>
Run targeted API tests and typecheck. Confirm no `NEXT_PUBLIC_BOOKING_ENABLED` appears in API flag code.
</verification>

<success_criteria>
`BOOKING_ENABLED=false` is enforced on backend mutation paths and canary rollback behavior is documented with hard fail gates.
</success_criteria>

<output>
After completion, create `.planning/phases/23-launch-foundation/23-03-SUMMARY.md`.
</output>
