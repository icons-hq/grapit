---
phase: 23-launch-foundation
plan: 08
type: execute
wave: 4
depends_on:
  - 23-03
  - 23-04
files_modified:
  - apps/web/lib/runtime-flags.ts
  - apps/web/hooks/use-runtime-flags.ts
  - apps/web/hooks/use-booking.ts
  - apps/web/hooks/__tests__/booking-disabled-runtime.test.tsx
  - apps/web/components/booking/booking-page.tsx
  - apps/web/app/performance/[id]/page.tsx
autonomous: true
requirements:
  - FLAG-02
  - I18N-02
must_haves:
  truths:
    - "Users see localized booking-disabled copy when `BOOKING_ENABLED=false`."
    - "Disabled booking UI cannot trigger seat lock, reservation prepare, or Toss payment request handlers."
    - "Web runtime flag reading does not rely on build-time `NEXT_PUBLIC_BOOKING_ENABLED`."
  artifacts:
    - path: "apps/web/lib/runtime-flags.ts"
      provides: "SSR/API runtime flag exposure for web"
    - path: "apps/web/hooks/__tests__/booking-disabled-runtime.test.tsx"
      provides: "Booking-disabled UI handler regression tests"
    - path: "apps/web/components/booking/booking-page.tsx"
      provides: "Booking-disabled public booking surface"
  key_links:
    - from: "apps/web/hooks/use-booking.ts"
      to: "runtime booking flag"
      via: "runtime flag fetch/hook"
      pattern: "bookingEnabled"
    - from: "apps/web/components/booking/booking-page.tsx"
      to: "apps/web/hooks/use-runtime-flags.ts"
      via: "disabled action guard"
      pattern: "bookingEnabled"
---

<objective>
Runtime booking-disabled UI를 기존 public booking/detail surfaces에 연결한다.

Purpose: D-01의 API-first hard gate와 별개로, 사용자가 disabled 상태를 명확히 보고 UI에서 seat lock/payment handler를 실수로 실행하지 않게 한다.
Output: runtime flag client/hook, disabled booking UI copy, handler-block regression tests.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-launch-foundation/23-CONTEXT.md
@.planning/phases/23-launch-foundation/23-UI-SPEC.md
@packages/shared/src/flags.ts
@packages/shared/src/constants/locales.ts
@apps/web/hooks/use-booking.ts
@apps/web/components/booking/booking-page.tsx
@apps/web/app/performance/[id]/page.tsx

<interfaces>
Booking-disabled copy:
```text
ko: 예매는 5월말 오픈 예정입니다
en: Ticket booking opens in late May
th: การจองบัตรจะเปิดปลายเดือนพฤษภาคม
zh-CN: 门票预订预计于5月下旬开放
zh-TW: 門票預訂預計於5月下旬開放
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Expose runtime booking flag to web without NEXT_PUBLIC freeze</name>
  <files>apps/web/lib/runtime-flags.ts, apps/web/hooks/use-runtime-flags.ts, apps/web/hooks/use-booking.ts, apps/web/hooks/__tests__/booking-disabled-runtime.test.tsx, apps/web/components/booking/booking-page.tsx, apps/web/app/performance/[id]/page.tsx</files>
  <read_first>packages/shared/src/flags.ts, apps/web/lib/api-client.ts, apps/web/hooks/use-booking.ts, apps/web/components/booking/booking-page.tsx, apps/web/app/performance/[id]/page.tsx, .planning/phases/23-launch-foundation/23-UI-SPEC.md</read_first>
  <behavior>
    - Web reads booking flag through runtime API/SSR exposure, not `NEXT_PUBLIC_BOOKING_ENABLED`.
    - Disabled state shows the five-locale booking-disabled copy from UI-SPEC.
    - Seat lock, reservation prepare, and Toss `requestPayment` UI handlers do not fire when disabled.
  </behavior>
  <action>Create `runtime-flags.ts` and `use-runtime-flags.ts` that consume an API/SSR runtime flag endpoint or server-injected value using shared flag names per D-01/D-03. Add disabled guards before `lockSeat.mutate`, `usePrepareReservation`, or Toss `requestPayment` references in booking/detail UI. Add tests that mock disabled runtime flags and assert lock, prepare, and payment request handlers are not called. Do not use `NEXT_PUBLIC_BOOKING_ENABLED` as source of truth.</action>
  <verify>
    <automated>pnpm --filter @grabit/web test -- booking-disabled-runtime.test.tsx && pnpm --filter @grabit/web typecheck && ! grep -R "NEXT_PUBLIC_BOOKING_ENABLED" apps/web && grep -R "requestPayment" apps/web/hooks/__tests__ apps/web/components/booking</automated>
  </verify>
  <acceptance_criteria>`apps/web` contains no `NEXT_PUBLIC_BOOKING_ENABLED`; disabled booking copy appears for all five locales; `booking-disabled-runtime.test.tsx` proves lock, prepare, and `requestPayment` handlers are not called while disabled.</acceptance_criteria>
  <done>FLAG-02 web disabled booking behavior matches backend hard gate and avoids build-time flag freezing.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser UI -> booking APIs | Client state is untrusted, but UI must prevent accidental disabled booking attempts. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-08-01 | Elevation of privilege | booking UI/hooks | mitigate | UI blocks handlers while API hard gate remains authoritative per D-01. |
| T-23-08-02 | Tampering | runtime flags | mitigate | Use shared flag names and runtime fetch/SSR exposure, not build-time public env. |
| T-23-08-03 | Information disclosure | disabled UI errors | mitigate | Display localized opening copy only; do not expose env values or internal flag config. |
</threat_model>

<verification>
Run web booking-disabled tests, web typecheck, and grep for forbidden `NEXT_PUBLIC_BOOKING_ENABLED`.
</verification>

<success_criteria>
Users see localized disabled booking copy, and disabled UI cannot trigger booking/payment handlers.
</success_criteria>

<output>
After completion, create `.planning/phases/23-launch-foundation/23-08-SUMMARY.md`.
</output>
