---
phase: 23-launch-foundation
plan: 16
type: execute
wave: 3
depends_on:
  - 23-01
  - 23-02
files_modified:
  - apps/web/components/i18n/locale-switcher.tsx
  - apps/web/components/i18n/locale-suggestion.tsx
  - apps/api/src/modules/user/user.controller.ts
  - apps/api/src/modules/user/user.service.ts
  - apps/api/src/modules/user/user.repository.ts
autonomous: true
requirements:
  - I18N-01
  - I18N-02
must_haves:
  truths:
    - "Locale switcher and suggestion UI make language choice explicit and never auto-redirect."
    - "Anonymous locale preference uses cookie/session state; logged-in preference persists to `users.preferred_locale`."
    - "Locale precedence is URL-first: `url > explicit-switch > user-profile > cookie > ko`."
  artifacts:
    - path: "apps/web/components/i18n/locale-switcher.tsx"
      provides: "Explicit locale switch UI"
    - path: "apps/web/components/i18n/locale-suggestion.tsx"
      provides: "Suggest-never-redirect language prompt"
    - path: "apps/api/src/modules/user/user.service.ts"
      provides: "Logged-in preferred locale persistence"
  key_links:
    - from: "apps/api/src/modules/user/user.service.ts"
      to: "apps/api/src/database/schema/users.ts"
      via: "preferredLocale persistence"
      pattern: "preferredLocale"
---

<objective>
Locale switch/suggestion UI and logged-in locale preference persistence를 구현한다.

Purpose: D-05/D-06 요구인 suggest-never-redirect와 user profile locale preference를 route shell과 분리해 검증 가능한 단위로 닫는다.
Output: `LocaleSwitcher`, `LocaleSuggestion`, user preferredLocale API/service/repository support.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-launch-foundation/23-CONTEXT.md
@.planning/phases/23-launch-foundation/23-RESEARCH.md
@.planning/phases/23-launch-foundation/23-UI-SPEC.md
@packages/shared/src/constants/locales.ts
@packages/shared/src/types/i18n.types.ts
@apps/web/components/layout/network-banner.tsx
@apps/web/components/ui/dropdown-menu.tsx
@apps/web/components/ui/sonner.tsx
@apps/api/src/modules/user/user.controller.ts
@apps/api/src/modules/user/user.service.ts
@apps/api/src/modules/user/user.repository.ts
@apps/api/src/database/schema/users.ts

<interfaces>
D-06 locale precedence:
```text
url > explicit-switch > user-profile > cookie > ko
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add explicit locale switch/suggestion UI and persist logged-in locale preference</name>
  <files>apps/web/components/i18n/locale-switcher.tsx, apps/web/components/i18n/locale-suggestion.tsx, apps/api/src/modules/user/user.controller.ts, apps/api/src/modules/user/user.service.ts, apps/api/src/modules/user/user.repository.ts</files>
  <read_first>apps/web/components/layout/network-banner.tsx, apps/web/components/ui/dropdown-menu.tsx, apps/web/components/ui/sonner.tsx, apps/api/src/modules/user/user.controller.ts, apps/api/src/modules/user/user.service.ts, apps/api/src/modules/user/user.repository.ts, apps/api/src/database/schema/users.ts, packages/shared/src/constants/locales.ts</read_first>
  <behavior>
    - Locale switcher shows active locale with `aria-current="true"`.
    - Suggestion copy appears once and dismissal persists for session.
    - Logged-in locale preference writes to `users.preferred_locale` using shared supported-locale validation.
    - Anonymous preference remains cookie-only and is not written through this API.
    - Conflict precedence is documented and tested as `url > explicit-switch > user-profile > cookie > ko`.
  </behavior>
  <action>Create `LocaleSwitcher` using shadcn `DropdownMenu` or `Select`, labels from UI-SPEC, `aria-current="true"`, and explicit click navigation only per D-05. Create `LocaleSuggestion` banner/toast with UI-SPEC copy and session dismissal; it may set cookie/session state but must never redirect automatically. Add user profile endpoint support for `preferredLocale` using shared `isSupportedLocale` per D-06. Update service/repository to read and write `users.preferred_locale` added by Plan 23-02. Add or extend user service/controller tests if existing files contain profile update tests; otherwise use API typecheck plus focused assertions in the nearest user spec. This task owns explicit switch persistence only; route resolution remains in Plan 23-04.</action>
  <verify>
    <automated>(pnpm --filter @grabit/api test -- user.service.spec.ts user.controller.spec.ts || pnpm --filter @grabit/api typecheck) && grep -R "preferredLocale" apps/api/src/modules/user && grep -R "aria-current" apps/web/components/i18n/locale-switcher.tsx && grep -R "다른 언어로 볼까요" apps/web/components/i18n/locale-suggestion.tsx</automated>
  </verify>
  <acceptance_criteria>`LocaleSwitcher` uses `aria-current`; `LocaleSuggestion` contains Korean suggestion copy and no redirect call; `user.service.ts` and/or `user.repository.ts` reads/writes `preferredLocale`; unsupported locale input is rejected; D-06 precedence string appears in service test or implementation comment.</acceptance_criteria>
  <done>Explicit locale choice is visible and logged-in locale preference persists without changing anonymous cookie behavior.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| user profile update -> stored locale | Client-provided locale preference is persisted to user profile. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-16-01 | Spoofing | locale preference API | mitigate | Validate against shared supported locale enum before storing. |
| T-23-16-02 | Tampering | locale routing preference | mitigate | Keep URL-first precedence and never auto-redirect per D-05/D-06. |
| T-23-16-03 | Repudiation | preference updates | accept | Locale preference is low-risk user setting; authenticated API write is sufficient for launch foundation. |
</threat_model>

<verification>
Run API user tests or typecheck fallback and greps for preferredLocale, aria-current, and suggestion copy.
</verification>

<success_criteria>
Explicit locale choice is visible, suggest-never-redirect is preserved, and logged-in locale preference persists.
</success_criteria>

<output>
After completion, create `.planning/phases/23-launch-foundation/23-16-SUMMARY.md`.
</output>
