---
phase: 23-launch-foundation
plan: 14
type: execute
wave: 3
depends_on:
  - 23-01
files_modified:
  - apps/web/lib/i18n/format.ts
  - apps/web/lib/i18n/format.test.ts
  - apps/web/components/i18n/kst-time.tsx
  - apps/web/components/i18n/currency-display.tsx
  - apps/web/components/i18n/__tests__/format-components.test.tsx
  - apps/web/app/performance/[id]/page.tsx
  - apps/web/app/performance/[id]/__tests__/performance-detail-formatting.test.tsx
autonomous: true
requirements:
  - I18N-02
must_haves:
  truths:
    - "Event-critical time displays include a KST anchor and may also show localized local time."
    - "Prices show KRW source amount plus estimated localized conversion with a disclaimer."
    - "Current public performance detail page uses the KST/KRW components for visible event date and price output."
  artifacts:
    - path: "apps/web/lib/i18n/format.ts"
      provides: "KST/local time and KRW/local currency formatting helpers"
    - path: "apps/web/components/i18n/kst-time.tsx"
      provides: "KST anchored time display component"
    - path: "apps/web/components/i18n/currency-display.tsx"
      provides: "KRW anchored currency display component"
    - path: "apps/web/app/performance/[id]/page.tsx"
      provides: "Public performance detail wiring for KST/KRW display"
  key_links:
    - from: "apps/web/components/i18n/kst-time.tsx"
      to: "apps/web/lib/i18n/format.ts"
      via: "formatEventTimeWithKstAnchor"
      pattern: "formatEventTimeWithKstAnchor"
    - from: "apps/web/components/i18n/currency-display.tsx"
      to: "apps/web/lib/i18n/format.ts"
      via: "formatKrwWithEstimate"
      pattern: "formatKrwWithEstimate"
    - from: "apps/web/app/performance/[id]/page.tsx"
      to: "apps/web/components/i18n/kst-time.tsx, apps/web/components/i18n/currency-display.tsx"
      via: "public performance detail date and price rendering"
      pattern: "KstTime|CurrencyDisplay"
---

<objective>
KST/KRW formatting helpers and display components를 만든다.

Purpose: D-07 요구인 event-critical KST anchor와 KRW source amount/disclaimer를 UI 전반에서 재사용 가능한 단위로 제공한다.
Output: formatting helpers, `KstTime`, `CurrencyDisplay`, public performance detail wiring, unit/component/page tests.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-launch-foundation/23-CONTEXT.md
@.planning/phases/23-launch-foundation/23-RESEARCH.md
@.planning/phases/23-launch-foundation/23-UI-SPEC.md
@packages/shared/src/constants/locales.ts
@apps/web/app/fonts.ts
@apps/web/components/ui/badge.tsx
@apps/web/app/performance/[id]/page.tsx

<interfaces>
D-07 formatting contract:
```text
Event-critical time: 2026.07.04 18:00 KST plus optional localized local time.
Currency: KRW source price plus estimated conversion and exchange-rate disclaimer.
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement KST/KRW formatting helpers and components</name>
  <files>apps/web/lib/i18n/format.ts, apps/web/lib/i18n/format.test.ts, apps/web/components/i18n/kst-time.tsx, apps/web/components/i18n/currency-display.tsx, apps/web/components/i18n/__tests__/format-components.test.tsx, apps/web/app/performance/[id]/page.tsx, apps/web/app/performance/[id]/__tests__/performance-detail-formatting.test.tsx</files>
  <read_first>packages/shared/src/constants/locales.ts, apps/web/app/fonts.ts, apps/web/components/ui/badge.tsx, apps/web/app/performance/[id]/page.tsx, .planning/phases/23-launch-foundation/23-UI-SPEC.md</read_first>
  <behavior>
    - `formatEventTimeWithKstAnchor` always includes `KST` for event/booking-critical times.
    - Localized secondary time is generated with `Intl.DateTimeFormat` for `ko`, `en`, `th`, `zh-CN`, and `zh-TW`.
    - `formatKrwWithEstimate` always shows KRW source amount, estimated local amount, and disclaimer text.
    - Public performance detail replaces inline date/price rendering with `KstTime` and `CurrencyDisplay`.
    - Current public surface shows `KST`, KRW source price, estimated conversion, and disclaimer without waiting for later pages.
    - Formatting uses native `Intl` and does not add `date-fns` unless native APIs cannot satisfy tests.
  </behavior>
  <action>Create `apps/web/lib/i18n/format.ts` exporting `formatEventTimeWithKstAnchor(input, locale, options)` and `formatKrwWithEstimate(krwAmount, locale, exchangeRate)`. Create small presentational components `KstTime` and `CurrencyDisplay` that use the helpers and UI-SPEC warning/informational copy. Wire both components into `apps/web/app/performance/[id]/page.tsx`: replace the local inline `formatDate`/`formatPrice` usage in the visible performance detail info panel and price tier list with `KstTime` and `CurrencyDisplay`. Use the active locale from the Phase 23 i18n routing/request helper when available, otherwise default to `ko`; do not introduce build-time locale constants that conflict with Plan 23-04. Use KRW as canonical source and make conversion explicitly estimated with wording equivalent to `exchange rate may change` per D-07. Add `performance-detail-formatting.test.tsx` proving the existing public detail page renders `KST`, KRW source amount, estimated conversion, and disclaimer for a fixture performance.</action>
  <verify>
    <automated>pnpm --filter @grabit/web test -- format.test.ts format-components.test.tsx performance-detail-formatting.test.tsx && grep -R "KST" apps/web/lib/i18n apps/web/components/i18n apps/web/app/performance && grep -R "exchange rate may change\\|환율" apps/web/lib/i18n apps/web/components/i18n apps/web/app/performance && grep -R "KstTime\\|CurrencyDisplay" 'apps/web/app/performance/[id]/page.tsx'</automated>
  </verify>
  <acceptance_criteria>Tests cover all five locales, KST anchor, KRW source amount, estimated conversion, disclaimer text, and the current public performance detail surface. `apps/web/app/performance/[id]/page.tsx` imports and renders `KstTime` and `CurrencyDisplay` instead of relying only on reusable component tests.</acceptance_criteria>
  <done>D-07 time/currency formatting is implemented and wired into a visible public event/detail surface with automated tests.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| locale/currency input -> UI formatting | Locale and exchange-rate inputs affect user-visible pricing and event time interpretation. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-14-01 | Tampering | `formatKrwWithEstimate` | mitigate | Always show KRW source amount and estimated-conversion disclaimer; never replace canonical KRW. |
| T-23-14-02 | Repudiation | event time display | mitigate | Event-critical displays always include explicit `KST` anchor. |
| T-23-14-03 | Tampering | exchange rate | accept | Live FX source is later payment/ops scope; this plan labels conversion as estimated and disclaimer-bound. |
</threat_model>

<verification>
Run formatting helper/component/page tests and greps for KST/KRW/disclaimer text on the public performance detail route.
</verification>

<success_criteria>
I18N-02 has reusable KST/KRW formatting helpers and visible public performance detail usage.
</success_criteria>

<output>
After completion, create `.planning/phases/23-launch-foundation/23-14-SUMMARY.md`.
</output>
