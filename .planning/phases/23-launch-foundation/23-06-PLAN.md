---
phase: 23-launch-foundation
plan: 06
type: execute
wave: 3
depends_on:
  - 23-01
  - 23-02
files_modified:
  - apps/api/src/modules/auth/auth.controller.ts
  - apps/api/src/modules/auth/auth.controller.spec.ts
  - apps/api/src/modules/auth/auth.service.ts
  - apps/api/src/modules/auth/auth.service.spec.ts
  - apps/api/src/modules/auth/auth.module.ts
  - apps/api/src/modules/auth/email/email.service.ts
  - apps/api/src/modules/auth/email/email.service.spec.ts
  - apps/api/src/modules/auth/email/templates/email-verification.tsx
  - apps/api/src/modules/auth/email/templates/email-verification.copy.ts
  - apps/api/src/modules/auth/email/templates/email-verification.copy.spec.ts
  - apps/api/src/modules/sms/sms.service.ts
  - apps/api/src/modules/sms/sms.service.spec.ts
  - apps/api/src/modules/sms/sms-copy.ts
  - apps/api/src/modules/sms/sms-copy.spec.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - I18N-02
must_haves:
  truths:
    - "Kakao, Naver, Google, and email auth remain supported; LINE remains absent."
    - "Email verification tokens expire after 30 minutes, support immediate resend, and latest token wins."
    - "Email verification template copy and SMS OTP template/copy exist for exactly `ko`, `en`, `th`, `zh-CN`, and `zh-TW`."
    - "Refresh token family cap keeps three active device families and revokes the oldest on fourth login."
    - "Refresh token reuse still revokes only the reused family, not every user session."
  artifacts:
    - path: "apps/api/src/modules/auth/auth.service.ts"
      provides: "Email verification and three-device policy"
    - path: "apps/api/src/modules/auth/email/templates/email-verification.tsx"
      provides: "Localized verification email template"
    - path: "apps/api/src/modules/auth/email/templates/email-verification.copy.ts"
      provides: "Five-locale email verification subject/body/status copy"
    - path: "apps/api/src/modules/sms/sms.service.ts"
      provides: "Five launch market SMS validation"
    - path: "apps/api/src/modules/sms/sms-copy.ts"
      provides: "Five-locale SMS OTP template and status copy"
  key_links:
    - from: "apps/api/src/modules/auth/auth.service.ts"
      to: "apps/api/src/database/schema/email-verification-tokens.ts"
      via: "hashed verification token rows"
      pattern: "emailVerificationTokens"
    - from: "apps/api/src/modules/auth/auth.service.ts"
      to: "apps/api/src/database/schema/refresh-tokens.ts"
      via: "distinct family cap"
      pattern: "family"
    - from: "apps/api/src/modules/auth/email/templates/email-verification.tsx"
      to: "apps/api/src/modules/auth/email/templates/email-verification.copy.ts"
      via: "locale copy lookup"
      pattern: "emailVerificationCopy"
    - from: "apps/api/src/modules/sms/sms.service.ts"
      to: "apps/api/src/modules/sms/sms-copy.ts"
      via: "OTP message template lookup"
      pattern: "smsOtpCopy"
---

<objective>
Auth launch foundation을 LINE 없이 확장한다.

Purpose: 이메일 인증, 3-device session policy, launch-country SMS validation을 추가하면서 기존 소셜 로그인과 refresh-token theft handling을 깨지 않게 한다.
Output: Auth endpoints/service changes, email template, SMS validation extension, focused tests.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-launch-foundation/23-CONTEXT.md
@.planning/phases/23-launch-foundation/23-VALIDATION.md
@.planning/phases/23-launch-foundation/23-PATTERNS.md
@apps/api/src/modules/auth/auth.controller.ts
@apps/api/src/modules/auth/auth.service.ts
@apps/api/src/modules/auth/auth.module.ts
@apps/api/src/modules/auth/email/email.service.ts
@apps/api/src/modules/auth/strategies/kakao.strategy.ts
@apps/api/src/modules/auth/strategies/naver.strategy.ts
@apps/api/src/modules/auth/strategies/google.strategy.ts
@apps/api/src/modules/sms/sms.service.ts
@apps/api/src/modules/sms/phone.util.ts

<interfaces>
Existing refresh theft behavior to preserve:
```typescript
if (tokenRecord.revokedAt) {
  await db.update(refreshTokens).set({ revokedAt: new Date() }).where(eq(refreshTokens.family, tokenRecord.family));
  throw new UnauthorizedException(...);
}
```
Email verification behavior:
```text
30 minute expiry, opaque token hash, immediate resend, resend rate limit, latest-token-wins, consumed state
```

Launch localized transactional copy contract:
```text
locales: ko, en, th, zh-CN, zh-TW
emailVerification keys: subject, bodyIntro, verifyCta, resendCta, resendLoading, resendSuccess, expired, verified, throttled, systemError
smsOtp keys: template, sent, resendCta, resendLoading, resendSuccess, expired, invalidCode, throttled, systemError
auth status/error keys: invalidCredentials, emailUnverified, verificationRequired, deviceLimitNotice, providerUnavailable
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add email verification issue/resend/consume flow</name>
  <files>apps/api/src/modules/auth/auth.controller.ts, apps/api/src/modules/auth/auth.controller.spec.ts, apps/api/src/modules/auth/auth.service.ts, apps/api/src/modules/auth/auth.service.spec.ts, apps/api/src/modules/auth/email/email.service.ts, apps/api/src/modules/auth/email/email.service.spec.ts, apps/api/src/modules/auth/email/templates/email-verification.tsx, apps/api/src/modules/auth/email/templates/email-verification.copy.ts, apps/api/src/modules/auth/email/templates/email-verification.copy.spec.ts</files>
  <read_first>apps/api/src/modules/auth/auth.controller.ts, apps/api/src/modules/auth/auth.service.ts, apps/api/src/modules/auth/email/email.service.ts, apps/api/src/modules/auth/email/templates/password-reset.tsx, apps/api/src/database/schema/email-verification-tokens.ts</read_first>
  <behavior>
    - Verification token expires 30 minutes after issue.
    - Resend is immediately visible but controller retains throttle/rate-limit protection.
    - Resend invalidates prior unconsumed tokens for the same user/email purpose.
    - Consuming an expired, consumed, or superseded token returns distinct errors.
    - Email verification subject/body/status copy exists for exactly `ko`, `en`, `th`, `zh-CN`, and `zh-TW`.
  </behavior>
  <action>Add endpoints under `/api/v1/auth/email-verification`: `POST request`, `POST resend`, and `POST verify`. Use opaque random token, store SHA-256 hash in `email_verification_tokens`, set `expiresAt = now + 30 minutes`, and set older unconsumed rows `supersededAt` or equivalent before inserting latest token. Create `email-verification.copy.ts` exporting `emailVerificationCopy` keyed by exactly `ko`, `en`, `th`, `zh-CN`, and `zh-TW` with keys `subject`, `bodyIntro`, `verifyCta`, `resendCta`, `resendLoading`, `resendSuccess`, `expired`, `verified`, `throttled`, and `systemError`. Extend `EmailService` with `sendEmailVerificationEmail(to, verificationLink, locale)` and template copy including Korean `인증 메일 다시 보내기`, `다시 보내는 중...`, `인증 메일을 다시 보냈습니다`, and `인증 링크가 만료되었습니다. 새 인증 메일을 요청해주세요.` where relevant. Do not expose raw token in logs or tests.</action>
  <verify>
    <automated>pnpm --filter @grabit/api test -- auth.service.spec.ts auth.controller.spec.ts email.service.spec.ts email-verification.copy.spec.ts && grep -R "30 \\* 60 \\* 1000\\|1800000" apps/api/src/modules/auth && grep -R "zh-TW" apps/api/src/modules/auth/email/templates/email-verification.copy.ts</automated>
  </verify>
  <acceptance_criteria>Tests cover 30-minute expiry, immediate resend, latest-token-wins, consumed token rejection, no raw token logging, and all ten `emailVerificationCopy` keys for exactly `ko`, `en`, `th`, `zh-CN`, and `zh-TW`.</acceptance_criteria>
  <done>Email verification satisfies AUTH-01 without changing existing login providers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce three active refresh-token families per user</name>
  <files>apps/api/src/modules/auth/auth.service.ts, apps/api/src/modules/auth/auth.service.spec.ts</files>
  <read_first>apps/api/src/modules/auth/auth.service.ts, apps/api/src/modules/auth/auth.service.spec.ts, apps/api/src/database/schema/refresh-tokens.ts, .planning/STATE.md</read_first>
  <behavior>
    - Each new login creates a new refresh-token family/device slot.
    - Fourth active family revokes the oldest active family for that user.
    - Existing reuse/theft handling revokes the reused family only.
    - User-visible notice can be returned after oldest session revocation.
  </behavior>
  <action>Add helper `enforceRefreshFamilyLimit(userId, maxFamilies = 3)` that queries distinct active families by user where `revokedAt is null` and `expiresAt > now`, ordered by oldest `createdAt`. Call it after creating a new login family in register/login/social completion flows. Return or include notice `다른 기기에서 로그인되어 가장 오래된 세션이 종료되었습니다.` when a family is revoked. Do not change revoked-token reuse behavior to revoke all user sessions.</action>
  <verify>
    <automated>pnpm --filter @grabit/api test -- auth.service.spec.ts && grep -R "가장 오래된 세션이 종료되었습니다" apps/api/src/modules/auth/auth.service.ts</automated>
  </verify>
  <acceptance_criteria>Tests cover 1-3 active families kept, fourth login revokes oldest, refresh within same family does not consume a new slot, and reused revoked token revokes only its family.</acceptance_criteria>
  <done>AUTH-02 session cap is enforced through refresh token families.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add launch-market SMS validation and LINE absence gates</name>
  <files>apps/api/src/modules/sms/sms.service.ts, apps/api/src/modules/sms/sms.service.spec.ts, apps/api/src/modules/sms/sms-copy.ts, apps/api/src/modules/sms/sms-copy.spec.ts, apps/api/src/modules/auth/auth.module.ts, apps/api/src/modules/auth/auth.controller.spec.ts</files>
  <read_first>apps/api/src/modules/sms/sms.service.ts, apps/api/src/modules/sms/sms.service.spec.ts, apps/api/src/modules/sms/phone.util.ts, apps/api/src/modules/auth/auth.module.ts, apps/api/src/modules/auth/strategies/kakao.strategy.ts, apps/api/src/modules/auth/strategies/naver.strategy.ts, apps/api/src/modules/auth/strategies/google.strategy.ts</read_first>
  <behavior>
    - Launch-country SMS validation covers Korea, US/English fallback, Thailand, China, Taiwan/Hong Kong-style Chinese labels as applicable to phone metadata.
    - Unsupported country selection remains possible unless provider evidence blocks it.
    - SMS OTP provider message template and auth status/error copy exist for exactly `ko`, `en`, `th`, `zh-CN`, and `zh-TW`.
    - No LINE strategy/provider/callback is registered.
  </behavior>
  <action>Extend SMS service validation/tests to explicitly cover five launch markets using `libphonenumber-js` strict parsing and existing Valkey hash-tag/rate-limit behavior. Create `sms-copy.ts` exporting `smsOtpCopy` keyed by exactly `ko`, `en`, `th`, `zh-CN`, and `zh-TW` with keys `template`, `sent`, `resendCta`, `resendLoading`, `resendSuccess`, `expired`, `invalidCode`, `throttled`, and `systemError`; `template` must include an OTP placeholder and never log/store a raw OTP outside the provider payload. Add auth status/error copy tests for `invalidCredentials`, `emailUnverified`, `verificationRequired`, `deviceLimitNotice`, and `providerUnavailable` across all five locales. Add auth module/controller tests asserting provider list includes Kakao, Naver, Google and does not include `line`, `LineStrategy`, `passport-line`, `/auth/line`, or LINE env vars. Preserve Phase 22 invalid phone 400 behavior.</action>
  <verify>
    <automated>pnpm --filter @grabit/api test -- sms.service.spec.ts sms-copy.spec.ts auth.controller.spec.ts && grep -R "zh-TW" apps/api/src/modules/sms/sms-copy.ts && ! (grep -R "LineStrategy\\|passport-line\\|/auth/line\\|LINE_CLIENT" apps/api/src apps/web/components/auth packages/shared/src)</automated>
  </verify>
  <acceptance_criteria>SMS tests include launch market examples; `sms-copy.spec.ts` asserts every SMS OTP template/status key and auth error/status key for exactly `ko`, `en`, `th`, `zh-CN`, and `zh-TW`; grep finds no LINE implementation identifiers in API/web/shared source.</acceptance_criteria>
  <done>AUTH-01 is covered as Kakao/Naver/Google/email plus email verification, with LINE explicitly absent.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| email link -> auth API | Verification tokens prove email ownership and are replay targets. |
| refresh cookie -> session store | Refresh token families represent device sessions and theft handling. |
| phone input -> SMS provider | User input reaches Infobip and Valkey rate-limit keys. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Elevation of privilege | booking APIs | accept | Booking hard gate is Plan 03. |
| T-23-02 | Tampering | localized auth copy | mitigate | Locale-aware email/SMS copy uses shared locale constants and no redirect side effects. |
| T-23-03 | Repudiation | legal-sensitive email copy | accept | Legal lock is Plan 05; auth emails are transactional. |
| T-23-04 | Spoofing | email verification, refresh tokens | mitigate | Hashed latest-token-wins tokens, 30-minute expiry, consumed state, throttled resend, family cap, and family-wide reuse revoke. |
| T-23-05 | Repudiation | SMS verification evidence | mitigate | Preserve rate-limit and sanitized logging; tests assert invalid phone 400 before provider side effects. |
| T-23-06 | Information disclosure | auth/SMS logs | mitigate | Do not log raw verification tokens, OTPs, reset links, or phone PII beyond existing masked patterns. |
</threat_model>

<verification>
Run targeted auth, email, and SMS unit tests plus API typecheck. Grep for forbidden LINE implementation identifiers.
</verification>

<success_criteria>
Email verification, three-device refresh family policy, launch-market SMS validation, and no-LINE scope are all enforced by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/23-launch-foundation/23-06-SUMMARY.md`.
</output>
