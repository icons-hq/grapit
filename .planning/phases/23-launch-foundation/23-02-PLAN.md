---
phase: 23-launch-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - apps/api/src/database/schema/users.ts
  - apps/api/src/database/schema/refresh-tokens.ts
  - apps/api/src/database/schema/email-verification-tokens.ts
  - apps/api/src/database/schema/consent-items.ts
  - apps/api/src/database/schema/consent-audit-logs.ts
  - apps/api/src/database/schema/translation-sources.ts
  - apps/api/src/database/schema/translation-drafts.ts
  - apps/api/src/database/schema/legal-content.ts
  - apps/api/src/database/schema/index.ts
  - apps/api/src/database/migrations/0007_phase23_launch_foundation.sql
  - apps/api/src/database/migrations/meta/0007_snapshot.json
  - apps/api/src/database/migrations/meta/_journal.json
autonomous: true
requirements:
  - FLAG-01
  - I18N-01
  - I18N-02
  - TRANS-01
  - TRANS-02
  - AUTH-01
  - AUTH-02
  - COMP-01
  - COMP-02
must_haves:
  truths:
    - "Database schema expands without dropping or renaming existing production columns/tables."
    - "Users can store preferred locale without invalidating existing users."
    - "Consent, translation, legal lock, email verification, and active refresh-family queries have schema support."
  artifacts:
    - path: "apps/api/src/database/schema/users.ts"
      provides: "preferred_locale column"
      contains: "preferredLocale"
    - path: "apps/api/src/database/schema/consent-audit-logs.ts"
      provides: "Immutable consent audit table"
      contains: "consentAuditLogs"
    - path: "apps/api/src/database/migrations/0007_phase23_launch_foundation.sql"
      provides: "Phase 23 expand-only migration"
  key_links:
    - from: "apps/api/src/database/schema/index.ts"
      to: "apps/api/src/database/schema/consent-audit-logs.ts"
      via: "schema export"
      pattern: "consentAuditLogs"
    - from: "apps/api/src/database/migrations/0007_phase23_launch_foundation.sql"
      to: "apps/api/src/database/schema/*.ts"
      via: "drizzle-kit generate"
      pattern: "CREATE TABLE|ALTER TABLE"
---

<objective>
Phase 23의 모든 schema-relevant 변경을 한 번에 expand-only로 만든다.

Purpose: 이후 API/web 구현이 안정적인 DB contract 위에서 진행되고, migration apply가 최종 검증 전에 실제로 확인되도록 한다.
Output: Drizzle schema files, generated SQL migration, reviewed/applied migration evidence.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/23-launch-foundation/23-CONTEXT.md
@.planning/phases/23-launch-foundation/23-RESEARCH.md
@.planning/phases/23-launch-foundation/23-PATTERNS.md
@apps/api/drizzle.config.ts
@apps/api/src/database/schema/users.ts
@apps/api/src/database/schema/refresh-tokens.ts
@apps/api/src/database/schema/terms-agreements.ts
@apps/api/src/database/schema/index.ts

<interfaces>
Existing Drizzle pattern:
```typescript
export const refreshTokens = pgTable('refresh_tokens', {
  id: uuid('id').defaultRandom().primaryKey(),
  userId: uuid('user_id').notNull().references(() => users.id, { onDelete: 'cascade' }),
  family: varchar('family', { length: 255 }).notNull(),
  revokedAt: timestamp('revoked_at', { withTimezone: true }),
}, (table) => [
  index('idx_refresh_tokens_family').on(table.family),
]);
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add expand-only Phase 23 schema contracts</name>
  <files>apps/api/src/database/schema/users.ts, apps/api/src/database/schema/refresh-tokens.ts, apps/api/src/database/schema/email-verification-tokens.ts, apps/api/src/database/schema/consent-items.ts, apps/api/src/database/schema/consent-audit-logs.ts, apps/api/src/database/schema/translation-sources.ts, apps/api/src/database/schema/translation-drafts.ts, apps/api/src/database/schema/legal-content.ts, apps/api/src/database/schema/index.ts</files>
  <read_first>apps/api/src/database/schema/users.ts, apps/api/src/database/schema/refresh-tokens.ts, apps/api/src/database/schema/terms-agreements.ts, apps/api/src/database/schema/performances.ts, apps/api/src/database/schema/index.ts, .planning/phases/23-launch-foundation/23-CONTEXT.md</read_first>
  <behavior>
    - `users.preferred_locale` is nullable or defaulted to `ko` without breaking existing rows.
    - Email verification tokens are hashed, 30-minute expirable, consumable, and latest-token-wins by user/email purpose.
    - Consent audit rows are append-only and queryable by item, version, language, timestamp, IP, and user.
    - Legal content supports manual `ko`/`en` only and cannot be represented as translatable draft rows.
  </behavior>
  <action>Add `preferredLocale` column to `users`. Add active-family query indexes to `refresh_tokens` such as `(user_id, family, revoked_at, expires_at)` if Drizzle supports it without destructive change. Create new tables: `email_verification_tokens`, `consent_items`, `consent_audit_logs`, `translation_sources`, `translation_drafts`, and `legal_content`. Use enum values for supported locales `ko`, `en`, `th`, `zh-CN`, `zh-TW`, translation statuses `draft`, `review`, `published`, `stale`, and legal content types `legal`, `notice`, `refund`, `booking_guide`. Export all new schema from `schema/index.ts`.</action>
  <verify>
    <automated>pnpm --filter @grabit/api typecheck && grep -R "preferredLocale" apps/api/src/database/schema/users.ts && grep -R "consentAuditLogs" apps/api/src/database/schema/index.ts && grep -R "emailVerificationTokens" apps/api/src/database/schema/index.ts</automated>
  </verify>
  <acceptance_criteria>`users.ts` contains `preferredLocale`; `schema/index.ts` exports `emailVerificationTokens`, `consentItems`, `consentAuditLogs`, `translationSources`, `translationDrafts`, and `legalContent`; no existing table/column is dropped or renamed.</acceptance_criteria>
  <done>All schema file modifications required by Phase 23 are complete before migration generation.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Generate, review, and apply Drizzle migration</name>
  <files>apps/api/src/database/migrations/0007_phase23_launch_foundation.sql, apps/api/src/database/migrations/meta/0007_snapshot.json, apps/api/src/database/migrations/meta/_journal.json</files>
  <read_first>apps/api/drizzle.config.ts, apps/api/src/database/schema/index.ts, apps/api/src/database/migrations/meta/_journal.json, AGENTS.md</read_first>
  <action>After Task 1 is complete, run `DOTENV_CONFIG_PATH=../../.env pnpm --filter @grabit/api exec drizzle-kit generate --name=phase23_launch_foundation`. Review the generated SQL and keep it expand-only: allowed statements are `CREATE TYPE`, `CREATE TABLE`, `ALTER TABLE ... ADD COLUMN`, `CREATE INDEX`, `CREATE UNIQUE INDEX`, and safe default/backfill statements. Disallow `DROP TABLE`, `DROP COLUMN`, `ALTER COLUMN ... TYPE`, destructive `RENAME`, and data deletion. Then run `DOTENV_CONFIG_PATH=../../.env pnpm --filter @grabit/api exec drizzle-kit migrate`. Use `drizzle-kit push` only as non-TTY fallback if migration metadata is unusable, and record that fallback in the SUMMARY. If DB credentials are unavailable, stop after generated migration + SQL review evidence and report the blocked apply explicitly.</action>
  <verify>
    <automated>DOTENV_CONFIG_PATH=../../.env pnpm --filter @grabit/api exec drizzle-kit generate --name=phase23_launch_foundation && ! (grep -Ei "DROP TABLE|DROP COLUMN|ALTER COLUMN .* TYPE|RENAME TO|DELETE FROM|TRUNCATE" apps/api/src/database/migrations/*phase23*launch*foundation*.sql) && DOTENV_CONFIG_PATH=../../.env pnpm --filter @grabit/api exec drizzle-kit migrate</automated>
  </verify>
  <acceptance_criteria>A migration matching `apps/api/src/database/migrations/*phase23*launch*foundation*.sql` exists; grep for destructive SQL returns no matches; `drizzle-kit migrate` completes or the SUMMARY records credential-unavailable evidence without pretending it ran.</acceptance_criteria>
  <done>Schema apply/verification gate is complete before Phase 23 final verification.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| schema -> production DB | Migration changes affect existing users, sessions, reservations, and legal/audit evidence. |
| operator query -> PII | Consent audit rows include personal identifiers and IP. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering | migration SQL | mitigate | Expand-only SQL review blocks destructive changes that could break production booking/auth data. |
| T-23-02 | Denial of service | locale schema | mitigate | Nullable/defaulted `preferred_locale` avoids blocking existing user reads. |
| T-23-03 | Repudiation | `legal_content`, `translation_drafts` | mitigate | Separate legal table and content types prevent legal copy from entering machine translation rows. |
| T-23-04 | Spoofing | `email_verification_tokens`, `refresh_tokens` | mitigate | Hashed tokens, expiry, consumed state, and active-family indexes support replay/theft controls. |
| T-23-05 | Repudiation | `consent_audit_logs` | mitigate | Append-only row model captures item/version/language/timestamp/IP/user. |
| T-23-06 | Information disclosure | consent audit schema | mitigate | Schema supports queryability while later API/UI masks PII by default. |
</threat_model>

<verification>
Run API typecheck, generate migration, destructive SQL grep, and migration apply command. The schema apply task is blocking.
</verification>

<success_criteria>
All DB contracts needed by flags, locale preference, translation/legal lock, email verification, refresh-family cap, and consent/audit exist and are migrated expand-only.
</success_criteria>

<output>
After completion, create `.planning/phases/23-launch-foundation/23-02-SUMMARY.md`.
</output>
