---
phase: 23-launch-foundation
plan: 09
type: execute
wave: 4
depends_on:
  - 23-06
  - 23-15
files_modified:
  - apps/web/components/auth/email-verification-status.tsx
  - apps/web/components/auth/__tests__/auth-email-verification.test.tsx
  - apps/web/components/auth/phone-verification.tsx
  - apps/web/components/auth/__tests__/phone-verification.test.tsx
  - apps/web/components/auth/login-form.tsx
  - apps/web/messages/ko.json
  - apps/web/messages/en.json
  - apps/web/messages/th.json
  - apps/web/messages/zh-CN.json
  - apps/web/messages/zh-TW.json
autonomous: true
requirements:
  - I18N-02
  - AUTH-01
  - AUTH-02
must_haves:
  truths:
    - "Email verification UI shows sent, resend, expired, verified, loading, throttled, and error states in all five launch locales."
    - "SMS OTP UI shows send/resend/loading/success/expired/invalid/throttled/system states in all five launch locales."
    - "Auth status copy distinguishes invalid credentials, email-unverified, verification-required, provider-unavailable, and three-device notices."
  artifacts:
    - path: "apps/web/components/auth/email-verification-status.tsx"
      provides: "Email verification state UI"
    - path: "apps/web/components/auth/phone-verification.tsx"
      provides: "Localized SMS OTP state UI"
    - path: "apps/web/messages/ko.json"
      provides: "Auth/email/SMS localized message namespace mirrored in en/th/zh-CN/zh-TW"
  key_links:
    - from: "apps/web/components/auth/email-verification-status.tsx"
      to: "apps/web/messages/*.json"
      via: "auth.emailVerification namespace"
      pattern: "auth\\.emailVerification|emailVerification"
    - from: "apps/web/components/auth/phone-verification.tsx"
      to: "packages/shared/src/i18n/launch-copy-keys.ts"
      via: "manifest-aligned OTP copy"
      pattern: "auth\\.otp|otp"
---

<objective>
Auth, email verification, SMS OTP UI copy를 five-locale launch contract에 맞춘다.

Purpose: AUTH-01/AUTH-02와 I18N-02가 API에서만 존재하지 않고 사용자가 구분 가능한 상태로 볼 수 있게 한다.
Output: email verification component, localized OTP/auth status copy, five-locale message keys, tests.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-launch-foundation/23-CONTEXT.md
@.planning/phases/23-launch-foundation/23-UI-SPEC.md
@packages/shared/src/i18n/launch-copy-keys.ts
@apps/web/components/auth/phone-verification.tsx
@apps/web/components/auth/login-form.tsx

<interfaces>
Required auth/OTP/email UI namespaces:
```text
locales: ko, en, th, zh-CN, zh-TW
auth.emailVerification: sent, resendCta, resendLoading, resendSuccess, expired, verified, throttled, systemError
auth.otp: sent, resendCta, resendLoading, resendSuccess, expired, invalidCode, throttled, systemError
auth.errors: invalidCredentials, emailUnverified, verificationRequired, providerUnavailable, deviceLimitNotice
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add localized email verification, OTP, and auth status states to auth UI</name>
  <files>apps/web/components/auth/email-verification-status.tsx, apps/web/components/auth/__tests__/auth-email-verification.test.tsx, apps/web/components/auth/phone-verification.tsx, apps/web/components/auth/__tests__/phone-verification.test.tsx, apps/web/components/auth/login-form.tsx, apps/web/messages/ko.json, apps/web/messages/en.json, apps/web/messages/th.json, apps/web/messages/zh-CN.json, apps/web/messages/zh-TW.json</files>
  <read_first>apps/web/components/auth/phone-verification.tsx, apps/web/components/auth/login-form.tsx, apps/web/lib/api-client.ts, packages/shared/src/i18n/launch-copy-keys.ts, .planning/phases/23-launch-foundation/23-UI-SPEC.md</read_first>
  <behavior>
    - Email verification UI shows sent, resend, loading, success, expired, verified, throttled, and system error states.
    - SMS OTP UI shows sent, resend, loading, success, expired, invalid code, throttled, and system error states.
    - Auth error/status UI distinguishes invalid credentials, email unverified, verification required, provider unavailable, and three-device notice.
    - All namespaces exist for exactly `ko`, `en`, `th`, `zh-CN`, and `zh-TW`.
    - Korean email resend button text is `인증 메일 다시 보내기`; loading is `다시 보내는 중...`.
    - Korean expired email copy is `인증 링크가 만료되었습니다. 새 인증 메일을 요청해주세요.`
  </behavior>
  <action>Create `EmailVerificationStatus` client component and wire it into auth/signup flow where email verification is required per D-14. Extend `phone-verification.tsx` to read localized OTP status copy instead of hardcoded Korean strings per D-08. Add `auth.emailVerification`, `auth.otp`, and `auth.errors` namespaces to `apps/web/messages/{ko,en,th,zh-CN,zh-TW}.json` with the keys listed in this plan's interfaces block and aligned with `launch-copy-keys.ts`. Use existing API client patterns for request/resend/verify endpoints. Use `aria-live="polite"` for resend/success status and `role="alert"` for expired/error states. Surface D-15 three-device notice copy without changing session logic implemented in Plan 23-06.</action>
  <verify>
    <automated>pnpm --filter @grabit/web test -- auth-email-verification.test.tsx phone-verification.test.tsx && grep -R "인증 메일 다시 보내기" apps/web/components/auth apps/web/messages/ko.json && test -f apps/web/messages/zh-TW.json</automated>
  </verify>
  <acceptance_criteria>`auth-email-verification.test.tsx` and `phone-verification.test.tsx` assert every `auth.emailVerification`, `auth.otp`, and `auth.errors` key exists for exactly `ko`, `en`, `th`, `zh-CN`, and `zh-TW`; UI tests cover sent, resend, loading, success, expired, verified/invalid, throttled, and system error states.</acceptance_criteria>
  <done>AUTH-01/I18N-02 email verification, SMS OTP, and auth error/status copy have visible localized UI states.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser UI -> auth/email/SMS APIs | UI displays auth status from server responses and user input. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-09-01 | Spoofing | email verification UI | mitigate | UI calls token-backed API and distinguishes expired/resend/throttled states. |
| T-23-09-02 | Information disclosure | auth UI errors | mitigate | Do not display raw tokens, OTPs, reset links, provider secrets, or full PII. |
| T-23-09-03 | Tampering | localized copy | mitigate | Tests enforce exact five-locale namespace coverage from shared manifest. |
</threat_model>

<verification>
Run web auth/email/SMS UI tests and greps for five-locale message files.
</verification>

<success_criteria>
Auth, email verification, and SMS OTP states are localized for all five launch locales with distinguishable error/status states.
</success_criteria>

<output>
After completion, create `.planning/phases/23-launch-foundation/23-09-SUMMARY.md`.
</output>
