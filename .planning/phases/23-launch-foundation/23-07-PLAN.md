---
phase: 23-launch-foundation
plan: 07
type: execute
wave: 4
depends_on:
  - 23-01
  - 23-02
  - 23-04
  - 23-05
  - 23-06
files_modified:
  - apps/api/src/modules/consent/consent.module.ts
  - apps/api/src/modules/consent/consent.controller.ts
  - apps/api/src/modules/consent/consent.service.ts
  - apps/api/src/modules/consent/consent.service.spec.ts
  - apps/api/src/modules/consent/consent-audit.controller.ts
  - apps/api/src/modules/consent/consent-audit.controller.spec.ts
  - apps/api/src/app.module.ts
  - apps/api/src/modules/auth/auth.service.ts
  - apps/api/src/modules/reservation/reservation.service.ts
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - AUTH-01
  - FLAG-02
must_haves:
  truths:
    - "Signup and fanmeet booking gates require itemized required consent, including cross-border transfer."
    - "Under-14 users are blocked without guardian consent flow."
    - "Consent audit rows are immutable and queryable by item, version, language, timestamp, IP, and user."
    - "Operator/admin query output masks PII by default."
  artifacts:
    - path: "apps/api/src/modules/consent/consent.service.ts"
      provides: "Consent capture and immutable audit write"
    - path: "apps/api/src/modules/consent/consent-audit.controller.ts"
      provides: "Admin/internal query surface"
    - path: "apps/api/src/modules/auth/auth.service.ts"
      provides: "Signup consent gate integration"
  key_links:
    - from: "apps/api/src/modules/auth/auth.service.ts"
      to: "apps/api/src/modules/consent/consent.service.ts"
      via: "signup consent capture"
      pattern: "ConsentService"
    - from: "apps/api/src/modules/consent/consent-audit.controller.ts"
      to: "apps/api/src/database/schema/consent-audit-logs.ts"
      via: "query filters"
      pattern: "itemKey|version|language|timestamp|ip|user"
---

<objective>
Multinational consent와 audit foundation을 API에서 구현한다.

Purpose: 가입/예매 consent evidence가 boolean agreement가 아니라 item/version/language/IP/user/timestamp 단위의 immutable row로 남고, 운영자가 masked 상태로 조회할 수 있게 한다.
Output: Consent module/service/controller, audit query endpoint, auth/reservation gate integration, tests.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-launch-foundation/23-CONTEXT.md
@.planning/phases/23-launch-foundation/23-VALIDATION.md
@.planning/phases/23-launch-foundation/23-PATTERNS.md
@packages/shared/src/schemas/consent.schema.ts
@apps/api/src/modules/auth/auth.service.ts
@apps/api/src/modules/reservation/reservation.service.ts
@apps/api/src/modules/admin/admin-dashboard.controller.ts
@apps/api/src/common/guards/roles.guard.ts

<interfaces>
Required item keys:
```text
terms, privacy, pipa_required, cross_border_transfer, pdpa_notice, pipl_notice, marketing
```
Required blocks:
```text
cross_border_transfer refused -> signup/fanmeet booking cannot proceed
under 14 -> blocked, no guardian flow
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement itemized consent capture service</name>
  <files>apps/api/src/modules/consent/consent.module.ts, apps/api/src/modules/consent/consent.controller.ts, apps/api/src/modules/consent/consent.service.ts, apps/api/src/modules/consent/consent.service.spec.ts, apps/api/src/app.module.ts</files>
  <read_first>packages/shared/src/schemas/consent.schema.ts, apps/api/src/database/schema/consent-items.ts, apps/api/src/database/schema/consent-audit-logs.ts, apps/api/src/common/pipes/zod-validation.pipe.ts, apps/api/src/modules/user/user.controller.ts</read_first>
  <behavior>
    - Consent capture writes one immutable audit row per item/version/language.
    - Required items block when missing/refused.
    - Marketing remains optional and separate.
    - Under-14 is blocked with `만 14세 미만은 가입할 수 없습니다`.
  </behavior>
  <action>Create `ConsentModule`, `ConsentController`, and `ConsentService` with methods `getActiveConsentItems(locale)`, `captureConsent(userId, dto, requestMeta)`, `assertRequiredConsents(dto)`, and `assertAgeAllowed(dateOfBirth)`. Persist immutable rows with item key, version, language, timestamp, user ID, IP, user agent, source flow `signup` or `booking`, and accepted/refused state. Use shared zod schemas via `ZodValidationPipe`.</action>
  <verify>
    <automated>pnpm --filter @grabit/api test -- consent.service.spec.ts && grep -R "만 14세 미만은 가입할 수 없습니다" apps/api/src/modules/consent</automated>
  </verify>
  <acceptance_criteria>Tests cover required item missing, cross-border refused, marketing optional, under-14 blocked, and one row per item/version/language.</acceptance_criteria>
  <done>COMP-01 consent capture is backed by immutable itemized evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add masked consent audit query endpoint</name>
  <files>apps/api/src/modules/consent/consent-audit.controller.ts, apps/api/src/modules/consent/consent-audit.controller.spec.ts, apps/api/src/modules/consent/consent.service.ts</files>
  <read_first>apps/api/src/modules/admin/admin-dashboard.controller.ts, apps/api/src/common/guards/roles.guard.ts, apps/api/src/common/decorators/roles.decorator.ts, apps/api/src/database/schema/consent-audit-logs.ts, packages/shared/src/schemas/consent.schema.ts</read_first>
  <behavior>
    - Admin can filter by item, version, language, timestamp range, IP, and user.
    - Email, phone, and IP are masked by default.
    - Raw PII is not returned unless an existing admin permission pattern explicitly supports it.
  </behavior>
  <action>Create `/api/v1/admin/consent-audit` guarded by `RolesGuard` and `@Roles('admin')`. Accept query fields `itemKey`, `version`, `language`, `from`, `to`, `ip`, `userId`, and `email`. Return dense rows with `itemKey`, `version`, `language`, `maskedUser`, `maskedIp`, `timestamp`, `sourceFlow`, and `accepted`. Implement `maskEmail`, `maskPhone`, and `maskIp` helpers in service or controller tests.</action>
  <verify>
    <automated>pnpm --filter @grabit/api test -- consent-audit.controller.spec.ts consent.service.spec.ts && grep -R "maskedIp" apps/api/src/modules/consent</automated>
  </verify>
  <acceptance_criteria>Controller tests cover every COMP-02 filter and default masked output; no response fixture contains full email, phone, or IP unless explicitly named `raw` and guarded.</acceptance_criteria>
  <done>COMP-02 has an operator-queryable, PII-masked audit API.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire consent gates into signup and booking preparation</name>
  <files>apps/api/src/modules/auth/auth.service.ts, apps/api/src/modules/reservation/reservation.service.ts, apps/api/src/modules/consent/consent.service.ts, apps/api/src/modules/consent/consent.service.spec.ts</files>
  <read_first>apps/api/src/modules/auth/auth.service.ts, apps/api/src/modules/reservation/reservation.service.ts, packages/shared/src/schemas/auth.schema.ts, packages/shared/src/schemas/booking.schema.ts, .planning/phases/23-launch-foundation/23-CONTEXT.md</read_first>
  <behavior>
    - Signup cannot complete without required consent evidence.
    - Fanmeet booking prepare cannot proceed if required cross-border consent is refused/missing.
    - Booking disabled gate from Plan 03 still runs before reservation/payment side effects.
  </behavior>
  <action>Inject `ConsentService` into `AuthService` and `ReservationService` where needed. During register/social completion, capture consent rows from the signup DTO and block under-14. During `prepareReservation`, after `BOOKING_ENABLED` hard gate and before reservation transaction, assert required fanmeet booking consent exists for the user, including `cross_border_transfer`. Use error copy `국외이전 동의가 필요합니다. 동의하지 않으면 가입 또는 팬미팅 예매를 진행할 수 없습니다.`</action>
  <verify>
    <automated>pnpm --filter @grabit/api test -- auth.service.spec.ts reservation.service.spec.ts consent.service.spec.ts && grep -R "국외이전 동의가 필요합니다" apps/api/src/modules</automated>
  </verify>
  <acceptance_criteria>Tests prove missing cross-border consent blocks signup/booking; disabled booking still blocks before consent/transaction side effects when `BOOKING_ENABLED=false`.</acceptance_criteria>
  <done>Required consent is enforced at signup and fanmeet booking entry points.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| user consent UI/API -> legal evidence | User choices become compliance evidence. |
| admin query -> PII-bearing audit rows | Operators query sensitive consent history. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Elevation of privilege | booking prepare | mitigate | Required cross-border consent is checked before fanmeet booking transaction, after booking-enabled hard gate. |
| T-23-02 | Tampering | consent language/version | mitigate | Shared schemas validate item/version/language before immutable write. |
| T-23-03 | Repudiation | consent audit rows | mitigate | Append-only per-item rows capture timestamp/IP/user/source flow. |
| T-23-04 | Spoofing | signup consent user | mitigate | Capture uses authenticated/social completion user context; no client-supplied user ID trust. |
| T-23-05 | Repudiation | audit query | mitigate | Query surface supports COMP-02 filters and preserves evidence timestamps. |
| T-23-06 | Information disclosure | audit API | mitigate | Mask email/phone/IP by default and guard admin endpoint. |
</threat_model>

<verification>
Run consent service/controller tests plus auth/reservation integration unit tests for consent gates.
</verification>

<success_criteria>
Consent requirements and audit queryability are enforced server-side with immutable, masked evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/23-launch-foundation/23-07-SUMMARY.md`.
</output>
