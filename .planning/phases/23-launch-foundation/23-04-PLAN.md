---
phase: 23-launch-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 23-01
  - 23-02
files_modified:
  - apps/web/package.json
  - apps/web/i18n/routing.ts
  - apps/web/i18n/request.ts
  - apps/web/i18n/routing.test.ts
  - apps/web/messages/ko.json
  - apps/web/messages/en.json
  - apps/web/messages/th.json
  - apps/web/messages/zh-CN.json
  - apps/web/messages/zh-TW.json
  - apps/web/proxy.ts
  - apps/web/app/layout.tsx
  - apps/web/app/sitemap.ts
  - apps/web/app/__tests__/sitemap.test.ts
  - apps/web/components/i18n/locale-switcher.tsx
  - apps/web/components/i18n/locale-suggestion.tsx
  - apps/web/components/ui/phone-input.tsx
  - apps/web/components/ui/__tests__/phone-input-i18n.test.tsx
  - apps/api/src/modules/user/user.controller.ts
  - apps/api/src/modules/user/user.service.ts
  - apps/api/src/modules/user/user.repository.ts
autonomous: true
requirements:
  - FLAG-01
  - I18N-01
  - I18N-02
  - COMP-01
must_haves:
  truths:
    - "Korean root URLs remain prefixless and are never auto-redirected."
    - "Foreign locales use `/en`, `/th`, `/zh-CN`, and `/zh-TW` prefixes with hreflang/sitemap support."
    - "Anonymous locale preference uses cookie; logged-in preference persists to `users.preferred_locale` with URL-first precedence."
  artifacts:
    - path: "apps/web/i18n/routing.ts"
      provides: "next-intl routing contract"
    - path: "apps/web/proxy.ts"
      provides: "Suggest-never-redirect middleware behavior"
    - path: "apps/web/app/sitemap.ts"
      provides: "Localized sitemap alternates"
  key_links:
    - from: "apps/web/proxy.ts"
      to: "apps/web/i18n/routing.ts"
      via: "routing import"
      pattern: "localePrefix"
    - from: "apps/api/src/modules/user/user.service.ts"
      to: "apps/api/src/database/schema/users.ts"
      via: "preferredLocale persistence"
      pattern: "preferredLocale"
---

<objective>
5개 locale routing과 locale-sensitive foundation을 만든다.

Purpose: M1 광고 오픈에서 해외 사용자가 명시적으로 언어를 선택할 수 있게 하되, 한국 SEO root URL과 session behavior를 보존한다.
Output: next-intl routing, messages, sitemap/hreflang, locale switch/suggestion UI, localized PhoneInput, user locale preference API.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-launch-foundation/23-CONTEXT.md
@.planning/phases/23-launch-foundation/23-RESEARCH.md
@.planning/phases/23-launch-foundation/23-UI-SPEC.md
@packages/shared/src/constants/locales.ts
@packages/shared/src/types/i18n.types.ts
@apps/web/proxy.ts
@apps/web/app/layout.tsx
@apps/web/components/ui/phone-input.tsx
@apps/api/src/modules/user/user.controller.ts
@apps/api/src/modules/user/user.service.ts

<interfaces>
Locale precedence to implement and test per D-06:
```text
url > explicit-switch > user-profile > cookie > ko
```
Locale labels:
```text
한국어, English, ไทย, 简体中文, 繁體中文
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement prefixless Korean and prefixed foreign routing</name>
  <files>apps/web/package.json, apps/web/i18n/routing.ts, apps/web/i18n/request.ts, apps/web/i18n/routing.test.ts, apps/web/proxy.ts, apps/web/app/layout.tsx, apps/web/messages/ko.json, apps/web/messages/en.json, apps/web/messages/th.json, apps/web/messages/zh-CN.json, apps/web/messages/zh-TW.json</files>
  <read_first>apps/web/package.json, apps/web/proxy.ts, apps/web/app/layout.tsx, packages/shared/src/constants/locales.ts, .planning/phases/23-launch-foundation/23-RESEARCH.md</read_first>
  <behavior>
    - `/` resolves Korean without redirect.
    - `/en`, `/th`, `/zh-CN`, `/zh-TW` resolve foreign locales.
    - `Accept-Language` can create suggestion state but never redirects.
  </behavior>
  <action>Add `next-intl` if not already installed. Create `i18n/routing.ts` using supported locales from shared constants, `defaultLocale: 'ko'`, and `localePrefix: 'as-needed'`. Create `i18n/request.ts` and minimal message JSON files containing booking-disabled and locale suggestion copy from UI-SPEC. Update `proxy.ts` to preserve existing `/admin/:path*` behavior while adding locale middleware/suggestion cookie behavior with no automatic redirect. Wrap `app/layout.tsx` with the required provider without changing the existing `LayoutShell`, `Providers`, or `Toaster` behavior.</action>
  <verify>
    <automated>pnpm --filter @grabit/web test -- i18n-routing.test.ts && pnpm --filter @grabit/web typecheck && grep -R "localePrefix" apps/web/i18n/routing.ts && ! grep -R "NextResponse.redirect" apps/web/proxy.ts</automated>
  </verify>
  <acceptance_criteria>`apps/web/i18n/routing.ts` contains `localePrefix`; `apps/web/proxy.ts` contains no `NextResponse.redirect`; message JSON files exist for all five locales.</acceptance_criteria>
  <done>Route-level i18n exists without Korean root auto-redirect.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add sitemap, hreflang, locale switcher, and suggestion UI</name>
  <files>apps/web/app/sitemap.ts, apps/web/app/__tests__/sitemap.test.ts, apps/web/components/i18n/locale-switcher.tsx, apps/web/components/i18n/locale-suggestion.tsx</files>
  <read_first>apps/web/app/legal/terms/page.tsx, apps/web/components/admin/admin-sidebar.tsx, apps/web/components/layout/network-banner.tsx, apps/web/components/ui/dropdown-menu.tsx, apps/web/components/ui/sonner.tsx, .planning/phases/23-launch-foundation/23-UI-SPEC.md</read_first>
  <behavior>
    - Locale switcher shows active locale with `aria-current="true"`.
    - Suggestion copy appears once and dismissal persists for session.
    - Sitemap includes alternates for `ko`, `en`, `th`, `zh-CN`, `zh-TW`.
  </behavior>
  <action>Create `LocaleSwitcher` using shadcn `DropdownMenu` or `Select`, labels from UI-SPEC, and explicit click navigation only. Create `LocaleSuggestion` banner/toast with UI-SPEC copy and session dismissal. Create `app/sitemap.ts` returning localized alternates for root and public legal/event surfaces without changing Korean canonical root. Tests assert all five locale alternates exist.</action>
  <verify>
    <automated>pnpm --filter @grabit/web test -- sitemap.test.ts && grep -R "aria-current" apps/web/components/i18n/locale-switcher.tsx && grep -R "다른 언어로 볼까요" apps/web/components/i18n/locale-suggestion.tsx</automated>
  </verify>
  <acceptance_criteria>`sitemap.test.ts` asserts `ko`, `en`, `th`, `zh-CN`, `zh-TW`; locale switcher uses `aria-current`; suggestion component contains Korean suggestion copy.</acceptance_criteria>
  <done>Users can explicitly choose locale and crawlers can discover localized URLs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Localize PhoneInput and persist user locale preference</name>
  <files>apps/web/components/ui/phone-input.tsx, apps/web/components/ui/__tests__/phone-input-i18n.test.tsx, apps/api/src/modules/user/user.controller.ts, apps/api/src/modules/user/user.service.ts, apps/api/src/modules/user/user.repository.ts</files>
  <read_first>apps/web/components/ui/phone-input.tsx, apps/web/components/auth/phone-verification.tsx, apps/api/src/modules/user/user.controller.ts, apps/api/src/modules/user/user.service.ts, apps/api/src/modules/user/user.repository.ts, packages/shared/src/constants/locales.ts</read_first>
  <behavior>
    - PhoneInput label set changes for `ko`, `en`, `th`, `zh-CN`, `zh-TW`.
    - Unsupported countries remain searchable/selectable.
    - Logged-in locale preference writes to `users.preferred_locale`; anonymous users rely on cookie.
  </behavior>
  <action>Update `PhoneInput` to accept optional `locale` prop and load `react-phone-number-input/locale/{ko,en,th,zh,zh-Hant}.json` or local labels as needed. Add user profile endpoint support for `preferredLocale` using shared locale validation. Implement precedence in service comments/tests as `url > explicit-switch > user-profile > cookie > ko`; this task owns profile persistence, while route choice stays in web i18n.</action>
  <verify>
    <automated>pnpm --filter @grabit/web test -- phone-input-i18n.test.tsx && pnpm --filter @grabit/api typecheck && grep -R "preferredLocale" apps/api/src/modules/user apps/web/components/ui/phone-input.tsx</automated>
  </verify>
  <acceptance_criteria>`phone-input-i18n.test.tsx` covers five locales; `user.service.ts` and/or `user.repository.ts` reads/writes `preferredLocale`; unsupported country search is not removed.</acceptance_criteria>
  <done>Locale preference and launch-market PhoneInput localization are wired.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| request headers -> routing | `Accept-Language` is untrusted and must not cause forced redirects. |
| user profile -> locale UI | Stored preference affects rendering but must be validated. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Elevation of privilege | booking UI copy | accept | API hard gate is in Plan 03; this plan only localizes display surfaces. |
| T-23-02 | Tampering | `proxy.ts`, sitemap | mitigate | Tests forbid `NextResponse.redirect` and assert Korean root remains prefixless. |
| T-23-03 | Repudiation | legal fallback labels | mitigate | Legal fallback copy is surfaced in later legal plan using locale contracts from this plan. |
| T-23-04 | Spoofing | locale preference update | mitigate | User locale API validates supported locale enum before storing. |
| T-23-05 | Repudiation | consent language | mitigate | Locale constants support immutable consent language capture in Plan 07. |
| T-23-06 | Information disclosure | PhoneInput/user profile | mitigate | No phone PII is logged; only locale preference is stored. |
</threat_model>

<verification>
Run web i18n/sitemap/PhoneInput tests and API/web typechecks for touched files.
</verification>

<success_criteria>
Five-locale routing, formatting foundation, explicit locale selection, localized PhoneInput, and user locale persistence are working without Korean root redirects.
</success_criteria>

<output>
After completion, create `.planning/phases/23-launch-foundation/23-04-SUMMARY.md`.
</output>
