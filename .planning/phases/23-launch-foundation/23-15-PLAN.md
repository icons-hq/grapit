---
phase: 23-launch-foundation
plan: 15
type: execute
wave: 3
depends_on:
  - 23-01
files_modified:
  - apps/web/components/ui/phone-input.tsx
  - apps/web/components/ui/__tests__/phone-input-i18n.test.tsx
  - packages/shared/src/i18n/launch-copy-keys.ts
  - packages/shared/src/i18n/launch-copy-keys.test.ts
  - packages/shared/src/index.ts
autonomous: true
requirements:
  - I18N-02
must_haves:
  truths:
    - "PhoneInput labels support `ko`, `en`, `th`, `zh-CN`, and `zh-TW` while unsupported countries remain searchable/selectable."
    - "Shared launch copy key manifest requires auth/email/OTP/SMS namespaces for exactly the five launch locales."
  artifacts:
    - path: "apps/web/components/ui/phone-input.tsx"
      provides: "Launch-locale PhoneInput labels"
    - path: "packages/shared/src/i18n/launch-copy-keys.ts"
      provides: "Cross-app required auth/email/OTP/SMS localized copy key manifest"
  key_links:
    - from: "packages/shared/src/index.ts"
      to: "packages/shared/src/i18n/launch-copy-keys.ts"
      via: "barrel export"
      pattern: "launch-copy-keys|launchCopyKeys"
---

<objective>
PhoneInput localization and launch copy manifest를 만든다.

Purpose: D-08/I18N-02의 five-launch-market localization contract를 UI input과 shared copy keys에서 먼저 고정한다.
Output: localized PhoneInput labels/tests, shared launch copy key manifest/tests/export.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-launch-foundation/23-CONTEXT.md
@.planning/phases/23-launch-foundation/23-UI-SPEC.md
@packages/shared/src/constants/locales.ts
@apps/web/components/ui/phone-input.tsx
@apps/web/components/auth/phone-verification.tsx

<interfaces>
I18N-02 launch copy manifest:
```text
locales: ko, en, th, zh-CN, zh-TW
namespaces: auth.emailVerification, auth.otp, auth.errors, sms.otp
required behavior: every namespace key must be present for all five launch locales; tests fail on missing or extra locale keys
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Localize PhoneInput labels and define auth/OTP/SMS copy key manifest</name>
  <files>apps/web/components/ui/phone-input.tsx, apps/web/components/ui/__tests__/phone-input-i18n.test.tsx, packages/shared/src/i18n/launch-copy-keys.ts, packages/shared/src/i18n/launch-copy-keys.test.ts, packages/shared/src/index.ts</files>
  <read_first>apps/web/components/ui/phone-input.tsx, apps/web/components/auth/phone-verification.tsx, packages/shared/src/constants/locales.ts, .planning/phases/23-launch-foundation/23-UI-SPEC.md</read_first>
  <behavior>
    - PhoneInput label set changes for `ko`, `en`, `th`, `zh-CN`, `zh-TW`.
    - Unsupported countries remain searchable/selectable.
    - Long Thai and Chinese labels do not remove existing min-height or popover behavior.
    - Shared copy manifest lists required `auth.emailVerification`, `auth.otp`, `auth.errors`, and `sms.otp` keys for exactly `ko`, `en`, `th`, `zh-CN`, and `zh-TW`.
  </behavior>
  <action>Update `PhoneInput` to accept optional `locale` prop and load `react-phone-number-input/locale/{ko,en,th,zh,zh-Hant}.json` or local labels as needed per D-08. Preserve existing shadcn/Radix popover, flags, min-height, and country search behavior. Create `packages/shared/src/i18n/launch-copy-keys.ts` exporting `LAUNCH_COPY_LOCALES = ['ko','en','th','zh-CN','zh-TW']` and required keys for `auth.emailVerification`, `auth.otp`, `auth.errors`, and `sms.otp`. Export it from `packages/shared/src/index.ts` so Phase 23 API templates and web messages align on the same manifest.</action>
  <verify>
    <automated>pnpm --filter @grabit/web test -- phone-input-i18n.test.tsx && pnpm --filter @grabit/shared test -- launch-copy-keys.test.ts && grep -R "locale.*PhoneInput\\|PhoneInput.*locale" apps/web/components/ui/phone-input.tsx && grep -R "zh-TW" packages/shared/src/i18n/launch-copy-keys.ts</automated>
  </verify>
  <acceptance_criteria>`phone-input-i18n.test.tsx` covers five locales and unsupported country search; `launch-copy-keys.test.ts` asserts required auth/email/OTP/SMS copy keys for exactly `ko`, `en`, `th`, `zh-CN`, and `zh-TW`; existing PhoneInput API remains backward-compatible when `locale` is omitted.</acceptance_criteria>
  <done>D-08/I18N-02 launch-market PhoneInput localization and shared localized copy key contract are wired without narrowing country selection.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| phone input locale -> UI labels | Locale affects country labels and SMS-related user input. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-15-01 | Denial of service | localized labels | mitigate | Tests cover long labels and preserve existing control dimensions/search behavior. |
| T-23-15-02 | Information disclosure | PhoneInput | mitigate | Do not log phone input or country selection; preserve existing no-PII behavior. |
| T-23-15-03 | Tampering | copy manifest | mitigate | Shared tests enforce exact five-locale key coverage. |
</threat_model>

<verification>
Run web PhoneInput tests, shared manifest tests, and greps for locale prop/export.
</verification>

<success_criteria>
PhoneInput supports five launch locales and shared copy keys constrain auth/email/OTP/SMS localization.
</success_criteria>

<output>
After completion, create `.planning/phases/23-launch-foundation/23-15-SUMMARY.md`.
</output>
