---
phase: 23-launch-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - docs/v2.0-fanmeet-milestone-spec.md
  - packages/shared/src/flags.ts
  - packages/shared/src/flags.test.ts
  - packages/shared/src/constants/locales.ts
  - packages/shared/src/constants/locales.test.ts
  - packages/shared/src/types/i18n.types.ts
  - packages/shared/src/schemas/consent.schema.ts
  - packages/shared/src/index.ts
  - packages/shared/src/constants/index.ts
autonomous: true
requirements:
  - FLAG-01
  - FLAG-02
  - I18N-01
  - I18N-02
  - TRANS-01
  - TRANS-02
  - AUTH-01
  - AUTH-02
  - COMP-01
  - COMP-02
must_haves:
  truths:
    - "Phase 23 executors see AUTH-01 as Kakao/Naver/Google/email plus email verification, with LINE explicitly excluded."
    - "Web and API import the same feature flag and locale contracts."
    - "Wave 0 tests exist for flag parsing and launch locale routing constants."
  artifacts:
    - path: "packages/shared/src/flags.ts"
      provides: "Shared runtime-safe feature flag parser"
      exports: ["FLAG_NAMES", "parseBooleanFlag", "readFeatureFlags"]
    - path: "packages/shared/src/constants/locales.ts"
      provides: "Five launch locale constants and prefix policy"
      exports: ["SUPPORTED_LOCALES", "DEFAULT_LOCALE", "LOCALE_PREFIXES"]
    - path: "packages/shared/src/schemas/consent.schema.ts"
      provides: "Consent item/version/language contracts"
  key_links:
    - from: "packages/shared/src/index.ts"
      to: "packages/shared/src/flags.ts"
      via: "barrel export"
      pattern: "export \\* from './flags'"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/23-launch-foundation/23-CONTEXT.md"
      via: "AUTH-01 text excludes LINE per D-13"
      pattern: "LINE.*excluded|LINE.*제외"
---

<objective>
Phase 23의 실행 기준을 먼저 고정한다.

Purpose: stale LINE 문구와 중복 flag/locale parsing이 뒤 wave를 오염시키지 않도록, source-of-truth 문서와 shared contract를 가장 먼저 만든다.
Output: LINE 제외가 반영된 planning docs, shared feature flag helper, launch locale constants, consent schema, Wave 0 tests.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-launch-foundation/23-CONTEXT.md
@.planning/phases/23-launch-foundation/23-VALIDATION.md
@.planning/phases/23-launch-foundation/23-PATTERNS.md
@docs/v2.0-fanmeet-milestone-spec.md

<interfaces>
Shared barrel pattern:
```typescript
export * from './schemas/auth.schema';
export * from './types/auth.types';
export * from './constants/index';
```

Required contracts to create:
```typescript
export const FLAG_NAMES = { BOOKING_ENABLED: 'BOOKING_ENABLED' } as const;
export function parseBooleanFlag(value: string | undefined, defaultValue?: boolean): boolean;
export function readFeatureFlags(env: Record<string, string | undefined>): { bookingEnabled: boolean };

export const SUPPORTED_LOCALES = ['ko', 'en', 'th', 'zh-CN', 'zh-TW'] as const;
export const DEFAULT_LOCALE = 'ko';
export const LOCALE_PREFIXES = { ko: '/', en: '/en', th: '/th', 'zh-CN': '/zh-CN', 'zh-TW': '/zh-TW' } as const;
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Reconcile stale LINE planning text per D-13</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, docs/v2.0-fanmeet-milestone-spec.md</files>
  <read_first>.planning/phases/23-launch-foundation/23-CONTEXT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, docs/v2.0-fanmeet-milestone-spec.md</read_first>
  <behavior>
    - AUTH-01 describes Kakao, Naver, Google, and email signup/login.
    - LINE remains visible only as excluded/stale historical context, never as Phase 23 implementation scope.
    - No `lineLogin`, `passport-line`, LINE callback, LINE button, or LINE env var is introduced.
  </behavior>
  <action>Update Phase 23/AUTH-01 text to say "Kakao, Naver, Google, or email" and add a parenthetical note that LINE is excluded by D-13. In `docs/v2.0-fanmeet-milestone-spec.md`, mark old Line/login references as superseded by Phase 23 D-13 rather than deleting historical context. Do not add any LINE implementation task or env var.</action>
  <verify>
    <automated>grep -n "AUTH-01" .planning/REQUIREMENTS.md && ! (grep -v "D-13\\|excluded\\|제외\\|superseded\\|stale" .planning/REQUIREMENTS.md .planning/ROADMAP.md docs/v2.0-fanmeet-milestone-spec.md | grep -Ei "LINE|Line|lineLogin|passport-line")</automated>
  </verify>
  <acceptance_criteria>`AUTH-01` includes `Kakao`, `Naver`, `Google`, and `email`; unqualified `LINE` implementation wording is absent; any remaining LINE mention is explicitly marked `D-13`, `excluded`, `제외`, `superseded`, or `stale`.</acceptance_criteria>
  <done>Planning docs cannot be interpreted as requiring LINE provider implementation in Phase 23.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create shared flag and locale contracts</name>
  <files>packages/shared/src/flags.ts, packages/shared/src/flags.test.ts, packages/shared/src/constants/locales.ts, packages/shared/src/constants/locales.test.ts, packages/shared/src/types/i18n.types.ts, packages/shared/src/index.ts, packages/shared/src/constants/index.ts</files>
  <read_first>packages/shared/src/index.ts, packages/shared/src/constants/index.ts, packages/shared/src/schemas/auth.schema.ts, .planning/phases/23-launch-foundation/23-CONTEXT.md, .planning/phases/23-launch-foundation/23-VALIDATION.md</read_first>
  <behavior>
    - `BOOKING_ENABLED` defaults to false.
    - Boolean parsing accepts `true`, `1`, `yes`, `on` as true and `false`, `0`, `no`, `off`, empty, and undefined as false.
    - Korean locale remains prefixless; foreign locales use `/en`, `/th`, `/zh-CN`, `/zh-TW`.
  </behavior>
  <action>Create `packages/shared/src/flags.ts` with `FLAG_NAMES.BOOKING_ENABLED`, `parseBooleanFlag`, and `readFeatureFlags(env)`. Create `packages/shared/src/constants/locales.ts` with `SUPPORTED_LOCALES`, `DEFAULT_LOCALE`, `LOCALE_PREFIXES`, `LOCALE_LABELS`, and `isSupportedLocale`. Export these through `packages/shared/src/index.ts` and `packages/shared/src/constants/index.ts`. Tests must not reference `NEXT_PUBLIC_BOOKING_ENABLED` as source of truth per D-01/D-03.</action>
  <verify>
    <automated>pnpm --filter @grabit/shared typecheck && pnpm --filter @grabit/shared build && pnpm --filter @grabit/shared exec vitest run src/flags.test.ts src/constants/locales.test.ts</automated>
  </verify>
  <acceptance_criteria>`packages/shared/src/flags.ts` contains `BOOKING_ENABLED`; `packages/shared/src/constants/locales.ts` contains all five locale codes; `grep -R "NEXT_PUBLIC_BOOKING_ENABLED" packages/shared/src` returns no matches.</acceptance_criteria>
  <done>Shared package exposes stable launch flag and locale contracts with passing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Define consent and i18n shared types</name>
  <files>packages/shared/src/schemas/consent.schema.ts, packages/shared/src/types/i18n.types.ts, packages/shared/src/index.ts</files>
  <read_first>packages/shared/src/schemas/auth.schema.ts, packages/shared/src/types/user.types.ts, .planning/phases/23-launch-foundation/23-CONTEXT.md, .planning/phases/23-launch-foundation/23-UI-SPEC.md</read_first>
  <behavior>
    - Consent items are itemized, versioned, language-tagged, and immutable at audit capture time.
    - Cross-border transfer is required; marketing is optional; under-14 is blocked.
    - Locale preference type supports cookie and user profile persistence.
  </behavior>
  <action>Create `consentItemSchema`, `consentCaptureSchema`, `consentAuditQuerySchema`, and inferred types for item keys `terms`, `privacy`, `pipa_required`, `cross_border_transfer`, `pdpa_notice`, `pipl_notice`, `marketing`. Create i18n types for `SupportedLocale`, `LocalePreferenceSource`, and `LocaleResolution` documenting precedence `url > explicit-switch > user-profile > cookie > ko` per D-06. Export through `packages/shared/src/index.ts`.</action>
  <verify>
    <automated>pnpm --filter @grabit/shared typecheck && grep -R "cross_border_transfer" packages/shared/src/schemas/consent.schema.ts && grep -R "url.*explicit-switch.*user-profile.*cookie.*ko" packages/shared/src/types/i18n.types.ts</automated>
  </verify>
  <acceptance_criteria>`consent.schema.ts` contains `cross_border_transfer`, `pdpa_notice`, `pipl_notice`, `marketing`; `i18n.types.ts` documents the D-06 precedence string exactly.</acceptance_criteria>
  <done>Downstream API and web plans can import consent/i18n contracts instead of inventing local shapes.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| planning docs -> executors | Stale source text can cause forbidden LINE scope or UI-only gates to be implemented. |
| env -> shared parser | Runtime strings become security-sensitive feature decisions. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Elevation of privilege | `BOOKING_ENABLED` contract | mitigate | Define `BOOKING_ENABLED` in shared parser with false default; later API plans gate lock/prepare/confirm before side effects. |
| T-23-02 | Tampering | locale routing constants | mitigate | Encode prefixless `ko` and prefixed foreign locales in tests so auto-redirect cannot become default. |
| T-23-03 | Repudiation | planning docs and content type contracts | mitigate | Mark legal translation exclusion and stale LINE scope explicitly before execution. |
| T-23-04 | Spoofing | auth scope contract | mitigate | AUTH-01 source text excludes LINE and preserves Kakao/Naver/Google/email only. |
| T-23-05 | Repudiation | consent schema | mitigate | Shared schema requires item/version/language/timestamp/IP/user fields for immutable audit rows. |
| T-23-06 | Information disclosure | audit query schema | mitigate | Shared query contract distinguishes masked operator output from raw evidence. |
</threat_model>

<verification>
Run `pnpm --filter @grabit/shared typecheck` and the two shared Vitest files. Confirm source docs no longer contain unqualified LINE implementation scope.
</verification>

<success_criteria>
All Phase 23 downstream plans can depend on shared flag, locale, i18n, and consent contracts; no executor can reasonably implement LINE from stale roadmap text.
</success_criteria>

<output>
After completion, create `.planning/phases/23-launch-foundation/23-01-SUMMARY.md`.
</output>
