---
phase: 26-m1-canary-cutover-gates
plan: 04
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - apps/api/src/modules/payment/toss-payments.client.ts
  - apps/api/src/modules/payment/toss-payments.client.spec.ts
  - apps/api/src/modules/payment/payment-webhook.controller.ts
  - apps/api/src/modules/payment/toss-webhook.controller.spec.ts
  - apps/api/src/modules/payment/payment.service.ts
  - apps/api/src/modules/payment/payment.service.spec.ts
  - .planning/phases/26-m1-canary-cutover-gates/26-GATE-LEDGER.json
  - .planning/phases/26-m1-canary-cutover-gates/26-GATE-LEDGER.md
  - .planning/phases/26-m1-canary-cutover-gates/evidence/26-04-toss-hardening.json
autonomous: false
requirements: [PAY-01]
user_setup:
  - service: toss-payments
    why: "D-24 requires rotating the exposed Toss test secret before final test-key rehearsal evidence is trusted."
    env_vars:
      - name: TOSS_SECRET_KEY
        source: "Toss Dashboard and Secret Manager; do not paste the raw value into repo artifacts."
    dashboard_config:
      - task: "Reissue/rotate the exposed test secret key and update Secret Manager/CI securely."
        location: "Toss Payments Dashboard -> Developers/API keys, then GCP Secret Manager/GitHub secrets"
must_haves:
  truths:
    - "Toss confirm/cancel POST calls are retry-safe with Idempotency-Key support."
    - "Toss webhook finalization re-queries Toss by paymentKey before applying final local state."
    - "The required TOSS_TEST_SECRET_ROTATION Gate Ledger row is PASS only after reissue/rotation plus Secret Manager/CI update evidence, otherwise BLOCKED or owner-approved non-PASS."
  artifacts:
    - path: apps/api/src/modules/payment/toss-payments.client.ts
      provides: "confirm/cancel/query client with idempotency"
    - path: apps/api/src/modules/payment/payment-webhook.controller.ts
      provides: "webhook re-query enforcement"
    - path: .planning/phases/26-m1-canary-cutover-gates/evidence/26-04-toss-hardening.json
      provides: "redacted Toss hardening evidence"
  key_links:
    - from: apps/api/src/modules/payment/payment-webhook.controller.ts
      to: apps/api/src/modules/payment/toss-payments.client.ts
      via: "queryPayment(paymentKey)"
      pattern: "queryPayment"
---

<objective>
Harden Toss test/live payment cutover paths before rehearsal and live-key smoke.

Purpose: Test-key rehearsal is necessary but not sufficient. Payment confirm/cancel retries and webhook payload trust must fail closed before any BOOKING_ENABLED=true decision.
Output: Toss client/query/idempotency changes, webhook re-query tests, and redacted key-rotation evidence.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-m1-canary-cutover-gates/26-CONTEXT.md
@.planning/phases/26-m1-canary-cutover-gates/26-RESEARCH.md
@.planning/phases/26-m1-canary-cutover-gates/26-PATTERNS.md
@apps/api/src/modules/payment/toss-payments.client.ts
@apps/api/src/modules/payment/payment-webhook.controller.ts
@apps/api/src/modules/payment/toss-webhook.controller.spec.ts
@apps/api/src/modules/payment/payment.service.ts
@apps/api/src/modules/payment/payment.service.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Toss idempotency and webhook re-query tests</name>
  <read_first>
    apps/api/src/modules/payment/toss-payments.client.ts
    apps/api/src/modules/payment/toss-webhook.controller.spec.ts
    apps/api/src/modules/payment/payment.service.spec.ts
    .planning/phases/26-m1-canary-cutover-gates/26-PATTERNS.md
  </read_first>
  <files>apps/api/src/modules/payment/toss-payments.client.spec.ts, apps/api/src/modules/payment/toss-webhook.controller.spec.ts, apps/api/src/modules/payment/payment.service.spec.ts</files>
  <behavior>
    - confirmPayment and cancelPayment send Idempotency-Key when provided.
    - queryPayment(paymentKey) uses server-side secret auth and redacted error behavior.
    - webhook DONE/CANCEL finalization uses provider query results and rejects mismatch between webhook payload and queried Toss state.
  </behavior>
  <action>Add failing tests for D-19 through D-23. Use the existing TossPaymentError and webhook duplicate-processing patterns. Tests must not embed real paymentKey, raw Toss secret, or webhook secret; use synthetic values with obvious test prefixes.</action>
  <verify>
    <automated>pnpm --filter @grabit/api exec vitest run src/modules/payment/toss-payments.client.spec.ts src/modules/payment/toss-webhook.controller.spec.ts src/modules/payment/payment.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>Tests fail if POST idempotency is absent or webhook code trusts payload state without queryPayment(paymentKey).</acceptance_criteria>
  <done>Payment hardening expectations are executable before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Toss query and idempotency hardening</name>
  <read_first>
    apps/api/src/modules/payment/toss-payments.client.ts
    apps/api/src/modules/payment/payment-webhook.controller.ts
    apps/api/src/modules/payment/payment.service.ts
    apps/api/src/modules/payment/toss-payments.client.spec.ts
    apps/api/src/modules/payment/toss-webhook.controller.spec.ts
  </read_first>
  <files>apps/api/src/modules/payment/toss-payments.client.ts, apps/api/src/modules/payment/payment-webhook.controller.ts, apps/api/src/modules/payment/payment.service.ts</files>
  <behavior>
    - Toss client exposes queryPayment(paymentKey).
    - Confirm/cancel calls accept idempotency keys without logging them.
    - Webhook processing re-queries provider state before local finalization.
  </behavior>
  <action>Implement queryPayment(paymentKey) with the existing Basic auth/error style. Add optional idempotencyKey support to confirmPayment and cancelPayment headers. Update webhook processing so PAYMENT_STATUS_CHANGED and CANCEL_STATUS_CHANGED final state transitions use queryPayment(paymentKey) as authority before local state changes. Preserve existing duplicate ledger behavior and fail closed on mismatch.</action>
  <verify>
    <automated>pnpm --filter @grabit/api exec vitest run src/modules/payment/toss-payments.client.spec.ts src/modules/payment/toss-webhook.controller.spec.ts src/modules/payment/payment.service.spec.ts && pnpm --filter @grabit/api typecheck</automated>
  </verify>
  <acceptance_criteria>Targeted payment tests and API typecheck pass; webhook mismatch cannot finalize a local payment/reservation/QR state.</acceptance_criteria>
  <done>Toss webhook integrity and retry safety are implemented for cutover rehearsal.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Rotate exposed Toss test secret and record redacted evidence</name>
  <read_first>
    .planning/phases/26-m1-canary-cutover-gates/26-CONTEXT.md
    .github/workflows/deploy.yml
    .planning/phases/26-m1-canary-cutover-gates/26-GATE-LEDGER.json
  </read_first>
  <files>.planning/phases/26-m1-canary-cutover-gates/evidence/26-04-toss-hardening.json, .planning/phases/26-m1-canary-cutover-gates/26-GATE-LEDGER.json, .planning/phases/26-m1-canary-cutover-gates/26-GATE-LEDGER.md</files>
  <action>D-24 requires reissuing the exposed Toss test secret before final rehearsal evidence is trusted. Ask the owner to rotate/reissue the test secret in Toss and update Secret Manager/CI without pasting the raw value. After owner confirmation, verify only redacted key class/prefix metadata, Secret Manager version timestamps, and deploy secret binding names. Update the TOSS_TEST_SECRET_ROTATION Gate Ledger row and 26-04 evidence: PASS only when rotation/reissue and Secret Manager/CI updates are evidenced; BLOCKED when rotation is incomplete, unavailable, or unverified; ACCEPTED_RISK or CONFIG_READY_NOT_DRILLED only when the owner explicitly approves the D-24 non-PASS state with compensating monitoring and rollback/close trigger. Do not mark TOSS_TEST_REHEARSAL as final trusted evidence while TOSS_TEST_SECRET_ROTATION is missing or BLOCKED.</action>
  <verify>
    <automated>node scripts/phase26/validate-gate-ledger.mjs --ledger .planning/phases/26-m1-canary-cutover-gates/26-GATE-LEDGER.json --strict && node -e "const fs=require('fs'); const rows=JSON.parse(fs.readFileSync('.planning/phases/26-m1-canary-cutover-gates/26-GATE-LEDGER.json','utf8')).gates; const row=rows.find(r=>r.gateId==='TOSS_TEST_SECRET_ROTATION'); if(!row) throw new Error('missing TOSS_TEST_SECRET_ROTATION'); if(row.state==='PASS' && (!row.evidenceRefs || row.evidenceRefs.length===0)) throw new Error('rotation PASS without evidence');"</automated>
  </verify>
  <acceptance_criteria>Evidence JSON and Gate Ledger record rotation status, provider/source timestamps, Secret Manager/CI binding names, and redacted metadata only; no raw Toss key appears in repo artifacts; missing rotation remains BLOCKED.</acceptance_criteria>
  <done>Toss test-key rehearsal cannot be treated as final evidence unless TOSS_TEST_SECRET_ROTATION is PASS or explicitly owner-approved as non-PASS with D-24 rationale.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Toss provider -> API webhook | Untrusted webhook payload crosses into payment state. |
| API -> Toss API | Server secret signs confirm/cancel/query calls. |
| operator dashboard -> repo artifact | Key rotation evidence must stay redacted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04-01 | Spoofing | payment webhook | mitigate | Re-query Toss by paymentKey before applying final state. |
| T-26-04-02 | Tampering | confirm/cancel retry | mitigate | Add Idempotency-Key support and tests for POST confirm/cancel. |
| T-26-04-03 | Information Disclosure | Toss secrets | mitigate | Do not log or artifact raw Toss keys; record only redacted prefix/class metadata. |
| T-26-04-04 | Repudiation | webhook processing | mitigate | Preserve existing webhook event ledger and record query/mismatch outcomes. |
| T-26-04-05 | Elevation of Privilege | live cutover | mitigate | Test-key success does not unlock BOOKING_ENABLED; final live gate remains Plan 26-10. |
</threat_model>

<verification>
Run targeted payment tests, API typecheck, strict ledger validation, and a redacted secret-pattern scan against changed files and evidence.
</verification>

<success_criteria>
Toss payment cutover paths are server-authoritative, retry-safe, webhook-safe, and secret-redacted; the required TOSS_TEST_SECRET_ROTATION gate is recorded before any test-key rehearsal or live-key smoke can unblock PAY-01.
</success_criteria>

<output>
After completion, create `.planning/phases/26-m1-canary-cutover-gates/26-04-SUMMARY.md`.
</output>
