---
phase: 26-m1-canary-cutover-gates
plan: 05
type: execute
wave: 3
depends_on: [26-01, 26-02]
files_modified:
  - scripts/phase26/rehearsal-smoke.mjs
  - scripts/phase26/cleanup-dry-run.sql
  - scripts/phase26/cleanup-test-event.sql
  - .planning/phases/26-m1-canary-cutover-gates/evidence/26-05-rehearsal.json
autonomous: false
requirements: [M1-01, PAY-01]
user_setup:
  - service: production-like test environment
    why: "D-11 requires a dedicated test event and D-13 forbids real Girl Rules/user/payment/ticket/seat mutations."
    env_vars:
      - name: PHASE26_TEST_PERFORMANCE_ID
        source: "Dedicated test event, not the real Girl Rules event"
      - name: PHASE26_TEST_SHOWTIME_ID
        source: "Dedicated test showtime"
      - name: PHASE26_TEST_ORDER_PREFIX
        source: "Operator-approved test order prefix"
must_haves:
  truths:
    - "Ticketing cutover rehearsal targets only a dedicated test event."
    - "Cleanup dry-run lists/counts rows before mutation and stops on unexpected rows."
    - "Queue, lock, prepare, safe test-key payment path, QR, refund/cancel, and cleanup evidence are captured."
  artifacts:
    - path: scripts/phase26/rehearsal-smoke.mjs
      provides: "ticketing cutover rehearsal CLI"
    - path: scripts/phase26/cleanup-dry-run.sql
      provides: "dry-run cleanup scope"
    - path: scripts/phase26/cleanup-test-event.sql
      provides: "guarded cleanup execution"
  key_links:
    - from: scripts/phase26/rehearsal-smoke.mjs
      to: scripts/phase26/cleanup-dry-run.sql
      via: "test event identifiers and order prefix"
      pattern: "PHASE26_TEST_"
---

<objective>
Build the dedicated test-event ticketing rehearsal and cleanup safety harness.

Purpose: Phase 26 must exercise the cutover path without mutating the real Girl Rules event, real users, real reservations, real payments, real tickets, or real seat state.
Output: Rehearsal smoke script, dry-run cleanup SQL, guarded cleanup SQL, and redacted rehearsal evidence.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-m1-canary-cutover-gates/26-CONTEXT.md
@.planning/phases/26-m1-canary-cutover-gates/26-RESEARCH.md
@.planning/phases/26-m1-canary-cutover-gates/26-PATTERNS.md
@scripts/smoke-valkey-production.mjs
@apps/api/src/modules/queue/queue.service.ts
@apps/api/src/modules/queue/guards/admission.guard.ts
@apps/api/src/modules/booking/booking.service.ts
@apps/api/src/modules/reservation/reservation.service.ts
@apps/api/src/modules/payment/toss-payments.client.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create guarded cleanup dry-run and execution SQL</name>
  <read_first>
    .planning/phases/26-m1-canary-cutover-gates/26-CONTEXT.md
    .planning/phases/26-m1-canary-cutover-gates/26-PATTERNS.md
    apps/api/src/database/schema/index.ts
  </read_first>
  <files>scripts/phase26/cleanup-dry-run.sql, scripts/phase26/cleanup-test-event.sql</files>
  <behavior>
    - Cleanup requires dedicated performanceId, showtimeId, order prefix, and test marker.
    - Dry-run returns counts/lists before mutation.
    - Execution stops if the real Girl Rules event, real production records, or unexpected rows are in scope.
  </behavior>
  <action>Create SQL cleanup scripts per D-13 through D-15. Use psql variables for test performanceId/showtimeId/order prefix/marker. Add denylist assertions for real Girl Rules identifiers and real production records. The execution script must require an explicit backup/restore-point confirmation variable and must abort when dry-run counts do not match expected test markers.</action>
  <verify>
    <automated>rg -n "PHASE26|performanceId|showtimeId|order" scripts/phase26/cleanup-dry-run.sql scripts/phase26/cleanup-test-event.sql && rg -n "Girl Rules|deny|backup|restore|rollback|unexpected" scripts/phase26/cleanup-dry-run.sql scripts/phase26/cleanup-test-event.sql</automated>
  </verify>
  <acceptance_criteria>Cleanup cannot run without dedicated identifiers, dry-run evidence, denylist protection, and backup/restore-point confirmation.</acceptance_criteria>
  <done>Production data safety is encoded before rehearsal execution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ticketing cutover rehearsal smoke</name>
  <read_first>
    scripts/smoke-valkey-production.mjs
    scripts/phase26/cleanup-dry-run.sql
    apps/api/src/modules/queue/queue.service.ts
    apps/api/src/modules/reservation/reservation.service.ts
    apps/api/src/modules/payment/toss-payments.client.ts
    scripts/phase26/field-scan-contract-smoke.mjs
  </read_first>
  <files>scripts/phase26/rehearsal-smoke.mjs, .planning/phases/26-m1-canary-cutover-gates/evidence/26-05-rehearsal.json</files>
  <behavior>
    - Rehearsal covers queue, seat lock, reservation prepare, payment confirm safe/test branch, QR issuance, refund/cancel, and cleanup dry-run.
    - Payment-provider high-volume load is not performed here.
    - Evidence redacts cookies, tokens, paymentKey, QR token, order IDs beyond the approved prefix, and PII.
  </behavior>
  <action>Create a Node CLI modeled on scripts/smoke-valkey-production.mjs. Require GRABIT_API_URL, GRABIT_SMOKE_AUTH_HEADER_FILE, PHASE26_TEST_PERFORMANCE_ID, PHASE26_TEST_SHOWTIME_ID, PHASE26_TEST_SEAT_ID, PHASE26_TEST_ORDER_PREFIX, and PHASE26_TEST_MARKER. Implement checks for queue entry, lock, prepare, safe payment/test branch, QR readiness, refund/cancel health, and cleanup dry-run. Write a redacted evidence fragment and leave final cleanup execution behind an explicit --execute-cleanup flag.</action>
  <verify>
    <automated>node scripts/phase26/rehearsal-smoke.mjs --help && node scripts/phase26/validate-gate-ledger.mjs --ledger .planning/phases/26-m1-canary-cutover-gates/26-GATE-LEDGER.json --strict</automated>
  </verify>
  <acceptance_criteria>The script refuses to run without dedicated test identifiers and emits redacted evidence only; --execute-cleanup is impossible without a successful dry-run.</acceptance_criteria>
  <done>Ticketing rehearsal can be run safely against a dedicated production-like test event.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Approve and run dedicated test-event rehearsal</name>
  <read_first>
    scripts/phase26/rehearsal-smoke.mjs
    scripts/phase26/cleanup-dry-run.sql
    scripts/phase26/cleanup-test-event.sql
    .planning/phases/26-m1-canary-cutover-gates/26-GATE-LEDGER.json
  </read_first>
  <files>.planning/phases/26-m1-canary-cutover-gates/evidence/26-05-rehearsal.json</files>
  <action>Before running rehearsal, ask the owner to confirm that PHASE26_TEST_PERFORMANCE_ID and PHASE26_TEST_SHOWTIME_ID are dedicated test records and not the real Girl Rules event. Run the rehearsal smoke. If cleanup dry-run returns unexpected rows, stop and record BLOCKED. If the owner does not approve cleanup execution, record cleanup as not executed rather than mutating data.</action>
  <verify>
    <automated>node scripts/phase26/rehearsal-smoke.mjs --help</automated>
  </verify>
  <acceptance_criteria>Rehearsal evidence exists or a clear BLOCKED reason exists; no cleanup mutation happens without dry-run and owner approval.</acceptance_criteria>
  <done>Dedicated test-event rehearsal is completed or safely blocked with evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rehearsal script -> production-like API | Automated test traffic can create real side effects. |
| cleanup SQL -> production DB | Cleanup can delete or mutate real records if scoped incorrectly. |
| payment branch -> Toss/test state | Payment rehearsal must stay safe and not create uncontrolled provider charges. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-05-01 | Tampering | cleanup SQL | mitigate | Require test IDs, order prefix, marker, denylist, dry-run counts, and backup/restore confirmation. |
| T-26-05-02 | Denial of Service | rehearsal load | mitigate | Rehearsal script runs functional smoke only; high-volume load is Plan 26-06. |
| T-26-05-03 | Information Disclosure | rehearsal evidence | mitigate | Redact auth headers, cookies, paymentKey, QR token, order IDs, and PII. |
| T-26-05-04 | Repudiation | cleanup approval | mitigate | Evidence records owner approval or BLOCKED state before mutation. |
</threat_model>

<verification>
Run SQL source assertions, rehearsal-smoke help mode, strict ledger validation, and the approved rehearsal command when dedicated test-event envs exist.
</verification>

<success_criteria>
The ticketing cutover path is rehearsed against a dedicated test event, and cleanup cannot touch real production data without explicit dry-run and approval evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/26-m1-canary-cutover-gates/26-05-SUMMARY.md`.
</output>
