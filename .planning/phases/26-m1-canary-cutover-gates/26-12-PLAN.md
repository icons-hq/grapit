---
phase: 26-m1-canary-cutover-gates
plan: 12
type: execute
wave: 3
depends_on: [26-11]
files_modified:
  - apps/web/app/admin/cutover/page.tsx
  - apps/web/components/admin/cutover-gate-ledger.tsx
  - apps/web/hooks/use-admin-cutover.ts
  - apps/web/components/admin/admin-sidebar.tsx
  - apps/web/e2e/admin-cutover.spec.ts
autonomous: true
requirements: [M1-01, LOAD-01, DR-01, INFRA-01, OPS-01, PAY-01, OPS-02]
must_haves:
  truths:
    - "Admin sidebar exposes a Cutover Gate page under the existing admin shell."
    - "Gate Ledger UI shows go/no-go summary, state counts, blocker-first rows, evidence freshness, approval state, and rollback/close trigger."
    - "CONFIG_READY_NOT_DRILLED and ACCEPTED_RISK are visibly non-PASS and final BOOKING_ENABLED=true action stays disabled without valid approval metadata."
  artifacts:
    - path: apps/web/app/admin/cutover/page.tsx
      provides: "admin cutover page route"
    - path: apps/web/components/admin/cutover-gate-ledger.tsx
      provides: "Gate Ledger table/cards/detail UI"
    - path: apps/web/hooks/use-admin-cutover.ts
      provides: "React Query hook for admin cutover API"
    - path: apps/web/components/admin/admin-sidebar.tsx
      provides: "admin navigation entry"
  key_links:
    - from: apps/web/hooks/use-admin-cutover.ts
      to: /api/v1/admin/cutover/gates
      via: "apiClient.get"
      pattern: "admin/cutover/gates"
---

<objective>
Build the admin Gate Ledger/cutover readiness surface required by the UI-SPEC.

Purpose: A one-person operator needs a dense, auditable admin page that shows whether ticketing cutover can proceed and why it is blocked, without relying on client-only gate decisions.
Output: Admin page, Gate Ledger component, hook, sidebar link, and route-level E2E smoke.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-m1-canary-cutover-gates/26-CONTEXT.md
@.planning/phases/26-m1-canary-cutover-gates/26-UI-SPEC.md
@.planning/phases/26-m1-canary-cutover-gates/26-PATTERNS.md
@apps/web/app/admin/operations/page.tsx
@apps/web/components/admin/operations-inbox.tsx
@apps/web/hooks/use-admin-operations.ts
@apps/web/components/admin/admin-sidebar.tsx
@apps/api/src/modules/admin/admin-cutover.controller.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add admin cutover route smoke</name>
  <read_first>
    apps/web/e2e/admin-security.spec.ts
    apps/web/e2e/admin-operations.spec.ts
    apps/web/hooks/use-admin-operations.ts
    .planning/phases/26-m1-canary-cutover-gates/26-UI-SPEC.md
  </read_first>
  <files>apps/web/e2e/admin-cutover.spec.ts</files>
  <behavior>
    - Admin can open /admin/cutover from the sidebar.
    - Page renders no-go summary, exact gate state text, first blocking gate, evidence/approval metadata, and disabled BOOKING_ENABLED=true action.
    - ACCEPTED_RISK and CONFIG_READY_NOT_DRILLED are not announced as passed.
  </behavior>
  <action>Add a Playwright route smoke using existing admin auth fixture patterns. Mock or seed the cutover API response with PASS, BLOCKED, ACCEPTED_RISK, and CONFIG_READY_NOT_DRILLED rows. Assert exact UI-SPEC copy for no-go, config-not-drilled, and accepted-risk states. Do not add manager scanner or ticket use-processing UI per D-28.</action>
  <verify>
    <automated>CI=1 TZ=UTC pnpm --filter @grabit/web test:e2e -- admin-cutover.spec.ts</automated>
  </verify>
  <acceptance_criteria>The spec fails if the route is missing, non-PASS states look passed, sidebar link is absent, or final enable action is not disabled with a blocking reason.</acceptance_criteria>
  <done>Admin cutover UI expectations are executable before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement admin cutover hook, page, ledger component, and sidebar link</name>
  <read_first>
    apps/web/app/admin/operations/page.tsx
    apps/web/components/admin/operations-inbox.tsx
    apps/web/hooks/use-admin-operations.ts
    apps/web/components/admin/admin-sidebar.tsx
    .planning/phases/26-m1-canary-cutover-gates/26-UI-SPEC.md
  </read_first>
  <files>apps/web/app/admin/cutover/page.tsx, apps/web/components/admin/cutover-gate-ledger.tsx, apps/web/hooks/use-admin-cutover.ts, apps/web/components/admin/admin-sidebar.tsx</files>
  <behavior>
    - Hook calls apiClient.get('/api/v1/admin/cutover/gates') with React Query key ['admin','cutover'].
    - Page uses existing admin shell density: title row, readiness cards, blocker-first table/cards, selected detail pane, and disabled final action area.
    - Mobile view uses stacked rows/dialog or sheet-friendly detail without horizontal overflow.
  </behavior>
  <action>Implement the UI with existing shadcn/ui primitives, lucide icons, and admin operations patterns. Add a sidebar item labeled 컷오버 게이트 under 운영. Render exact persisted states PASS, FAIL, ACCEPTED_RISK, CONFIG_READY_NOT_DRILLED, and BLOCKED. Keep CONFIG_READY_NOT_DRILLED amber/non-PASS. The BOOKING_ENABLED=true CTA must remain disabled until finalEnableAllowed is true and must show the first blocking gate reason. Do not show raw Toss keys, payment keys, QR tokens, cookies, OTPs, PII, or raw evidence payloads.</action>
  <verify>
    <automated>pnpm --filter @grabit/web exec vitest run components/admin --passWithNoTests && pnpm --filter @grabit/web typecheck</automated>
  </verify>
  <acceptance_criteria>Admin cutover route compiles, sidebar navigation works, state chips and blocker summary match UI-SPEC, and no raw secret-like data is rendered.</acceptance_criteria>
  <done>Admin Gate Ledger/cutover surface is implemented in the existing admin shell.</done>
</task>

<task type="auto">
  <name>Task 3: Verify admin cutover UI and preserve gate semantics</name>
  <read_first>
    apps/web/e2e/admin-cutover.spec.ts
    apps/web/app/admin/cutover/page.tsx
    apps/web/components/admin/cutover-gate-ledger.tsx
    apps/web/hooks/use-admin-cutover.ts
  </read_first>
  <files>apps/web/e2e/admin-cutover.spec.ts</files>
  <action>Run the admin cutover route smoke and targeted web checks. If the route cannot hit a live API in local E2E, keep the API mocked in the spec and record that production API wiring is covered by Plan 26-11. Verify that no UI copy says CONFIG_READY_NOT_DRILLED is PASS and that final enable remains disabled for unapproved non-PASS rows.</action>
  <verify>
    <automated>CI=1 TZ=UTC pnpm --filter @grabit/web test:e2e -- admin-cutover.spec.ts && rg -n "컷오버 게이트|CONFIG_READY_NOT_DRILLED|ACCEPTED_RISK|BOOKING_ENABLED=true|아직 라이브 예매를 열 수 없습니다" apps/web/app/admin/cutover/page.tsx apps/web/components/admin/cutover-gate-ledger.tsx apps/web/components/admin/admin-sidebar.tsx</automated>
  </verify>
  <acceptance_criteria>Browser smoke passes or records an actionable failure, and grep confirms the critical UI-SPEC copy/state semantics exist in the planned files.</acceptance_criteria>
  <done>UI-SPEC Gate Ledger/cutover surface is covered by executable browser verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin API -> browser UI | Gate state data controls operator perception of cutover readiness. |
| browser UI -> operator action | Disabled/enabled affordances influence BOOKING_ENABLED go/no-go. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-12-01 | Spoofing | cutover UI state | mitigate | UI renders only server-provided finalEnableAllowed and firstBlockingGate; no client-only PASS calculation. |
| T-26-12-02 | Information Disclosure | evidence preview | mitigate | Render redacted refs/metadata only and never raw provider payloads or secrets. |
| T-26-12-03 | Repudiation | accepted-risk display | mitigate | Show approvalState, approver, monitoring, and rollback/close trigger for every non-PASS approval. |
| T-26-12-04 | Elevation of Privilege | final cutover CTA | mitigate | CTA stays disabled without server-approved readiness; actual mutation remains Plan 26-10 runbook/CLI. |
</threat_model>

<verification>
Run admin cutover Playwright smoke, web typecheck, and grep assertions for critical UI-SPEC state copy.
</verification>

<success_criteria>
The admin UI required by 26-UI-SPEC exists, is reachable from the admin sidebar, displays every gate state without collapsing non-PASS into PASS, and keeps final cutover disabled until server-side readiness allows it.
</success_criteria>

<output>
After completion, create `.planning/phases/26-m1-canary-cutover-gates/26-12-SUMMARY.md`.
</output>
