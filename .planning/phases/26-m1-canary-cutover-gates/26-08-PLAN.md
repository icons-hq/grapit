---
phase: 26-m1-canary-cutover-gates
plan: 08
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - scripts/phase26/infra-evidence.mjs
  - docs/runbooks/phase26-dr-infra-gate.md
  - .planning/phases/26-m1-canary-cutover-gates/evidence/26-08-dr-infra.json
autonomous: false
requirements: [DR-01, INFRA-01]
user_setup:
  - service: gcp-cloud-sql
    why: "Cloud SQL PITR/restore and HA/read-replica checks may require project permissions and safe restore target approval."
    env_vars:
      - name: PHASE26_DR_APPROVED
        source: "Owner approval for restore target, cost, and timing"
must_haves:
  truths:
    - "Cloud Run rollback, Cloud SQL PITR/restore path, and Valkey reconnect/failure behavior are actually verified or explicitly blocked."
    - "pgBouncer, HA/read replica, and DB pool sizing are recorded as PASS only when drilled; otherwise CONFIG_READY_NOT_DRILLED or ACCEPTED_RISK."
    - "Every GCP command uses the Grabit project and Seoul region explicitly."
  artifacts:
    - path: scripts/phase26/infra-evidence.mjs
      provides: "GCP/DB/Valkey evidence collector"
    - path: docs/runbooks/phase26-dr-infra-gate.md
      provides: "DR and infra gate runbook"
  key_links:
    - from: scripts/phase26/infra-evidence.mjs
      to: .github/workflows/deploy.yml
      via: "Cloud Run env and DB_POOL_MAX inspection"
      pattern: "grapit-491806"
---

<objective>
Create the DR and infrastructure evidence gates for payment cutover.

Purpose: DR-01 and INFRA-01 require platform evidence, but undrilled infrastructure cannot be marked PASS.
Output: Evidence collector, DR/infra runbook, and redacted provider evidence.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-m1-canary-cutover-gates/26-CONTEXT.md
@.planning/phases/26-m1-canary-cutover-gates/26-RESEARCH.md
@.planning/phases/26-m1-canary-cutover-gates/26-PATTERNS.md
@docs/runbooks/phase24-production-operations-handling.md
@scripts/smoke-valkey-production.mjs
@.github/workflows/deploy.yml
@apps/api/src/database/drizzle.provider.ts
@apps/api/src/health/redis.health.indicator.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement infra evidence collector</name>
  <read_first>
    scripts/smoke-valkey-production.mjs
    .github/workflows/deploy.yml
    apps/api/src/database/drizzle.provider.ts
    apps/api/src/health/redis.health.indicator.ts
  </read_first>
  <files>scripts/phase26/infra-evidence.mjs, .planning/phases/26-m1-canary-cutover-gates/evidence/26-08-dr-infra.json</files>
  <action>Create a Node CLI that collects Cloud Run service/revision/traffic/env summaries, Cloud SQL backup/PITR/HA/read-replica metadata, DB_POOL_MAX/pool timeout settings, pgBouncer config evidence if present, and Valkey health/reconnect smoke output. Default to --project=grapit-491806 and --region=asia-northeast3. Redact DATABASE_URL, Redis URLs, secrets, cookies, and provider tokens.</action>
  <verify>
    <automated>node scripts/phase26/infra-evidence.mjs --help && rg -n "grapit-491806|asia-northeast3|DB_POOL_MAX|PITR|Valkey|CONFIG_READY_NOT_DRILLED" scripts/phase26/infra-evidence.mjs</automated>
  </verify>
  <acceptance_criteria>Collector has help mode, explicit project/region defaults, redaction, and separate classifications for PASS, BLOCKED, CONFIG_READY_NOT_DRILLED, and ACCEPTED_RISK.</acceptance_criteria>
  <done>DR/infra evidence can be collected without wrong-project false positives.</done>
</task>

<task type="auto">
  <name>Task 2: Write DR and infra gate runbook</name>
  <read_first>
    docs/runbooks/phase24-production-operations-handling.md
    .planning/phases/26-m1-canary-cutover-gates/26-CONTEXT.md
    .planning/phases/26-m1-canary-cutover-gates/26-RESEARCH.md
  </read_first>
  <files>docs/runbooks/phase26-dr-infra-gate.md</files>
  <action>Create a runbook for D-12 covering actual Cloud Run rollback drill, Cloud SQL PITR/restore to a safe target, Valkey reconnect/failure smoke, pgBouncer transaction pooling evidence, Cloud SQL HA/read-replica evidence, and per-instance DB pool sizing. The runbook must say pgBouncer/HA/read replica/pool sizing are PASS only when actually drilled; otherwise classify as CONFIG_READY_NOT_DRILLED or ACCEPTED_RISK with owner approval and rollback trigger.</action>
  <verify>
    <automated>rg -n "Cloud Run rollback|Cloud SQL|PITR|restore|Valkey|pgBouncer|HA|read replica|DB_POOL_MAX|CONFIG_READY_NOT_DRILLED|ACCEPTED_RISK" docs/runbooks/phase26-dr-infra-gate.md</automated>
  </verify>
  <acceptance_criteria>Runbook distinguishes actual drills from config evidence and provides safe-target approval steps for restore drills.</acceptance_criteria>
  <done>DR-01 and INFRA-01 have operator-safe execution instructions.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Approve and execute safe DR drill targets</name>
  <read_first>
    docs/runbooks/phase26-dr-infra-gate.md
    scripts/phase26/infra-evidence.mjs
    .planning/phases/26-m1-canary-cutover-gates/26-GATE-LEDGER.json
  </read_first>
  <files>.planning/phases/26-m1-canary-cutover-gates/evidence/26-08-dr-infra.json</files>
  <action>Ask the owner to approve Cloud SQL restore target, timing, and cost before any restore/PITR drill. Run Cloud Run rollback evidence, Cloud SQL PITR/restore safe-target checks, Valkey reconnect/failure smoke, and infra config evidence where approved. If approval or permissions are missing, record BLOCKED or CONFIG_READY_NOT_DRILLED; do not mark undrilled infra as PASS.</action>
  <verify>
    <automated>node scripts/phase26/infra-evidence.mjs --help</automated>
  </verify>
  <acceptance_criteria>Evidence file records PASS only for actual successful drills and preserves CONFIG_READY_NOT_DRILLED/ACCEPTED_RISK/BLOCKED for non-drilled or unapproved items.</acceptance_criteria>
  <done>DR and infra gates have safe, traceable evidence for final cutover.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI -> GCP control plane | Commands can inspect or modify production infrastructure. |
| restore drill -> Cloud SQL data | PITR/restore must target safe instances only. |
| infra evidence -> cutover decision | Undrilled config can be misrepresented as PASS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-08-01 | Tampering | Cloud SQL restore | mitigate | Require owner-approved safe target and explicit project/region before restore. |
| T-26-08-02 | Denial of Service | Cloud Run rollback | mitigate | Run rollback drill with known previous revision and record rollback readiness. |
| T-26-08-03 | Information Disclosure | infra evidence | mitigate | Redact DATABASE_URL, Redis URLs, secrets, and provider tokens. |
| T-26-08-04 | Repudiation | config-only evidence | mitigate | Preserve CONFIG_READY_NOT_DRILLED distinct from PASS with timestamps and approver fields. |
</threat_model>

<verification>
Run infra-evidence help mode, runbook assertions, and approved GCP/Valkey evidence commands with explicit Grabit project/region.
</verification>

<success_criteria>
DR-01 and INFRA-01 are evidenced without unsafe restore actions, wrong-project checks, or false PASS classification.
</success_criteria>

<output>
After completion, create `.planning/phases/26-m1-canary-cutover-gates/26-08-SUMMARY.md`.
</output>
