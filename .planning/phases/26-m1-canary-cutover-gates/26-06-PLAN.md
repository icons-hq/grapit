---
phase: 26-m1-canary-cutover-gates
plan: 06
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - scripts/k6/phase26-baseline.js
  - scripts/k6/phase26-stress.js
  - scripts/phase26/record-k6-evidence.mjs
  - docs/runbooks/phase26-load-gate.md
  - .planning/phases/26-m1-canary-cutover-gates/evidence/26-06-load.json
autonomous: false
requirements: [LOAD-01]
user_setup:
  - service: load-test target approval
    why: "D-09 requires 10k/20k load attempts against an approved dedicated test-event target."
    env_vars:
      - name: PHASE26_LOAD_APPROVED
        source: "Owner approval for target, time window, and dedicated test-event identifiers"
must_haves:
  truths:
    - "k6 baseline and stress scripts enforce p95 under 2 seconds and error rate under 1%."
    - "Load targets the dedicated test event and does not high-volume hit Toss payment provider APIs."
    - "Failed k6 gates block cutover unless explicitly owner-approved as ACCEPTED_RISK."
  artifacts:
    - path: scripts/k6/phase26-baseline.js
      provides: "10k baseline scenario"
    - path: scripts/k6/phase26-stress.js
      provides: "20k stress scenario"
    - path: docs/runbooks/phase26-load-gate.md
      provides: "operator load gate instructions"
  key_links:
    - from: scripts/phase26/record-k6-evidence.mjs
      to: .planning/phases/26-m1-canary-cutover-gates/evidence/26-06-load.json
      via: "summary export parsing"
      pattern: "p\\(95\\).*2000"
---

<objective>
Create and run the k6 load gates for ticketing cutover.

Purpose: LOAD-01 requires 10k baseline and 20k stress attempts with p95 < 2s and error rate < 1%, using dedicated test-event scope and fail-closed gate semantics.
Output: k6 scripts, evidence recorder, runbook, and load-gate evidence.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-m1-canary-cutover-gates/26-CONTEXT.md
@.planning/phases/26-m1-canary-cutover-gates/26-RESEARCH.md
@.planning/phases/26-m1-canary-cutover-gates/26-PATTERNS.md
@scripts/phase26/rehearsal-smoke.mjs
@apps/api/src/modules/traffic/traffic-defense.service.ts
@apps/api/src/modules/queue/queue.service.ts
@docs/runbooks/phase24-production-operations-handling.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add k6 baseline and stress scenarios</name>
  <read_first>
    .planning/phases/26-m1-canary-cutover-gates/26-CONTEXT.md
    .planning/phases/26-m1-canary-cutover-gates/26-RESEARCH.md
    apps/api/src/modules/traffic/traffic-defense.service.ts
    apps/api/src/modules/queue/queue.service.ts
  </read_first>
  <files>scripts/k6/phase26-baseline.js, scripts/k6/phase26-stress.js</files>
  <behavior>
    - Both scripts define thresholds http_req_duration p(95)<2000 and http_req_failed rate<0.01.
    - Baseline and stress use env-configurable rate, duration, VUs, API URL, performanceId, showtimeId, and queue/read/mutation mix.
    - Toss confirm/cancel/query are not included in high-volume paths.
  </behavior>
  <action>Create two k6 scripts per D-09 through D-11. Use constant-arrival-rate scenarios and env defaults that can be scaled by the operator. Exercise public detail/runtime flags/queue read paths at volume, sample lock/prepare safely, and exclude high-volume payment provider calls. Hard-fail when dedicated test identifiers are missing.</action>
  <verify>
    <automated>rg -n "http_req_duration|p\\(95\\)<2000|http_req_failed|rate<0\\.01|PHASE26_TEST|constant-arrival-rate" scripts/k6/phase26-baseline.js scripts/k6/phase26-stress.js</automated>
  </verify>
  <acceptance_criteria>Both k6 scripts encode the required thresholds and dedicated test-event guardrails.</acceptance_criteria>
  <done>LOAD-01 scenarios are executable without embedding real event IDs or secrets.</done>
</task>

<task type="auto">
  <name>Task 2: Add load evidence recorder and runbook</name>
  <read_first>
    scripts/k6/phase26-baseline.js
    scripts/k6/phase26-stress.js
    docs/runbooks/phase24-production-operations-handling.md
    .planning/phases/26-m1-canary-cutover-gates/26-GATE-LEDGER.json
  </read_first>
  <files>scripts/phase26/record-k6-evidence.mjs, docs/runbooks/phase26-load-gate.md, .planning/phases/26-m1-canary-cutover-gates/evidence/26-06-load.json</files>
  <action>Create a Node evidence recorder that reads k6 JSON summaries and writes baseline/stress p95, error rate, target, timestamp, and PASS/FAIL/BLOCKED classification. Create a runbook with docker k6 commands, target approval checklist, Cloud Run/DB/Valkey side metric checks, stop criteria, and ledger handling. Preserve failed gates as FAIL or ACCEPTED_RISK only with owner approval.</action>
  <verify>
    <automated>node scripts/phase26/record-k6-evidence.mjs --help && rg -n "docker run --rm -i grafana/k6|10k|20k|p95|error rate|ACCEPTED_RISK|BLOCKED" docs/runbooks/phase26-load-gate.md</automated>
  </verify>
  <acceptance_criteria>Recorder and runbook classify failed/missing load evidence as non-PASS and provide exact commands using Docker k6 fallback.</acceptance_criteria>
  <done>Load-gate evidence can be captured and fed into the final Gate Ledger.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run approved 10k and 20k load attempts</name>
  <read_first>
    docs/runbooks/phase26-load-gate.md
    scripts/k6/phase26-baseline.js
    scripts/k6/phase26-stress.js
    scripts/phase26/record-k6-evidence.mjs
  </read_first>
  <files>.planning/phases/26-m1-canary-cutover-gates/evidence/26-06-load.json</files>
  <action>Ask the owner to confirm the target, time window, and dedicated test event. Run the baseline and stress commands only when approved. Record p95/error-rate results. If approval, credentials, or target readiness is missing, write BLOCKED evidence. If thresholds fail, write FAIL and do not convert it to PASS; accepted-risk progression is reserved for final Gate Ledger approval per D-01 through D-04.</action>
  <verify>
    <automated>docker run --rm grafana/k6 version</automated>
  </verify>
  <acceptance_criteria>10k and 20k attempts have PASS/FAIL/BLOCKED evidence with p95/error-rate values, or the absence of approval is explicitly BLOCKED.</acceptance_criteria>
  <done>LOAD-01 is evidenced without uncontrolled production load or payment-provider stress.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| k6 runner -> production-like API | Synthetic traffic can affect real availability. |
| load evidence -> cutover decision | Metrics decide whether cutover can proceed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-06-01 | Denial of Service | k6 scripts | mitigate | Require owner approval, dedicated test event, stop criteria, and no high-volume Toss calls. |
| T-26-06-02 | Tampering | k6 evidence | mitigate | Recorder writes p95/error-rate from summary exports and preserves FAIL/BLOCKED states. |
| T-26-06-03 | Repudiation | operator approval | mitigate | Evidence records target, time window, approval state, and command shape. |
| T-26-06-04 | Information Disclosure | load logs | mitigate | Scripts and recorder do not persist auth headers, cookies, payment keys, or PII. |
</threat_model>

<verification>
Run source assertions for thresholds, recorder help mode, docker k6 version, and approved baseline/stress commands when the target is approved.
</verification>

<success_criteria>
LOAD-01 has executable 10k/20k gates with strict thresholds and redacted pass/fail evidence that final cutover can trust.
</success_criteria>

<output>
After completion, create `.planning/phases/26-m1-canary-cutover-gates/26-06-SUMMARY.md`.
</output>
