---
phase: 22-preflight-closure
plan: "06"
type: execute
wave: 4
depends_on:
  - "22-05"
files_modified:
  - apps/api/src/modules/sms/sms.controller.ts
  - apps/api/src/modules/sms/sms.service.ts
  - apps/api/src/modules/sms/sms.service.spec.ts
  - .planning/phases/22-preflight-closure/22-UAT.md
autonomous: true
gap_closure: true
requirements:
  - PREF-01
must_haves:
  truths:
    - "Invalid-but-regex-valid international SMS phone input returns HTTP 400 user-facing validation, not production 500."
    - "Invalid phone input is rejected before Valkey counters, OTP state, cooldown, or Infobip work."
    - "Valid reserved phone input with no OTP still returns the existing expired-code path."
  artifacts:
    - path: "apps/api/src/modules/sms/sms.service.ts"
      provides: "SMS service phone normalization and HTTP exception boundary"
    - path: "apps/api/src/modules/sms/sms.service.spec.ts"
      provides: "Regression tests for invalid international phone handling"
    - path: ".planning/phases/22-preflight-closure/22-UAT.md"
      provides: "Closed UAT gap record"
---

<objective>
Close the Phase 22 production UAT gap where invalid-but-regex-valid international SMS phone numbers escaped `parseE164()` as plain `Error` and produced HTTP 500.

Purpose: SMS send and verify endpoints must convert user phone validation failures into HTTP 400 before any provider or Valkey side effect.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
@/Users/sangwopark19/.codex/plugins/cache/openai-curated/superpowers/c5debd62/skills/test-driven-development/SKILL.md
</execution_context>

<context>
@.planning/phases/22-preflight-closure/22-UAT.md
@apps/api/src/modules/sms/sms.service.ts
@apps/api/src/modules/sms/sms.service.spec.ts
@apps/api/src/modules/sms/phone.util.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SMS invalid international phone regression tests</name>
  <files>apps/api/src/modules/sms/sms.service.spec.ts</files>
  <action>
    Add regression coverage for an invalid-but-regex-valid international number such as `+9991234567`.

    Required assertions:
    - `sendVerificationCode()` rejects with `BadRequestException`.
    - invalid `sendVerificationCode()` does not call Redis cooldown/counter/pipeline work or Infobip.
    - `verifyCode()` rejects with `BadRequestException`.
    - invalid `verifyCode()` does not call Redis verify counter or Lua work.
    - Existing valid no-OTP verify behavior still returns `GoneException`.
  </action>
  <verify>
    <automated>pnpm --filter @grabit/api test -- src/modules/sms/sms.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - New tests fail before the production fix because `parseE164()` currently throws a plain `Error`.
    - Tests target behavior, not implementation-only internals.
  </acceptance_criteria>
  <done>Regression tests prove the production 500 path is reproducible locally.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize phone parsing failures at the service boundary</name>
  <files>apps/api/src/modules/sms/sms.service.ts</files>
  <action>
    Add a small service-layer normalization boundary around `parseE164()`.

    Required behavior:
    - If `parseE164()` throws the public phone validation message, rethrow `BadRequestException('올바른 휴대폰 번호를 입력해주세요')`.
    - Use the normalized helper in `sendVerificationCode()`, `verifyCode()`, and `isPhoneVerified()`.
    - Preserve China mainland rejection, valid no-OTP `GoneException`, rate limit behavior, and Valkey rollback behavior.
  </action>
  <verify>
    <automated>pnpm --filter @grabit/api test -- src/modules/sms/sms.service.spec.ts</automated>
    <automated>pnpm --filter @grabit/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Invalid-but-regex-valid international input returns a Nest `BadRequestException`.
    - No SMS provider or Valkey side effects occur before invalid phone rejection.
    - Existing SMS service tests pass.
  </acceptance_criteria>
  <done>SMS service converts phone normalization failures to user-facing HTTP 400 before side effects.</done>
</task>

<task type="auto">
  <name>Task 3: Close the Phase 22 UAT gap and summarize execution</name>
  <files>.planning/phases/22-preflight-closure/22-UAT.md, .planning/phases/22-preflight-closure/22-06-SUMMARY.md</files>
  <action>
    Update the Phase 22 UAT gap record to mark test 9 resolved by the service fix and create a plan summary with commands run.
  </action>
  <verify>
    <automated>rg -n "test: 9|status: resolved|22-06" .planning/phases/22-preflight-closure/22-UAT.md .planning/phases/22-preflight-closure/22-06-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `22-UAT.md` no longer records the invalid phone gap as failed.
    - `22-06-SUMMARY.md` records commits, changed files, and verification output.
  </acceptance_criteria>
  <done>Phase 22 UAT gap closure is documented and resumable.</done>
</task>

</tasks>

<verification>
Run:
- `pnpm --filter @grabit/api test -- src/modules/sms/sms.service.spec.ts`
- `pnpm --filter @grabit/api typecheck`

Manual production redeploy and rerun of UAT test 9 remains a follow-up unless production deployment is explicitly requested.
</verification>

<success_criteria>
- Invalid-but-regex-valid international SMS input is handled as HTTP 400 locally.
- No provider/Valkey side effects happen before invalid phone rejection.
- Phase 22 UAT gap is closed in planning artifacts with a summary.
</success_criteria>
