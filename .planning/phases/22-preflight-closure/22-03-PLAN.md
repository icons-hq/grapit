---
phase: 22-preflight-closure
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/smoke-valkey-production.mjs
  - .planning/phases/22-preflight-closure/22-HARDENING-REGISTER.md
autonomous: true
requirements:
  - PREF-03
must_haves:
  truths:
    - "D-21: Valkey/R2/SMS/email/legal fragile points close as concrete fix, ACCEPTED_RISK, or BLOCKER."
    - "D-22: Pre-existing debug sessions are linked to a Phase 22 fix, converted to explicit risk, or closed."
    - "D-23: Direct fixes stay on already-shipped operational surfaces only."
    - "Production Valkey smoke evidence writes into a Phase 22 traceability path by default."
  artifacts:
    - path: "scripts/smoke-valkey-production.mjs"
      provides: "Repeatable production Valkey smoke with Phase 22 default artifact path"
    - path: ".planning/phases/22-preflight-closure/22-HARDENING-REGISTER.md"
      provides: "Operational fragility closure register for PREF-03"
  key_links:
    - from: "scripts/smoke-valkey-production.mjs"
      to: ".planning/phases/22-preflight-closure/artifacts/valkey-smoke.md"
      via: "defaultArtifactUrl when GRABIT_SMOKE_ARTIFACT is not set"
---

<objective>
Close the known Phase 22 hardening traceability gap and create the operational fragility register.

Purpose: PREF-03 cannot pass if production smoke evidence defaults into an archived Phase 20 path or if Valkey/R2/SMS/email/legal risks are described informally instead of classified as fixes, accepted risks, or blockers.

Output: Updated Valkey smoke script default artifact path and `22-HARDENING-REGISTER.md`.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-preflight-closure/22-CONTEXT.md
@.planning/phases/22-preflight-closure/22-RESEARCH.md
@.planning/phases/22-preflight-closure/22-VALIDATION.md
@.planning/phases/22-preflight-closure/22-PATTERNS.md
@scripts/smoke-valkey-production.mjs
@apps/api/src/modules/sms/sms.service.ts
@apps/api/src/modules/admin/upload.service.ts
@apps/api/src/modules/auth/email/email.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Valkey smoke default artifact path</name>
  <files>scripts/smoke-valkey-production.mjs</files>
  <read_first>
    - scripts/smoke-valkey-production.mjs
    - .planning/phases/22-preflight-closure/22-RESEARCH.md
    - .planning/phases/22-preflight-closure/22-VALIDATION.md
    - .planning/phases/22-preflight-closure/22-PATTERNS.md
  </read_first>
  <action>
    Update only `scripts/smoke-valkey-production.mjs`.

    Change the default artifact URL from:
    `../.planning/phases/20-valkey-production-connectivity-contract/20-HUMAN-UAT.md`

    To:
    `../.planning/phases/22-preflight-closure/artifacts/valkey-smoke.md`

    Keep `const artifactPath = process.env.GRABIT_SMOKE_ARTIFACT ?? fileURLToPath(defaultArtifactUrl);` unchanged so explicit operator overrides still work.

    Update the `usage()` help text line for `GRABIT_SMOKE_ARTIFACT` from `Default: script-root 20-HUMAN-UAT.md` to `Default: .planning/phases/22-preflight-closure/artifacts/valkey-smoke.md`.

    Do not change smoke check logic, Cloud Run query logic, Sentry/log redaction, Redis URL redaction, command shape generation, or Socket.IO behavior. This implements D-21, D-22, D-23, and T-22-04 by keeping production smoke evidence inside Phase 22 traceability.
  </action>
  <verify>
    <automated>rg -n "22-preflight-closure/artifacts/valkey-smoke.md|GRABIT_SMOKE_ARTIFACT" scripts/smoke-valkey-production.mjs</automated>
    <automated>node scripts/smoke-valkey-production.mjs --help | rg "22-preflight-closure/artifacts/valkey-smoke.md"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/smoke-valkey-production.mjs` no longer contains `20-valkey-production-connectivity-contract/20-HUMAN-UAT.md`.
    - `scripts/smoke-valkey-production.mjs` still contains `process.env.GRABIT_SMOKE_ARTIFACT ?? fileURLToPath(defaultArtifactUrl)`.
    - `node scripts/smoke-valkey-production.mjs --help` prints `.planning/phases/22-preflight-closure/artifacts/valkey-smoke.md`.
  </acceptance_criteria>
  <done>Production Valkey smoke script defaults to a Phase 22 artifact path while preserving overrides and redaction.</done>
</task>

<task type="auto">
  <name>Task 2: Create operational hardening register</name>
  <files>.planning/phases/22-preflight-closure/22-HARDENING-REGISTER.md</files>
  <read_first>
    - .planning/phases/22-preflight-closure/22-HARDENING-REGISTER.md (if it already exists)
    - .planning/STATE.md
    - .planning/milestones/v1.1-MILESTONE-AUDIT.md
    - .planning/phases/22-preflight-closure/22-CONTEXT.md
    - .planning/phases/22-preflight-closure/22-RESEARCH.md
    - .planning/phases/22-preflight-closure/22-PATTERNS.md
    - scripts/smoke-valkey-production.mjs
    - apps/api/src/modules/sms/sms.service.ts
    - apps/api/src/modules/admin/upload.service.ts
    - apps/api/src/modules/auth/email/email.service.ts
  </read_first>
  <action>
    Create `.planning/phases/22-preflight-closure/22-HARDENING-REGISTER.md`.

    Required sections:
    - `# Phase 22 Hardening Register`
    - `## Disposition Rules`
    - `## Register`
    - `## Debug Session Routing`
    - `## Smoke And Evidence Commands`

    `## Disposition Rules` must state that every finding closes as exactly one of `concrete fix`, `ACCEPTED_RISK`, or `BLOCKER`. `ACCEPTED_RISK` requires maintainer/operator approvals with owner and KST date. `BLOCKER` requires owner and next action.

    `## Register` must use columns `Area`, `Finding`, `Threat Ref`, `Evidence`, `Disposition`, `Owner`, `Accepted By`, `Checked At / Due`, `Next Action`.

    Include at minimum these rows:
    - Area `Valkey`, finding `Production smoke artifact path defaulted to Phase 20 path`, threat `T-22-04`, disposition `concrete fix`, evidence `scripts/smoke-valkey-production.mjs defaultArtifactUrl -> .planning/phases/22-preflight-closure/artifacts/valkey-smoke.md`.
    - Area `Valkey`, finding `Cloud Run -> Valkey health/lua/socketio/logs production smoke missing`, threat `T-22-04`, disposition `BLOCKER`, next action `Run node scripts/smoke-valkey-production.mjs --checks=health,lua,socketio,logs or record ACCEPTED_RISK with D-02 approvals`.
    - Area `R2`, finding `Production R2 local fallback or missing provider config must not be PASS`, threat `T-22-05`, disposition `BLOCKER`, next action `Collect R2 provider/config evidence or record ACCEPTED_RISK with D-02 approvals`.
    - Area `SMS`, finding `isPhoneVerified is signup idempotency support only`, threat `T-22-03`, disposition `ACCEPTED_RISK` unless stronger binding evidence exists, next action `Record maintainer/operator acceptance or link a shipped-surface fix`.
    - Area `SMS`, finding `Real-device OTP/provider observation missing`, threat `T-22-06`, disposition `BLOCKER`, next action `Complete 22-HUMAN-UAT.md SMS gate`.
    - Area `Email`, finding `Gmail reset-to-login/provider observation missing`, threat `T-22-06`, disposition `BLOCKER`, next action `Complete 22-HUMAN-UAT.md Email gate`.
    - Area `Legal`, finding `Public URL/mailbox/factual sign-off missing`, threat `T-22-06`, disposition `BLOCKER`, next action `Complete 22-HUMAN-UAT.md Legal gate`.

    `## Debug Session Routing` must list these debug sessions from STATE and route each to `closed`, `v2.0 risk`, or `Phase 22 fix task`: `signup-sms-otp-verify-wrong`, `password-reset-email-not-delivered-prod`, and `legal-pages-404-heygrabit`.

    `## Smoke And Evidence Commands` must include:
    `node scripts/smoke-valkey-production.mjs --checks=health,lua,socketio,logs`
    and state that the default artifact is `.planning/phases/22-preflight-closure/artifacts/valkey-smoke.md`.
  </action>
  <verify>
    <automated>test -f .planning/phases/22-preflight-closure/22-HARDENING-REGISTER.md</automated>
    <automated>rg -n "concrete fix|ACCEPTED_RISK|BLOCKER|T-22-03|T-22-04|T-22-05|T-22-06" .planning/phases/22-preflight-closure/22-HARDENING-REGISTER.md</automated>
    <automated>rg -n "signup-sms-otp-verify-wrong|password-reset-email-not-delivered-prod|legal-pages-404-heygrabit|22-preflight-closure/artifacts/valkey-smoke.md" .planning/phases/22-preflight-closure/22-HARDENING-REGISTER.md</automated>
  </verify>
  <acceptance_criteria>
    - `22-HARDENING-REGISTER.md` contains the exact strings `D-21`, `D-22`, and `D-23`.
    - `22-HARDENING-REGISTER.md` contains rows for areas `Valkey`, `R2`, `SMS`, `Email`, and `Legal`.
    - `22-HARDENING-REGISTER.md` contains threat refs `T-22-03`, `T-22-04`, `T-22-05`, and `T-22-06`.
    - `22-HARDENING-REGISTER.md` contains the exact command `node scripts/smoke-valkey-production.mjs --checks=health,lua,socketio,logs`.
  </acceptance_criteria>
  <done>Hardening register exists and the known Valkey smoke traceability issue has a concrete fix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Smoke script -> evidence artifact | Production command output is appended into a git-tracked Phase 22 artifact path. |
| Provider/runtime state -> hardening register | Live Valkey/R2/SMS/email/legal observations become launch readiness classifications. |
| Shipped operational surface -> code fix | Direct code edits are allowed only for already-shipped operational surfaces. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Mitigation | Block/Accept Criteria |
|-----------|----------|----------|-----------|------------|-----------------------|
| T-22-04 | high | Tampering / Availability | `scripts/smoke-valkey-production.mjs` | Patch default artifact path to Phase 22 and keep `GRABIT_SMOKE_ARTIFACT` override. | Block if smoke output writes outside `.planning/phases/22-preflight-closure/`. |
| T-22-05 | high | Availability / Information Disclosure | `22-HARDENING-REGISTER.md`, R2 upload surface | R2 local fallback or missing provider config cannot be classified as PASS without direct production evidence. | Block if R2 row is PASS-equivalent without provider/config evidence; accept only as `ACCEPTED_RISK` with D-02 approvals. |
| T-22-03 | high | Spoofing / Elevation of Privilege | SMS verified flag classification | Register states `isPhoneVerified` is signup idempotency support only, not standalone public auth proof. | Block if register or UAT treats `isPhoneVerified` as public authentication proof. |
| T-22-01 | high | Information Disclosure | Smoke artifact and register | Existing smoke redaction must be preserved; register stores command shape and redacted evidence paths only. | Block if Redis URLs, auth headers, phone numbers, reset links, or secrets are committed. |
</threat_model>

<verification>
Run:

```bash
node scripts/smoke-valkey-production.mjs --help | rg "22-preflight-closure/artifacts/valkey-smoke.md"
rg -n "20-valkey-production-connectivity-contract/20-HUMAN-UAT.md" scripts/smoke-valkey-production.mjs && exit 1 || true
test -f .planning/phases/22-preflight-closure/22-HARDENING-REGISTER.md
rg -n "Valkey|R2|SMS|Email|Legal|concrete fix|ACCEPTED_RISK|BLOCKER" .planning/phases/22-preflight-closure/22-HARDENING-REGISTER.md
```
</verification>

<success_criteria>
PREF-03 has a concrete hardening register, and production Valkey smoke evidence can no longer land in an archived Phase 20 default path.
</success_criteria>

<output>
After completion, create `.planning/phases/22-preflight-closure/22-03-SUMMARY.md`.
</output>
