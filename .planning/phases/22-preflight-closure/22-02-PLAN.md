---
phase: 22-preflight-closure
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/22-preflight-closure/22-VALIDATION-BASELINE.md
autonomous: true
requirements:
  - PREF-02
must_haves:
  truths:
    - "D-18: Each v1.1 validation gap is classified as COMPLETE, ACCEPTED_CAVEAT, or BLOCKER."
    - "D-19: Human-needed or operator-needed evidence remains visible and is not converted into automated proof."
    - "D-20: Traceability points to existing evidence or the Phase 22 baseline without implying prior work was newly executed."
  artifacts:
    - path: ".planning/phases/22-preflight-closure/22-VALIDATION-BASELINE.md"
      provides: "v1.1 validation gap baseline for PREF-02"
  key_links:
    - from: ".planning/phases/22-preflight-closure/22-VALIDATION-BASELINE.md"
      to: ".planning/milestones/v1.1-MILESTONE-AUDIT.md"
      via: "Gap rows cite historical audit findings"
---

<objective>
Create the v1.1 launch-readiness validation baseline for PREF-02.

Purpose: Phase 23 must start from a clear baseline instead of inherited ambiguous `human_needed`, missing verification, and deferred evidence states.

Output: `22-VALIDATION-BASELINE.md` classifying historical gaps as `COMPLETE`, `ACCEPTED_CAVEAT`, or `BLOCKER`.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.1-MILESTONE-AUDIT.md
@.planning/milestones/v1.1-ROADMAP.md
@.planning/phases/22-preflight-closure/22-CONTEXT.md
@.planning/phases/22-preflight-closure/22-RESEARCH.md
@.planning/phases/22-preflight-closure/22-VALIDATION.md
@.planning/phases/22-preflight-closure/22-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create v1.1 validation baseline classification</name>
  <files>.planning/phases/22-preflight-closure/22-VALIDATION-BASELINE.md</files>
  <read_first>
    - .planning/phases/22-preflight-closure/22-VALIDATION-BASELINE.md (if it already exists)
    - .planning/STATE.md
    - .planning/milestones/v1.1-MILESTONE-AUDIT.md
    - .planning/milestones/v1.1-ROADMAP.md
    - .planning/phases/22-preflight-closure/22-CONTEXT.md
    - .planning/phases/22-preflight-closure/22-VALIDATION.md
  </read_first>
  <action>
    Create `.planning/phases/22-preflight-closure/22-VALIDATION-BASELINE.md`.

    Required sections:
    - `# Phase 22 Validation Baseline`
    - `## Classification Rules`
    - `## v1.1 Gap Matrix`
    - `## Historical Artifact References`
    - `## Baseline Summary`

    `## Classification Rules` must implement D-18, D-19, and D-20. Define:
    - `COMPLETE`: existing evidence path proves the launch-readiness behavior.
    - `ACCEPTED_CAVEAT`: evidence exists but a human/provider/dashboard caveat remains visible with owner/date or explicit reason.
    - `BLOCKER`: evidence is missing/failing and can break the launch path.

    `## v1.1 Gap Matrix` must use columns `Area`, `Source`, `Requirement / Legacy ID`, `Classification`, `Evidence Path`, `Reason`, `Owner`, `Next Action`.

    Include at minimum these rows from STATE and the v1.1 milestone audit:
    - `SMS real-device OTP`, source `.planning/milestones/v1.1-MILESTONE-AUDIT.md`, legacy id `SMS-02`, initial classification `BLOCKER`, reason `Post-CROSSSLOT production real-device UAT and zero-CROSSSLOT/Sentry window remain unchecked.`
    - `Email reset-to-login`, source `.planning/milestones/v1.1-MILESTONE-AUDIT.md`, legacy id `CUTOVER-05 / DEBT-01`, classification based on current evidence: use `COMPLETE` only if Phase 18 evidence proves reset email -> confirm -> login; otherwise `BLOCKER`.
    - `Legal public/sign-off`, source `.planning/milestones/v1.1-MILESTONE-AUDIT.md`, legacy id `Phase 16 legal gates`, initial classification `BLOCKER`, reason `External factual sign-off, mailbox receipt, and production URL smoke checks remain open.`
    - `Valkey production runtime`, source `.planning/milestones/v1.1-MILESTONE-AUDIT.md`, legacy id `VALK-03 / VALK-04 / VALK-05`, initial classification `BLOCKER`, reason `Live Cloud Run -> Valkey smoke and provider observation evidence remain missing.`
    - `R2 production evidence`, source `.planning/milestones/v1.1-MILESTONE-AUDIT.md`, legacy id `R2-02`, classification `ACCEPTED_CAVEAT` if historical R2 CORS/upload evidence exists but live Phase 22 provider proof is missing; otherwise `BLOCKER`.
    - `Missing/stale verification artifacts`, source `.planning/STATE.md`, legacy id `verification_gap Phase 10, 10.1, 11, 12, 13, 14, 15, 16, 18, 20`, classification `ACCEPTED_CAVEAT`, reason `Historical validation artifact cleanup is preserved as caveat unless the gap blocks Phase 23 launch readiness.`

    `## Historical Artifact References` must list exact `git show bd8220e:<path>` commands for Phase 14, 15, 16, 18, 20, and 21 verification/HUMAN-UAT artifacts found in research:
    - `.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-VERIFICATION.md`
    - `.planning/phases/15-resend-heygrabit-com-cutover-transactional-email-secret-mana/15-VERIFICATION.md`
    - `.planning/phases/16-legal-pages-launch-url/16-VERIFICATION.md`
    - `.planning/phases/18-password-reset-production-api-origin-fix/18-VERIFICATION.md`
    - `.planning/phases/20-valkey-production-connectivity-contract/20-VERIFICATION.md`
    - `.planning/phases/21-verification-artifact-backfill/21-VERIFICATION.md`

    Do not edit any historical Phase 14/15/16/18/20/21 artifact.
  </action>
  <verify>
    <automated>test -f .planning/phases/22-preflight-closure/22-VALIDATION-BASELINE.md</automated>
    <automated>rg -n "COMPLETE|ACCEPTED_CAVEAT|BLOCKER|SMS-02|CUTOVER-05|DEBT-01|VALK-03|VALK-04|VALK-05|R2-02" .planning/phases/22-preflight-closure/22-VALIDATION-BASELINE.md</automated>
    <automated>rg -n "git show bd8220e:.planning/phases/(14|15|16|18|20|21)-" .planning/phases/22-preflight-closure/22-VALIDATION-BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - `22-VALIDATION-BASELINE.md` contains the exact strings `D-18`, `D-19`, and `D-20`.
    - `22-VALIDATION-BASELINE.md` contains the table header `| Area | Source | Requirement / Legacy ID | Classification | Evidence Path | Reason | Owner | Next Action |`.
    - `22-VALIDATION-BASELINE.md` includes rows for `SMS real-device OTP`, `Email reset-to-login`, `Legal public/sign-off`, `Valkey production runtime`, `R2 production evidence`, and `Missing/stale verification artifacts`.
    - No historical phase artifact file is modified by this plan.
  </acceptance_criteria>
  <done>Validation baseline exists and preserves historical caveats without fabricating new evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Verify baseline has no unsupported status labels</name>
  <files>.planning/phases/22-preflight-closure/22-VALIDATION-BASELINE.md</files>
  <read_first>
    - .planning/phases/22-preflight-closure/22-VALIDATION-BASELINE.md
    - .planning/phases/22-preflight-closure/22-VALIDATION.md
  </read_first>
  <action>
    Review the baseline table and remove any classification value that is not exactly `COMPLETE`, `ACCEPTED_CAVEAT`, or `BLOCKER`.

    Add a `## Baseline Summary` section with:
    - `Total Rows: <number>`
    - `COMPLETE: <number>`
    - `ACCEPTED_CAVEAT: <number>`
    - `BLOCKER: <number>`
    - `Phase 23 Readiness Impact: <No blockers | Blocked by listed rows>`

    If any row is `ACCEPTED_CAVEAT`, ensure its `Reason` cell names the caveat and its `Owner` cell is not empty. If any row is `BLOCKER`, ensure its `Next Action` cell is not `-`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='.planning/phases/22-preflight-closure/22-VALIDATION-BASELINE.md'; const s=fs.readFileSync(p,'utf8'); for (const label of ['COMPLETE','ACCEPTED_CAVEAT','BLOCKER','Phase 23 Readiness Impact']) if(!s.includes(label)) throw new Error(label+' missing');"</automated>
  </verify>
  <acceptance_criteria>
    - `22-VALIDATION-BASELINE.md` contains `Phase 23 Readiness Impact:`.
    - Every `ACCEPTED_CAVEAT` row has a non-empty `Reason` and `Owner`.
    - Every `BLOCKER` row has a non-empty `Next Action`.
  </acceptance_criteria>
  <done>Validation baseline is internally consistent and ready for final ledger aggregation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Archived evidence -> active baseline | Historical artifacts from git history inform a current launch-readiness baseline. |
| Manual evidence state -> validation classification | Human-needed evidence is interpreted into launch vocabulary. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Mitigation | Block/Accept Criteria |
|-----------|----------|----------|-----------|------------|-----------------------|
| T-22-02 | high | Repudiation | `22-VALIDATION-BASELINE.md` | D-19/D-20 require historical references and caveats; no archived file is rewritten. | Block if baseline claims a historical artifact was newly executed or edits archived Phase 14/15/16/18/20/21 files. |
| T-22-06 | high | Repudiation | Validation baseline | Each `COMPLETE` requires evidence path; missing provider observation remains `ACCEPTED_CAVEAT` or `BLOCKER`. | Block if `COMPLETE` appears with empty evidence path or human-needed reason. |
| T-22-01 | high | Information Disclosure | Baseline evidence paths | Use only redacted artifact paths and command shapes, not raw dashboard payloads. | Block if phone numbers, reset links, cookies, bearer headers, or provider secrets appear in evidence cells. |
</threat_model>

<verification>
Run:

```bash
test -f .planning/phases/22-preflight-closure/22-VALIDATION-BASELINE.md
rg -n "D-18|D-19|D-20|COMPLETE|ACCEPTED_CAVEAT|BLOCKER" .planning/phases/22-preflight-closure/22-VALIDATION-BASELINE.md
rg -n "SMS real-device OTP|Email reset-to-login|Legal public/sign-off|Valkey production runtime|R2 production evidence|Missing/stale verification artifacts" .planning/phases/22-preflight-closure/22-VALIDATION-BASELINE.md
```
</verification>

<success_criteria>
PREF-02 is satisfied when the maintainer can open one Phase 22 baseline file and see every inherited validation gap classified with evidence path, explicit caveat, or blocker next action.
</success_criteria>

<output>
After completion, create `.planning/phases/22-preflight-closure/22-02-SUMMARY.md`.
</output>
