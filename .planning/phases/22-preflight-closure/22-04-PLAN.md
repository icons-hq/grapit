---
phase: 22-preflight-closure
plan: "04"
type: execute
wave: 2
depends_on:
  - "22-01"
files_modified:
  - .planning/phases/22-preflight-closure/22-HUMAN-UAT.md
autonomous: false
requirements:
  - PREF-01
user_setup:
  - service: real-device-sms
    why: "SMS OTP happy path and failure-copy evidence require a real phone/device."
  - service: gmail-resend-sentry-gcp
    why: "Email PASS requires Resend accepted id, Gmail receipt, reset confirm, login, and provider observation."
  - service: legal-mailboxes-and-operator-signoff
    why: "Legal PASS requires support/privacy mailbox receipts and factual operator sign-off."
must_haves:
  truths:
    - "D-05/D-06: SMS PASS proves happy path and distinct wrong-code, expired/resend, and system-error copy."
    - "D-07: SMS evidence includes a targeted last-1-hour observation for sms.verify_failed, CROSSSLOT, and Sentry provider=valkey."
    - "D-08/D-13: SMS and email artifacts are redacted before commit."
    - "D-10/D-12: Email PASS proves Gmail receipt and password reset email -> reset confirm -> login with the new password."
    - "D-11: Naver/Daum untested status is ACCEPTED_RISK with D-02 approvals, not BLOCKER."
    - "D-14/D-15/D-16: Legal PASS proves public URLs, Footer, dialogs, robots/canonical, mailboxes, and factual sign-off."
    - "D-09/D-17: Failed shipped SMS/email/legal launch surfaces may become Phase 22 blockers or fixes; broader fanmeet scope is not added."
  artifacts:
    - path: ".planning/phases/22-preflight-closure/22-HUMAN-UAT.md"
      provides: "Operator-completed SMS/email/legal evidence for PREF-01"
  key_links:
    - from: ".planning/phases/22-preflight-closure/22-HUMAN-UAT.md"
      to: "apps/api/src/modules/sms/sms.service.ts"
      via: "SMS observation names sms.verify_failed, CROSSSLOT, provider=valkey"
    - from: ".planning/phases/22-preflight-closure/22-HUMAN-UAT.md"
      to: "apps/api/src/modules/auth/email/email.service.ts"
      via: "Email evidence records Resend id and email-service observation"
---

<objective>
Complete the launch-facing human/operator UAT gates for SMS, email, and legal evidence.

Purpose: PREF-01 is not satisfied by code tests alone; it requires real-device/provider/mailbox/operator evidence or explicit accepted risk/blocker classification.

Output: Updated `22-HUMAN-UAT.md` with SMS, email, and legal rows classified as `PASS`, `ACCEPTED_RISK`, or `BLOCKER`.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-preflight-closure/22-HUMAN-UAT.md
@.planning/phases/22-preflight-closure/22-EVIDENCE-LEDGER.md
@.planning/phases/22-preflight-closure/22-CONTEXT.md
@.planning/phases/22-preflight-closure/22-VALIDATION.md
@.planning/phases/22-preflight-closure/22-PATTERNS.md
@apps/api/src/modules/sms/sms.service.ts
@apps/web/components/auth/phone-verification.tsx
@apps/api/src/modules/auth/email/email.service.ts
@apps/web/app/auth/reset-password/page.tsx
@apps/web/components/layout/footer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated guards before manual UAT</name>
  <files>.planning/phases/22-preflight-closure/22-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/22-preflight-closure/22-HUMAN-UAT.md
    - .planning/phases/22-preflight-closure/22-VALIDATION.md
    - apps/api/src/modules/sms/sms.service.spec.ts
    - apps/api/test/sms-cluster-crossslot.integration.spec.ts
    - apps/api/src/modules/auth/email/email.service.spec.ts
    - apps/web/app/auth/reset-password/__tests__/reset-password.test.tsx
    - apps/web/content/legal/__tests__/legal-content.test.ts
    - apps/web/app/legal/__tests__/metadata.test.ts
    - apps/web/components/layout/__tests__/footer.test.tsx
  </read_first>
  <action>
    Run the focused automated commands from `22-VALIDATION.md` and append a `## Automated Guard Results` section to `22-HUMAN-UAT.md`.

    Commands:
    - `pnpm --filter @grabit/api test -- src/modules/sms/sms.service.spec.ts src/modules/auth/email/email.service.spec.ts`
    - `pnpm --filter @grabit/web test -- app/auth/reset-password/__tests__/reset-password.test.tsx content/legal/__tests__/legal-content.test.ts app/legal/__tests__/metadata.test.ts components/layout/__tests__/footer.test.tsx`
    - `pnpm --filter @grabit/api test:integration -- sms-cluster-crossslot` when Docker/testcontainers are available; if unavailable, record `BLOCKER` or `ACCEPTED_RISK` with reason instead of marking it PASS.

    Record command, exit status, KST timestamp, owner `Maintainer`, and redacted output path if output is saved. Do not paste tokens, cookies, full phone numbers, email addresses, OTPs, or reset links.
  </action>
  <verify>
    <automated>rg -n "## Automated Guard Results|sms.service.spec.ts|email.service.spec.ts|reset-password.test.tsx|legal-content.test.ts|metadata.test.ts|footer.test.tsx" .planning/phases/22-preflight-closure/22-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `22-HUMAN-UAT.md` contains `## Automated Guard Results`.
    - `22-HUMAN-UAT.md` records each focused command exactly as listed in the action.
    - Any failed or skipped automated guard is recorded as `BLOCKER` or `ACCEPTED_RISK`, never `PASS`.
  </acceptance_criteria>
  <done>Automated guard evidence is recorded before manual/provider evidence is requested.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Complete SMS, email, and legal human evidence</name>
  <files>.planning/phases/22-preflight-closure/22-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/22-preflight-closure/22-HUMAN-UAT.md
    - .planning/phases/22-preflight-closure/22-CONTEXT.md
    - .planning/phases/22-preflight-closure/22-UI-SPEC.md
  </read_first>
  <action>
    Pause for operator evidence collection. Ask the user/operator to perform these exact gates and provide redacted evidence paths or explicit risk acceptance:

    SMS:
    1. On a real iOS or Android device, run `https://heygrabit.com/signup`.
    2. Complete `send-code -> SMS received -> verify-code success -> signup step3 verified`.
    3. Capture wrong-code, expired/resend, and system-error copy as distinct UI states.
    4. Check the last 1 hour around the UAT timestamp for `sms.verify_failed`, `CROSSSLOT`, and Sentry `provider=valkey`.

    Email:
    1. Trigger password reset for a Gmail inbox.
    2. Record Resend accepted email id.
    3. Confirm Gmail receipt.
    4. Complete reset confirm.
    5. Log in with the new password.
    6. Record Cloud Run/Sentry email-service result.
    7. If Naver/Daum remain untested, record `ACCEPTED_RISK` only with maintainer and operator approval per D-02.

    Legal:
    1. Check public `/legal/terms`, `/legal/privacy`, `/legal/marketing`.
    2. Check Footer `/legal/terms`, `/legal/privacy`, and `mailto:support@heygrabit.com`.
    3. Check signup/booking legal dialog content.
    4. Check production robots/canonical.
    5. Send mailbox receipts to `support@heygrabit.com` and `privacy@heygrabit.com`.
    6. Record factual sign-off for business identity, representative, business registration number, mail-order registration number, address, customer support contact, privacy/support mailbox, and effective date.

    Redaction is mandatory: no raw OTP, full phone number, full email address, reset link, reset token, cookies, bearer headers, or provider secrets.
  </action>
  <verify>
    <automated>test -f .planning/phases/22-preflight-closure/22-HUMAN-UAT.md</automated>
    <automated>rg -n "## SMS Real-Device Gate|## Email Reset-To-Login Gate|## Legal Public And Sign-Off Gate|PASS|ACCEPTED_RISK|BLOCKER|Evidence Path|Checked At|Owner" .planning/phases/22-preflight-closure/22-HUMAN-UAT.md</automated>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('.planning/phases/22-preflight-closure/22-HUMAN-UAT.md','utf8'); const required=['## SMS Real-Device Gate','## Email Reset-To-Login Gate','## Legal Public And Sign-Off Gate','Evidence Path','Checked At','Owner']; for (const item of required) if(!s.includes(item)) throw new Error(item+' missing'); const labels=['PASS','ACCEPTED_RISK','BLOCKER']; if(!labels.some((label)=>s.includes(label))) throw new Error('status label missing');"</automated>
  </verify>
  <acceptance_criteria>
    - The checkpoint response includes one of `PASS`, `ACCEPTED_RISK`, or `BLOCKER` for SMS, Email, and Legal.
    - `ACCEPTED_RISK` responses include maintainer approval date and operator approval date.
    - `PASS` responses include redacted evidence paths and checked-at KST timestamps.
    - `BLOCKER` responses include owner and next action.
  </acceptance_criteria>
  <done>Human/operator evidence is available to record or the missing proof is explicitly classified.</done>
</task>

<task type="auto">
  <name>Task 3: Record checkpoint results in HUMAN-UAT</name>
  <files>.planning/phases/22-preflight-closure/22-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/22-preflight-closure/22-HUMAN-UAT.md
    - .planning/phases/22-preflight-closure/22-CONTEXT.md
    - .planning/phases/22-preflight-closure/22-UI-SPEC.md
  </read_first>
  <action>
    Update `22-HUMAN-UAT.md` from the checkpoint response.

    For SMS, Email, and Legal, fill `Status`, `Evidence Path`, `Checked At`, `Owner`, `Risk / Caveat`, and `Next Action`. Use only `PASS`, `ACCEPTED_RISK`, or `BLOCKER`.

    If the user/operator provided evidence but it includes unredacted sensitive values, do not paste it. Record `BLOCKER` with next action `Redact evidence and rerun checkpoint`.

    If any gate is `ACCEPTED_RISK`, add a `## Accepted Risk Approvals` section with columns `Gate`, `Maintainer Approval`, `Operator Approval`, `Risk`, `Review Trigger`.
  </action>
  <verify>
    <automated>rg -n "\\| SMS|\\| Email|\\| Legal|PASS|ACCEPTED_RISK|BLOCKER|## Accepted Risk Approvals" .planning/phases/22-preflight-closure/22-HUMAN-UAT.md</automated>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('.planning/phases/22-preflight-closure/22-HUMAN-UAT.md','utf8'); for (const gate of ['SMS','Email','Legal']) if(!s.includes(gate)) throw new Error(gate+' missing'); for (const bad of ['raw OTP','reset link pasted','Bearer ey']) if(s.includes(bad)) throw new Error('unsafe marker '+bad);"</automated>
  </verify>
  <acceptance_criteria>
    - `22-HUMAN-UAT.md` contains filled rows for `SMS`, `Email`, and `Legal`.
    - No filled row uses a status outside `PASS`, `ACCEPTED_RISK`, or `BLOCKER`.
    - Any `ACCEPTED_RISK` row has maintainer and operator approval fields.
    - Any `BLOCKER` row has a non-empty next action.
  </acceptance_criteria>
  <done>HUMAN-UAT records launch-facing SMS, email, and legal evidence or blocker/risk classification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Real device/browser/provider evidence -> markdown | Human UAT output is committed as planning evidence after redaction. |
| SMS verification state -> launch proof | Signup OTP behavior is observed without treating idempotency support as general authentication proof. |
| Legal/operator sign-off -> launch readiness | Factual operator approval is recorded without implying external legal counsel review. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Mitigation | Block/Accept Criteria |
|-----------|----------|----------|-----------|------------|-----------------------|
| T-22-01 | high | Information Disclosure | `22-HUMAN-UAT.md` | Redact OTPs, phone numbers, email addresses, reset links, tokens, cookies, auth headers, and secrets before writing. | Block if unredacted sensitive values are present. |
| T-22-03 | high | Spoofing / Elevation of Privilege | SMS UAT | Record `isPhoneVerified` only as signup idempotency support; SMS PASS requires real-device flow and provider observation. | Block if SMS row treats the verified flag alone as public auth proof. |
| T-22-06 | high | Repudiation | SMS/email/legal PASS rows | PASS requires provider/public-surface observation, owner, timestamp, and evidence path. | Block if PASS row lacks direct evidence path or checked-at KST timestamp. |
| T-22-02 | high | Repudiation | Historical references in UAT | Do not rewrite Phase 14/15/16 UAT as newly executed. | Accept only Phase 22 evidence paths or explicit historical context references. |
</threat_model>

<verification>
Run after checkpoint recording:

```bash
rg -n "SMS Real-Device Gate|Email Reset-To-Login Gate|Legal Public And Sign-Off Gate|PASS|ACCEPTED_RISK|BLOCKER" .planning/phases/22-preflight-closure/22-HUMAN-UAT.md
pnpm --filter @grabit/api test -- src/modules/sms/sms.service.spec.ts src/modules/auth/email/email.service.spec.ts
pnpm --filter @grabit/web test -- app/auth/reset-password/__tests__/reset-password.test.tsx content/legal/__tests__/legal-content.test.ts app/legal/__tests__/metadata.test.ts components/layout/__tests__/footer.test.tsx
pnpm test
```

Wave 2 validation sampling: run `pnpm test` after Plan `22-04` records the checkpoint results and before Wave 3 starts. Manual evidence still keeps this plan `autonomous: false`; the automated checks verify the filled `22-HUMAN-UAT.md` artifact shape, not the real-device/provider access itself.
</verification>

<success_criteria>
PREF-01 is ready for final aggregation when SMS, Email, and Legal rows are each `PASS`, `ACCEPTED_RISK`, or `BLOCKER` with the required evidence, approvals, or next action.
</success_criteria>

<output>
After completion, create `.planning/phases/22-preflight-closure/22-04-SUMMARY.md`.
</output>
