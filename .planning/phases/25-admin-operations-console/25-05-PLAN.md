---
phase: 25-admin-operations-console
plan: "05"
type: execute
wave: 3
depends_on:
  - "25-04"
  - "25-21"
files_modified:
  - apps/api/src/database/schema/seat-inventories.ts
  - apps/api/src/database/schema/admin-audit-logs.ts
  - apps/api/src/database/schema/admin-access-allowlist.ts
  - apps/api/src/database/schema/seat-operation-history.ts
  - apps/api/src/database/schema/index.ts
  - apps/api/src/database/schema/phase25-admin-operations.schema.spec.ts
autonomous: true
requirements:
  - ADMIN-03
  - ADMIN-04
requirements_addressed: [ADMIN-03, ADMIN-04]
must_haves:
  truths:
    - "General sensitive-action audit, allowlist, raw export evidence, and seat history schema exist before migration."
    - "Seat status includes durable `disabled`, not client-only disabled state."
  artifacts:
    - path: "apps/api/src/database/schema/admin-audit-logs.ts"
      provides: "General masked admin audit table"
      contains: "admin_audit_logs"
    - path: "apps/api/src/database/schema/admin-access-allowlist.ts"
      provides: "IP allowlist and exception records"
    - path: "apps/api/src/database/schema/seat-operation-history.ts"
      provides: "Seat disable/reactivate/manual-open history"
  key_links:
    - from: "apps/api/src/database/schema/seat-inventories.ts"
      to: "apps/api/src/database/schema/seat-operation-history.ts"
      via: "seat identity and status"
      pattern: "disabled"
---

<objective>
Add security, audit, export, and seat-operation schema contracts.

Purpose: ADMIN-03/04 and D-10 through D-15 require durable audit and history before high-risk operator actions are implemented.
Output: Drizzle schemas and tests for admin audit logs, allowlist records, seat disabled state, and seat-operation history.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-admin-operations-console/25-CONTEXT.md
@.planning/phases/25-admin-operations-console/25-RESEARCH.md
@.planning/phases/25-admin-operations-console/25-PATTERNS.md
@.planning/phases/24-traffic-booking-payment-core/24-12-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add generalized audit and allowlist schema</name>
  <files>
    apps/api/src/database/schema/admin-audit-logs.ts
    apps/api/src/database/schema/admin-access-allowlist.ts
    apps/api/src/database/schema/index.ts
    apps/api/src/database/schema/phase25-admin-operations.schema.spec.ts
  </files>
  <read_first>
    apps/api/src/database/schema/booking-operation-audit-logs.ts
    apps/api/src/database/schema/consent-audit-logs.ts
    apps/api/src/database/schema/index.ts
    packages/shared/src/schemas/admin-operations.schema.ts
  </read_first>
  <behavior>
    - Audit schema can record event publish/update, refund/admin refund, CS escalation, seat operation, reservation export, and security/permission changes.
    - Audit diff fields are masked JSON, not raw exported row storage.
    - Allowlist schema supports env/bootstrap source plus DB-managed exceptions with audit linkage.
  </behavior>
  <action>
    Create `admin_audit_logs` and `admin_access_allowlist` schemas. Audit rows must include actor, action, resource type/id, status, reason, changed fields, safe before/after snapshots, IP, user agent, request id, and timestamps. Include action values for `event.publish`, `event.update`, `refund.admin_refund`, `support.escalate`, `seat.disable`, `seat.reactivate`, `seat.manual_open`, `reservations.export_raw`, `security.allowlist.update`, and `security.permission.update`. Export the schemas.
  </action>
  <verify>
    <automated>pnpm --filter @grabit/api test -- src/database/schema/phase25-admin-operations.schema.spec.ts && pnpm --filter @grabit/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    Schema tests prove D-10 action coverage, masked diff columns, allowlist source/status fields, and index exports exist.
  </acceptance_criteria>
  <done>D-10/D-11 audit schema coverage is complete before feature writes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add disabled seat state and operation history schema</name>
  <files>
    apps/api/src/database/schema/seat-inventories.ts
    apps/api/src/database/schema/seat-operation-history.ts
    apps/api/src/database/schema/index.ts
    apps/api/src/database/schema/phase25-admin-operations.schema.spec.ts
  </files>
  <read_first>
    apps/api/src/database/schema/seat-inventories.ts
    apps/api/src/database/schema/booking-operation-audit-logs.ts
    packages/shared/src/schemas/admin-operations.schema.ts
  </read_first>
  <behavior>
    - `seatStatusEnum` includes `disabled`.
    - History stores actor, action, seat identity, before/after state, reason, audit id, and timestamps.
  </behavior>
  <action>
    Add `disabled` to seat inventory status and create `seat_operation_history` for disable/reactivate/manual-open evidence. Keep history separate from the generalized audit table because operators need a seat-centric history view. Export all new schema objects.
  </action>
  <verify>
    <automated>pnpm --filter @grabit/api test -- src/database/schema/phase25-admin-operations.schema.spec.ts && pnpm --filter @grabit/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    Tests prove durable disabled state and seat history table are available before seat operation API/UI plans.
  </acceptance_criteria>
  <done>ADMIN-04 seat-operation storage is ready for migration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin action -> audit/history storage | Sensitive operator actions become immutable evidence. |
| Seat operation -> inventory state | Capacity-impacting operations change sellable inventory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03 | Information Disclosure | `admin_audit_logs` | mitigate | Store masked diffs and export metadata only. |
| T-25-04 | Spoofing / Denial of Service | allowlist records | mitigate | Track source, status, actor, and audit linkage. |
| T-25-09 | Tampering / Repudiation | seat operation history | mitigate | Persist reasoned before/after history and audit linkage. |
</threat_model>

<verification>
Run schema tests and typecheck. The migration gate must follow this plan before feature verification.
</verification>

<success_criteria>
Security/audit/export/seat operation schema is durable and explicit before SQL generation.
</success_criteria>

<output>
After completion, create `.planning/phases/25-admin-operations-console/25-05-SUMMARY.md`.
</output>
