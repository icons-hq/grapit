---
phase: 25-admin-operations-console
plan: "24"
type: execute
wave: 12
depends_on:
  - "25-12"
  - "25-15"
  - "25-22"
  - "25-23"
files_modified:
  - packages/shared/src/schemas/admin-operations.schema.ts
  - packages/shared/src/schemas/admin-operations.schema.test.ts
  - apps/api/src/modules/admin/admin-seat-operations.controller.ts
  - apps/api/src/modules/admin/admin-seat-operations.controller.spec.ts
  - apps/api/src/modules/admin/admin-seat-operations.service.ts
  - apps/api/src/modules/admin/admin-seat-operations.service.spec.ts
  - apps/web/components/admin/__tests__/seat-operations-panel.test.tsx
  - apps/web/e2e/admin-export-and-seat-ops.spec.ts
autonomous: true
gap_closure: true
requirements:
  - ADMIN-03
  - ADMIN-04
requirements_addressed: [ADMIN-03, ADMIN-04]

must_haves:
  truths:
    - "Malformed admin seat operation showtime IDs are rejected as validation errors before PostgreSQL UUID comparisons."
    - "Valid production-shaped UUID showtime IDs still work for admin seat operation history, disable, and reactivate flows."
    - "Phase 25 keeps D-08 admin MFA as accepted risk and does not add MFA work."
  artifacts:
    - path: "packages/shared/src/schemas/admin-operations.schema.ts"
      provides: "UUID validation contract for admin seat operation showtime IDs"
    - path: "apps/api/src/modules/admin/admin-seat-operations.controller.ts"
      provides: "Controller-level history query validation that returns 400 for malformed showtimeId"
    - path: "apps/api/src/modules/admin/admin-seat-operations.service.ts"
      provides: "Defensive service boundary that prevents malformed showtimeId from reaching Drizzle/PostgreSQL"
    - path: "apps/api/src/modules/admin/admin-seat-operations.controller.spec.ts"
      provides: "400-not-500 API regression coverage for malformed history query showtimeId"
  key_links:
    - from: "packages/shared/src/schemas/admin-operations.schema.ts"
      to: "apps/api/src/modules/admin/admin-seat-operations.controller.ts"
      via: "shared zod schema import for showtimeId validation"
      pattern: "adminSeatOperationShowtimeIdSchema"
    - from: "apps/api/src/modules/admin/admin-seat-operations.controller.ts"
      to: "apps/api/src/common/pipes/zod-validation.pipe.ts"
      via: "ZodValidationPipe on history query and mutation bodies"
      pattern: "ZodValidationPipe\\(seatOperationHistoryQuerySchema\\)"
    - from: "apps/api/src/modules/admin/admin-seat-operations.service.ts"
      to: "apps/api/src/database/schema/index.js"
      via: "validated UUID before Drizzle eq() on uuid columns"
      pattern: "safeParse|parse"
---

<objective>
Close the Phase 25 automated UAT gap where malformed admin seat operation `showtimeId` values reach PostgreSQL UUID comparisons and surface as 500 errors.

Purpose: ADMIN-04 seat operations must fail operator input mistakes at the validation boundary, and ADMIN-03 audit/security posture must not hide backend 500s behind malformed admin input. D-08 admin MFA remains accepted risk and is not part of this gap closure.
Output: one focused validation/test fix plan for `$gsd-execute-phase 25 --gaps-only`.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-admin-operations-console/25-CONTEXT.md
@.planning/phases/25-admin-operations-console/25-RESEARCH.md
@.planning/phases/25-admin-operations-console/25-UAT.md
@.planning/phases/25-admin-operations-console/25-VERIFICATION.md
@.planning/phases/25-admin-operations-console/25-12-SUMMARY.md
@.planning/phases/25-admin-operations-console/25-22-SUMMARY.md
@.planning/phases/25-admin-operations-console/25-23-SUMMARY.md
</context>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | Phase 25 | Operators can run seat operations without internal server errors from malformed admin input. | 25-24 | COVERED | This closes only the diagnosed UAT gap after the base Phase 25 console plans.
REQ | ADMIN-03 | Sensitive admin actions keep validation/audit/security posture and D-08 accepted risk. | 25-24 | COVERED | MFA remains accepted risk per D-08; no MFA work is added.
REQ | ADMIN-04 | Seat disable/reactivate/history use production-shaped showtime IDs safely. | 25-24 | COVERED | Fix is limited to admin seat-operation validation and test fixtures.
RESEARCH | Zod shared contracts | Existing stack uses shared Zod schemas and `ZodValidationPipe` for API validation. | 25-24 | COVERED | Uses current Zod v3 workspace pattern; no dependency changes.
RESEARCH | Seat operation API/backend | Seat inventory is DB-authoritative and UUID-backed. | 25-24 | COVERED | Malformed IDs must not reach Drizzle/PostgreSQL `eq()` comparisons.
CONTEXT | D-08 | Admin MFA is accepted risk. | 25-24 | COVERED | Preserve current visible accepted-risk state; do not implement or test MFA.
CONTEXT | D-12 | Seat disable/reactivate/history belongs in the dedicated seat operations panel. | 25-24 | COVERED | Fixture normalization applies only to dedicated seat-operation tests.
CONTEXT | D-13 | Seat operations require reason, confirmation, and audit. | 25-24 | COVERED | Existing reason/confirmation behavior remains intact while showtime validation is tightened.
UAT | Test 8 | Malformed `showtimeId=showtime-1` returns 500 instead of validation-level 400. | 25-24 | COVERED | Add regression coverage proving 400 and no service/DB call.
BOUNDARY | Phase 26/27 | Live booking, Toss live keys, canary, k6, DR, field QR, settlement, event-day monitoring. | NONE | EXCLUDED | Explicitly out of scope for this gap closure.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validate admin seat operation showtime IDs before database access</name>
  <files>packages/shared/src/schemas/admin-operations.schema.ts, packages/shared/src/schemas/admin-operations.schema.test.ts, apps/api/src/modules/admin/admin-seat-operations.controller.ts, apps/api/src/modules/admin/admin-seat-operations.controller.spec.ts, apps/api/src/modules/admin/admin-seat-operations.service.ts, apps/api/src/modules/admin/admin-seat-operations.service.spec.ts</files>
  <read_first>
    .planning/phases/25-admin-operations-console/25-UAT.md
    .planning/phases/25-admin-operations-console/25-VERIFICATION.md
    .planning/phases/25-admin-operations-console/25-12-SUMMARY.md
    packages/shared/src/schemas/admin-operations.schema.ts
    packages/shared/src/schemas/admin-operations.schema.test.ts
    apps/api/src/common/pipes/zod-validation.pipe.ts
    apps/api/src/modules/admin/admin-seat-operations.controller.ts
    apps/api/src/modules/admin/admin-seat-operations.service.ts
    apps/api/src/modules/admin/admin-seat-operations.service.spec.ts
    apps/api/src/modules/admin/__tests__/admin-dashboard.controller.spec.ts
  </read_first>
  <behavior>
    - Test 1: `adminSeatOperationRequestSchema` rejects `showtime-1` and accepts `00000000-0000-4000-8000-000000000001`.
    - Test 2: `GET /admin/seat-operations/history?showtimeId=showtime-1` returns HTTP 400 through `ZodValidationPipe` and does not call `AdminSeatOperationsService.listHistory`.
    - Test 3: direct `AdminSeatOperationsService.listHistory({ showtimeId: "showtime-1" })` and malformed mutation input throw `BadRequestException` before `db.select` or `db.transaction`.
    - Test 4: valid UUID fixture paths for disable, reactivate, and history still pass and keep reason, confirmation, audit, and broadcast expectations unchanged.
  </behavior>
  <action>Create an exported shared `adminSeatOperationShowtimeIdSchema` using `z.string().uuid('유효한 회차 ID가 필요합니다')`. Use that schema for `adminSeatOperationRequestSchema.showtimeId` and `adminSeatOperationHistorySchema.showtimeId`; keep `seatKey`, `reason`, and `confirmed` behavior unchanged. In `AdminSeatOperationsController`, use the same shared schema in `seatOperationHistoryQuerySchema` instead of a local non-empty string rule. Add a focused controller spec following the local NestJS/supertest pattern from `apps/api/src/modules/admin/__tests__/admin-dashboard.controller.spec.ts`; override admin guards, mock `AdminSeatOperationsService`, assert malformed history query returns 400, and assert the service mock is not called. Add defensive service validation at the start of `performOperation` and `listHistory` so any non-controller caller also receives `BadRequestException` before Drizzle/PostgreSQL UUID comparisons. Replace Phase 25 admin seat-operation service fixtures that currently use `showtime-1` with the stable valid UUID `00000000-0000-4000-8000-000000000001`; do not sweep unrelated `showtime-1` fixtures in other domains.</action>
  <verify>
    <automated>pnpm --filter @grabit/shared test -- src/schemas/admin-operations.schema.test.ts && pnpm --filter @grabit/api exec vitest run modules/admin/admin-seat-operations.controller.spec.ts modules/admin/admin-seat-operations.service.spec.ts && pnpm --filter @grabit/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    Malformed admin seat operation showtime IDs fail with validation-level `BadRequestException`/HTTP 400 before any Drizzle UUID comparison; valid UUID fixtures keep existing seat operation behavior green; no MFA, live booking, payment cutover, canary, load, DR, field QR, settlement, or event-day monitoring work is introduced.
  </acceptance_criteria>
  <done>`showtime-1` cannot reach API/service database comparisons for admin seat operations, and API regression tests prove 400 instead of 500.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize admin seat operation UI test fixtures to production UUIDs</name>
  <files>apps/web/components/admin/__tests__/seat-operations-panel.test.tsx, apps/web/e2e/admin-export-and-seat-ops.spec.ts</files>
  <read_first>
    .planning/phases/25-admin-operations-console/25-UAT.md
    .planning/phases/25-admin-operations-console/25-22-SUMMARY.md
    .planning/phases/25-admin-operations-console/25-23-SUMMARY.md
    apps/web/components/admin/__tests__/seat-operations-panel.test.tsx
    apps/web/e2e/admin-export-and-seat-ops.spec.ts
  </read_first>
  <behavior>
    - Test 1: `SeatOperationsPanel` component tests use the same stable valid UUID in input props, mocked history rows, expected URLs, and mutation payloads.
    - Test 2: `admin-export-and-seat-ops.spec.ts` fills a valid UUID in the dedicated seat operations panel while preserving the reason-gated modal checks.
    - Test 3: the Phase 25 admin seat-operation fixture files no longer contain `showtime-1`; unrelated legacy fixtures outside this gap are not changed.
  </behavior>
  <action>Update only the Phase 25 admin seat-operation UI/component/E2E fixtures that model production API inputs from `showtime-1` to `00000000-0000-4000-8000-000000000001`. Keep the current mocked API responses, route interception style, reason-gated disable/reactivate modal assertions, and reservation-detail immediate-open checks. Do not add client-only MFA, live booking enablement, Toss live-key handling, canary/load/DR checks, field QR, settlement, or event-day monitoring.</action>
  <verify>
    <automated>pnpm --filter @grabit/web test -- components/admin/__tests__/seat-operations-panel.test.tsx && ! rg -n "showtime-1" packages/shared/src/schemas/admin-operations.schema.test.ts apps/api/src/modules/admin/admin-seat-operations.service.spec.ts apps/web/components/admin/__tests__/seat-operations-panel.test.tsx apps/web/e2e/admin-export-and-seat-ops.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    Seat-operation UI tests use production-shaped UUID input while preserving existing admin workflow assertions, and the targeted gap files have no remaining `showtime-1` fixture masking UUID validation.
  </acceptance_criteria>
  <done>Phase 25 seat-operation test fixtures no longer normalize malformed showtime IDs as happy-path admin inputs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser -> admin API | Operator-controlled query/body values cross into NestJS validation. |
| Admin API -> PostgreSQL | Validated `showtimeId` values are used in UUID column comparisons through Drizzle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-24-01 | Denial of Service | `GET /admin/seat-operations/history` | mitigate | Reject malformed `showtimeId` with Zod UUID validation before database work. |
| T-25-24-02 | Information Disclosure | PostgreSQL UUID parse error surface | mitigate | Return validation-level 400 instead of surfacing internal DB errors. |
| T-25-24-03 | Tampering | seat disable/reactivate mutations | mitigate | Apply the same UUID validation to mutation input while preserving existing reason, confirmation, audit, and broadcast gates. |
| T-25-24-04 | Repudiation | D-08 MFA accepted-risk ledger | accept | D-08 explicitly keeps MFA as accepted risk; this plan does not change MFA status or claim MFA PASS. |
</threat_model>

<verification>
Run the task-level automated commands. After implementation, rerun the diagnosed UAT slice manually or with browser automation: submit `/admin/seat-operations` history with `showtime-1` and confirm the network response is HTTP 400, not HTTP 500.
</verification>

<success_criteria>
The diagnosed Phase 25 UAT gap is closed when malformed admin seat operation showtime IDs are rejected at validation level, valid UUID seat operation paths remain green, targeted Phase 25 seat-operation fixtures use UUID-shaped happy-path IDs, and D-08 admin MFA remains accepted risk.
</success_criteria>

<output>
After completion, create `.planning/phases/25-admin-operations-console/25-24-SUMMARY.md`
</output>
