---
phase: 25-admin-operations-console
plan: "11"
type: execute
wave: 6
depends_on:
  - "25-07"
files_modified:
  - apps/api/src/modules/admin/admin-booking.controller.ts
  - apps/api/src/modules/admin/admin-booking.service.ts
  - apps/api/src/modules/admin/admin-booking.service.spec.ts
  - apps/web/hooks/use-reservations.ts
  - apps/web/components/admin/reservation-export-panel.tsx
  - apps/web/components/admin/__tests__/reservation-export-panel.test.tsx
  - apps/web/e2e/admin-export-and-seat-ops.spec.ts
autonomous: true
requirements:
  - ADMIN-03
  - ADMIN-04
requirements_addressed: [ADMIN-03, ADMIN-04]
must_haves:
  truths:
    - "Reservation CSV export supports all seven D-14 filters."
    - "D-15: Raw PII export requires reason, confirmation, CSV injection mitigation, and metadata-only audit without logging exported raw values."
    - "Admin refund writes D-10 `refund.admin_refund` audit evidence."
  artifacts:
    - path: "apps/web/components/admin/reservation-export-panel.tsx"
      provides: "Seven-filter raw reservation CSV export UI"
    - path: "apps/api/src/modules/admin/admin-booking.service.ts"
      provides: "Export and admin refund audit integration"
  key_links:
    - from: "reservation-export-panel.tsx"
      to: "/api/v1/admin/bookings/export"
      via: "useReservations export mutation"
      pattern: "reservations.export_raw"
---

<objective>
Implement reservation CSV export and admin refund audit coverage.

Purpose: ADMIN-04 requires seven-filter export, and D-10/D-15 require raw export plus admin refund audit evidence without storing exported raw PII.
Output: export API/UI, admin refund audit updates, component tests, and E2E spec deferred to final wiring.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-admin-operations-console/25-CONTEXT.md
@.planning/phases/25-admin-operations-console/25-UI-SPEC.md
@.planning/phases/25-admin-operations-console/25-PATTERNS.md
@.planning/phases/25-admin-operations-console/25-07-SUMMARY.md
@.planning/phases/24-traffic-booking-payment-core/24-12-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement export and refund audit backend</name>
  <files>
    apps/api/src/modules/admin/admin-booking.controller.ts
    apps/api/src/modules/admin/admin-booking.service.ts
    apps/api/src/modules/admin/admin-booking.service.spec.ts
  </files>
  <read_first>
    packages/shared/src/schemas/admin-operations.schema.ts
    apps/api/src/modules/admin/admin-booking.controller.ts
    apps/api/src/modules/admin/admin-booking.service.ts
    apps/api/src/modules/admin/csv-export.util.ts
    apps/api/src/modules/admin/admin-audit.service.ts
  </read_first>
  <behavior>
    - Export endpoint rejects missing reason and validates event, tier, zone/floor, reservation status, domestic/overseas, payment method, and date range filters.
    - Export streams CSV and does not store raw generated files.
    - Export audit stores actor, filters, export type, status, reason, IP/user-agent, and timestamp, not row values.
    - Admin refund path writes `refund.admin_refund` audit.
  </behavior>
  <action>
    Add `/api/v1/admin/bookings/export` with `reservations.export_raw` capability and DTO validation for all seven D-14 filters. Generate streamed CSV with `safeCsvCell`; do not upload or persist raw CSV files. Extend existing admin refund handling to call `AdminAuditService` with `refund.admin_refund`, safe resource metadata, reason/status, and masked diff.
  </action>
  <verify>
    <automated>pnpm --filter @grabit/api test -- src/modules/admin/admin-booking.service.spec.ts && pnpm --filter @grabit/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    Tests cover seven filters, missing reason rejection, formula neutralization, metadata-only audit, no raw file retention, and admin refund audit.
  </acceptance_criteria>
  <done>D-14/D-15 export and D-10 admin refund audit are implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build reservation export UI</name>
  <files>
    apps/web/hooks/use-reservations.ts
    apps/web/components/admin/reservation-export-panel.tsx
    apps/web/components/admin/__tests__/reservation-export-panel.test.tsx
    apps/web/e2e/admin-export-and-seat-ops.spec.ts
  </files>
  <read_first>
    apps/web/components/admin/admin-booking-dashboard.tsx
    apps/web/hooks/use-reservations.ts
    apps/web/components/admin/consent-audit-table.tsx
    apps/web/e2e/helpers/auth.ts
  </read_first>
  <behavior>
    - Export panel shows all seven filters before final raw CSV confirmation.
    - Confirmation requires reason and displays raw PII warning copy.
    - E2E spec covers export route only after final wiring runs it.
  </behavior>
  <action>
    Add `ReservationExportPanel` with event, tier, zone/floor, reservation status, domestic/overseas, payment method, and date range filters. Add `use-reservations.ts` export mutation with reason payload and download handling. Confirm dialog must echo filter summary and UI-SPEC raw PII warning before enabling export. Create/update `admin-export-and-seat-ops.spec.ts`, but do not run it until Plan 25-23 and full verification in 25-15.
  </action>
  <verify>
    <automated>pnpm --filter @grabit/web test -- components/admin/__tests__/reservation-export-panel.test.tsx && pnpm --filter @grabit/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    Tests verify all filter labels, reason gating, raw PII warning, and no export action before confirmation.
  </acceptance_criteria>
  <done>Reservation export UI satisfies ADMIN-04 without premature route-level E2E.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin export API -> CSV file | Raw reservation PII leaves backend control. |
| Admin refund action -> payment/refund state | Money-impacting operator action must be attributable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03 | Information Disclosure | reservation export | mitigate | Reasoned confirmation, metadata-only audit, CSV sanitization, no retention. |
| T-25-08 | Tampering | export filters | mitigate | Validate all seven filters server-side and echo summary before export. |
| T-25-06 | Repudiation | admin refund | mitigate | Write `refund.admin_refund` masked audit event. |
</threat_model>

<verification>
Run API unit and web component/typecheck commands. Full export/seat E2E waits until final wiring.
</verification>

<success_criteria>
Reservation export and admin refund audit are reasoned, masked, and retention-safe.
</success_criteria>

<output>
After completion, create `.planning/phases/25-admin-operations-console/25-11-SUMMARY.md`.
</output>
