---
phase: 25-admin-operations-console
plan: "03"
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/shared/src/schemas/admin-operations.schema.ts
  - packages/shared/src/types/admin-operations.types.ts
  - packages/shared/src/types/user.types.ts
  - packages/shared/src/index.ts
  - apps/api/src/common/decorators/admin-capabilities.decorator.ts
  - apps/api/src/common/guards/admin-capabilities.guard.ts
  - apps/api/src/common/guards/admin-capabilities.guard.spec.ts
  - apps/api/src/modules/admin/admin-fixtures.ts
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02
  - ADMIN-03
  - ADMIN-04
requirements_addressed: [ADMIN-01, ADMIN-02, ADMIN-03, ADMIN-04]
must_haves:
  truths:
    - "Capability bundles are backend contracts, not a mandatory approval workflow."
    - "Existing `admin` users keep all capabilities for launch verification."
    - "MFA is represented as accepted-risk/deferred status, not implemented."
  artifacts:
    - path: "packages/shared/src/schemas/admin-operations.schema.ts"
      provides: "Admin capability, audit, support, export, and seat operation DTO contracts"
      exports: ["adminCapabilitySchema", "adminAuditEventSchema"]
    - path: "apps/api/src/common/guards/admin-capabilities.guard.ts"
      provides: "Server-side capability enforcement"
      exports: ["AdminCapabilitiesGuard"]
  key_links:
    - from: "packages/shared/src/schemas/admin-operations.schema.ts"
      to: "apps/api/src/common/guards/admin-capabilities.guard.ts"
      via: "AdminCapability type"
      pattern: "event.publish|reservations.export_raw|security.manage"
---

<objective>
Define the Phase 25 admin operations contract and server-side capability model.

Purpose: ADMIN-01/03 require RBAC and audit, while D-01/D-02 prohibit slow mandatory approval bureaucracy. Capabilities must enforce sensitive actions without changing the publish flow into multi-user ceremony.
Output: shared admin operations schemas/types, capability decorator/guard, and deterministic admin fixture helpers.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-admin-operations-console/25-CONTEXT.md
@.planning/phases/25-admin-operations-console/25-RESEARCH.md
@.planning/phases/25-admin-operations-console/25-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define shared admin operations contracts</name>
  <files>
    packages/shared/src/schemas/admin-operations.schema.ts
    packages/shared/src/types/admin-operations.types.ts
    packages/shared/src/types/user.types.ts
    packages/shared/src/index.ts
    apps/api/src/modules/admin/admin-fixtures.ts
  </files>
  <read_first>
    .planning/phases/25-admin-operations-console/25-CONTEXT.md
    packages/shared/src/types/user.types.ts
    packages/shared/src/schemas/performance.schema.ts
    packages/shared/src/index.ts
  </read_first>
  <behavior>
    - Capability bundles include `operator`, `reviewer`, `approver`, `finance`, and `admin`.
    - `admin` remains all-capabilities superuser for existing seeded admin verification.
    - Test fixtures can create non-persisted role/capability users without changing production seed users.
  </behavior>
  <action>
    Create shared schemas/types for admin capabilities, publish lifecycle, support inbox rows, FAQ/notice authoring, audit events, allowlist records, reservation export filters, seat operations, and security status. Required capabilities are `event.write`, `event.publish`, `support.manage`, `support.escalate`, `reservations.export_raw`, `seat.disable`, `seat.reactivate`, `seat.manual_open`, `banner.manage`, `audit.read`, and `security.manage`. Keep `admin` as all-capabilities superuser; use fixture-only bundle users for tests instead of adding mandatory seeded operator/reviewer/approver/finance accounts.
  </action>
  <verify>
    <automated>pnpm --filter @grabit/shared typecheck && pnpm --filter @grabit/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    Shared exports compile, fixture helpers are test-only, and no schema names MFA as implemented or complete.
  </acceptance_criteria>
  <done>Phase 25 contracts are importable by API and web plans.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add backend capability guard</name>
  <files>
    apps/api/src/common/decorators/admin-capabilities.decorator.ts
    apps/api/src/common/guards/admin-capabilities.guard.ts
    apps/api/src/common/guards/admin-capabilities.guard.spec.ts
  </files>
  <read_first>
    apps/api/src/common/guards/roles.guard.ts
    apps/api/src/common/decorators/roles.decorator.ts
    packages/shared/src/schemas/admin-operations.schema.ts
  </read_first>
  <behavior>
    - Missing user denies access.
    - Existing `admin` role passes all capabilities.
    - Fixture bundle roles pass only their declared capabilities.
  </behavior>
  <action>
    Implement `@AdminCapabilities()` with Nest `SetMetadata` and `AdminCapabilitiesGuard` using `Reflector`. Keep existing `@Roles('admin')` compatibility, but require explicit capabilities on sensitive endpoints in later plans. The guard must fail closed for missing users and must not depend on client-side hidden buttons.
  </action>
  <verify>
    <automated>pnpm --filter @grabit/api test -- src/common/guards/admin-capabilities.guard.spec.ts && pnpm --filter @grabit/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    Guard tests cover admin superuser, bundle-scoped allow, bundle-scoped deny, and unauthenticated deny.
  </acceptance_criteria>
  <done>Server-side capability enforcement is ready before feature endpoints are added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser -> protected API | Client role/capability hints are untrusted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Elevation of Privilege | `AdminCapabilitiesGuard` | mitigate | Enforce capabilities server-side with admin superuser compatibility. |
| T-25-10 | Repudiation | MFA status contract | mitigate | Represent MFA only as `deferred_accepted_risk`; do not expose PASS. |
</threat_model>

<verification>
Run shared/API typecheck and guard tests before schema or feature plans consume these contracts.
</verification>

<success_criteria>
Downstream plans have a single capability/audit/security contract and do not invent approval workflow semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/25-admin-operations-console/25-03-SUMMARY.md`.
</output>
