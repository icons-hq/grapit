---
phase: 25-admin-operations-console
plan: "06"
type: execute
wave: 4
depends_on:
  - "25-04"
  - "25-05"
  - "25-21"
files_modified:
  - apps/api/src/database/migrations/0015_phase25_admin_operations_console.sql
  - apps/api/src/database/migrations/meta/0015_snapshot.json
  - apps/api/src/database/migrations/meta/_journal.json
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02
  - ADMIN-03
  - ADMIN-04
requirements_addressed: [ADMIN-01, ADMIN-02, ADMIN-03, ADMIN-04]
must_haves:
  truths:
    - "All Phase 25 schema changes are generated, reviewed, and applied or explicitly blocked before feature verification."
    - "The migration encodes zh-TW, venue/transport, support, audit, allowlist, banner, and disabled-seat changes."
  artifacts:
    - path: "apps/api/src/database/migrations/0015_phase25_admin_operations_console.sql"
      provides: "Phase 25 Drizzle migration SQL"
      contains: "phase25_admin_operations_console"
    - path: "apps/api/src/database/migrations/meta/_journal.json"
      provides: "Migration journal entry"
      contains: "0015_phase25_admin_operations_console"
  key_links:
    - from: "apps/api/src/database/schema/*.ts"
      to: "apps/api/src/database/migrations/0015_phase25_admin_operations_console.sql"
      via: "drizzle-kit generate"
      pattern: "admin_audit_logs|support_threads|seat_operation_history|zh-TW"
---

<objective>
Run the blocking Drizzle migration gate for Phase 25.

Purpose: checker requires Drizzle migration/apply verification after schema changes and before feature verification.
Output: generated SQL, Drizzle snapshot metadata, migration journal update, and recorded migrate result or truthful local blocker.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/25-admin-operations-console/25-CONTEXT.md
@.planning/phases/25-admin-operations-console/25-RESEARCH.md
@.planning/phases/25-admin-operations-console/25-PATTERNS.md
@.planning/phases/23-launch-foundation/23-02-SUMMARY.md
@.planning/phases/24-traffic-booking-payment-core/24-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Generate, inspect, and apply migration</name>
  <files>
    apps/api/src/database/migrations/0015_phase25_admin_operations_console.sql
    apps/api/src/database/migrations/meta/0015_snapshot.json
    apps/api/src/database/migrations/meta/_journal.json
  </files>
  <read_first>
    AGENTS.md
    apps/api/src/database/schema/users.ts
    apps/api/src/database/schema/performances.ts
    apps/api/src/database/schema/venues.ts
    apps/api/src/database/schema/banners.ts
    apps/api/src/database/schema/support-threads.ts
    apps/api/src/database/schema/support-messages.ts
    apps/api/src/database/schema/support-faqs.ts
    apps/api/src/database/schema/support-notices.ts
    apps/api/src/database/schema/admin-audit-logs.ts
    apps/api/src/database/schema/admin-access-allowlist.ts
    apps/api/src/database/schema/seat-inventories.ts
    apps/api/src/database/schema/seat-operation-history.ts
    apps/api/src/database/migrations/0014_locale_ja_drop_zh_tw.sql
  </read_first>
  <action>
    From the monorepo root, run `DOTENV_CONFIG_PATH=../../.env pnpm --filter @grabit/api exec drizzle-kit generate --name=phase25_admin_operations_console`. Confirm or rename the generated SQL to `0015_phase25_admin_operations_console.sql` if 0015 remains the next migration number. Inspect SQL for destructive statements before apply. Then run `DOTENV_CONFIG_PATH=../../.env pnpm --filter @grabit/api exec drizzle-kit migrate`. If local DB credentials or Postgres are unavailable, run SQL syntax/destructive-grep checks, record the exact missing local dependency in the summary, and mark migration apply as a blocking local dependency rather than PASS.
  </action>
  <verify>
    <automated>DOTENV_CONFIG_PATH=../../.env pnpm --filter @grabit/api exec drizzle-kit generate --name=phase25_admin_operations_console</automated>
    <automated>! (rg -n "DROP TABLE|DROP COLUMN|TRUNCATE|DELETE FROM|RENAME TO" apps/api/src/database/migrations/0015_phase25_admin_operations_console.sql | rg -v "DELETE FROM \"consent_items\" WHERE \"locale\" = 'ja'")</automated>
    <automated>DOTENV_CONFIG_PATH=../../.env pnpm --filter @grabit/api exec drizzle-kit migrate</automated>
  </verify>
  <acceptance_criteria>
    SQL includes `zh-TW`, venue/transport changes, banner scheduling, support tables, admin audit logs, allowlist records, `disabled`, and `seat_operation_history`; local migrate succeeds or a blocker is explicitly recorded.
  </acceptance_criteria>
  <done>Feature plans have a real migrated database contract or an explicit blocker before verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Drizzle schema -> SQL migration | TypeScript schema becomes production database changes. |
| Local environment -> migration apply | Root `.env` and local DB availability determine whether apply can be verified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-11 | Tampering | migration SQL | mitigate | Review destructive SQL and locale enum replacement before apply. |
| T-25-12 | Repudiation | schema gate evidence | mitigate | Record exact generate/migrate commands; no silent PASS without apply. |
</threat_model>

<verification>
This plan blocks every feature plan. Do not run route-level or feature verification until 25-06 has a successful migrate result or a clearly recorded blocker.
</verification>

<success_criteria>
Phase 25 schema is generated, reviewed, and locally applied or truthfully blocked before feature implementation verifies behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/25-admin-operations-console/25-06-SUMMARY.md`.
</output>
