---
phase: 25-admin-operations-console
plan: "08"
type: execute
wave: 6
depends_on:
  - "25-07"
files_modified:
  - apps/api/src/modules/admin/admin-performance.controller.ts
  - apps/api/src/modules/admin/admin.service.ts
  - apps/api/src/modules/admin/admin.service.spec.ts
  - apps/web/hooks/use-admin.ts
  - apps/web/components/admin/event-publish-confirmation-dialog.tsx
  - apps/web/components/admin/performance-form.tsx
  - apps/web/components/admin/__tests__/event-publish-confirmation.test.tsx
  - apps/web/e2e/admin-event-publish.spec.ts
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-03
requirements_addressed: [ADMIN-01, ADMIN-03]
must_haves:
  truths:
    - "D-01: A single admin can update and publish an event quickly without separate operator/reviewer/approver roles."
    - "D-02: Event lifecycle states and publish checklist remain the smallest planner-selected model that satisfies ADMIN-01."
    - "D-03: Event publish requires confirmation and masked audit before committing."
    - "Event update and event publish both write D-10 audit evidence."
    - "Venue and transport fields appear in edit UI and publish summary."
  artifacts:
    - path: "apps/web/components/admin/event-publish-confirmation-dialog.tsx"
      provides: "Reasoned publish confirmation with changed-field, venue, transport, sale, and seat summary"
    - path: "apps/api/src/modules/admin/admin.service.ts"
      provides: "Transactional event update/publish with audit"
  key_links:
    - from: "apps/web/components/admin/performance-form.tsx"
      to: "apps/api/src/modules/admin/admin-performance.controller.ts"
      via: "useAdmin publish/update mutations"
      pattern: "publish"
---

<objective>
Complete admin-led event registration, venue/transport editing, and publish flow.

Purpose: ADMIN-01 requires event registration/review/publish states, and checker requires explicit venue/transport plus D-10 event update audit coverage.
Output: backend publish/update behavior, event publish dialog, form integration, tests, and an E2E spec that will run after AdminModule wiring.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-admin-operations-console/25-CONTEXT.md
@.planning/phases/25-admin-operations-console/25-UI-SPEC.md
@.planning/phases/25-admin-operations-console/25-PATTERNS.md
@.planning/phases/25-admin-operations-console/25-07-SUMMARY.md
@.planning/phases/24-traffic-booking-payment-core/24-15-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement backend event update/publish audit</name>
  <files>
    apps/api/src/modules/admin/admin-performance.controller.ts
    apps/api/src/modules/admin/admin.service.ts
    apps/api/src/modules/admin/admin.service.spec.ts
  </files>
  <read_first>
    packages/shared/src/schemas/performance.schema.ts
    packages/shared/src/schemas/admin-operations.schema.ts
    apps/api/src/modules/admin/admin-performance.controller.ts
    apps/api/src/modules/admin/admin.service.ts
    apps/api/src/modules/admin/admin-audit.service.ts
  </read_first>
  <behavior>
    - Event update writes `event.update` audit with safe changed fields, including venue/transport changes.
    - Publish request requires reason and confirmed changed-field summary, moves lifecycle to `published`, and writes `event.publish` audit.
    - Missing required Korean or English content blocks publish without public status mutation.
  </behavior>
  <action>
    Add publish/update DTO handling to admin performance endpoints and service methods. Use `@AdminCapabilities('event.write')` for update and `@AdminCapabilities('event.publish')` for publish. Use the existing admin-led flow per D-01/D-02; do not require separate reviewer/approver users. Use `AdminAuditService` for `event.update` and `event.publish`, including actor, resource, status, reason, safe before/after, IP, user agent, and changed fields.
  </action>
  <verify>
    <automated>pnpm --filter @grabit/api test -- src/modules/admin/admin.service.spec.ts && pnpm --filter @grabit/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    Tests cover update audit, publish audit, publish blocked by missing ko/en content, and no mandatory multi-user approval sequence.
  </acceptance_criteria>
  <done>Backend event lifecycle satisfies ADMIN-01 and D-10 event audit coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire venue/transport and publish review UI</name>
  <files>
    apps/web/hooks/use-admin.ts
    apps/web/components/admin/event-publish-confirmation-dialog.tsx
    apps/web/components/admin/performance-form.tsx
    apps/web/components/admin/__tests__/event-publish-confirmation.test.tsx
    apps/web/e2e/admin-event-publish.spec.ts
  </files>
  <read_first>
    apps/web/components/admin/performance-form.tsx
    apps/web/hooks/use-admin.ts
    apps/web/components/admin/admin-booking-detail-modal.tsx
    apps/web/e2e/helpers/auth.ts
  </read_first>
  <behavior>
    - Locale tabs render in order `ko`, `en`, `th`, `zh-CN`, `zh-TW`.
    - Venue and transport summary appear in form and publish confirmation.
    - Publish dialog shows UI-SPEC copy and disables confirm until reason/confirmation requirements are met.
  </behavior>
  <action>
    Extend the existing `PerformanceForm`; do not replace it with a wizard. Add venue/transport controls, publish review summary, sale/payment/ticket-limit visibility, and `EventPublishConfirmationDialog`. Extend `use-admin.ts` with publish/update mutations and query invalidation. Create/update `admin-event-publish.spec.ts`, but keep route-level execution for Plan 25-23 after `AdminModule` registers new controllers.
  </action>
  <verify>
    <automated>pnpm --filter @grabit/web test -- components/admin/__tests__/event-publish-confirmation.test.tsx && pnpm --filter @grabit/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    Component tests see exact publish copy, venue/transport summary, locale order, and disabled reason gating; E2E spec exists but is not executed in this plan.
  </acceptance_criteria>
  <done>Event registration UI covers venue/transport and publish confirmation without running route-level E2E before module wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser -> publish/update API | Publish reason and changed-field summaries are untrusted request data. |
| Publish lifecycle -> public catalog | Internal operational state can accidentally leak into sales status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Elevation of Privilege | publish/update endpoints | mitigate | Require server-side capabilities. |
| T-25-02 | Tampering | publish lifecycle | mitigate | Keep internal lifecycle separate from public status. |
| T-25-06 | Information Disclosure | event audit diff | mitigate | Use `AdminAuditService` masked diff for update and publish. |
</threat_model>

<verification>
Run API unit and web component/typecheck commands only. Route-level E2E is deferred until AdminModule/sidebar wiring in 25-23 and full verification in 25-15.
</verification>

<success_criteria>
ADMIN-01 event registration/publish behavior includes venue/transport, confirmation, and complete D-10 event audit coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/25-admin-operations-console/25-08-SUMMARY.md`.
</output>
