---
phase: 25-admin-operations-console
plan: "07"
type: execute
wave: 5
depends_on:
  - "25-06"
files_modified:
  - apps/api/src/modules/admin/admin-audit.service.ts
  - apps/api/src/modules/admin/admin-audit.service.spec.ts
  - apps/api/src/modules/admin/admin-security.service.ts
  - apps/api/src/modules/admin/admin-security.service.spec.ts
  - apps/api/src/modules/admin/csv-export.util.ts
  - apps/api/src/modules/admin/csv-export.util.spec.ts
autonomous: true
requirements:
  - ADMIN-03
  - ADMIN-04
requirements_addressed: [ADMIN-03, ADMIN-04]
must_haves:
  truths:
    - "D-11: Sensitive-action audit writes are centralized with masked diff fields before feature plans call them."
    - "D-09: IP allowlist uses env/bootstrap plus DB-managed changes with audit evidence for allowlist exceptions or access denials."
    - "CSV export cells are escaped and formula-neutralized before raw export work."
  artifacts:
    - path: "apps/api/src/modules/admin/admin-audit.service.ts"
      provides: "Central masked audit writer/query helper"
      exports: ["AdminAuditService"]
    - path: "apps/api/src/modules/admin/admin-security.service.ts"
      provides: "Production-aware allowlist decision helper"
      exports: ["AdminSecurityService"]
    - path: "apps/api/src/modules/admin/csv-export.util.ts"
      provides: "CSV escaping and formula injection mitigation"
      exports: ["safeCsvCell"]
  key_links:
    - from: "apps/api/src/modules/admin/csv-export.util.ts"
      to: "reservation export plans"
      via: "safeCsvCell"
      pattern: "^[=+\\-@\\t\\r]"
---

<objective>
Implement shared audit, allowlist, and CSV safety primitives.

Purpose: D-09 through D-15 require these primitives before event, support, refund, export, seat, security, and banner features write sensitive evidence.
Output: `AdminAuditService`, `AdminSecurityService`, `safeCsvCell`, and focused tests.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-admin-operations-console/25-CONTEXT.md
@.planning/phases/25-admin-operations-console/25-RESEARCH.md
@.planning/phases/25-admin-operations-console/25-PATTERNS.md
@.planning/phases/25-admin-operations-console/25-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement masked audit writer/query helper</name>
  <files>
    apps/api/src/modules/admin/admin-audit.service.ts
    apps/api/src/modules/admin/admin-audit.service.spec.ts
  </files>
  <read_first>
    apps/api/src/database/schema/admin-audit-logs.ts
    apps/api/src/modules/consent/consent.service.ts
    apps/api/src/modules/admin/admin-booking.service.ts
    packages/shared/src/schemas/admin-operations.schema.ts
  </read_first>
  <behavior>
    - Audit masks email, phone, IP, tokens, secrets, OTPs, credentials, and raw export rows.
    - Audit supports D-10 actions: event publish/update, admin refund, CS escalation, seat operation, reservation export, and security/permission changes.
    - Audit supports `success`, `denied`, and `failed` statuses.
  </behavior>
  <action>
    Create `AdminAuditService` as the only writer/query helper for generalized admin audit rows. It must accept transaction clients where callers need audit and mutation atomicity, produce masked changed-field before/after payloads, and never store raw CSV row values. Include tests for all D-10 action names and masking rules.
  </action>
  <verify>
    <automated>pnpm --filter @grabit/api test -- src/modules/admin/admin-audit.service.spec.ts && pnpm --filter @grabit/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    Tests prove D-10 action coverage and that raw PII/secrets/export rows never persist in audit diff output.
  </acceptance_criteria>
  <done>Feature plans can call one masked audit service for sensitive actions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement allowlist and CSV safety helpers</name>
  <files>
    apps/api/src/modules/admin/admin-security.service.ts
    apps/api/src/modules/admin/admin-security.service.spec.ts
    apps/api/src/modules/admin/csv-export.util.ts
    apps/api/src/modules/admin/csv-export.util.spec.ts
  </files>
  <read_first>
    apps/api/src/common/request-ip.ts
    apps/api/src/database/schema/admin-access-allowlist.ts
    apps/api/src/modules/admin/admin-audit.service.ts
    packages/shared/src/schemas/admin-operations.schema.ts
  </read_first>
  <behavior>
    - Production allowlist evaluates env/bootstrap CIDRs plus DB-managed overrides.
    - Non-production bypass is explicit and tested, not implicit.
    - Allowlist denial/exception/change writes audit evidence.
    - `safeCsvCell` quotes all cells and neutralizes `=`, `+`, `-`, `@`, tab, and carriage return starts.
  </behavior>
  <action>
    Implement `AdminSecurityService` using the existing trusted request IP helper. The resolved policy is: env/bootstrap allowlist provides the safe default, DB-managed exceptions/changes are allowed through `security.manage`, local/dev bypass is allowed only outside production, and every denial/exception/change writes `AdminAuditService` evidence. Implement `safeCsvCell` and CSV row helpers for later export plans.
  </action>
  <verify>
    <automated>pnpm --filter @grabit/api test -- src/modules/admin/admin-security.service.spec.ts src/modules/admin/csv-export.util.spec.ts && pnpm --filter @grabit/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    Tests prove allowlist decision behavior, audit writes for denial/change, explicit non-prod bypass, and CSV formula neutralization.
  </acceptance_criteria>
  <done>ADMIN-03 allowlist and ADMIN-04 CSV safety primitives are available before feature APIs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin API -> audit storage | Sensitive request/action metadata is written durably. |
| Proxy headers -> allowlist decision | Request IP can be spoofed if extraction differs by endpoint. |
| Export generation -> admin workstation | Raw PII leaves backend control as CSV. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03 | Information Disclosure | audit/CSV helpers | mitigate | Mask sensitive data and sanitize CSV cells centrally. |
| T-25-04 | Spoofing / Denial of Service | allowlist decision | mitigate | Use one trusted IP resolver and audited env+DB policy. |
| T-25-10 | Repudiation | security status | mitigate | Keep MFA separate as accepted risk; do not claim MFA enforcement. |
</threat_model>

<verification>
Run API unit tests and typecheck before any feature plan consumes audit/security/export helpers.
</verification>

<success_criteria>
High-risk Phase 25 features share one audited, masked, production-aware security primitive layer.
</success_criteria>

<output>
After completion, create `.planning/phases/25-admin-operations-console/25-07-SUMMARY.md`.
</output>
