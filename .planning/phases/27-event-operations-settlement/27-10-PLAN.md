---
phase: 27-event-operations-settlement
plan: 10
type: execute
wave: 1
depends_on:
  - 27-01
  - 27-03
files_modified:
  - apps/web/package.json
  - pnpm-lock.yaml
  - apps/web/components/field/qr-ticket-image.tsx
  - apps/web/components/field/__tests__/qr-ticket-image.test.tsx
autonomous: true
requirements:
  - QR-02
requirements_addressed:
  - QR-02
must_haves:
  truths:
    - "D-01 buyer QR component renders a real scannable QR image, not metadata-only state."
    - "D-03 QR value is an HTTPS Grabit check-in URL that a normal phone camera can open."
    - "D-05 raw QR token/JTI is not printed as visible fallback text."
  artifacts:
    - path: "apps/web/components/field/qr-ticket-image.tsx"
      provides: "shared buyer QR renderer"
    - path: "apps/web/package.json"
      provides: "qrcode.react and idb dependency declarations"
  key_links:
    - from: "apps/web/components/field/qr-ticket-image.tsx"
      to: "apps/web/components/booking/booking-complete.tsx"
      via: "component import in Plan 27-11"
      pattern: "QrTicketImage"
---

<objective>
Add web dependencies and the reusable QR ticket image component.

Purpose: provide a tested QR renderer that later buyer surfaces can use without exposing raw token/JTI text.
Output: `qrcode.react`/`idb` dependencies, `QrTicketImage`, and green component tests.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-event-operations-settlement/27-CONTEXT.md
@.planning/phases/27-event-operations-settlement/27-RESEARCH.md
@.planning/phases/27-event-operations-settlement/27-UI-SPEC.md
@.planning/phases/27-event-operations-settlement/27-PATTERNS.md
@apps/web/package.json
@apps/web/components/field/__tests__/qr-ticket-image.test.tsx
</context>

<tasks>
<task type="auto">
  <name>Task 1: Install QR/offline web dependencies</name>
  <files>apps/web/package.json, pnpm-lock.yaml</files>
  <read_first>
    .planning/phases/27-event-operations-settlement/27-RESEARCH.md
    apps/web/package.json
    pnpm-lock.yaml
  </read_first>
  <action>Add `qrcode.react` and `idb` to `@grabit/web` using `pnpm --filter @grabit/web add qrcode.react idb`. Use `qrcode.react` for QR rendering and reserve `idb` for Plan 27-13 offline pending store. Do not add `@zxing/browser` unless the executor finds a direct requirement for in-browser camera scanning; locked decisions say normal phone camera opens the HTTPS QR URL.</action>
  <verify>
    <automated>pnpm --filter @grabit/web exec node -e "import('qrcode.react').then(() => import('idb')).then(() => console.log('phase27 deps ok'))"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/package.json` contains `qrcode.react` and `idb`.
    - `pnpm-lock.yaml` is updated.
    - No `@zxing/browser` dependency is added without a documented scanner-page decision.
  </acceptance_criteria>
  <done>Required web dependencies are installed and importable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement QrTicketImage</name>
  <files>apps/web/components/field/qr-ticket-image.tsx, apps/web/components/field/__tests__/qr-ticket-image.test.tsx</files>
  <read_first>
    .planning/phases/27-event-operations-settlement/27-UI-SPEC.md
    .planning/phases/27-event-operations-settlement/27-PATTERNS.md
    apps/web/components/field/__tests__/qr-ticket-image.test.tsx
  </read_first>
  <action>Implement `QrTicketImage` using `QRCodeSVG` from `qrcode.react`. Accept props for `value`, `title`, and optional `size`, default to a stable square at least 220px with white background, explicit quiet-zone margin, `level="M"`, and accessible title `티켓 검표 QR`. Render only the QR image and safe label/copy; do not render the raw `value`, token, JTI, JWT payload, or full URL as text even on failure. If generation fails, show retry/fallback UI without printing the token.</action>
  <verify>
    <automated>pnpm --filter @grabit/web exec vitest run components/field/__tests__/qr-ticket-image.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Test finds a QR SVG/canvas/image element and stable square container.
    - Test asserts the encoded HTTPS URL value is not visible as text.
    - Test asserts raw token and raw JTI sample strings are absent from rendered text.
  </acceptance_criteria>
  <done>Reusable QR image component passes tests and preserves raw-token non-rendering.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| signed QR token -> browser QR renderer | Token may be encoded in QR value but must not be printed as visible text. |
| web dependencies -> scanner/offline UI | New dependencies must not introduce unnecessary camera/native scanner scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-10-QR-REPLAY | R | QR image | accept | Replay is mitigated server-side in Plan 27-06; this component only encodes the URL. |
| T-27-10-AUTHZ | E | QR renderer | accept | Authorization is enforced by scanner API/UI plans; QR rendering grants no action. |
| T-27-10-TOKEN-LEAK | I | QrTicketImage | mitigate | Component tests assert raw token/JTI/full URL are not visible text. |
| T-27-10-OFFLINE-TAMPER | T | idb dependency | transfer | Offline store is implemented and tested in Plan 27-13. |
| T-27-10-PII-LEAK | I | QR fallback UI | mitigate | Failure fallback never prints raw token/JTI or PII. |
| T-27-10-SCOPE-CREEP | D | dependency set | mitigate | Do not add hardware/native or in-browser camera scanner dependency unless separately justified. |
</threat_model>

<verification>
Run:
- `pnpm --filter @grabit/web exec node -e "import('qrcode.react').then(() => import('idb')).then(() => console.log('phase27 deps ok'))"`
- `pnpm --filter @grabit/web exec vitest run components/field/__tests__/qr-ticket-image.test.tsx`
- `pnpm --filter @grabit/web typecheck`
</verification>

<success_criteria>
The project has a tested QR renderer that can encode an HTTPS check-in URL without exposing raw token/JTI text, and offline dependency support is available for later UI work.
</success_criteria>

<output>
After completion, create `.planning/phases/27-event-operations-settlement/27-10-SUMMARY.md`.
</output>
