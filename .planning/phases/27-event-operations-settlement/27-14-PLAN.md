---
phase: 27-event-operations-settlement
plan: 14
type: execute
wave: 5
depends_on:
  - 27-03
  - 27-08
files_modified:
  - apps/web/app/admin/field-monitor/page.tsx
  - apps/web/components/field/field-monitor.tsx
  - apps/web/hooks/use-field-monitor.ts
  - apps/web/components/admin/admin-sidebar.tsx
  - apps/web/components/field/__tests__/field-monitor.test.tsx
autonomous: true
requirements:
  - FIELD-01
requirements_addressed:
  - FIELD-01
must_haves:
  truths:
    - "D-23 first screen answers whether entry is proceeding normally."
    - "D-24 field monitor shows required KPIs."
    - "D-25 scan logs are secondary drill-down/filter data."
    - "D-26 abnormal alerts cover duplicate spike, rejected/tampered, refunded/cancelled, offline backlog, and sync failures."
    - "D-22 offline pending/synced/rejected counts are visible to field monitor."
  artifacts:
    - path: "apps/web/components/field/field-monitor.tsx"
      provides: "KPI-first monitor UI"
    - path: "apps/web/app/admin/field-monitor/page.tsx"
      provides: "admin monitor route"
    - path: "apps/web/components/admin/admin-sidebar.tsx"
      provides: "full admin/finance navigation entry; scanner-only excluded by scanner shell"
  key_links:
    - from: "apps/web/components/field/field-monitor.tsx"
      to: "/api/v1/field/monitor"
      via: "use-field-monitor monitor query"
      pattern: "refetchInterval.*10000"
---

<objective>
Build the KPI-first field monitor admin surface.

Purpose: let operators see entry health, abnormal alerts, and offline backlog without starting from raw scan logs.
Output: admin field monitor page, component, hook, sidebar link, and green monitor tests.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-event-operations-settlement/27-CONTEXT.md
@.planning/phases/27-event-operations-settlement/27-UI-SPEC.md
@.planning/phases/27-event-operations-settlement/27-PATTERNS.md
@apps/web/components/field/__tests__/field-monitor.test.tsx
@apps/web/components/admin/admin-booking-dashboard.tsx
@apps/web/components/admin/admin-sidebar.tsx
@apps/web/hooks/use-admin-dashboard.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement FieldMonitor component and hook</name>
  <files>apps/web/components/field/field-monitor.tsx, apps/web/hooks/use-field-monitor.ts, apps/web/components/field/__tests__/field-monitor.test.tsx</files>
  <read_first>
    .planning/phases/27-event-operations-settlement/27-UI-SPEC.md
    apps/web/components/field/__tests__/field-monitor.test.tsx
    apps/web/components/admin/admin-booking-dashboard.tsx
    apps/web/hooks/use-admin-dashboard.ts
    packages/shared/src/schemas/field-operations.schema.ts
  </read_first>
  <action>Create `apps/web/hooks/use-field-monitor.ts` and implement `useFieldMonitorSummary()` with React Query `refetchInterval: 10_000` while visible and manual refresh support. Implement `FieldMonitor` with 4-8 compact KPI stat cards before logs: entered, not-entered, entry rate, duplicate scans, rejected scans, offline pending, offline synced, latest abnormal alerts. Render alerts pinned above secondary scan logs and include filters for event, showtime, scan result, offline state, scanner account, and time range. Use API summary/offline counts from Plan 27-08 directly; do not depend on the scanner offline UI/store from Plan 27-13. Use UI-SPEC colors/copy and never render raw token/JTI/full user-agent/raw PII.</action>
  <verify>
    <automated>pnpm --filter @grabit/web exec vitest run components/field/__tests__/field-monitor.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Test proves KPI cards appear before any log table.
    - Test proves all D-24 KPI labels and D-26 alerts render.
    - Test proves pending and synced offline counts are distinct.
    - Test proves raw token/JTI/PII strings are absent.
    - Field monitor renders API-provided offline counts directly and does not import the scanner offline store/UI from Plan 27-13.
  </acceptance_criteria>
  <done>Field monitor component is KPI-first and passes tests.</done>
</task>

<task type="auto">
  <name>Task 2: Wire admin route and navigation</name>
  <files>apps/web/app/admin/field-monitor/page.tsx, apps/web/components/admin/admin-sidebar.tsx</files>
  <read_first>
    apps/web/components/admin/admin-sidebar.tsx
    apps/web/app/admin/layout.tsx
    apps/web/components/field/field-monitor.tsx
    .planning/phases/27-event-operations-settlement/27-UI-SPEC.md
  </read_first>
  <action>Create `/admin/field-monitor` route rendering `FieldMonitor`. Add `현장 모니터` under admin `운영` navigation for full admin/finance/operator-capable users following existing sidebar grouping. Do not wire scanner-only users into the full admin sidebar; scanner-only users use `/field/check-in` minimal shell from Plan 27-12.</action>
  <verify>
    <automated>pnpm --filter @grabit/web typecheck &amp;&amp; pnpm --filter @grabit/web exec vitest run components/field/__tests__/field-monitor.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `/admin/field-monitor` page exists.
    - Sidebar contains `현장 모니터` for admin operations users.
    - No scanner-only route relies on full admin sidebar.
  </acceptance_criteria>
  <done>Field monitor route and admin navigation are wired.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| monitor API -> browser dashboard | Aggregated alerts/logs could leak raw scan or PII details. |
| admin nav -> scanner-only accounts | Navigation must not expose full admin routes to scanner-only users. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-14-QR-REPLAY | R | KPI duplicate card | mitigate | Duplicate scans are a top-level KPI and alert category. |
| T-27-14-AUTHZ | E | admin route/sidebar | mitigate | Scanner-only users remain on minimal scanner shell; admin monitor route follows admin guard. |
| T-27-14-TOKEN-LEAK | I | monitor rows | mitigate | Tests assert no raw token/JTI/full user-agent/raw PII. |
| T-27-14-OFFLINE-FALSE-PASS | R | offline cards | mitigate | Pending and synced are separate KPI states; pending backlog alert is visible. |
| T-27-14-ALERT-BLINDSPOT | D/R | FieldMonitor | mitigate | D-26 alert categories are pinned above logs. |
| T-27-14-PRIVILEGE-ESCALATION | E | sidebar | mitigate | No refund/reservation/user/content/security/settlement/raw export links are added to scanner shell. |
</threat_model>

<verification>
Run:
- `pnpm --filter @grabit/web exec vitest run components/field/__tests__/field-monitor.test.tsx`
- `pnpm --filter @grabit/web typecheck`
</verification>

<success_criteria>
Operators can open a KPI-first field monitor with required counts and alerts; scanner-only users remain outside the full admin sidebar.
</success_criteria>

<output>
After completion, create `.planning/phases/27-event-operations-settlement/27-14-SUMMARY.md`.
</output>
