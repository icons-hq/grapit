---
phase: 27-event-operations-settlement
plan: 07
type: execute
wave: 3
depends_on:
  - 27-06
files_modified:
  - apps/api/src/modules/field-operations/offline-sync.controller.ts
  - apps/api/src/modules/field-operations/offline-sync.service.ts
  - apps/api/src/modules/field-operations/offline-sync.service.spec.ts
  - apps/api/src/modules/field-operations/field-operations.module.ts
autonomous: true
requirements:
  - QR-02
  - FIELD-01
requirements_addressed:
  - QR-02
  - FIELD-01
must_haves:
  truths:
    - "D-18 scanner remains online-first."
    - "D-19/D-20 local offline attempts are pending only and contain scanner/showtime/device attempt context without raw PII."
    - "D-21 server sync decides synced/rejected final state for every pending attempt."
    - "D-22 offline sync results are visible to scanner and monitor as pending, synced, or rejected."
  artifacts:
    - path: "apps/api/src/modules/field-operations/offline-sync.service.ts"
      provides: "server conflict resolution for pending scan attempts"
    - path: "apps/api/src/modules/field-operations/offline-sync.controller.ts"
      provides: "scanner-only offline sync endpoint"
  key_links:
    - from: "apps/api/src/modules/field-operations/offline-sync.service.ts"
      to: "apps/api/src/modules/field-operations/field-check-in.service.ts"
      via: "server re-verification/consume outcome logic"
      pattern: "FieldCheckInService"
---

<objective>
Implement the server-side offline sync endpoint that converts local pending attempts into server-final outcomes.

Purpose: keep offline fallback safe by making local attempts pending evidence only, with PostgreSQL/server verification deciding final state.
Output: offline sync controller/service/module wiring and green offline sync tests.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-event-operations-settlement/27-CONTEXT.md
@.planning/phases/27-event-operations-settlement/27-RESEARCH.md
@.planning/phases/27-event-operations-settlement/27-PATTERNS.md
@.planning/phases/27-event-operations-settlement/27-VALIDATION.md
@apps/api/src/modules/field-operations/field-check-in.service.ts
@apps/api/src/modules/field-operations/offline-sync.service.spec.ts
@apps/api/src/database/schema/ticket-scan-events.ts
@apps/api/src/common/guards/admin-capabilities.guard.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement OfflineSyncService conflict resolution</name>
  <files>apps/api/src/modules/field-operations/offline-sync.service.ts, apps/api/src/modules/field-operations/offline-sync.service.spec.ts</files>
  <read_first>
    .planning/phases/27-event-operations-settlement/27-CONTEXT.md
    apps/api/src/modules/field-operations/offline-sync.service.spec.ts
    apps/api/src/modules/field-operations/field-check-in.service.ts
    apps/api/src/database/schema/ticket-scan-events.ts
    packages/shared/src/schemas/field-operations.schema.ts
  </read_first>
  <action>Implement `OfflineSyncService.syncPendingAttempts()` so each pending attempt is treated as untrusted input. Re-run server verification/consume conflict logic, dedupe by `deviceAttemptId`, insert `ticket_scan_events` with `source='offline_sync'`, and return per-attempt `synced` or `rejected` results. Handle stale/recovered connectivity fixtures from the RED spec and reject duplicate, tampered, refunded/cancelled, expired, wrong-showtime, and already-used attempts. Do not trust client-provided local accepted state.</action>
  <verify>
    <automated>pnpm --filter @grabit/api exec vitest run src/modules/field-operations/offline-sync.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Sync result contains `pending`, `synced`, or `rejected` state only from server resolution.
    - Duplicate `deviceAttemptId` is idempotent.
    - Rejected sync outcomes include redacted reason and no raw token/JTI/PII.
    - Offline local acceptance is never represented as final success before server sync.
  </acceptance_criteria>
  <done>Offline sync service safely resolves pending attempts through server truth.</done>
</task>

<task type="auto">
  <name>Task 2: Wire scanner-only offline sync endpoint</name>
  <files>apps/api/src/modules/field-operations/offline-sync.controller.ts, apps/api/src/modules/field-operations/field-operations.module.ts</files>
  <read_first>
    apps/api/src/modules/field-operations/field-check-in.controller.ts
    apps/api/src/modules/field-operations/field-operations.module.ts
    apps/api/src/common/guards/admin-capabilities.guard.ts
    packages/shared/src/schemas/admin-operations.schema.ts
  </read_first>
  <action>Create `OfflineSyncController` under the field operations API namespace with a POST sync endpoint protected by `field.scan.sync`. Register the controller/service in `FieldOperationsModule`. Accept only shared offline sync request schema payloads and return shared sync response schemas. Do not expose refund, settlement, reservation management, user management, content, security, or raw export capabilities through this controller.</action>
  <verify>
    <automated>pnpm --filter @grabit/api typecheck &amp;&amp; pnpm --filter @grabit/api exec vitest run src/modules/field-operations/offline-sync.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Sync endpoint uses `AdminCapabilities('field.scan.sync')`.
    - Controller has no `@Public()` and no broad full-admin-only shortcut that bypasses capability checks.
    - Module registers sync service/controller without breaking verify/consume tests.
  </acceptance_criteria>
  <done>Offline sync API is routable and scanner-capability protected.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser IndexedDB queue -> sync API | Client-stored pending attempts can be stale or tampered. |
| sync API -> scan events/monitor | Sync results feed monitor and settlement evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-07-QR-REPLAY | R | sync conflict resolver | mitigate | Server re-verifies every pending attempt and returns duplicate/already-used as rejected when appropriate. |
| T-27-07-AUTHZ | E | OfflineSyncController | mitigate | Endpoint requires `field.scan.sync`; scanner receives no unrelated admin capabilities. |
| T-27-07-TAMPER-ENUMERATION | I | sync rejection payload | mitigate | Rejections include redacted reason only, not sensitive lookup details. |
| T-27-07-OFFLINE-TAMPER | T/R | OfflineSyncService | mitigate | Treat every local attempt as untrusted; dedupe by deviceAttemptId and finalize only via server verification. |
| T-27-07-PII-RAW-TOKEN-LEAK | I | sync event metadata | mitigate | Store masked JTI/redacted token reference and avoid raw PII in scan events. |
| T-27-07-MONITOR-FALSE-PASS | R | sync result state | mitigate | Return pending/synced/rejected states for monitor consumption; no local green success. |
</threat_model>

<verification>
Run:
- `pnpm --filter @grabit/api exec vitest run src/modules/field-operations/offline-sync.service.spec.ts`
- `pnpm --filter @grabit/api typecheck`
</verification>

<success_criteria>
Offline fallback remains online-first and pending-only locally; recovered connectivity sync produces server-final synced/rejected evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/27-event-operations-settlement/27-07-SUMMARY.md`.
</output>
