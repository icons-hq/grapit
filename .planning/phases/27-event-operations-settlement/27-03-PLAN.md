---
phase: 27-event-operations-settlement
plan: 03
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/web/components/field/__tests__/qr-ticket-image.test.tsx
  - apps/web/components/field/__tests__/scanner-check-in.test.tsx
  - apps/web/components/field/__tests__/field-monitor.test.tsx
  - apps/web/components/admin/__tests__/settlement-dashboard.test.tsx
  - apps/web/e2e/phase27-qr-check-in.spec.ts
  - apps/web/e2e/phase27-offline-sync.spec.ts
autonomous: true
requirements:
  - QR-02
  - FIELD-01
  - POST-01
requirements_addressed:
  - QR-02
  - FIELD-01
  - POST-01
must_haves:
  truths:
    - "D-01/D-03 buyer web tests require a real QR element encoding an HTTPS Grabit check-in URL."
    - "D-08/D-11 scanner UI tests require regular-user denial and no full admin sidebar for scanner-only users."
    - "D-18/D-22 offline UI/E2E tests require pending, synced, and rejected states."
    - "D-23/D-25 monitor tests require KPI-first cards before logs."
    - "D-27/D-31 settlement dashboard tests require dashboard plus CSV export with scanner denial."
  artifacts:
    - path: "apps/web/e2e/phase27-qr-check-in.spec.ts"
      provides: "buyer QR -> protected route -> scanner entry -> duplicate rejection browser contract"
    - path: "apps/web/e2e/phase27-offline-sync.spec.ts"
      provides: "offline pending/reconnect/rejected conflict browser contract"
  key_links:
    - from: "apps/web/components/field/__tests__/scanner-check-in.test.tsx"
      to: "apps/web/e2e/phase27-qr-check-in.spec.ts"
      via: "shared user-visible scanner outcome copy"
      pattern: "입장 처리"
---

<objective>
Create Phase 27 web component and Playwright RED contracts before UI implementation.

Purpose: make buyer QR, scanner, offline, monitor, and settlement UI requirements executable before product surfaces are changed.
Output: missing component tests and E2E specs from `27-VALIDATION.md`.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-event-operations-settlement/27-CONTEXT.md
@.planning/phases/27-event-operations-settlement/27-VALIDATION.md
@.planning/phases/27-event-operations-settlement/27-UI-SPEC.md
@.planning/phases/27-event-operations-settlement/27-PATTERNS.md
@apps/web/components/booking/booking-complete.tsx
@apps/web/components/reservation/reservation-detail.tsx
@apps/web/e2e/phase26-qr-visibility.spec.ts
@apps/web/e2e/booking-complete-qr.spec.ts
@apps/web/components/admin/__tests__/reservation-export-panel.test.tsx
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add buyer QR and scanner component tests</name>
  <files>apps/web/components/field/__tests__/qr-ticket-image.test.tsx, apps/web/components/field/__tests__/scanner-check-in.test.tsx</files>
  <read_first>
    .planning/phases/27-event-operations-settlement/27-CONTEXT.md
    .planning/phases/27-event-operations-settlement/27-UI-SPEC.md
    .planning/phases/27-event-operations-settlement/27-PATTERNS.md
    apps/web/components/booking/booking-complete.tsx
    apps/web/components/reservation/reservation-detail.tsx
    apps/web/components/admin/__tests__/reservation-export-panel.test.tsx
  </read_first>
  <behavior>
    - QR image test requires an SVG/canvas/image QR area at least 200px square and no visible raw token/JTI text per D-01/D-02/D-05.
    - Scanner test requires verify-first UI, sticky/full-width mobile `입장 처리` only for processable results, and disabled action for duplicate/refunded/tampered/wrong-showtime results per D-12/D-13/D-14.
    - Scanner test requires regular member access denied copy `이 티켓을 검표할 권한이 없습니다` per D-08.
  </behavior>
  <action>Create RED component tests for future `QrTicketImage` and `ScannerCheckIn` components. Use Testing Library and existing admin component test patterns. Assert exact UI-SPEC copy: `QR 티켓이 준비되었습니다. 입장 시 현장 스태프가 QR을 확인합니다.`, `현장 검표 결과가 최종 입장 기준입니다.`, `입장 가능 티켓입니다`, `이미 입장 처리된 티켓입니다`, `확인할 수 없는 QR입니다`, and `보류 상태는 최종 입장 증거가 아닙니다`.</action>
  <verify>
    <automated>test -f apps/web/components/field/__tests__/qr-ticket-image.test.tsx &amp;&amp; test -f apps/web/components/field/__tests__/scanner-check-in.test.tsx &amp;&amp; rg "QR 티켓이 준비되었습니다|현장 검표 결과가 최종 입장 기준입니다|raw.*(token|JTI)|200px|qr" apps/web/components/field/__tests__/qr-ticket-image.test.tsx &amp;&amp; rg "입장 가능 티켓입니다|이미 입장 처리된 티켓입니다|확인할 수 없는 QR입니다|보류 상태는 최종 입장 증거가 아닙니다|이 티켓을 검표할 권한이 없습니다" apps/web/components/field/__tests__/scanner-check-in.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Wave 0 verify passes by static contract inspection; tests remain intentionally RED under Vitest until `components/field/qr-ticket-image.tsx` and `components/field/scanner-check-in.tsx` are implemented.
    - Tests assert raw token, raw JTI, JWT payload, and full deep-link URL are not visible text.
    - Tests assert scanner-only route does not render full admin sidebar labels.
  </acceptance_criteria>
  <done>Buyer QR and scanner UI RED contracts exist and are statically verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add monitor and settlement component tests</name>
  <files>apps/web/components/field/__tests__/field-monitor.test.tsx, apps/web/components/admin/__tests__/settlement-dashboard.test.tsx</files>
  <read_first>
    .planning/phases/27-event-operations-settlement/27-CONTEXT.md
    .planning/phases/27-event-operations-settlement/27-UI-SPEC.md
    .planning/phases/27-event-operations-settlement/27-PATTERNS.md
    apps/web/components/admin/admin-booking-dashboard.tsx
    apps/web/components/admin/__tests__/reservation-export-panel.test.tsx
  </read_first>
  <behavior>
    - Field monitor test requires 4-8 compact KPI cards before any scan log table per D-23/D-25.
    - Monitor test requires entered, not-entered, entry rate, duplicate scans, rejected scans, offline pending, offline synced, and latest abnormal alerts per D-24/D-26.
    - Settlement dashboard test requires tabs `요약`, `입장/노쇼`, `결제/환불`, `내보내기`, export confirmation, reason gating, and scanner-only denial per D-27/D-29/D-31.
  </behavior>
  <action>Create RED tests for future `FieldMonitor` and `SettlementDashboard` components. Assert existing admin density and UI-SPEC copy rather than decorative landing-page layouts. Include negative assertions for raw token/JTI, raw PII row preview, and scanner-only export access.</action>
  <verify>
    <automated>test -f apps/web/components/field/__tests__/field-monitor.test.tsx &amp;&amp; test -f apps/web/components/admin/__tests__/settlement-dashboard.test.tsx &amp;&amp; rg "entered|not-entered|entry rate|duplicate scans|rejected scans|offline pending|offline synced|latest abnormal" apps/web/components/field/__tests__/field-monitor.test.tsx &amp;&amp; rg "요약|입장/노쇼|결제/환불|내보내기|정산 데이터를 내보내시겠습니까|scanner-only" apps/web/components/admin/__tests__/settlement-dashboard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Wave 0 verify passes by static contract inspection; component tests remain intentionally RED until KPI-first `FieldMonitor`, dashboard tabs, and export confirmation exist.
    - Tests assert no raw log table is the first primary screen.
  </acceptance_criteria>
  <done>Monitor and settlement UI RED contracts exist and are statically verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Phase 27 Playwright contracts</name>
  <files>apps/web/e2e/phase27-qr-check-in.spec.ts, apps/web/e2e/phase27-offline-sync.spec.ts</files>
  <read_first>
    .planning/phases/27-event-operations-settlement/27-CONTEXT.md
    .planning/phases/27-event-operations-settlement/27-VALIDATION.md
    .planning/phases/27-event-operations-settlement/27-UI-SPEC.md
    apps/web/e2e/phase26-qr-visibility.spec.ts
    apps/web/e2e/booking-complete-qr.spec.ts
  </read_first>
  <behavior>
    - QR check-in E2E covers buyer QR image, protected check-in route, scanner-only entry, no auto-consume-on-open, and duplicate rejection.
    - Offline E2E covers pending queue, recovered connectivity sync, and rejected conflict state.
    - Both E2E specs assert no visible raw token/JTI/payment key/cookie/PII.
  </behavior>
  <action>Create Playwright specs using existing Phase 26 route-mocking and raw-secret negative assertion patterns. The tests may be RED until implementation plans add routes/components, but they must be executable and scoped to Phase 27 files. Do not require a real phone camera in automated E2E; real phone-camera verification belongs to Plan 27-16 manual evidence.</action>
  <verify>
    <automated>test -f apps/web/e2e/phase27-qr-check-in.spec.ts &amp;&amp; test -f apps/web/e2e/phase27-offline-sync.spec.ts &amp;&amp; rg "/field/check-in|입장 처리가 완료되었습니다|duplicate|raw.*(token|JTI)" apps/web/e2e/phase27-qr-check-in.spec.ts &amp;&amp; rg "offline|pending|sync|rejected" apps/web/e2e/phase27-offline-sync.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - E2E specs contain explicit route flows for `/field/check-in` and offline sync.
    - E2E specs assert opening QR URL alone never produces `입장 처리가 완료되었습니다`.
    - Specs document real phone-camera verification as manual-only, not simulated as PASS.
    - Wave 0 verify passes by static contract inspection; implementation plans run the actual Playwright green command.
  </acceptance_criteria>
  <done>Phase 27 browser RED contracts are committed and statically verified.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| buyer QR UI -> scanner route | QR URL can be opened by any phone camera, so UI must not grant entry or leak raw token text. |
| scanner local queue -> server sync | Offline queue UI must not imply final admission before server sync. |
| admin dashboard -> CSV download | Settlement UI can expose sensitive export capabilities if capability gating is wrong. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03-QR-REPLAY | R | Playwright QR flow | mitigate | E2E requires duplicate rejection after first successful manual consume. |
| T-27-03-AUTHZ | E | scanner UI tests | mitigate | Tests require normal member denial and no admin sidebar for scanner-only flow. |
| T-27-03-TOKEN-LEAK | I | QR image/scanner tests | mitigate | Tests assert raw token/JTI/JWT/full URL are absent from visible UI. |
| T-27-03-OFFLINE-FALSE-PASS | R/T | offline UI/E2E | mitigate | Offline tests require pending and post-sync outcomes, not green local success. |
| T-27-03-MONITOR-LEAK | I | field monitor test | mitigate | Monitor tests forbid raw token and raw PII in KPI/log rows. |
| T-27-03-CSV-INJECTION | T | settlement dashboard test | mitigate | UI test requires export confirmation and backend Plan 27-09 validates `safeCsvRows`. |
| T-27-03-PRIVILEGE-ESCALATION | E | settlement dashboard test | mitigate | Scanner-only denial is tested on settlement/export UI. |
</threat_model>

<verification>
Overall Wave 0 verification is the three task-level static `<automated>` gates above. Do not run the web Vitest or Playwright commands in this plan; they are expected to be RED until implementation plans 27-10 through 27-15 create the components, routes, and browser flows.
</verification>

<success_criteria>
All web and Playwright Wave 0 gaps from `27-VALIDATION.md` are represented as concrete executable tests before UI implementation plans depend on them.
</success_criteria>

<output>
After completion, create `.planning/phases/27-event-operations-settlement/27-03-SUMMARY.md`.
</output>
