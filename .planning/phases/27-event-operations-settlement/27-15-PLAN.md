---
phase: 27-event-operations-settlement
plan: 15
type: execute
wave: 6
depends_on:
  - 27-03
  - 27-09
  - 27-14
files_modified:
  - apps/web/app/admin/settlement/page.tsx
  - apps/web/components/admin/settlement-dashboard.tsx
  - apps/web/hooks/use-admin-settlement.ts
  - apps/web/components/admin/admin-sidebar.tsx
  - apps/web/components/admin/admin-user-management.tsx
  - apps/web/components/admin/__tests__/settlement-dashboard.test.tsx
  - apps/web/components/admin/__tests__/admin-user-management.test.tsx
autonomous: true
requirements:
  - POST-01
  - QR-02
requirements_addressed:
  - POST-01
  - QR-02
must_haves:
  truths:
    - "D-27 settlement dashboard exists in addition to CSV export."
    - "D-28 dashboard summary includes sales/payment/refund/entry/no-show review data."
    - "D-29 export actions cover entry status, no-show reservations, reservation/payment/refund summary, and settlement/accounting input."
    - "D-31 settlement/export remains full admin or finance scope, not scanner-only."
    - "D-09/D-11 full admins can assign scanner-only staff permissions while scanner-only staff do not see unrelated admin routes."
  artifacts:
    - path: "apps/web/components/admin/settlement-dashboard.tsx"
      provides: "settlement dashboard and export UI"
    - path: "apps/web/app/admin/settlement/page.tsx"
      provides: "admin settlement route"
    - path: "apps/web/components/admin/admin-user-management.tsx"
      provides: "scanner bundle assignment labels"
  key_links:
    - from: "apps/web/components/admin/settlement-dashboard.tsx"
      to: "/api/v1/admin/settlement"
      via: "use-admin-settlement hook"
      pattern: "admin/settlement"
---

<objective>
Build the settlement dashboard/export UI and expose scanner permission assignment to full admins.

Purpose: close POST-01 with an operator-reviewable dashboard and protected CSV export controls while preserving scanner-only least privilege.
Output: settlement route/component/hook/sidebar/user-management updates and green UI tests.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-event-operations-settlement/27-CONTEXT.md
@.planning/phases/27-event-operations-settlement/27-UI-SPEC.md
@.planning/phases/27-event-operations-settlement/27-PATTERNS.md
@apps/web/components/admin/__tests__/settlement-dashboard.test.tsx
@apps/web/components/admin/__tests__/admin-user-management.test.tsx
@apps/web/components/admin/reservation-export-panel.tsx
@apps/web/hooks/use-reservations.ts
@apps/web/components/admin/admin-sidebar.tsx
@apps/web/components/admin/admin-user-management.tsx
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement settlement dashboard and export hooks</name>
  <files>apps/web/components/admin/settlement-dashboard.tsx, apps/web/hooks/use-admin-settlement.ts, apps/web/components/admin/__tests__/settlement-dashboard.test.tsx</files>
  <read_first>
    .planning/phases/27-event-operations-settlement/27-UI-SPEC.md
    apps/web/components/admin/__tests__/settlement-dashboard.test.tsx
    apps/web/components/admin/reservation-export-panel.tsx
    apps/web/hooks/use-reservations.ts
    packages/shared/src/schemas/field-operations.schema.ts
  </read_first>
  <action>Implement `useAdminSettlementSummary()` and `useAdminSettlementExport()` using `apiClient` for JSON and `fetch()`/blob download for CSV. Implement `SettlementDashboard` with tabs `요약`, `입장/노쇼`, `결제/환불`, `내보내기`; filters for event, showtime, date range, payment method, reservation status, entry status, and refund status; export buttons for all D-29 datasets; confirmation copy `정산 데이터를 내보내시겠습니까?`; reason gating; and no raw PII row preview. Disable exports until required filters and reason exist.</action>
  <verify>
    <automated>pnpm --filter @grabit/web exec vitest run components/admin/__tests__/settlement-dashboard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Dashboard test sees all four maximum tabs and all four dataset export actions.
    - Export confirmation includes filter summary, actor/reason fields, and sensitive export warning.
    - Scanner-only denial state renders and export buttons are unavailable for scanner-only users.
    - No raw PII rows are previewed in the browser.
  </acceptance_criteria>
  <done>Settlement dashboard and export UI pass tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire settlement route, sidebar, and scanner permission UI</name>
  <files>apps/web/app/admin/settlement/page.tsx, apps/web/components/admin/admin-sidebar.tsx, apps/web/components/admin/admin-user-management.tsx, apps/web/components/admin/__tests__/admin-user-management.test.tsx</files>
  <read_first>
    apps/web/components/admin/admin-sidebar.tsx
    apps/web/components/admin/admin-user-management.tsx
    apps/web/components/admin/__tests__/admin-user-management.test.tsx
    apps/web/components/admin/settlement-dashboard.tsx
    packages/shared/src/schemas/admin-operations.schema.ts
  </read_first>
  <action>Create `/admin/settlement` route rendering `SettlementDashboard`. Add `정산·내보내기` under admin `운영` navigation after `현장 모니터`, for full admin/finance-capable users only. Update `admin-user-management.tsx` labels so full admins can select the `scanner` bundle and see field scan capability labels. Keep reason + explicit confirmation required for permission changes. Do not expose settlement nav or full admin sidebar to scanner-only users.</action>
  <verify>
    <automated>pnpm --filter @grabit/web exec vitest run components/admin/__tests__/settlement-dashboard.test.tsx components/admin/__tests__/admin-user-management.test.tsx &amp;&amp; pnpm --filter @grabit/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `/admin/settlement` page exists.
    - Sidebar contains `정산·내보내기` without removing existing admin links.
    - `admin-user-management` can display/select `scanner` bundle with explicit confirmation and reason.
    - Scanner bundle UI does not include settlement/export/refund/reservation/user/content/security/raw export capabilities.
  </acceptance_criteria>
  <done>Settlement route/navigation and scanner permission assignment UI are wired.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin settlement API -> browser export UI | Export UI can expose sensitive CSV data or bypass reason/confirmation. |
| admin user management -> scanner accounts | Mislabeling scanner capabilities can accidentally grant full admin powers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-15-QR-REPLAY | R | settlement entry/no-show UI | mitigate | Dashboard separates entered/no-show/duplicate data from backend summary. |
| T-27-15-AUTHZ | E | settlement route/sidebar | mitigate | Scanner-only denial state and no scanner settlement nav. |
| T-27-15-TOKEN-LEAK | I | dashboard rows | mitigate | No raw token/JTI/full URL/raw PII preview in browser. |
| T-27-15-OFFLINE-FALSE-PASS | R | settlement summary | mitigate | Pending offline counts remain distinct from synced entry evidence. |
| T-27-15-CSV-INJECTION | T/I | export UI | mitigate | Backend uses safe CSV; UI requires confirmation/reason and does not render raw CSV rows. |
| T-27-15-SCANNER-ESCALATION | E | admin-user-management | mitigate | Scanner bundle labels show only field scan capabilities and tests exclude sensitive admin capabilities. |
</threat_model>

<verification>
Run:
- `pnpm --filter @grabit/web exec vitest run components/admin/__tests__/settlement-dashboard.test.tsx components/admin/__tests__/admin-user-management.test.tsx`
- `pnpm --filter @grabit/web typecheck`
</verification>

<success_criteria>
Full admin/finance users can review settlement summaries and trigger guarded CSV exports; scanner-only users cannot access settlement/export, while full admins can assign scanner-only permissions safely.
</success_criteria>

<output>
After completion, create `.planning/phases/27-event-operations-settlement/27-15-SUMMARY.md`.
</output>
