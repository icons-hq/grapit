---
phase: 27-event-operations-settlement
plan: 08
type: execute
wave: 4
depends_on:
  - 27-07
files_modified:
  - apps/api/src/modules/field-operations/field-monitor.controller.ts
  - apps/api/src/modules/field-operations/field-monitor.service.ts
  - apps/api/src/modules/field-operations/field-monitor.service.spec.ts
  - apps/api/src/modules/field-operations/field-operations.module.ts
autonomous: true
requirements:
  - FIELD-01
requirements_addressed:
  - FIELD-01
must_haves:
  truths:
    - "D-23 field monitor is KPI-first and answers whether entry is proceeding normally."
    - "D-24 monitor exposes entered, not-entered, entry rate, duplicate scans, rejected scans, offline pending, offline synced, and latest abnormal alerts."
    - "D-25 logs are secondary drill-down/filter data."
    - "D-26 alerts cover duplicate spikes, rejected/tampered scans, refunded/cancelled scan attempts, offline backlog, and sync failures."
  artifacts:
    - path: "apps/api/src/modules/field-operations/field-monitor.service.ts"
      provides: "field monitor KPI aggregation"
    - path: "apps/api/src/modules/field-operations/field-monitor.controller.ts"
      provides: "monitor read API for admin/field UI"
  key_links:
    - from: "apps/api/src/modules/field-operations/field-monitor.service.ts"
      to: "apps/api/src/database/schema/ticket-scan-events.ts"
      via: "KPI and alert aggregation"
      pattern: "ticketScanEvents"
---

<objective>
Implement the event-day field monitor API.

Purpose: give operators a KPI-first real-time-enough view of entry health, duplicate/rejected scans, and offline backlog.
Output: monitor controller/service/module wiring and green field monitor tests.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-event-operations-settlement/27-CONTEXT.md
@.planning/phases/27-event-operations-settlement/27-RESEARCH.md
@.planning/phases/27-event-operations-settlement/27-UI-SPEC.md
@.planning/phases/27-event-operations-settlement/27-PATTERNS.md
@apps/api/src/modules/field-operations/field-monitor.service.spec.ts
@apps/api/src/database/schema/ticket-scan-events.ts
@apps/api/src/database/schema/reservations.ts
@apps/api/src/database/schema/tickets.ts
@apps/api/src/modules/admin/admin-dashboard.service.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement KPI-first monitor aggregation</name>
  <files>apps/api/src/modules/field-operations/field-monitor.service.ts, apps/api/src/modules/field-operations/field-monitor.service.spec.ts</files>
  <read_first>
    apps/api/src/modules/field-operations/field-monitor.service.spec.ts
    apps/api/src/database/schema/ticket-scan-events.ts
    apps/api/src/database/schema/reservations.ts
    apps/api/src/database/schema/tickets.ts
    packages/shared/src/schemas/field-operations.schema.ts
  </read_first>
  <action>Implement `FieldMonitorService.getSummary()` and any needed log/filter helper. Aggregate from `ticket_scan_events`, tickets, reservations, payments, and showtimes to return the D-24 KPI fields first. Use configurable default thresholds for duplicate spike, rejected/tampered attempts, refunded/cancelled attempts, offline pending backlog, and sync failures; document the defaults in code comments only where they clarify test expectations. Keep scan logs secondary and redacted.</action>
  <verify>
    <automated>pnpm --filter @grabit/api exec vitest run src/modules/field-operations/field-monitor.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Summary includes enteredCount, notEnteredCount, entryRate, duplicateScanCount, rejectedScanCount, offlinePendingCount, offlineSyncedCount, alerts, and lastUpdatedAt.
    - Alerts include all D-26 categories.
    - Logs/filter rows do not expose raw token/JTI or raw PII.
  </acceptance_criteria>
  <done>Monitor aggregation returns KPI-first redacted summary and alerts.</done>
</task>

<task type="auto">
  <name>Task 2: Wire monitor endpoint with admin capability protection</name>
  <files>apps/api/src/modules/field-operations/field-monitor.controller.ts, apps/api/src/modules/field-operations/field-operations.module.ts</files>
  <read_first>
    apps/api/src/modules/field-operations/field-operations.module.ts
    apps/api/src/modules/admin/admin-booking.controller.ts
    apps/api/src/common/guards/admin-capabilities.guard.ts
    packages/shared/src/schemas/admin-operations.schema.ts
  </read_first>
  <action>Create `FieldMonitorController` with GET endpoints for summary and secondary logs under the field operations/admin namespace. Protect summary/log endpoints with either full admin/finance/operator capability that already allows operations monitoring or an explicit field-monitor capability if Plan 27-01 added it; scanner-only may read only the showtime-scoped monitor data required by D-22 if product chooses to show pending/synced/rejected counts to field staff, but scanner-only must not access settlement or raw export surfaces. Register the controller/service in `FieldOperationsModule`.</action>
  <verify>
    <automated>pnpm --filter @grabit/api typecheck &amp;&amp; pnpm --filter @grabit/api exec vitest run src/modules/field-operations/field-monitor.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Monitor endpoints are authenticated and capability-guarded.
    - Summary endpoint returns a compact object before log rows.
    - Controller does not expose raw export or settlement data.
  </acceptance_criteria>
  <done>Field monitor API is routable, guarded, and green against tests.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scan events -> monitor API | Aggregates can leak sensitive scan/token/user data if raw rows are exposed. |
| monitor API -> scanner/admin clients | Scanner-only users must not gain settlement/raw export privilege through monitor data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-08-QR-REPLAY | R | duplicate KPI/alerts | mitigate | Duplicate scans are counted and alerted, not hidden inside success counts. |
| T-27-08-AUTHZ | E | FieldMonitorController | mitigate | Capability guard controls monitor access and excludes settlement/raw export data. |
| T-27-08-TAMPER-LEAK | I | alert/log payloads | mitigate | Rejected/tampered alerts use redacted references only. |
| T-27-08-OFFLINE-FALSE-PASS | R | offline KPIs | mitigate | Pending and synced counts are separate; pending backlog is not success. |
| T-27-08-PII-RAW-TOKEN-LEAK | I | log rows | mitigate | Logs are secondary and omit raw token/JTI/full user-agent/PII. |
| T-27-08-ALERT-BLINDSPOT | D/R | alert thresholds | mitigate | Alerts cover duplicate spike, rejected/tampered, refunded/cancelled, offline backlog, and sync failure categories. |
</threat_model>

<verification>
Run:
- `pnpm --filter @grabit/api exec vitest run src/modules/field-operations/field-monitor.service.spec.ts`
- `pnpm --filter @grabit/api typecheck`
</verification>

<success_criteria>
Operators can fetch a KPI-first field monitor summary with required D-24 counts and D-26 alerts, while raw logs remain secondary and redacted.
</success_criteria>

<output>
After completion, create `.planning/phases/27-event-operations-settlement/27-08-SUMMARY.md`.
</output>
