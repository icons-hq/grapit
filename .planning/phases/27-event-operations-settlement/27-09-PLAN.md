---
phase: 27-event-operations-settlement
plan: 09
type: execute
wave: 2
depends_on:
  - 27-01
  - 27-02
  - 27-05
files_modified:
  - apps/api/src/modules/admin/admin-settlement.controller.ts
  - apps/api/src/modules/admin/settlement-export.service.ts
  - apps/api/src/modules/admin/settlement-export.service.spec.ts
  - apps/api/src/modules/admin/admin.module.ts
autonomous: true
requirements:
  - POST-01
requirements_addressed:
  - POST-01
must_haves:
  truths:
    - "D-27 settlement is dashboard plus CSV export, not CSV-only."
    - "D-28 settlement summary shows event-level sales/payment/refund/entry/no-show data."
    - "D-29 exports include entry status, no-show reservation list, reservation/payment/refund summary, and settlement/accounting input data."
    - "D-30 external accounting/tax/PG formal integration stays out of scope."
    - "D-31 scanner-only users cannot access settlement/export."
  artifacts:
    - path: "apps/api/src/modules/admin/settlement-export.service.ts"
      provides: "settlement summary and CSV dataset generation"
    - path: "apps/api/src/modules/admin/admin-settlement.controller.ts"
      provides: "admin/finance settlement dashboard and export API"
  key_links:
    - from: "apps/api/src/modules/admin/settlement-export.service.ts"
      to: "apps/api/src/modules/admin/csv-export.util.ts"
      via: "safeCsvRows"
      pattern: "safeCsvRows"
---

<objective>
Implement the protected settlement summary and CSV export API.

Purpose: close POST-01 by giving full admin/finance users audited post-event dashboard data and safe CSV exports while denying scanner-only users.
Output: settlement controller/service/module wiring and green settlement export tests.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-event-operations-settlement/27-CONTEXT.md
@.planning/phases/27-event-operations-settlement/27-RESEARCH.md
@.planning/phases/27-event-operations-settlement/27-PATTERNS.md
@.planning/phases/27-event-operations-settlement/27-VALIDATION.md
@apps/api/src/modules/admin/admin-booking.controller.ts
@apps/api/src/modules/admin/admin-booking.service.ts
@apps/api/src/modules/admin/csv-export.util.ts
@apps/api/src/modules/admin/admin-audit.service.ts
@apps/api/src/modules/admin/admin.module.ts
@apps/api/src/modules/admin/settlement-export.service.spec.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement settlement aggregation and CSV datasets</name>
  <files>apps/api/src/modules/admin/settlement-export.service.ts, apps/api/src/modules/admin/settlement-export.service.spec.ts</files>
  <read_first>
    apps/api/src/modules/admin/settlement-export.service.spec.ts
    apps/api/src/modules/admin/admin-booking.service.ts
    apps/api/src/modules/admin/csv-export.util.ts
    apps/api/src/modules/admin/admin-audit.service.ts
    apps/api/src/database/schema/ticket-scan-events.ts
    packages/shared/src/schemas/field-operations.schema.ts
  </read_first>
  <action>Implement `SettlementExportService` with summary and export methods for datasets `entry_status`, `no_show_reservations`, `reservation_payment_refund_summary`, and `settlement_accounting_input`. Use `safeCsvRows` for every CSV, require export reason and filters, label files as `정산 입력 자료`, and audit `settlement.export` with actor, reason, filters, dataset, row count, and no raw CSV row retention. Derive data from reservations, payments, refunds if present, tickets, and `ticket_scan_events`.</action>
  <verify>
    <automated>pnpm --filter @grabit/api exec vitest run src/modules/admin/settlement-export.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - All four D-29 dataset identifiers are implemented.
    - Formula-leading CSV values are neutralized by `safeCsvRows`.
    - Audit metadata excludes raw exported row values.
    - Service does not implement external accounting/tax/PG formal mapping beyond internal input data.
  </acceptance_criteria>
  <done>Settlement summary/export service passes backend contract tests.</done>
</task>

<task type="auto">
  <name>Task 2: Wire settlement controller and AdminModule</name>
  <files>apps/api/src/modules/admin/admin-settlement.controller.ts, apps/api/src/modules/admin/admin.module.ts</files>
  <read_first>
    apps/api/src/modules/admin/admin-booking.controller.ts
    apps/api/src/modules/admin/admin.module.ts
    apps/api/src/common/guards/admin-capabilities.guard.ts
    packages/shared/src/schemas/admin-operations.schema.ts
  </read_first>
  <action>Create `AdminSettlementController` with summary and CSV export endpoints under `/api/v1/admin/settlement` or the local admin namespace. Protect endpoints with `AdminCapabilitiesGuard` and finance/full-admin capability such as `settlement.export` or `reservations.export_raw`; do not permit `scanner` bundle. For CSV responses, set `Content-Type: text/csv`, `Content-Disposition` with event/showtime/generated date but no buyer names, and `Cache-Control: no-store`. Register controller/service in `AdminModule`.</action>
  <verify>
    <automated>pnpm --filter @grabit/api typecheck &amp;&amp; pnpm --filter @grabit/api exec vitest run src/modules/admin/settlement-export.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Settlement endpoints are authenticated and capability-protected.
    - Scanner-only capability cannot satisfy settlement/export guard.
    - CSV responses use `no-store` and attachment filenames without buyer names.
  </acceptance_criteria>
  <done>Settlement API is routable, audited, and denied to scanner-only users.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| database rows -> CSV export | Reservation/payment/refund/entry data can contain PII and formula-leading values. |
| scanner account -> admin settlement API | Scanner-only users must not access finance/export surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-09-QR-REPLAY | R | settlement entry dataset | mitigate | Entry status/no-show exports derive from scan events and duplicate outcomes are not counted as entry success. |
| T-27-09-AUTHZ | E | AdminSettlementController | mitigate | Finance/full-admin capability required; scanner bundle denied. |
| T-27-09-TOKEN-LEAK | I | settlement rows | mitigate | Export does not include raw QR token/JTI/JWT/cookies/payment keys. |
| T-27-09-OFFLINE-FALSE-PASS | R | entry/no-show export | mitigate | Pending offline attempts remain separate from synced entry evidence. |
| T-27-09-PII-LEAK | I | export preview/audit | mitigate | Audit stores metadata only; browser preview is handled by UI Plan 27-15 without raw PII rows. |
| T-27-09-CSV-INJECTION | T/I | CSV generation | mitigate | Every dataset uses `safeCsvRows`; tests include formula-leading values. |
| T-27-09-SCANNER-ESCALATION | E | route guard | mitigate | Scanner-only users have no `settlement.export` or `reservations.export_raw` access. |
</threat_model>

<verification>
Run:
- `pnpm --filter @grabit/api exec vitest run src/modules/admin/settlement-export.service.spec.ts`
- `pnpm --filter @grabit/api typecheck`
</verification>

<success_criteria>
Full admin/finance users can fetch settlement summary and audited safe CSV exports; scanner-only users cannot access settlement/export.
</success_criteria>

<output>
After completion, create `.planning/phases/27-event-operations-settlement/27-09-SUMMARY.md`.
</output>
