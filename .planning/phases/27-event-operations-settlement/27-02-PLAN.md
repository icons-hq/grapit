---
phase: 27-event-operations-settlement
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/api/src/modules/field-operations/field-check-in.service.spec.ts
  - apps/api/src/modules/field-operations/offline-sync.service.spec.ts
  - apps/api/src/modules/field-operations/field-monitor.service.spec.ts
  - apps/api/src/modules/admin/settlement-export.service.spec.ts
autonomous: true
requirements:
  - QR-02
  - FIELD-01
  - POST-01
requirements_addressed:
  - QR-02
  - FIELD-01
  - POST-01
must_haves:
  truths:
    - "D-14 server tests define final authority for normal, duplicate, tampered, refunded/cancelled, expired, wrong-showtime, and already-used results."
    - "D-18/D-21 offline tests define pending-only local attempts and server conflict resolution."
    - "D-24/D-26 monitor tests define KPI-first counts and abnormal alerts."
    - "D-29/D-31 settlement tests define protected dashboard/export datasets and scanner denial."
  artifacts:
    - path: "apps/api/src/modules/field-operations/field-check-in.service.spec.ts"
      provides: "RED contract for verify/consume outcomes"
    - path: "apps/api/src/modules/field-operations/offline-sync.service.spec.ts"
      provides: "RED contract for offline pending sync resolution"
    - path: "apps/api/src/modules/field-operations/field-monitor.service.spec.ts"
      provides: "RED contract for event-day KPI aggregation"
    - path: "apps/api/src/modules/admin/settlement-export.service.spec.ts"
      provides: "RED contract for settlement CSV/export authorization"
  key_links:
    - from: "apps/api/src/modules/field-operations/field-check-in.service.spec.ts"
      to: "apps/api/src/modules/ticket/qr-ticket.service.ts"
      via: "verifyTicketForScannerContract and token verification mocks"
      pattern: "verifyTicketForScannerContract"
---

<objective>
Create Phase 27 backend RED tests before implementing field operations and settlement APIs.

Purpose: satisfy Wave 0 validation gaps so later backend plans have executable contracts instead of inferred behavior.
Output: API/service test files for check-in, offline sync, monitor, and settlement export.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-event-operations-settlement/27-CONTEXT.md
@.planning/phases/27-event-operations-settlement/27-VALIDATION.md
@.planning/phases/27-event-operations-settlement/27-PATTERNS.md
@apps/api/src/modules/ticket/qr-ticket.service.ts
@apps/api/src/modules/admin/admin-booking.service.spec.ts
@apps/api/src/modules/admin/csv-export.util.spec.ts
@apps/api/src/modules/admin/admin-audit.service.spec.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add check-in consume outcome tests</name>
  <files>apps/api/src/modules/field-operations/field-check-in.service.spec.ts</files>
  <read_first>
    .planning/phases/27-event-operations-settlement/27-CONTEXT.md
    .planning/phases/27-event-operations-settlement/27-VALIDATION.md
    .planning/phases/27-event-operations-settlement/27-PATTERNS.md
    apps/api/src/modules/ticket/qr-ticket.service.ts
    apps/api/src/modules/ticket/qr-ticket.service.spec.ts
  </read_first>
  <behavior>
    - Normal active ticket consumes once and records `entered`.
    - Second consume returns `duplicate` or `already_used` with prior scan/check-in timestamp and safe staff/device context per D-15.
    - Tampered token, refunded/cancelled, expired, wrong-showtime, and already-used inputs are rejected without sensitive lookup leakage per D-14/D-16/D-17.
    - Verify path never updates `tickets.usedAt` per D-12.
  </behavior>
  <action>Create the service spec with chainable Drizzle/mock patterns from `qr-ticket.service.spec.ts` and `admin-booking.service.spec.ts`. Tests should fail until `FieldCheckInService` exists. Assert no serialized result contains raw QR token, full raw JTI, bearer token, cookie, payment key, phone, or email.</action>
  <verify>
    <automated>test -f apps/api/src/modules/field-operations/field-check-in.service.spec.ts &amp;&amp; rg "FieldCheckInService|verifyTicketForScannerContract" apps/api/src/modules/field-operations/field-check-in.service.spec.ts &amp;&amp; rg "normal|duplicate|already_used|tampered|refunded|cancelled|expired|wrong-showtime|already-used" apps/api/src/modules/field-operations/field-check-in.service.spec.ts &amp;&amp; rg "usedAt|raw.*(token|JTI)|payment key|phone|email" apps/api/src/modules/field-operations/field-check-in.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Spec imports the future `FieldCheckInService` and asserts all D-14 outcome names.
    - Spec asserts opening/verify does not mutate `usedAt`.
    - Spec includes duplicate prior-context assertion with redacted values only.
    - Wave 0 verify passes by static contract inspection; the spec remains intentionally RED when Vitest runs before implementation.
  </acceptance_criteria>
  <done>Backend check-in RED contract is committed and statically verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add offline sync and monitor tests</name>
  <files>apps/api/src/modules/field-operations/offline-sync.service.spec.ts, apps/api/src/modules/field-operations/field-monitor.service.spec.ts</files>
  <read_first>
    .planning/phases/27-event-operations-settlement/27-CONTEXT.md
    .planning/phases/27-event-operations-settlement/27-VALIDATION.md
    .planning/phases/27-event-operations-settlement/27-PATTERNS.md
    apps/api/src/modules/admin/admin-booking.service.spec.ts
  </read_first>
  <behavior>
    - Offline pending attempts sync to server as `synced` or `rejected`, never final local success per D-18/D-21/D-22.
    - Sync re-verifies duplicate, tampered, refunded/cancelled, expired, and wrong-showtime attempts on the server.
    - Field monitor summary returns entered, not-entered, entry rate, duplicate scans, rejected scans, offline pending count, offline synced count, and latest abnormal alerts per D-24.
    - Alerts cover duplicate spikes, rejected/tampered attempts, refunded/cancelled attempts, offline backlog, and sync failures per D-26.
  </behavior>
  <action>Create RED specs for `OfflineSyncService` and `FieldMonitorService`. Use deterministic fixture rows for scan events, reservations, payments, tickets, and showtimes. Do not rely on WebSocket; tests should target 10-second polling-friendly aggregate DTOs from `field-operations.schema.ts` once Plan 27-01 lands.</action>
  <verify>
    <automated>test -f apps/api/src/modules/field-operations/offline-sync.service.spec.ts &amp;&amp; test -f apps/api/src/modules/field-operations/field-monitor.service.spec.ts &amp;&amp; rg "pending|synced|rejected|server.*re-?verif|duplicate|tampered|wrong-showtime" apps/api/src/modules/field-operations/offline-sync.service.spec.ts &amp;&amp; rg "entered|not-entered|entry rate|duplicate scans|rejected scans|offline pending|offline synced|abnormal alerts" apps/api/src/modules/field-operations/field-monitor.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Offline spec contains stale/recovered connectivity conflict cases.
    - Monitor spec contains KPI-first aggregate assertions before log row assertions.
    - Tests assert scan/monitor rows never include raw token/JTI or raw PII fields.
    - Wave 0 verify passes by static contract inspection; implementation plans run the actual Vitest green command.
  </acceptance_criteria>
  <done>Offline sync and field monitor backend RED contracts are committed and statically verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add settlement export API tests</name>
  <files>apps/api/src/modules/admin/settlement-export.service.spec.ts</files>
  <read_first>
    .planning/phases/27-event-operations-settlement/27-CONTEXT.md
    .planning/phases/27-event-operations-settlement/27-VALIDATION.md
    .planning/phases/27-event-operations-settlement/27-PATTERNS.md
    apps/api/src/modules/admin/admin-booking.service.spec.ts
    apps/api/src/modules/admin/csv-export.util.spec.ts
    apps/api/src/modules/admin/admin-audit.service.spec.ts
  </read_first>
  <behavior>
    - Export datasets include `entry_status`, `no_show_reservations`, `reservation_payment_refund_summary`, and `settlement_accounting_input` per D-29.
    - CSV generation uses `safeCsvRows` and neutralizes formula-leading values.
    - Export requires finance/full-admin capability and denies scanner-only users per D-31.
    - Audit metadata stores actor, reason, filters, dataset, and row count without raw CSV rows.
  </behavior>
  <action>Create RED tests for `SettlementExportService` using existing `safeCsvRows` and admin audit test patterns. Include a scanner-only denial test and a formula-injection row containing values beginning with `=`, `+`, `-`, `@`, tab, and carriage return.</action>
  <verify>
    <automated>test -f apps/api/src/modules/admin/settlement-export.service.spec.ts &amp;&amp; rg "entry_status|no_show_reservations|reservation_payment_refund_summary|settlement_accounting_input" apps/api/src/modules/admin/settlement-export.service.spec.ts &amp;&amp; rg "safeCsvRows|formula|scanner-only|settlement.export" apps/api/src/modules/admin/settlement-export.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Spec names all four required export dataset identifiers.
    - Spec fails if implementation builds CSV with manual join instead of `safeCsvRows`.
    - Spec asserts scanner-only denial for settlement export.
    - Wave 0 verify passes by static contract inspection; implementation plans run the actual Vitest green command.
  </acceptance_criteria>
  <done>Settlement export backend RED contract is committed and statically verified.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QR URL/token -> API service | Untrusted QR data enters verify, consume, and offline sync paths. |
| API aggregate/export -> operator | Monitor/export output may leak raw token, PII, or unsafe CSV content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02-QR-REPLAY | R/T | field-check-in tests | mitigate | Tests require duplicate/already-used result and prior context, not silent success. |
| T-27-02-AUTHZ | E | settlement tests | mitigate | Scanner-only denial tests protect settlement/export surfaces. |
| T-27-02-TAMPER-LEAK | I | field-check-in tests | mitigate | Tampered token tests assert operator-readable rejection without sensitive lookup details. |
| T-27-02-OFFLINE-FALSE-PASS | T/R | offline sync tests | mitigate | Tests require server re-verification and pending-only local semantics. |
| T-27-02-PII-RAW-TOKEN-LEAK | I | monitor/export tests | mitigate | Result serializers are asserted to omit raw token/JTI/payment/cookie/PII fields. |
| T-27-02-CSV-INJECTION | T/I | settlement CSV tests | mitigate | Formula-leading values must be neutralized through `safeCsvRows`. |
</threat_model>

<verification>
Overall Wave 0 verification is the three task-level static `<automated>` gates above. Do not run the backend Vitest command in this plan; it is expected to be RED until implementation plans 27-06, 27-07, 27-08, and 27-09 create the services and make the contracts green.
</verification>

<success_criteria>
All backend Wave 0 gaps from `27-VALIDATION.md` have executable RED tests before implementation plans depend on them.
</success_criteria>

<output>
After completion, create `.planning/phases/27-event-operations-settlement/27-02-SUMMARY.md`.
</output>
