---
phase: 27-event-operations-settlement
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/shared/src/schemas/field-operations.schema.ts
  - packages/shared/src/schemas/field-operations.schema.test.ts
  - packages/shared/src/schemas/admin-operations.schema.ts
  - packages/shared/src/schemas/admin-operations.schema.test.ts
  - packages/shared/src/types/admin-operations.types.ts
  - packages/shared/src/index.ts
autonomous: true
requirements:
  - QR-02
  - FIELD-01
  - POST-01
requirements_addressed:
  - QR-02
  - FIELD-01
  - POST-01
must_haves:
  truths:
    - "D-09/D-10 scanner-only is represented as lower-privilege admin capabilities, not full admin."
    - "D-14/D-18/D-22 field operation DTOs model server-authoritative verify, consume, offline sync, monitor, and settlement export outcomes."
    - "D-31 scanner-only capability does not include settlement, refund, reservation management, user management, content, security, or raw export access."
  artifacts:
    - path: "packages/shared/src/schemas/field-operations.schema.ts"
      provides: "Phase 27 zod request/response contracts"
    - path: "packages/shared/src/schemas/admin-operations.schema.ts"
      provides: "scanner capability bundle and excluded sensitive capabilities"
    - path: "packages/shared/src/index.ts"
      provides: "shared export wiring for executor imports"
  key_links:
    - from: "packages/shared/src/schemas/admin-operations.schema.ts"
      to: "packages/shared/src/types/admin-operations.types.ts"
      via: "ADMIN_CAPABILITY_BUNDLE_CAPABILITIES scanner bundle"
      pattern: "scanner.*field\\.scan\\.(verify|consume|sync)"
---

<objective>
Create the shared Phase 27 contracts that every backend and web plan consumes.

Purpose: lock server-authoritative field scan semantics, scanner-only RBAC vocabulary, and settlement export DTOs before implementation.
Output: `field-operations` schemas/tests, scanner capability bundle/tests, shared barrel exports.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/27-event-operations-settlement/27-CONTEXT.md
@.planning/phases/27-event-operations-settlement/27-RESEARCH.md
@.planning/phases/27-event-operations-settlement/27-VALIDATION.md
@.planning/phases/27-event-operations-settlement/27-PATTERNS.md
@packages/shared/src/schemas/admin-operations.schema.ts
@packages/shared/src/types/admin-operations.types.ts
@packages/shared/src/index.ts

<interfaces>
Existing admin capability identifiers live in `ADMIN_CAPABILITIES`, `ADMIN_CAPABILITY_BUNDLES`, `ADMIN_CAPABILITY_BUNDLE_CAPABILITIES`, `adminUserPermissionUpdateSchema`, and `resolveAdminCapabilitySnapshot()`. Extend those exact exports.
</interfaces>
</context>

<source_audit>
GOAL covered by plans 27-01 through 27-16. REQ coverage: QR-02 plans 27-01,02,03,05,06,07,10,11,12,13,16; FIELD-01 plans 27-01,02,03,05,08,13,14,16; OPS-03 plans 27-04,16; POST-01 plans 27-01,02,03,05,09,15,16; POST-02 plans 27-04,16. RESEARCH coverage: qrcode.react/idb in 27-10/13, Drizzle/PostgreSQL truth in 27-05/06/07/08/09, safeCsvRows in 27-09, UI-SPEC in 27-11/12/13/14/15. CONTEXT D-01..D-34 are covered across must_haves and task acceptance criteria; deferred scanner hardware/native app/accounting integration/admin retrospective UI are excluded.
</source_audit>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define field operation zod contracts</name>
  <files>packages/shared/src/schemas/field-operations.schema.ts, packages/shared/src/schemas/field-operations.schema.test.ts, packages/shared/src/index.ts</files>
  <read_first>
    .planning/phases/27-event-operations-settlement/27-CONTEXT.md
    .planning/phases/27-event-operations-settlement/27-VALIDATION.md
    .planning/phases/27-event-operations-settlement/27-PATTERNS.md
    packages/shared/src/schemas/booking.schema.ts
    packages/shared/src/schemas/admin-operations.schema.ts
    packages/shared/src/index.ts
  </read_first>
  <behavior>
    - Test `fieldCheckInVerifyRequestSchema` accepts an opaque `token` or QR URL plus optional `showtimeId`, but no raw PII.
    - Test `fieldCheckInConsumeRequestSchema` requires explicit manual action fields: `token`, `showtimeId`, `deviceAttemptId`, and `confirmed: true` per D-12/D-13.
    - Test offline sync schemas model `pending`, `synced`, and `rejected`, and include `deviceAttemptId`, `scannerUserId`, `showtimeId`, `attemptedAt`, and redacted token reference per D-20/D-22.
    - Test monitor and settlement DTOs include D-24 KPIs and D-29 export dataset names.
  </behavior>
  <action>Create `packages/shared/src/schemas/field-operations.schema.ts` with exported zod schemas and inferred types for verify, consume, offline sync, field monitor summary/log filters, settlement summary, and settlement export request/response. Use exact outcome strings for `processable`, `entered`, `duplicate`, `tampered`, `refunded_cancelled`, `expired`, `wrong_showtime`, `already_used`, `offline_pending`, `synced`, and `rejected`. Export the new schema file from `packages/shared/src/index.ts`. Do not include buyer email, phone, full raw token/JTI, payment key, cookies, or unmasked IP in any schema-visible monitor/export row.</action>
  <verify>
    <automated>pnpm --dir packages/shared exec vitest run schemas/field-operations.schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/shared/src/schemas/field-operations.schema.ts` exports verify, consume, offline sync, monitor, and settlement schemas.
    - The tests assert D-12/D-13 separation: verify has no state-changing confirmation field, consume requires `confirmed: true`.
    - The tests assert D-20/D-22 offline states and no schema field named `email`, `phone`, `rawToken`, `rawJti`, `paymentKey`, or `cookie`.
    - `packages/shared/src/index.ts` exports `./schemas/field-operations.schema`.
  </acceptance_criteria>
  <done>Shared field operation contracts compile and their contract tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add scanner-only capability bundle</name>
  <files>packages/shared/src/schemas/admin-operations.schema.ts, packages/shared/src/schemas/admin-operations.schema.test.ts, packages/shared/src/types/admin-operations.types.ts</files>
  <read_first>
    .planning/phases/27-event-operations-settlement/27-CONTEXT.md
    .planning/phases/25-admin-operations-console/25-03-SUMMARY.md
    packages/shared/src/schemas/admin-operations.schema.ts
    packages/shared/src/types/admin-operations.types.ts
  </read_first>
  <behavior>
    - Test `scanner` is present in `ADMIN_CAPABILITY_BUNDLES`.
    - Test scanner bundle contains only `field.scan.verify`, `field.scan.consume`, and `field.scan.sync`.
    - Test scanner bundle excludes `reservations.export_raw`, `security.manage`, refund, reservation management, user management, content, settlement, and raw export capabilities per D-10/D-31.
    - Test `resolveAdminCapabilitySnapshot()` returns `superuser: false` for scanner users.
  </behavior>
  <action>Extend the existing admin capability contract with `field.scan.verify`, `field.scan.consume`, `field.scan.sync`, and, if needed by settlement plans, `settlement.export`. Add the `scanner` bundle with only scan capabilities. Keep full `admin` superuser behavior unchanged for production compatibility, and keep finance/full-admin settlement access separate from scanner-only users per D-09/D-10/D-31.</action>
  <verify>
    <automated>pnpm --dir packages/shared exec vitest run schemas/admin-operations.schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ADMIN_CAPABILITY_BUNDLE_CAPABILITIES.scanner` contains exactly scanner scan capabilities.
    - No scanner test snapshot includes `reservations.export_raw`, `security.manage`, `settlement.export`, `refund`, `user`, or `content` capability names.
    - `resolveAdminCapabilitySnapshot()` treats scanner as non-superuser.
  </acceptance_criteria>
  <done>Scanner-only capability vocabulary exists and is tested as lower privilege than full admin.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared contracts -> API/web | Invalid DTOs can normalize unsafe scanner, offline, monitor, or export behavior. |
| admin capability resolver -> route guards | Scanner-only users must not inherit finance/admin capabilities. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01-QR-REPLAY | R/E | field outcome schemas | mitigate | Model duplicate/already-used outcomes explicitly so later consume APIs cannot silently treat replay as success. |
| T-27-01-AUTHZ | E | scanner capability bundle | mitigate | `scanner` bundle contains only `field.scan.verify`, `field.scan.consume`, and `field.scan.sync`; no settlement/refund/user/security/raw export capabilities. |
| T-27-01-TOKEN-LEAK | I | shared DTO fields | mitigate | Contract tests forbid visible/raw token, raw JTI, payment key, cookie, phone, and email fields in monitor/export rows. |
| T-27-01-OFFLINE-TAMPER | T/R | offline sync schemas | mitigate | Offline DTO requires pending/synced/rejected states and device attempt id; final server resolution is represented as a response outcome. |
| T-27-01-CSV-INJECTION | T/I | settlement export request | mitigate | Export dataset enum is only a contract here; Plan 27-09 must use `safeCsvRows` for generated CSVs. |
| T-27-01-PRIVILEGE-ESCALATION | E | admin capability resolver | mitigate | Scanner bundle test asserts non-superuser and excludes every sensitive admin capability named by the phase security requirement. |
</threat_model>

<verification>
Run:
- `pnpm --dir packages/shared exec vitest run schemas/field-operations.schema.test.ts schemas/admin-operations.schema.test.ts`
- `pnpm --filter @grabit/shared typecheck`
</verification>

<success_criteria>
Scanner-only RBAC and Phase 27 DTO contracts are explicit, tested, exported, and reference D-09/D-10/D-12/D-13/D-20/D-22/D-24/D-29/D-31 without implementing any deferred hardware/native/accounting/admin-retrospective scope.
</success_criteria>

<output>
After completion, create `.planning/phases/27-event-operations-settlement/27-01-SUMMARY.md`.
</output>
