---
phase: 27-event-operations-settlement
plan: 12
type: execute
wave: 4
depends_on:
  - 27-03
  - 27-06
  - 27-10
  - 27-11
files_modified:
  - apps/web/app/field/check-in/page.tsx
  - apps/web/components/field/scanner-check-in.tsx
  - apps/web/hooks/use-field-operations.ts
  - apps/web/components/field/__tests__/scanner-check-in.test.tsx
  - apps/web/e2e/phase27-qr-check-in.spec.ts
autonomous: true
requirements:
  - QR-02
requirements_addressed:
  - QR-02
must_haves:
  truths:
    - "D-06/D-07 phone camera opens the Grabit check-in web page and logged-out users return after login."
    - "D-08 normal member accounts see access denied for check-in actions."
    - "D-11 scanner-only UI uses a minimal mobile-first shell, not full admin sidebar."
    - "D-12 opening the route verifies only; D-13 `입장 처리` is manual."
  artifacts:
    - path: "apps/web/app/field/check-in/page.tsx"
      provides: "protected scanner-only route"
    - path: "apps/web/components/field/scanner-check-in.tsx"
      provides: "verify/result/manual consume UI"
    - path: "apps/web/hooks/use-field-operations.ts"
      provides: "verify/consume API hooks"
  key_links:
    - from: "apps/web/components/field/scanner-check-in.tsx"
      to: "/api/v1/field/check-in/consume"
      via: "use-field-operations consume mutation"
      pattern: "field/check-in/consume"
---

<objective>
Build the scanner-only check-in route and online verify/consume UI.

Purpose: complete the phone-camera URL -> scanner login/access -> manual `입장 처리` path without exposing full admin.
Output: field check-in page, scanner component, hooks, component tests, and QR check-in E2E.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-event-operations-settlement/27-CONTEXT.md
@.planning/phases/27-event-operations-settlement/27-UI-SPEC.md
@.planning/phases/27-event-operations-settlement/27-PATTERNS.md
@apps/web/components/field/__tests__/scanner-check-in.test.tsx
@apps/web/e2e/phase27-qr-check-in.spec.ts
@apps/web/app/admin/layout.tsx
@apps/web/hooks/use-reservations.ts
@apps/web/components/admin/admin-sidebar.tsx
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement scanner hooks and component</name>
  <files>apps/web/hooks/use-field-operations.ts, apps/web/components/field/scanner-check-in.tsx, apps/web/components/field/__tests__/scanner-check-in.test.tsx</files>
  <read_first>
    .planning/phases/27-event-operations-settlement/27-UI-SPEC.md
    apps/web/components/field/__tests__/scanner-check-in.test.tsx
    apps/web/hooks/use-reservations.ts
    packages/shared/src/schemas/field-operations.schema.ts
  </read_first>
  <action>Create `useFieldCheckInVerify()` and `useFieldCheckInConsume()` hooks using existing `apiClient`/`apiUrl` patterns. Implement `ScannerCheckIn` to show the server result band first, ticket identity second, and sticky/full-width mobile `입장 처리` only when the server result is processable. Disable/relabel action after success. Render exact UI-SPEC copy for valid, processed, duplicate, tampered, refunded/cancelled, wrong-showtime, and access-denied states. Do not render raw token/JTI/full URL text.</action>
  <verify>
    <automated>pnpm --filter @grabit/web exec vitest run components/field/__tests__/scanner-check-in.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Component test passes for verify, manual consume, duplicate, rejection, and access denied states.
    - `입장 처리` appears only for server-verified processable tickets.
    - Opening/verify state never displays completed entry copy before consume mutation succeeds.
    - No raw token/JTI/full URL text appears.
  </acceptance_criteria>
  <done>Scanner component and hooks pass online check-in tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement minimal scanner route and QR check-in E2E</name>
  <files>apps/web/app/field/check-in/page.tsx, apps/web/e2e/phase27-qr-check-in.spec.ts</files>
  <read_first>
    apps/web/app/admin/layout.tsx
    apps/web/components/admin/admin-sidebar.tsx
    apps/web/e2e/phase27-qr-check-in.spec.ts
    apps/web/components/field/scanner-check-in.tsx
    apps/web/stores/auth-store.ts
  </read_first>
  <action>Create `/field/check-in` as a minimal mobile-first scanner shell outside the full admin sidebar. It reads the QR token/check-in URL parameter, routes logged-out visitors to login with return target, denies normal members with `이 티켓을 검표할 권한이 없습니다`, and allows scanner/full-admin users to verify and manually consume. Update `phase27-qr-check-in.spec.ts` to cover buyer QR deep-link route, login-return behavior if existing auth mocks support it, scanner-only entry, no consume-on-open, and duplicate rejection.</action>
  <verify>
    <automated>CI=1 TZ=UTC pnpm --filter @grabit/web test:e2e -- phase27-qr-check-in.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Scanner route does not render full admin sidebar labels.
    - Normal member route shows access denied and cannot call consume.
    - E2E proves opening the route alone does not show `입장 처리가 완료되었습니다`.
    - E2E proves second consume/scan returns duplicate/already-used copy.
  </acceptance_criteria>
  <done>Scanner-only route completes online check-in browser flow.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public QR URL -> web route | The URL may be opened by normal users or logged-out users. |
| scanner UI -> consume API | UI must not expose consume unless server says processable and user has scanner capability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-12-QR-REPLAY | R | scanner UI/E2E | mitigate | E2E covers duplicate/already-used after first consume. |
| T-27-12-NORMAL-USER-ACTION | E | `/field/check-in` | mitigate | Normal members get access denied; route is outside full admin shell. |
| T-27-12-TAMPER-LEAK | I | scanner result UI | mitigate | Tampered copy is generic and no raw lookup details are rendered. |
| T-27-12-OFFLINE-FALSE-PASS | R | scanner UI | transfer | Offline pending UI is implemented in Plan 27-13; online scanner does not claim offline success. |
| T-27-12-PII-RAW-TOKEN-LEAK | I | scanner component | mitigate | Tests assert raw token/JTI/full URL/PII absence. |
| T-27-12-PRIVILEGE-ESCALATION | E | scanner shell | mitigate | Scanner route uses minimal shell and does not expose refund/reservation/user/content/security/settlement/raw export links. |
</threat_model>

<verification>
Run:
- `pnpm --filter @grabit/web exec vitest run components/field/__tests__/scanner-check-in.test.tsx`
- `CI=1 TZ=UTC pnpm --filter @grabit/web test:e2e -- phase27-qr-check-in.spec.ts`
- `pnpm --filter @grabit/web typecheck`
</verification>

<success_criteria>
Phone-camera QR URLs land on a protected scanner-only web route, normal users are denied, and scanner staff must press `입장 처리` for server-authoritative entry.
</success_criteria>

<output>
After completion, create `.planning/phases/27-event-operations-settlement/27-12-SUMMARY.md`.
</output>
