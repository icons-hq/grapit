---
phase: 27-event-operations-settlement
plan: 11
type: execute
wave: 2
depends_on:
  - 27-03
  - 27-10
files_modified:
  - apps/web/components/booking/booking-complete.tsx
  - apps/web/components/reservation/reservation-detail.tsx
  - apps/web/e2e/booking-complete-qr.spec.ts
  - apps/web/e2e/phase26-qr-visibility.spec.ts
autonomous: true
requirements:
  - QR-02
requirements_addressed:
  - QR-02
must_haves:
  truths:
    - "D-01 payment complete and My Page show a real scannable QR code image."
    - "D-02 buyer QR metadata is limited to reservation number, performance title, showtime, seats, and ticket status."
    - "D-03 QR payload is an HTTPS Grabit check-in URL."
    - "D-04 invalid buyer QR states are visible honestly while scanner remains source of truth."
    - "D-05 raw QR token/JTI are not rendered as visible text outside the QR image/URL."
  artifacts:
    - path: "apps/web/components/booking/booking-complete.tsx"
      provides: "payment complete QR card integration"
    - path: "apps/web/components/reservation/reservation-detail.tsx"
      provides: "My Page QR card integration"
  key_links:
    - from: "apps/web/components/booking/booking-complete.tsx"
      to: "apps/web/components/field/qr-ticket-image.tsx"
      via: "QrTicketImage import"
      pattern: "QrTicketImage"
---

<objective>
Replace metadata-only buyer QR cards with real QR images on payment complete and My Page.

Purpose: make the buyer side of the real-world scan path testable before scanner UI is used at the venue.
Output: updated buyer QR cards and strengthened QR visibility E2E tests.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-event-operations-settlement/27-CONTEXT.md
@.planning/phases/27-event-operations-settlement/27-UI-SPEC.md
@.planning/phases/27-event-operations-settlement/27-PATTERNS.md
@apps/web/components/field/qr-ticket-image.tsx
@apps/web/components/booking/booking-complete.tsx
@apps/web/components/reservation/reservation-detail.tsx
@apps/web/e2e/booking-complete-qr.spec.ts
@apps/web/e2e/phase26-qr-visibility.spec.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Render real QR images on buyer surfaces</name>
  <files>apps/web/components/booking/booking-complete.tsx, apps/web/components/reservation/reservation-detail.tsx</files>
  <read_first>
    .planning/phases/27-event-operations-settlement/27-CONTEXT.md
    .planning/phases/27-event-operations-settlement/27-UI-SPEC.md
    apps/web/components/field/qr-ticket-image.tsx
    apps/web/components/booking/booking-complete.tsx
    apps/web/components/reservation/reservation-detail.tsx
    packages/shared/src/schemas/booking.schema.ts
  </read_first>
  <action>Build the HTTPS Grabit check-in URL from existing `qrTicket.token` and the current public web origin. Render `QrTicketImage` in both payment complete and My Page reservation detail cards. Keep stable square QR area, Card/Badge/Button language, minimal D-02 metadata, and copy `현장 검표 결과가 최종 입장 기준입니다.` Remove visible masked JTI/Ticket ID metadata from these buyer cards. Keep invalid/non-active states visible with honest status and retry/My Page guidance without printing token/JTI.</action>
  <verify>
    <automated>pnpm --filter @grabit/web exec vitest run components/booking/__tests__ components/reservation --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - Payment complete and My Page render a QR image when `qrTicket.status === 'ACTIVE'`.
    - Visible metadata is limited to reservation number, performance title, showtime, seats, and ticket status.
    - Raw token, raw JTI, JWT payload, and full URL are not visible text.
    - Buyer copy does not imply QR possession guarantees entry.
  </acceptance_criteria>
  <done>Buyer QR cards use real scannable QR images and preserve no-raw-token UI rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Strengthen buyer QR browser evidence</name>
  <files>apps/web/e2e/booking-complete-qr.spec.ts, apps/web/e2e/phase26-qr-visibility.spec.ts</files>
  <read_first>
    apps/web/e2e/booking-complete-qr.spec.ts
    apps/web/e2e/phase26-qr-visibility.spec.ts
    apps/web/components/booking/booking-complete.tsx
    apps/web/components/reservation/reservation-detail.tsx
  </read_first>
  <action>Update existing QR E2E coverage so Phase 26 metadata visibility still passes but Phase 27 additionally requires a QR SVG/canvas/image and an HTTPS check-in URL encoded in component props/attributes where testable. Keep negative raw-secret assertions for token, JTI, JWT payload, payment key, cookie, and full URL visible text. Do not turn real phone-camera scanning into automated PASS evidence; Plan 27-16 owns manual device proof.</action>
  <verify>
    <automated>CI=1 TZ=UTC pnpm --filter @grabit/web test:e2e -- booking-complete-qr.spec.ts phase26-qr-visibility.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - E2E fails if QR card regresses to metadata-only.
    - E2E fails if raw token/JTI/full URL becomes visible text.
    - Existing Phase 26 QR visibility assertions remain compatible with Phase 27 real QR rendering.
  </acceptance_criteria>
  <done>Buyer QR browser evidence proves real QR rendering without leaking raw ticket secrets.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reservation detail API -> buyer QR DOM | Signed QR token enters client rendering and could leak as text. |
| buyer QR -> phone camera route | Possessing/opening the URL must not consume the ticket. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-11-QR-REPLAY | R | buyer QR | transfer | Replay/duplicate protection is server-side in Plan 27-06; buyer UI states source-of-truth copy. |
| T-27-11-AUTHZ | E | buyer QR deep link | transfer | Check-in route/API denies normal members in Plan 27-12/06. |
| T-27-11-TOKEN-LEAK | I | BookingComplete/ReservationDetail | mitigate | Remove visible JTI metadata and assert raw token/JTI/full URL are absent. |
| T-27-11-OFFLINE-FALSE-PASS | R | buyer copy | mitigate | Copy says scanner result is final, not QR possession. |
| T-27-11-PII-LEAK | I | buyer metadata | mitigate | Metadata limited to D-02 fields; no raw payment/user secrets. |
| T-27-11-SCOPE | D | buyer cards | mitigate | No scanner hardware/native app UI added. |
</threat_model>

<verification>
Run:
- `pnpm --filter @grabit/web exec vitest run components/booking/__tests__ components/reservation --passWithNoTests`
- `CI=1 TZ=UTC pnpm --filter @grabit/web test:e2e -- booking-complete-qr.spec.ts phase26-qr-visibility.spec.ts`
- `pnpm --filter @grabit/web typecheck`
</verification>

<success_criteria>
Buyer payment complete and My Page reservation detail expose a real scannable QR image encoding an HTTPS check-in URL, while raw token/JTI/full URL text remains absent.
</success_criteria>

<output>
After completion, create `.planning/phases/27-event-operations-settlement/27-11-SUMMARY.md`.
</output>
