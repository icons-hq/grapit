---
phase: 27-event-operations-settlement
plan: 13
type: execute
wave: 5
depends_on:
  - 27-07
  - 27-12
files_modified:
  - apps/web/lib/field/offline-scan-store.ts
  - apps/web/components/field/offline-sync-status.tsx
  - apps/web/components/field/scanner-check-in.tsx
  - apps/web/hooks/use-field-operations.ts
  - apps/web/components/field/__tests__/scanner-check-in.test.tsx
  - apps/web/e2e/phase27-offline-sync.spec.ts
autonomous: true
requirements:
  - QR-02
  - FIELD-01
requirements_addressed:
  - QR-02
  - FIELD-01
must_haves:
  truths:
    - "D-18 online-first behavior remains default."
    - "D-19 network failure after authenticated scanner session stores a local pending attempt."
    - "D-20 local pending data contains scanner context, showtime scope, QR reference, attempt timestamp, device-local attempt id, and state without raw PII."
    - "D-21 recovered connectivity syncs to server for final synced/rejected state."
    - "D-22 scanner UI exposes pending, synced, and rejected results."
  artifacts:
    - path: "apps/web/lib/field/offline-scan-store.ts"
      provides: "IndexedDB pending scan attempt store"
    - path: "apps/web/components/field/offline-sync-status.tsx"
      provides: "offline pending/synced/rejected UI"
  key_links:
    - from: "apps/web/lib/field/offline-scan-store.ts"
      to: "/api/v1/field/offline-sync"
      via: "sync mutation in use-field-operations"
      pattern: "offline.*sync"
---

<objective>
Add offline pending scan storage, sync UI, and browser verification.

Purpose: rehearse stale/recovered connectivity without treating local offline state as final admission evidence.
Output: idb-backed store, offline sync status component, scanner integration, and offline E2E.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-event-operations-settlement/27-CONTEXT.md
@.planning/phases/27-event-operations-settlement/27-RESEARCH.md
@.planning/phases/27-event-operations-settlement/27-UI-SPEC.md
@.planning/phases/27-event-operations-settlement/27-PATTERNS.md
@apps/web/components/field/scanner-check-in.tsx
@apps/web/components/field/__tests__/scanner-check-in.test.tsx
@apps/web/e2e/phase27-offline-sync.spec.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement IndexedDB pending scan store</name>
  <files>apps/web/lib/field/offline-scan-store.ts, apps/web/components/field/__tests__/scanner-check-in.test.tsx</files>
  <read_first>
    .planning/phases/27-event-operations-settlement/27-RESEARCH.md
    .planning/phases/27-event-operations-settlement/27-UI-SPEC.md
    apps/web/components/field/__tests__/scanner-check-in.test.tsx
    packages/shared/src/schemas/field-operations.schema.ts
  </read_first>
  <action>Create an `idb` store named `grabit-field-scans` with object store `pendingScanAttempts`, key path `deviceAttemptId`, and indexes for `showtimeId` and `syncState`. Persist scanner account id, event/showtime scope, redacted QR reference/token reference, attemptedAt, deviceAttemptId, and syncState. Do not store buyer email, phone, raw token, raw JTI, full URL text, payment key, cookie, or unmasked IP. Await transaction completion before UI reports a pending scan as stored.</action>
  <verify>
    <automated>pnpm --filter @grabit/web exec vitest run components/field/__tests__/scanner-check-in.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Store functions can add, list, update, and remove pending scan attempts.
    - Tests assert pending attempt records avoid raw PII and raw token/JTI fields.
    - Pending state is amber/warning in UI expectations, never green.
  </acceptance_criteria>
  <done>Offline pending store persists safe scanner attempt metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire offline sync UI and E2E</name>
  <files>apps/web/components/field/offline-sync-status.tsx, apps/web/components/field/scanner-check-in.tsx, apps/web/hooks/use-field-operations.ts, apps/web/e2e/phase27-offline-sync.spec.ts</files>
  <read_first>
    apps/web/lib/field/offline-scan-store.ts
    apps/web/components/field/scanner-check-in.tsx
    apps/web/hooks/use-field-operations.ts
    apps/web/e2e/phase27-offline-sync.spec.ts
    .planning/phases/27-event-operations-settlement/27-UI-SPEC.md
  </read_first>
  <action>Add `OfflineSyncStatus` and scanner integration. When online consume fails from network failure after an authenticated scanner session is available, store a pending attempt and show exact copy `네트워크 문제로 보류 스캔에 저장했습니다. 연결이 복구되면 서버와 동기화하세요.` and `보류 상태는 최종 입장 증거가 아닙니다`. Add `보류 스캔 동기화` action that calls the server sync endpoint and updates attempts to synced/rejected. Update E2E to simulate network failure, recovered connectivity, server synced result, and rejected conflict result.</action>
  <verify>
    <automated>pnpm --filter @grabit/web exec vitest run components/field/__tests__/scanner-check-in.test.tsx &amp;&amp; CI=1 TZ=UTC pnpm --filter @grabit/web test:e2e -- phase27-offline-sync.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Scanner UI shows pending banner before current ticket details when pending attempts exist.
    - Sync results show pending/synced/rejected counts or rows.
    - E2E proves local pending is not final and server rejected conflict stays visible.
    - Raw token/JTI/full URL/PII are absent from offline UI.
  </acceptance_criteria>
  <done>Offline fallback UI and E2E prove pending/recovered/rejected behavior.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network failure -> local IndexedDB | Local storage can be inspected or tampered by the browser user. |
| local pending queue -> server sync | Client-side pending attempts must not be accepted as final evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-13-QR-REPLAY | R | offline sync E2E | mitigate | E2E covers rejected duplicate/conflict after reconnect. |
| T-27-13-AUTHZ | E | sync mutation | mitigate | Sync call uses scanner-only endpoint from Plan 27-07. |
| T-27-13-TAMPER-LEAK | I | offline UI | mitigate | UI renders redacted references and no sensitive lookup details. |
| T-27-13-OFFLINE-TAMPER | T/R | IndexedDB store | mitigate | Store pending metadata only; server sync finalizes every attempt. |
| T-27-13-PII-RAW-TOKEN-LEAK | I | IndexedDB + UI | mitigate | Store acceptance forbids raw token/JTI/PII/payment/cookie fields. |
| T-27-13-FALSE-ACCEPTANCE | R | scanner copy | mitigate | Copy explicitly says pending is not final admission evidence. |
</threat_model>

<verification>
Run:
- `pnpm --filter @grabit/web exec vitest run components/field/__tests__/scanner-check-in.test.tsx`
- `CI=1 TZ=UTC pnpm --filter @grabit/web test:e2e -- phase27-offline-sync.spec.ts`
- `pnpm --filter @grabit/web typecheck`
</verification>

<success_criteria>
Scanner UI stores offline attempts only as pending, syncs them after connectivity recovery, and shows synced/rejected server outcomes without leaking raw QR/PII data.
</success_criteria>

<output>
After completion, create `.planning/phases/27-event-operations-settlement/27-13-SUMMARY.md`.
</output>
