---
phase: 20-valkey-production-connectivity-contract
plan: 03
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - apps/api/test/booking-cluster-lua.integration.spec.ts
autonomous: true
requirements:
  - VALK-03
  - SC-2
  - SC-3
must_haves:
  truths:
    - "Booking lock/status/unlock Lua paths run under Valkey Cluster without CROSSSLOT."
    - "All booking multi-key Lua keys use explicit KEYS and shared {showtimeId} hash tags."
    - "Phase 19 lock ownership semantics remain unchanged."
  artifacts:
    - path: "apps/api/test/booking-cluster-lua.integration.spec.ts"
      provides: "Booking Lua cluster-mode integration guard"
      contains: "ADDSLOTSRANGE"
  key_links:
    - from: "apps/api/test/booking-cluster-lua.integration.spec.ts"
      to: "apps/api/src/modules/booking/booking.service.ts"
      via: "BookingService lockSeat/getSeatStatus/unlockSeat and ownership helpers"
      pattern: "new BookingService"
    - from: "apps/api/test/booking-cluster-lua.integration.spec.ts"
      to: "Valkey Cluster"
      via: "ioredis Cluster and CLUSTER KEYSLOT checks"
      pattern: "CLUSTER.*KEYSLOT"
---

<objective>
Add a booking-specific Valkey Cluster guard for the Lua paths users depend on.

Purpose: Phase 14 proved SMS OTP hash tags under cluster mode; Phase 20 must prove the booking seat lock Lua path against the same class of production risk without changing Phase 19 ownership semantics.
Output: A Docker/testcontainers integration spec that runs booking lock, status, unlock, and ownership helper paths against a single-shard `valkey/valkey:8` cluster.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-valkey-production-connectivity-contract/20-CONTEXT.md
@.planning/phases/20-valkey-production-connectivity-contract/20-RESEARCH.md
@.planning/phases/20-valkey-production-connectivity-contract/20-VALIDATION.md
@.planning/phases/20-valkey-production-connectivity-contract/20-PATTERNS.md
@.planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-03-SUMMARY.md
@.planning/phases/19-seat-lock-ownership-enforcement/19-02-SUMMARY.md
@.planning/phases/19-seat-lock-ownership-enforcement/19-03-SUMMARY.md
@apps/api/test/sms-cluster-crossslot.integration.spec.ts
@apps/api/src/modules/booking/__tests__/booking.service.integration.spec.ts
@apps/api/src/modules/booking/booking.service.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create booking cluster Lua integration spec</name>
  <files>apps/api/test/booking-cluster-lua.integration.spec.ts</files>
  <read_first>
    - apps/api/test/sms-cluster-crossslot.integration.spec.ts
    - apps/api/src/modules/booking/__tests__/booking.service.integration.spec.ts
    - apps/api/src/modules/booking/booking.service.ts
    - apps/api/src/modules/booking/providers/redis.provider.ts
    - .planning/phases/20-valkey-production-connectivity-contract/20-CONTEXT.md
    - .planning/phases/20-valkey-production-connectivity-contract/20-RESEARCH.md
    - .planning/phases/20-valkey-production-connectivity-contract/20-PATTERNS.md
    - .planning/phases/14-sms-otp-crossslot-fix-sms-valkey-cluster-hash-tag/14-03-SUMMARY.md
    - .planning/phases/19-seat-lock-ownership-enforcement/19-02-SUMMARY.md
  </read_first>
  <behavior>
    - Test: legacy keys without a shared `{showtimeId}` hash tag reject with `/CROSSSLOT/`.
    - Test: `BookingService.lockSeat(userId, showtimeId, seatId)` succeeds when injected with `IORedis.Cluster`.
    - Test: `BookingService.getSeatStatus(showtimeId)` returns the locked seat under cluster mode.
    - Test: `BookingService.unlockSeat(userId, showtimeId, seatId)` removes the lock under cluster mode.
    - Test: `assertOwnedSeatLocks()` and `consumeOwnedSeatLocks()` keep Phase 19 owner/missing/other-owner behavior when backed by cluster mode.
    - Test: `CLUSTER KEYSLOT` proves `{showtimeId}:user-seats:{userId}`, `{showtimeId}:seat:{seatId}`, and `{showtimeId}:locked-seats` share one slot.
  </behavior>
  <action>
    Create `apps/api/test/booking-cluster-lua.integration.spec.ts` using the Phase 14 single-node cluster pattern: `GenericContainer('valkey/valkey:8')`, `--cluster-enabled yes`, runtime `CONFIG SET cluster-announce-ip`, runtime `CONFIG SET cluster-announce-port`, `CLUSTER ADDSLOTSRANGE 0 16383`, dynamic `buildNatMap()`, and `new IORedis.Cluster([{ host, port }], ...)`.

    Instantiate `BookingService` with the cluster client and the existing minimal mock DB/gateway pattern from `booking.service.integration.spec.ts`. Do not modify `booking.service.ts`, `reservation.service.ts`, lock ownership Lua scripts, or production code in this plan per D-20 and D-22. The spec is the artifact that proves D-04 and D-17 for booking paths.
  </action>
  <verify>
    <automated>pnpm --filter @grabit/api test:integration -- booking-cluster-lua</automated>
    <automated>rg -n "booking-cluster-lua|cluster-enabled|ADDSLOTSRANGE|CROSSSLOT|CLUSTER', 'KEYSLOT'|assertOwnedSeatLocks|consumeOwnedSeatLocks" apps/api/test/booking-cluster-lua.integration.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/test/booking-cluster-lua.integration.spec.ts` exists.
    - The file contains `GenericContainer('valkey/valkey:8')`.
    - The file contains `ADDSLOTSRANGE`.
    - The file contains `CROSSSLOT`.
    - The file contains `CLUSTER', 'KEYSLOT'`.
    - The file contains `assertOwnedSeatLocks` and `consumeOwnedSeatLocks`.
    - `pnpm --filter @grabit/api test:integration -- booking-cluster-lua` exits 0 on a machine with Docker/testcontainers available; if Docker is unavailable, record the exact container runtime error in `20-03-SUMMARY.md` and still run the grep gates.
  </acceptance_criteria>
  <done>Booking Lua cluster integration spec exists, includes same-slot and CROSSSLOT guards, and passes when Docker/testcontainers is available.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Testcontainers Valkey -> BookingService | Cluster-mode Valkey behavior is injected into the real booking service Lua paths. |
| Lua KEYS/ARGV -> Valkey Cluster | User-derived IDs become Redis keys and args; cluster slot safety depends on key construction. |
| Phase 19 ownership helpers -> Phase 20 tests | Existing production authorization semantics must be verified, not rewritten. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Information Disclosure | Integration spec logs | mitigate | Use synthetic UUIDs and user IDs only; do not log `REDIS_URL`, auth headers, cookies, JWTs, phone numbers, payment data, or private customer data. |
| T-20-02 | Tampering | Production Redis fallback | transfer | This plan is test-only; Plan 20-01 and Plan 20-02 mitigate production fallback. |
| T-20-03 | Denial of Service | Cluster mode mismatch | mitigate | The spec uses `IORedis.Cluster` and fails if booking Lua paths are not cluster-safe. |
| T-20-04 | Tampering | Booking multi-key Lua | mitigate | Add explicit CROSSSLOT negative guard, `CLUSTER KEYSLOT` same-slot proof, and lock/status/unlock/ownership helper cluster tests. |
| T-20-05 | Integrity | Production inventory mutation | mitigate | The test uses an ephemeral local Valkey container only; it never mutates production inventory. |
| T-20-06 | Denial of Service | Cloud Run scaling | transfer | This plan does not scale Cloud Run; Plan 20-04 owns temporary scale pre-state and restoration. |
</threat_model>

<verification>
Run all checks below before writing the plan summary:

```bash
pnpm --filter @grabit/api test:integration -- booking-cluster-lua
rg -n "GenericContainer\\('valkey/valkey:8'\\)|ADDSLOTSRANGE|CROSSSLOT|CLUSTER', 'KEYSLOT'|assertOwnedSeatLocks|consumeOwnedSeatLocks" apps/api/test/booking-cluster-lua.integration.spec.ts
git diff -- apps/api/src/modules/booking/booking.service.ts apps/api/src/modules/reservation/reservation.service.ts
```

The final `git diff` command must show no production service changes from this plan.
</verification>

<success_criteria>
- Booking Lua lock/status/unlock and Phase 19 ownership helpers pass under Valkey Cluster.
- The spec proves same-slot `{showtimeId}` hash tags and catches legacy no-hash-tag CROSSSLOT drift.
- No booking/reservation ownership production semantics are changed.
</success_criteria>

<output>
After completion, create `.planning/phases/20-valkey-production-connectivity-contract/20-03-SUMMARY.md`.
</output>
