---
phase: 20-valkey-production-connectivity-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/config/redis.config.ts
  - apps/api/src/modules/booking/providers/redis.provider.ts
  - apps/api/src/modules/booking/providers/__tests__/redis.provider.spec.ts
  - .github/workflows/deploy.yml
autonomous: true
requirements:
  - VALK-02
  - VALK-05
  - SC-3
must_haves:
  truths:
    - "Production declares VALKEY_MODE=cluster instead of relying on implicit standalone behavior."
    - "NODE_ENV=production with missing REDIS_URL or missing/invalid VALKEY_MODE fails closed."
    - "REDIS_CLIENT creates an ioredis Cluster client only when VALKEY_MODE=cluster."
  artifacts:
    - path: "apps/api/src/config/redis.config.ts"
      provides: "redis.url and redis.mode config namespace"
      contains: "VALKEY_MODE"
    - path: "apps/api/src/modules/booking/providers/redis.provider.ts"
      provides: "REDIS_CLIENT factory with standalone/cluster mode contract"
      contains: "Cluster"
    - path: ".github/workflows/deploy.yml"
      provides: "Cloud Run production VALKEY_MODE=cluster env contract"
      contains: "VALKEY_MODE=cluster"
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "apps/api/src/config/redis.config.ts"
      via: "VALKEY_MODE=cluster environment variable"
      pattern: "VALKEY_MODE=cluster"
    - from: "apps/api/src/config/redis.config.ts"
      to: "apps/api/src/modules/booking/providers/redis.provider.ts"
      via: "ConfigService redis.mode lookup"
      pattern: "redis\\.mode"
---

<objective>
Make the production Valkey mode contract explicit and fail-closed.

Purpose: Phase 20 must remove the implicit `new IORedis(url)` production assumption because live research shows `grabit-valkey` is `ACTIVE / CLUSTER / shardCount=1 / VALKEY_8_0`.
Output: `VALKEY_MODE=cluster` in deploy config, `redis.mode` in API config, and a `REDIS_CLIENT` factory that chooses standalone vs cluster intentionally without leaking `REDIS_URL`.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-valkey-production-connectivity-contract/20-CONTEXT.md
@.planning/phases/20-valkey-production-connectivity-contract/20-RESEARCH.md
@.planning/phases/20-valkey-production-connectivity-contract/20-VALIDATION.md
@.planning/phases/20-valkey-production-connectivity-contract/20-PATTERNS.md
@apps/api/src/config/redis.config.ts
@apps/api/src/modules/booking/providers/redis.provider.ts
@apps/api/src/modules/booking/providers/__tests__/redis.provider.spec.ts
@.github/workflows/deploy.yml

<interfaces>
Created for downstream plans:

```typescript
export type ValkeyMode = 'standalone' | 'cluster';
export type RedisRuntimeClient = 'in-memory' | 'ioredis-standalone' | 'ioredis-cluster';

export interface RedisRuntimeMetadata {
  mode: ValkeyMode | 'in-memory';
  client: RedisRuntimeClient;
  configured: boolean;
}

export function getRedisRuntimeMetadata(redis: unknown): RedisRuntimeMetadata;
```

Implement these exports in `apps/api/src/modules/booking/providers/redis.provider.ts`. Plan 20-02 will import `getRedisRuntimeMetadata()` for sanitized health output.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add VALKEY_MODE config and provider contract tests</name>
  <files>apps/api/src/config/redis.config.ts, apps/api/src/modules/booking/providers/__tests__/redis.provider.spec.ts</files>
  <read_first>
    - apps/api/src/config/redis.config.ts
    - apps/api/src/modules/booking/providers/__tests__/redis.provider.spec.ts
    - .planning/phases/20-valkey-production-connectivity-contract/20-CONTEXT.md
    - .planning/phases/20-valkey-production-connectivity-contract/20-RESEARCH.md
    - .planning/phases/20-valkey-production-connectivity-contract/20-PATTERNS.md
    - .planning/phases/17-local-dev-health-indicator-fix-inmemoryredis-ping-capability/17-02-SUMMARY.md
  </read_first>
  <behavior>
    - Test: `NODE_ENV=production`, `REDIS_URL=redis://10.0.0.1:6379`, and missing `VALKEY_MODE` throws an error containing `VALKEY_MODE is required in production`.
    - Test: `VALKEY_MODE=sentinel` throws an error containing `VALKEY_MODE must be one of: standalone, cluster`.
    - Test: thrown errors and console output do not contain `redis://`, `10.0.0.1`, `Authorization`, `Cookie`, or `JWT`.
    - Test: `VALKEY_MODE=cluster` creates a client with runtime metadata `{ mode: 'cluster', client: 'ioredis-cluster', configured: true }`.
    - Test: development with empty `REDIS_URL` still returns `InMemoryRedis` metadata `{ mode: 'in-memory', client: 'in-memory', configured: false }`.
  </behavior>
  <action>
    Extend `redis.config.ts` to expose `mode: process.env['VALKEY_MODE'] ?? ''` in the existing `registerAs('redis', ...)` namespace per D-01 and D-02.

    Update `redis.provider.spec.ts` before implementation. Replace the current `createMockConfig(url: string)` helper with a key-aware helper that returns values for `redis.url` and `redis.mode`. Add focused tests for `VALKEY_MODE`, invalid enum values, production missing mode, cluster metadata, local `InMemoryRedis` metadata, and redaction per D-03, D-09, and D-18. Do not add tests for unrelated booking/reservation ownership semantics per D-20.
  </action>
  <verify>
    <automated>pnpm --filter @grabit/api exec vitest run src/modules/booking/providers/__tests__/redis.provider.spec.ts --run</automated>
    <automated>rg -n "VALKEY_MODE|redis\\.mode" apps/api/src/config/redis.config.ts apps/api/src/modules/booking/providers/__tests__/redis.provider.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/config/redis.config.ts` contains the exact string `VALKEY_MODE`.
    - `apps/api/src/modules/booking/providers/__tests__/redis.provider.spec.ts` contains the exact strings `VALKEY_MODE is required in production`, `VALKEY_MODE must be one of: standalone, cluster`, `ioredis-cluster`, and `redis://`.
    - The focused Vitest command exits 0 after Task 2 implementation.
    - No task in this plan modifies `apps/api/src/modules/reservation/reservation.service.ts`.
  </acceptance_criteria>
  <done>`VALKEY_MODE` config/test contract exists, redaction tests are present, and provider tests pass after implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement standalone/cluster REDIS_CLIENT creation</name>
  <files>apps/api/src/modules/booking/providers/redis.provider.ts</files>
  <read_first>
    - apps/api/src/modules/booking/providers/redis.provider.ts
    - apps/api/src/modules/booking/providers/__tests__/redis.provider.spec.ts
    - .planning/phases/20-valkey-production-connectivity-contract/20-CONTEXT.md
    - .planning/phases/20-valkey-production-connectivity-contract/20-RESEARCH.md
    - .planning/phases/20-valkey-production-connectivity-contract/20-PATTERNS.md
    - .planning/phases/07-valkey/07-01-SUMMARY.md
    - .planning/phases/07-valkey/07-03-SUMMARY.md
  </read_first>
  <action>
    In `redis.provider.ts`, keep the existing `REDIS_CLIENT` provider and `InMemoryRedis` architecture per D-05 and D-21. Add exported `ValkeyMode`, `RedisRuntimeClient`, `RedisRuntimeMetadata`, and `getRedisRuntimeMetadata(redis)` as defined in this plan's `<interfaces>` block.

    Parse `redis.url` with `new URL(url)` instead of string splitting. For `VALKEY_MODE=standalone`, preserve the existing `new IORedis(url, { maxRetriesPerRequest: 3, lazyConnect: true, retryStrategy })` path. For `VALKEY_MODE=cluster`, create `new Cluster([{ host, port }], { lazyConnect: true, scaleReads: 'master', enableReadyCheck: true, redisOptions: { maxRetriesPerRequest: 3 }, clusterRetryStrategy })`. Import `Cluster` from `ioredis`; do not introduce another Redis provider or service-layer rewrites.

    Fail closed when `NODE_ENV=production` and `REDIS_URL` is empty, `VALKEY_MODE` is empty, or `VALKEY_MODE` is not `standalone` or `cluster`. Error messages must mention the env var name but must not include the raw `REDIS_URL`, host, auth headers, cookies, JWTs, phone numbers, payment data, or private customer data. Attach non-enumerable runtime metadata to each returned client so Plan 20-02 can expose sanitized health detail.
  </action>
  <verify>
    <automated>pnpm --filter @grabit/api exec vitest run src/modules/booking/providers/__tests__/redis.provider.spec.ts --run</automated>
    <automated>pnpm --filter @grabit/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/modules/booking/providers/redis.provider.ts` contains `export type ValkeyMode = 'standalone' | 'cluster'`.
    - `apps/api/src/modules/booking/providers/redis.provider.ts` contains `new Cluster`.
    - `apps/api/src/modules/booking/providers/redis.provider.ts` contains `getRedisRuntimeMetadata`.
    - `rg -n "REDIS_URL.*redis://" apps/api/src/modules/booking/providers/redis.provider.ts` prints no matches.
    - Focused provider tests and API typecheck both exit 0.
  </acceptance_criteria>
  <done>`REDIS_CLIENT` supports explicit standalone/cluster creation with redacted fail-closed production errors and passing tests/typecheck.</done>
</task>

<task type="auto">
  <name>Task 3: Pin Cloud Run deploy contract to VALKEY_MODE=cluster</name>
  <files>.github/workflows/deploy.yml</files>
  <read_first>
    - .github/workflows/deploy.yml
    - .planning/phases/20-valkey-production-connectivity-contract/20-CONTEXT.md
    - .planning/phases/20-valkey-production-connectivity-contract/20-RESEARCH.md
    - .planning/phases/20-valkey-production-connectivity-contract/20-PATTERNS.md
    - .planning/phases/07-valkey/07-03-SUMMARY.md
  </read_first>
  <action>
    Add `VALKEY_MODE=cluster` to the existing API Cloud Run `env_vars` block in `.github/workflows/deploy.yml` directly beside `NODE_ENV=production` and `FRONTEND_URL=${{ vars.CLOUD_RUN_WEB_URL }}`. Keep `REDIS_URL=redis-url:latest`, `--network=default`, `--subnet=default`, `--vpc-egress=private-ranges-only`, `--min-instances=0`, and `--session-affinity` unchanged per D-02, D-11, and D-12.
  </action>
  <verify>
    <automated>rg -n "VALKEY_MODE=cluster|REDIS_URL=redis-url:latest|--vpc-egress=private-ranges-only|--min-instances=0" .github/workflows/deploy.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/deploy.yml` contains exactly one `VALKEY_MODE=cluster` line.
    - `.github/workflows/deploy.yml` still contains `REDIS_URL=redis-url:latest`.
    - `.github/workflows/deploy.yml` still contains `--vpc-egress=private-ranges-only`.
    - `.github/workflows/deploy.yml` still contains `--min-instances=0`; do not permanently raise min instances.
  </acceptance_criteria>
  <done>Cloud Run deploy config pins `VALKEY_MODE=cluster` while preserving `REDIS_URL`, VPC egress, and scale-to-zero settings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cloud Run env -> API config | Production secrets and mode declarations cross from Cloud Run/Secret Manager into NestJS config. |
| API config -> Redis client | `REDIS_URL` and `VALKEY_MODE` determine the concrete Valkey client used by all lock and pub/sub paths. |
| CI logs -> repository artifacts | Test failures may print configuration errors into logs and summaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Information Disclosure | `redis.provider.ts`, `redis.provider.spec.ts` | mitigate | Test that config errors and metadata omit `redis://`, raw host/IP, `Authorization`, `Cookie`, `JWT`, phone numbers, payment data, and private customer data. |
| T-20-02 | Tampering | `REDIS_CLIENT` provider | mitigate | Preserve production missing `REDIS_URL` hard-fail and add production missing/invalid `VALKEY_MODE` hard-fail; production cannot reach `InMemoryRedis`. |
| T-20-03 | Denial of Service | `VALKEY_MODE` selection | mitigate | Invalid or missing production mode fails tests/startup instead of becoming informational. Deploy pins `VALKEY_MODE=cluster`. |
| T-20-04 | Tampering | Booking Lua key slots | transfer | This plan does not touch Lua scripts; Plan 20-03 adds cluster-mode booking Lua coverage with shared hash tags and explicit `KEYS`. |
| T-20-05 | Integrity | Production lock smoke | transfer | This plan does not mutate production inventory; Plan 20-04 requires operator-approved fixture and unlock/TTL proof. |
| T-20-06 | Denial of Service | Cloud Run scaling | mitigate | Keep `.github/workflows/deploy.yml` `--min-instances=0`; no temporary scale command is introduced in this plan. |
</threat_model>

<verification>
Run all checks below before writing the plan summary:

```bash
pnpm --filter @grabit/api exec vitest run src/modules/booking/providers/__tests__/redis.provider.spec.ts --run
pnpm --filter @grabit/api typecheck
rg -n "VALKEY_MODE=cluster|REDIS_URL=redis-url:latest|--vpc-egress=private-ranges-only|--min-instances=0" .github/workflows/deploy.yml
rg -n "new Cluster|getRedisRuntimeMetadata|VALKEY_MODE" apps/api/src/modules/booking/providers/redis.provider.ts apps/api/src/config/redis.config.ts
```
</verification>

<success_criteria>
- `VALKEY_MODE=cluster` is part of the production deploy contract.
- `REDIS_CLIENT` supports `standalone` and `cluster` modes without changing booking/reservation ownership semantics.
- Production mode mismatch inputs fail closed with redacted errors.
- Provider tests and typecheck pass.
</success_criteria>

<output>
After completion, create `.planning/phases/20-valkey-production-connectivity-contract/20-01-SUMMARY.md`.
</output>
