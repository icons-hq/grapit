---
phase: 24-traffic-booking-payment-core
plan: "13"
type: execute
wave: 9
depends_on:
  - "24-11"
  - "24-17"
files_modified:
  - apps/api/src/modules/ticket/ticket.module.ts
  - apps/api/src/modules/ticket/ticket.controller.ts
  - apps/api/src/modules/ticket/qr-ticket.service.ts
  - apps/api/src/modules/ticket/qr-ticket.service.spec.ts
  - apps/api/src/modules/auth/email/email.service.ts
  - apps/web/components/booking/booking-complete.tsx
  - apps/web/components/reservation/reservation-detail.tsx
  - apps/web/e2e/booking-complete-qr.spec.ts
autonomous: true
requirements:
  - QR-01
user_setup:
  - service: cloud-run-env
    why: "QR signing needs a dedicated secret and version metadata."
    env_vars:
      - name: QR_TICKET_SECRET
        source: "Secret Manager / Cloud Run env"
      - name: QR_TICKET_SECRET_VERSION
        source: "Secret version label or manual rotation string"
must_haves:
  truths:
    - "Successful booking issues a QR ticket immediately and exposes it from booking complete and My Page."
    - "The system schedules a D-1 QR email resend through durable background work."
    - "D-19: QR tickets are issued immediately after successful payment confirmation using JWT/HMAC payload and remain visible in My Page."
    - "D-20: QR email is scheduled for D-1, 24 hours before the event, as part of the Phase 24 `QR-01` contract."
  artifacts:
    - path: "apps/api/src/modules/ticket/qr-ticket.service.ts"
      provides: "QR JWT/HMAC issuance and scheduling"
      contains: "QR_TICKET_SECRET"
    - path: "apps/web/components/booking/booking-complete.tsx"
      provides: "immediate QR reassurance UI"
      contains: "QR 티켓 보기"
    - path: "apps/web/components/reservation/reservation-detail.tsx"
      provides: "My Page QR visibility"
      contains: "QR"
  key_links:
    - from: "qr-ticket.service.ts"
      to: "email.service.ts"
      via: "D-1 QR resend scheduling"
      pattern: "emailScheduledAt"
    - from: "reservation-detail.tsx"
      to: "ticket.controller.ts"
      via: "immediate ticket visibility"
      pattern: "QR"
---

<objective>
Implement QR issuance, D-1 QR email scheduling, and immediate QR visibility surfaces.

Purpose: D-19 and D-20 require QR ticket issuance to be part of the completed booking contract, not a later operational add-on.
Output: ticket module/service/controller, email scheduling integration, booking-complete QR UI, My Page QR visibility, and E2E coverage.
Decision coverage: D-19: QR JWT/HMAC tickets issue immediately after successful payment and are visible from booking complete and My Page. D-20: a D-1 QR email resend is scheduled for 24 hours before the event as part of the same ticket contract.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-traffic-booking-payment-core/24-CONTEXT.md
@.planning/phases/24-traffic-booking-payment-core/24-RESEARCH.md
@.planning/phases/24-traffic-booking-payment-core/24-UI-SPEC.md
@.planning/milestones/v1.0-phases/04-booking-payment/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add QR ticket issuance, status lookup, and D-1 email scheduling</name>
  <files>
    apps/api/src/modules/ticket/ticket.module.ts
    apps/api/src/modules/ticket/ticket.controller.ts
    apps/api/src/modules/ticket/qr-ticket.service.ts
    apps/api/src/modules/ticket/qr-ticket.service.spec.ts
    apps/api/src/modules/auth/email/email.service.ts
  </files>
  <read_first>
    apps/api/src/modules/auth/email/email.service.ts
    apps/api/src/modules/reservation/reservation.service.ts
    .planning/phases/24-traffic-booking-payment-core/24-RESEARCH.md
  </read_first>
  <action>
    Add a ticket module/controller/service that issues HS256 QR tickets with `qrTokenJti` and `secretVersion` immediately after successful payment confirmation per D-19. Use the existing email service pattern to schedule the D-1 resend at showtime minus 24 hours per D-20, relying on the job infrastructure from Plan 24-11 instead of a separate mail scheduler.
  </action>
  <acceptance_criteria>
    - `qr-ticket.service.ts` contains `QR_TICKET_SECRET`, `qrTokenJti`, and D-1 scheduling logic.
    - `qr-ticket.service.spec.ts` covers ticket issuance and scheduling.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/api test -- src/modules/ticket/qr-ticket.service.spec.ts && pnpm --filter @grabit/api typecheck</automated>
  </verify>
  <done>
    QR issuance and D-1 resend scheduling are durable backend behaviors.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface QR visibility on booking complete and reservation detail</name>
  <files>
    apps/web/components/booking/booking-complete.tsx
    apps/web/components/reservation/reservation-detail.tsx
    apps/web/e2e/booking-complete-qr.spec.ts
  </files>
  <read_first>
    apps/web/components/booking/booking-complete.tsx
    apps/web/components/reservation/reservation-detail.tsx
    .planning/phases/24-traffic-booking-payment-core/24-UI-SPEC.md
  </read_first>
  <action>
    Update `booking-complete.tsx` so the primary follow-up is `QR 티켓 보기`, with the D-1 resend notice visible per the UI spec. Update `reservation-detail.tsx` so My Page exposes the QR ticket immediately after successful booking instead of waiting for an email. Add `booking-complete-qr.spec.ts` to verify QR visibility from completion flow.
  </action>
  <acceptance_criteria>
    - `booking-complete.tsx` contains `QR 티켓 보기` and `QR 티켓 안내 메일은 공연 24시간 전에 다시 발송됩니다.`.
    - `reservation-detail.tsx` renders immediate QR visibility.
    - `booking-complete-qr.spec.ts` exists.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/web test:e2e --grep "booking complete" && pnpm --filter @grabit/web typecheck</automated>
  </verify>
  <done>
    Users can see their QR ticket immediately after booking and from My Page.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| confirmed payment -> signed ticket issuance | Signed admission artifacts are created here. |
| ticket status API -> user ticket surfaces | Ticket visibility leaves the API here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-24 | S/T | `qr-ticket.service.ts` | mitigate | Use a dedicated QR secret, persist `qrTokenJti`/`secretVersion`, and issue QR only after confirmed payment state exists (Task 1). |
| T-24-25 | I | `booking-complete.tsx`, `reservation-detail.tsx` | mitigate | Render QR availability only from ticket status/read models, not from client-side assumptions or unconfirmed redirect state (Task 2). |
</threat_model>

<verification>
- `pnpm --filter @grabit/api test -- src/modules/ticket/qr-ticket.service.spec.ts`
- `pnpm --filter @grabit/api typecheck`
- `pnpm --filter @grabit/web test:e2e --grep "booking complete"`
- `pnpm --filter @grabit/web typecheck`
</verification>

<success_criteria>
- QR tickets are issued immediately after success and visible from both completion and My Page.
- D-1 QR email resend scheduling is part of the backend ticket contract.
</success_criteria>

<output>
After completion, create `.planning/phases/24-traffic-booking-payment-core/24-13-SUMMARY.md`
</output>
