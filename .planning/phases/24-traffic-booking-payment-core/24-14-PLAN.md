---
phase: 24-traffic-booking-payment-core
plan: "14"
type: execute
wave: 10
depends_on:
  - "24-11"
  - "24-13"
files_modified:
  - apps/web/components/reservation/refund-timeline.tsx
  - apps/web/components/reservation/reservation-detail.tsx
  - apps/web/components/reservation/cancel-confirm-modal.tsx
  - apps/web/components/reservation/__tests__/refund-timeline.test.tsx
autonomous: true
requirements:
  - REFUND-01
must_haves:
  truths:
    - "Reservation detail shows refund preview, refund timeline, and delayed reopen expectations before destructive confirmation."
    - "Refund failure or delay remains visible to the user until resolved."
  artifacts:
    - path: "apps/web/components/reservation/refund-timeline.tsx"
      provides: "visible refund state timeline"
      contains: "환불 요청됨"
    - path: "apps/web/components/reservation/cancel-confirm-modal.tsx"
      provides: "refund preview confirmation surface"
      contains: "환불"
  key_links:
    - from: "refund-timeline.tsx"
      to: "reservation-detail.tsx"
      via: "reservation status detail"
      pattern: "환불"
---

<objective>
Implement the user-facing refund preview and refund timeline surfaces.

Purpose: D-16 and D-17 are incomplete until reservation detail makes refund amount, state progression, and delayed-reopen expectations visible to the user.
Output: refund timeline component, reservation-detail integration, destructive-confirm modal updates, and component tests.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-traffic-booking-payment-core/24-CONTEXT.md
@.planning/phases/24-traffic-booking-payment-core/24-UI-SPEC.md
@.planning/phases/24-traffic-booking-payment-core/24-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add refund preview, refund timeline, and delay guidance to reservation surfaces</name>
  <files>
    apps/web/components/reservation/refund-timeline.tsx
    apps/web/components/reservation/reservation-detail.tsx
    apps/web/components/reservation/cancel-confirm-modal.tsx
    apps/web/components/reservation/__tests__/refund-timeline.test.tsx
  </files>
  <read_first>
    apps/web/components/reservation/reservation-detail.tsx
    apps/web/components/reservation/cancel-confirm-modal.tsx
    .planning/phases/24-traffic-booking-payment-core/24-UI-SPEC.md
  </read_first>
  <action>
    Create `refund-timeline.tsx` to display the user-visible D-16 states `requested`, `sent_to_pg`, `processing_at_pg`, `completed`, and `failed`, plus delay-to-CS guidance. Update `reservation-detail.tsx` to show refund preview details and delayed reopen expectation per D-17, and refactor `cancel-confirm-modal.tsx` so `AlertDialog` is used only for the irreversible confirm step after the preview/explanation surface has been shown. Add `refund-timeline.test.tsx` for timeline states and delay CTA coverage.
  </action>
  <acceptance_criteria>
    - `refund-timeline.tsx` exists and contains `환불 요청됨`, `PG 전달됨`, `환불 처리 중`, `환불 완료`, and `환불 실패`.
    - `reservation-detail.tsx` shows delayed reopen expectation copy.
    - `refund-timeline.test.tsx` exists and covers at least one non-terminal delay state.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/web test -- components/reservation/__tests__/refund-timeline.test.tsx && pnpm --filter @grabit/web typecheck</automated>
  </verify>
  <done>
    Users can see refund preview, state progression, and delayed-reopen expectations clearly from reservation detail.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| refund state API -> reservation detail UI | Financial/refund state is rendered to users here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-26 | R/I | `refund-timeline.tsx`, `reservation-detail.tsx` | mitigate | Keep each refund state visible and test delay/failure copy so operationally important non-terminal states cannot be hidden behind a generic “취소 처리 중” label (Task 1). |
</threat_model>

<verification>
- `pnpm --filter @grabit/web test -- components/reservation/__tests__/refund-timeline.test.tsx`
- `pnpm --filter @grabit/web typecheck`
</verification>

<success_criteria>
- Refund preview and refund state timeline are visible from reservation detail.
- Delayed reopen and failed-refund expectations are explicit to the user.
</success_criteria>

<output>
After completion, create `.planning/phases/24-traffic-booking-payment-core/24-14-SUMMARY.md`
</output>
