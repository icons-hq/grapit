---
phase: 24-traffic-booking-payment-core
plan: "04"
type: execute
wave: 3
depends_on:
  - "24-01"
  - "24-02"
  - "24-03"
files_modified:
  - apps/web/hooks/use-queue.ts
  - apps/web/components/booking/queue-waiting.tsx
  - apps/web/components/booking/floor-selector.tsx
  - apps/web/components/booking/booking-page.tsx
  - apps/web/components/booking/seat-map-viewer.tsx
  - apps/web/stores/use-booking-store.ts
  - apps/web/hooks/use-booking.ts
  - apps/web/components/booking/__tests__/floor-selector.test.tsx
  - apps/web/e2e/booking-queue.spec.ts
  - apps/web/messages/ko.json
  - apps/web/messages/en.json
  - apps/web/messages/th.json
  - apps/web/messages/zh-CN.json
  - apps/web/messages/zh-TW.json
autonomous: true
requirements:
  - TRAF-01
  - BOOK-01
must_haves:
  truths:
    - "A user who enters `/booking` sees queue position, ETA, remaining seats, and auto-entry instead of a blind redirect."
    - "A user can switch floors without losing selections, and the seat summary groups selections by floor label."
    - "UI ticket-limit guidance and selected-seat state align with the server-side floor-aware booking policy."
  artifacts:
    - path: "apps/web/components/booking/queue-waiting.tsx"
      provides: "Queue waiting surface with auto-entry and failure messaging"
      contains: "예매 대기열에서 입장 순서를 기다리고 있습니다"
    - path: "apps/web/components/booking/floor-selector.tsx"
      provides: "Multi-floor navigation UI"
      contains: "ToggleGroup"
    - path: "apps/web/stores/use-booking-store.ts"
      provides: "Floor-aware booking client state"
      contains: "floorKey"
    - path: "apps/web/e2e/booking-queue.spec.ts"
      provides: "Queue admission and failure-state verification"
      contains: "queue"
  key_links:
    - from: "apps/web/hooks/use-queue.ts"
      to: "apps/web/components/booking/queue-waiting.tsx"
      via: "position/ETA/remaining-seat read model"
      pattern: "etaSeconds|remainingSeats"
    - from: "apps/web/stores/use-booking-store.ts"
      to: "apps/web/components/booking/booking-page.tsx"
      via: "floor-aware selected seat state"
      pattern: "floorKey|floorLabel"
    - from: "apps/web/components/booking/booking-page.tsx"
      to: "apps/web/hooks/use-booking.ts"
      via: "lock/unlock requests and restored floor-aware selections"
      pattern: "useLockSeat|useMyLocks"
---

<objective>
Wire the queue admission UX and multi-floor seat-selection UI into the existing booking route.

Purpose: users must be able to enter through the transparent queue and then compare/select seats across floors without losing context or exceeding event policy.
Output: queue hook/waiting screen, floor selector, floor-aware booking state, localized copy, and booking E2E coverage.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/24-traffic-booking-payment-core/24-CONTEXT.md
@.planning/phases/24-traffic-booking-payment-core/24-RESEARCH.md
@.planning/phases/24-traffic-booking-payment-core/24-VALIDATION.md
@.planning/phases/24-traffic-booking-payment-core/24-UI-SPEC.md
@.planning/phases/23-launch-foundation/23-08-SUMMARY.md

<interfaces>
From `apps/web/stores/use-booking-store.ts`:
```ts
interface BookingState {
  selectedShowtimeId: string | null;
  selectedSeats: SeatSelection[];
  setShowtime: (id: string | null) => void;
  addSeat: (seat: SeatSelection) => void;
  removeSeat: (seatId: string) => void;
}
```

From `apps/web/components/booking/booking-page.tsx`:
```ts
const MAX_SEATS = 4;
const seatConfig: SeatMapConfig | null = performance?.seatMap?.seatConfig ?? null;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the queue waiting surface, queue hook, and localized failure states</name>
  <files>
    apps/web/hooks/use-queue.ts
    apps/web/components/booking/queue-waiting.tsx
    apps/web/e2e/booking-queue.spec.ts
    apps/web/messages/ko.json
    apps/web/messages/en.json
    apps/web/messages/th.json
    apps/web/messages/zh-CN.json
    apps/web/messages/zh-TW.json
  </files>
  <read_first>
    .planning/phases/24-traffic-booking-payment-core/24-CONTEXT.md
    .planning/phases/24-traffic-booking-payment-core/24-UI-SPEC.md
    .planning/phases/24-traffic-booking-payment-core/24-VALIDATION.md
    apps/web/messages/ko.json
    apps/web/messages/en.json
    apps/web/messages/th.json
    apps/web/messages/zh-CN.json
    apps/web/messages/zh-TW.json
  </read_first>
  <behavior>
    - `/booking` entry shows queue position, ETA, remaining seats, and auto-entry copy, with `enter now` treated as fallback-only (D-01, D-05).
    - Queue failure states render distinct localized copy for expired admission, rate-limit retry, security challenge, and hard block (D-08).
    - Queue UI preserves the existing Phase 23 locale foundation across all five locales.
  </behavior>
  <action>
    Create `apps/web/hooks/use-queue.ts` to call the new queue entry/status APIs and model the admitted/waiting/expired/challenge/block states. Build `apps/web/components/booking/queue-waiting.tsx` as the full-page waiting surface described by the UI spec, including auto-entry behavior and a fallback action only when automatic navigation fails. Add or update the exact visible-copy keys in all five locale message files for queue waiting, auto-entry, expired admission, rate-limit retry, security challenge, and hard block. Add `apps/web/e2e/booking-queue.spec.ts` to prove the route shows queue status before booking and localizes each failure state separately.
  </action>
  <acceptance_criteria>
    - `apps/web/components/booking/queue-waiting.tsx` contains the exact Korean strings `예매 대기열에서 입장 순서를 기다리고 있습니다` and `입장 가능 상태입니다. 예매 화면으로 이동합니다`.
    - All five `apps/web/messages/*.json` files contain queue waiting, expired, rate-limit, challenge, and blocked keys.
    - `apps/web/e2e/booking-queue.spec.ts` exists and references queue position or ETA assertions.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/web test:e2e --grep "queue" && pnpm --filter @grabit/web test -- booking-queue</automated>
  </verify>
  <done>
    Queue UI is localized and verifiably distinguishes waiting, admission, retry, challenge, and hard-block states.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add floor-aware booking UI and grouped selection summary</name>
  <files>
    apps/web/components/booking/floor-selector.tsx
    apps/web/components/booking/booking-page.tsx
    apps/web/components/booking/seat-map-viewer.tsx
    apps/web/stores/use-booking-store.ts
    apps/web/hooks/use-booking.ts
    apps/web/components/booking/__tests__/floor-selector.test.tsx
  </files>
  <read_first>
    .planning/phases/24-traffic-booking-payment-core/24-CONTEXT.md
    .planning/phases/24-traffic-booking-payment-core/24-UI-SPEC.md
    apps/web/components/booking/booking-page.tsx
    apps/web/components/booking/seat-map-viewer.tsx
    apps/web/stores/use-booking-store.ts
    apps/web/hooks/use-booking.ts
  </read_first>
  <behavior>
    - Floor switching does not clear selected seats or reset booking state, and the selection summary groups seats by floor label (D-11).
    - Dynamic ticket-limit helper copy reflects event policy instead of hardcoded `4-seat` wording (D-12, D-13).
    - Restored lock state and click handling continue to work with floor-aware `seatKey` identity, not only visible `seatId` strings (research pitfall 2).
  </behavior>
  <action>
    Add a sticky `floor-selector.tsx` using `ToggleGroup` for 2-7 floor options and integrate it into `booking-page.tsx` above the seat map. Refactor `use-booking-store.ts` so selected seats carry `floorKey`, `floorLabel`, and `seatKey`, and update restore/lock/unlock logic in `booking-page.tsx` and `use-booking.ts` to use the floor-aware payload returned by Plans 24-01/24-03. Update the summary panel/sheet to group selections by floor label, preserve tier swatches, and render the dynamic event ticket-limit helper from the new booking policy instead of the old `MAX_SEATS=4` copy. Keep existing zoom/pan/minimap behavior in `seat-map-viewer.tsx`; Phase 24 adds floor navigation around it rather than rewriting rendering.
  </action>
  <acceptance_criteria>
    - `apps/web/components/booking/floor-selector.tsx` exists and contains `ToggleGroup`.
    - `apps/web/stores/use-booking-store.ts` contains `floorKey`, `floorLabel`, and `seatKey`.
    - `apps/web/components/booking/booking-page.tsx` no longer uses the hardcoded helper text `최대 4석`.
    - `apps/web/components/booking/__tests__/floor-selector.test.tsx` exists and asserts floor switching preserves selections.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/web test -- components/booking/__tests__/floor-selector.test.tsx hooks/__tests__/use-booking.test.tsx && pnpm --filter @grabit/web typecheck</automated>
  </verify>
  <done>
    Users can browse floors, keep selections across floors, and see floor-grouped summaries with policy-aligned ticket-limit messaging.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| queue status API -> booking route | Admission state is rendered to the user here. |
| performance detail/policy -> booking client state | Floor arrays and ticket-policy data are consumed client-side here. |
| restored locks -> local seat selection | Floor-aware identity is reconstructed from server state here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-12 | I | `queue-waiting.tsx` | mitigate | Render only queue/session state needed for the user; do not expose raw internal env or control-path secrets while still differentiating failure states (Task 1). |
| T-24-13 | T | `use-booking-store.ts`, `booking-page.tsx` | mitigate | Carry `floorKey`, `floorLabel`, and `seatKey` in client state so restored selections cannot confuse same-label seats across floors (Task 2). |
| T-24-14 | D | `booking-queue.spec.ts`, floor selector UI | mitigate | Add E2E/component coverage for queue auto-entry and floor switching so regressions in queue or floor state are caught before canary (Task 1, Task 2). |
</threat_model>

<verification>
- `pnpm --filter @grabit/web test:e2e --grep "queue"`
- `pnpm --filter @grabit/web test -- components/booking/__tests__/floor-selector.test.tsx hooks/__tests__/use-booking.test.tsx`
- `pnpm --filter @grabit/web typecheck`
</verification>

<success_criteria>
- `/booking` presents queue progress and localized failure states instead of a blind redirect.
- Multi-floor seat selection preserves selections across floors and groups them in the summary.
- Booking UI copy reflects event policy rather than a hardcoded 4-seat assumption.
</success_criteria>

<output>
After completion, create `.planning/phases/24-traffic-booking-payment-core/24-04-SUMMARY.md`
</output>
