---
phase: 24-traffic-booking-payment-core
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/database/schema/phase24-booking-core.schema.spec.ts
  - packages/shared/src/types/booking.types.ts
  - packages/shared/src/schemas/booking.schema.ts
  - packages/shared/src/index.ts
autonomous: true
requirements:
  - TRAF-01
  - BOOK-01
  - BOOK-02
  - BOOK-03
  - PAY-02
  - REFUND-01
  - REFUND-02
  - QR-01
must_haves:
  truths:
    - "Shared booking contracts carry queue admission, floor-aware seat identity, payment deadline, refund timeline, and QR ticket fields before implementation fans out."
    - "Downstream plans do not rely on single-floor `seatId` identity or on lock TTL as the only payment timer."
  artifacts:
    - path: "packages/shared/src/types/booking.types.ts"
      provides: "Phase 24 canonical booking DTOs"
      contains: "floorKey"
    - path: "packages/shared/src/schemas/booking.schema.ts"
      provides: "Phase 24 booking zod schemas"
      contains: "paymentDeadlineAt"
    - path: "apps/api/src/database/schema/phase24-booking-core.schema.spec.ts"
      provides: "Schema contract gate"
      contains: "qrTokenJti"
  key_links:
    - from: "packages/shared/src/types/booking.types.ts"
      to: "packages/shared/src/schemas/booking.schema.ts"
      via: "shared DTO + zod contract"
      pattern: "floorKey|seatKey|refundTimeline|qrTicket"
    - from: "packages/shared/src/index.ts"
      to: "apps/api/src/modules/*"
      via: "barrel exports"
      pattern: "booking"
---

<objective>
Define the shared Phase 24 booking-core contracts before any schema or runtime work begins.

Purpose: D-03, D-10, D-12, D-16, D-19, and D-20 must be locked into shared DTO/schema contracts so later queue, booking, payment, refund, and QR plans implement one canonical shape.
Output: shared booking DTOs, zod schemas, barrel exports, and a RED schema contract test.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-traffic-booking-payment-core/24-CONTEXT.md
@.planning/phases/24-traffic-booking-payment-core/24-RESEARCH.md
@.planning/phases/24-traffic-booking-payment-core/24-VALIDATION.md
@.planning/phases/24-traffic-booking-payment-core/24-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the RED contract test for Phase 24 shared booking fields</name>
  <files>
    apps/api/src/database/schema/phase24-booking-core.schema.spec.ts
  </files>
  <read_first>
    .planning/phases/24-traffic-booking-payment-core/24-CONTEXT.md
    .planning/phases/24-traffic-booking-payment-core/24-RESEARCH.md
    packages/shared/src/types/booking.types.ts
    packages/shared/src/schemas/booking.schema.ts
  </read_first>
  <action>
    Create a RED contract test that fails until the shared booking layer exposes queue-admission identifiers for D-03, floor-aware `floorKey`/`floorLabel`/`seatKey` for D-10 and D-11, event-policy and `paymentDeadlineAt` fields for D-12 and D-13, visible refund timeline fields for D-16, and QR ticket fields for D-19 and D-20. The test must inspect the shared contract surface and assert that single-floor-only `seatId` assumptions are no longer sufficient.
  </action>
  <acceptance_criteria>
    - `apps/api/src/database/schema/phase24-booking-core.schema.spec.ts` exists.
    - The test file contains `queueSessionId`, `floorKey`, `seatKey`, `paymentDeadlineAt`, `refundTimeline`, and `qrTicket`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/api exec vitest run src/database/schema/phase24-booking-core.schema.spec.ts</automated>
  </verify>
  <done>
    A RED gate exists for every shared booking-core field required by Phase 24.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement shared booking DTOs, zod schemas, and exports</name>
  <files>
    packages/shared/src/types/booking.types.ts
    packages/shared/src/schemas/booking.schema.ts
    packages/shared/src/index.ts
  </files>
  <read_first>
    apps/api/src/database/schema/phase24-booking-core.schema.spec.ts
    packages/shared/src/types/booking.types.ts
    packages/shared/src/schemas/booking.schema.ts
    packages/shared/src/index.ts
  </read_first>
  <action>
    Extend the shared booking contracts so downstream plans consume one floor-aware, queue-aware, payment-aware shape: `floorKey`, `floorLabel`, `seatKey`, admission/session payloads per D-03, event-policy and payment deadline fields per D-12 and D-13, overseas disclaimer/payment branch fields per D-15, visible refund timeline data per D-16, delayed reopen metadata per D-17, and QR ticket payloads per D-19 and D-20. Re-export every new schema/type through `packages/shared/src/index.ts`.
  </action>
  <acceptance_criteria>
    - `packages/shared/src/types/booking.types.ts` contains `floorKey`, `floorLabel`, `seatKey`, `paymentDeadlineAt`, `refundTimeline`, and `qrTicket`.
    - `packages/shared/src/schemas/booking.schema.ts` validates floor-aware seat selections and payment/refund/QR payloads.
    - `packages/shared/src/index.ts` re-exports the Phase 24 booking contracts.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/api exec vitest run src/database/schema/phase24-booking-core.schema.spec.ts && pnpm --filter @grabit/shared test -- booking.schema.test.ts</automated>
  </verify>
  <done>
    Shared booking contracts are green and ready for schema/runtime plans.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web/shared DTO input -> API validation | Untrusted booking/payment/refund payloads enter the server through these contracts. |
| shared contract -> downstream plans | A weak contract here would duplicate unsafe assumptions across the whole phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | T | `booking.types.ts` | mitigate | Define `floorKey + seatKey` and queue/payment/refund/QR fields explicitly so later plans cannot infer or spoof identity from incomplete DTOs (Task 2). |
| T-24-02 | D | `booking.schema.ts` | mitigate | Make the RED contract test fail until Phase 24 timing/state fields exist, preventing silent reuse of pre-Phase-24 single-timer assumptions (Task 1, Task 2). |
</threat_model>

<verification>
- `pnpm --filter @grabit/api exec vitest run src/database/schema/phase24-booking-core.schema.spec.ts`
- `pnpm --filter @grabit/shared test -- booking.schema.test.ts`
</verification>

<success_criteria>
- Shared DTOs and zod schemas express the complete Phase 24 booking-core state surface.
- Single-floor-only identity and single-timer assumptions are removed from the shared contract layer.
</success_criteria>

<output>
After completion, create `.planning/phases/24-traffic-booking-payment-core/24-01-SUMMARY.md`
</output>
