---
phase: 24-traffic-booking-payment-core
plan: "17"
type: execute
wave: 8
depends_on:
  - "24-09"
  - "24-10"
files_modified:
  - apps/web/app/booking/[performanceId]/complete/page.tsx
  - apps/web/hooks/use-booking.ts
  - apps/web/e2e/toss-payment-phase24.spec.ts
autonomous: true
requirements:
  - BOOK-02
  - PAY-02
user_setup:
  - service: toss-payments
    why: "PAY-02 foreign payment branches require real Toss sandbox/webhook configuration."
    env_vars:
      - name: NEXT_PUBLIC_TOSS_CLIENT_KEY
        source: "Toss Payments test client key"
      - name: TOSS_SECRET_KEY
        source: "Toss Payments test secret key"
    dashboard_config:
      - task: "Confirm overseas card, Alipay+, and TrueMoney visibility on the test merchant; capture any sandbox limitation as manual verification evidence."
        location: "Toss Payments Developer Console -> Payment Methods"
      - task: "Register the public webhook URL for `PAYMENT_STATUS_CHANGED` and `CANCEL_STATUS_CHANGED`."
        location: "Toss Payments Developer Console -> Webhooks"
must_haves:
  truths:
    - "Pending, failed, or expired payment returns show inline recovery instead of toast-only feedback."
    - "Payment completion UI distinguishes asynchronous wait, recoverable failure, and expired lock/deadline states instead of assuming immediate success."
    - "Toss payment E2E covers the return/recovery states exposed by the backend async branches."
  artifacts:
    - path: "apps/web/app/booking/[performanceId]/complete/page.tsx"
      provides: "pending/failure/expired payment recovery UI"
      contains: "pending"
    - path: "apps/web/hooks/use-booking.ts"
      provides: "payment status recovery hook contract"
      contains: "paymentStatus"
    - path: "apps/web/e2e/toss-payment-phase24.spec.ts"
      provides: "payment recovery E2E coverage"
      contains: "toss-payment"
  key_links:
    - from: "complete/page.tsx"
      to: "use-booking.ts"
      via: "pending/failure/expired recovery state"
      pattern: "pending|failed|expired"
    - from: "toss-payment-phase24.spec.ts"
      to: "complete/page.tsx"
      via: "return/recovery assertions"
      pattern: "pending|failure|expired"
---

<objective>
Implement the pending/failure/expired payment recovery UI on the completion route.

Purpose: BOOK-02 and PAY-02 are incomplete until the complete page renders the asynchronous and failure branches from Plan 24-09 as explicit recovery states rather than assuming every return is a success.
Output: complete-page recovery UI, hook support for recovery states, and Toss payment recovery E2E coverage.
Decision coverage: D-15: completion/recovery states stay consistent with the overseas disclaimer and payment-deadline semantics established on confirm. D-21: the completion route and E2E verify Toss widget return/recovery branches rather than a separate custom payment flow.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-traffic-booking-payment-core/24-CONTEXT.md
@.planning/phases/24-traffic-booking-payment-core/24-UI-SPEC.md
@.planning/phases/24-traffic-booking-payment-core/24-VALIDATION.md
@.planning/phases/24-traffic-booking-payment-core/24-10-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render pending, failed, and expired payment recovery states on the complete page</name>
  <files>
    apps/web/app/booking/[performanceId]/complete/page.tsx
    apps/web/hooks/use-booking.ts
  </files>
  <read_first>
    apps/web/app/booking/[performanceId]/complete/page.tsx
    apps/web/hooks/use-booking.ts
    .planning/phases/24-traffic-booking-payment-core/24-UI-SPEC.md
  </read_first>
  <action>
    Update `use-booking.ts` and `complete/page.tsx` so the completion route distinguishes pending asynchronous payment, recoverable failure, and expired lock/deadline returns from Plan 24-09, then renders inline recovery blocks instead of toast-only or success-assuming behavior. Reuse the payment copy namespace added in Plan 24-10 so recovery states stay consistent with the D-15 disclaimer/deadline language.
  </action>
  <acceptance_criteria>
    - `complete/page.tsx` contains inline pending/failure/expired recovery handling.
    - `use-booking.ts` exposes the payment status/recovery data consumed by `complete/page.tsx`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/web test -- hooks/__tests__/use-booking.test.tsx && pnpm --filter @grabit/web typecheck</automated>
  </verify>
  <done>
    Users are not silently stranded on the complete route when payment is still pending or has failed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend Toss payment E2E coverage for recovery states</name>
  <files>
    apps/web/e2e/toss-payment-phase24.spec.ts
  </files>
  <read_first>
    apps/web/e2e/toss-payment-phase24.spec.ts
    apps/web/app/booking/[performanceId]/complete/page.tsx
    .planning/phases/24-traffic-booking-payment-core/24-VALIDATION.md
  </read_first>
  <action>
    Extend `toss-payment-phase24.spec.ts` so the Phase 24 payment E2E asserts pending async return, recoverable failure, and expired return behavior on the complete route, not just the confirm-page deadline/disclaimer gate. Keep the scenarios aligned with the webhook/status branches defined in Plan 24-09 and the UI contract from Phase 24.
  </action>
  <acceptance_criteria>
    - `toss-payment-phase24.spec.ts` asserts pending, failure, and expired return states.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/web test:e2e --grep "toss-payment" && pnpm --filter @grabit/web typecheck</automated>
  </verify>
  <done>
    Payment recovery states are covered by automated browser verification.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend payment status -> complete-page UI | Provider/backend state becomes user-visible next steps here. |
| complete-page recovery UI -> repeat booking/payment attempts | Recovery guidance influences how users re-enter scarce-seat/payment flows here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-27 | D | `complete/page.tsx`, `use-booking.ts` | mitigate | Render distinct pending/failure/expired recovery states so async or failed payments do not strand users behind a false success surface (Task 1). |
| T-24-28 | R | `toss-payment-phase24.spec.ts` | mitigate | Cover pending/failure/expired routes in E2E so the recovery UX stays aligned with the backend async branches during regressions (Task 2). |
</threat_model>

<verification>
- `pnpm --filter @grabit/web test -- hooks/__tests__/use-booking.test.tsx`
- `pnpm --filter @grabit/web test:e2e --grep "toss-payment"`
- `pnpm --filter @grabit/web typecheck`
</verification>

<success_criteria>
- Completion route shows explicit pending, failure, and expired recovery UI instead of assuming every payment return is a success.
- Toss payment E2E covers the confirm-to-complete recovery path, including asynchronous and expired branches.
</success_criteria>

<output>
After completion, create `.planning/phases/24-traffic-booking-payment-core/24-17-SUMMARY.md`
</output>
