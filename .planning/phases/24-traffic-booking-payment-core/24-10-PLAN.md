---
phase: 24-traffic-booking-payment-core
plan: "10"
type: execute
wave: 7
depends_on:
  - "24-07"
  - "24-09"
files_modified:
  - apps/web/app/booking/[performanceId]/confirm/page.tsx
  - apps/web/app/booking/[performanceId]/complete/page.tsx
  - apps/web/components/booking/toss-payment-widget.tsx
  - apps/web/components/booking/payment-deadline-banner.tsx
  - apps/web/hooks/use-booking.ts
  - apps/web/e2e/toss-payment-phase24.spec.ts
  - apps/web/messages/ko.json
  - apps/web/messages/en.json
  - apps/web/messages/th.json
  - apps/web/messages/zh-CN.json
  - apps/web/messages/zh-TW.json
autonomous: true
requirements:
  - BOOK-02
  - PAY-02
user_setup:
  - service: toss-payments
    why: "PAY-02 foreign payment branches require real Toss sandbox/webhook configuration."
    env_vars:
      - name: NEXT_PUBLIC_TOSS_CLIENT_KEY
        source: "Toss Payments test client key"
      - name: TOSS_SECRET_KEY
        source: "Toss Payments test secret key"
    dashboard_config:
      - task: "Confirm overseas card, Alipay+, and TrueMoney visibility on the test merchant; capture any sandbox limitation as manual verification evidence."
        location: "Toss Payments Developer Console -> Payment Methods"
      - task: "Register the public webhook URL for `PAYMENT_STATUS_CHANGED` and `CANCEL_STATUS_CHANGED`."
        location: "Toss Payments Developer Console -> Webhooks"
must_haves:
  truths:
    - "Confirm/payment shows a visible 7-minute payment deadline distinct from the seat-lock timer."
    - "Overseas payment methods require an explicit disclaimer checkbox immediately before payment confirmation."
    - "Pending, failed, or expired payment returns show inline recovery instead of toast-only feedback."
  artifacts:
    - path: "apps/web/components/booking/payment-deadline-banner.tsx"
      provides: "7-minute payment deadline UI"
      contains: "02:00"
    - path: "apps/web/components/booking/toss-payment-widget.tsx"
      provides: "provider-specific request branching"
      contains: "FOREIGN_EASY_PAY"
    - path: "apps/web/e2e/toss-payment-phase24.spec.ts"
      provides: "deadline/disclaimer/payment-branch E2E"
      contains: "toss-payment"
  key_links:
    - from: "confirm/page.tsx"
      to: "toss-payment-widget.tsx"
      via: "disclaimer-gated requestPayment"
      pattern: "해외 결제 및 환불 유의사항에 동의합니다"
    - from: "complete/page.tsx"
      to: "use-booking.ts"
      via: "pending/failure recovery"
      pattern: "paymentDeadlineAt|pending"
---

<objective>
Implement the frontend payment deadline, overseas disclaimer, and recovery UX.

Purpose: D-15 and BOOK-02 are visible-user-contract requirements; the confirm and complete pages must show the right timer, consent, and failure recovery states for the backend payment branches.
Output: deadline banner, updated Toss widget usage, confirm/complete UI changes, localized copy, and E2E coverage.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-traffic-booking-payment-core/24-CONTEXT.md
@.planning/phases/24-traffic-booking-payment-core/24-UI-SPEC.md
@.planning/phases/24-traffic-booking-payment-core/24-VALIDATION.md
@.planning/milestones/v1.0-phases/04-booking-payment/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render the payment deadline and provider-specific payment UI</name>
  <files>
    apps/web/app/booking/[performanceId]/confirm/page.tsx
    apps/web/app/booking/[performanceId]/complete/page.tsx
    apps/web/components/booking/toss-payment-widget.tsx
    apps/web/components/booking/payment-deadline-banner.tsx
    apps/web/hooks/use-booking.ts
    apps/web/e2e/toss-payment-phase24.spec.ts
  </files>
  <read_first>
    apps/web/app/booking/[performanceId]/confirm/page.tsx
    apps/web/app/booking/[performanceId]/complete/page.tsx
    apps/web/components/booking/toss-payment-widget.tsx
    apps/web/hooks/use-booking.ts
    .planning/phases/24-traffic-booking-payment-core/24-UI-SPEC.md
  </read_first>
  <action>
    Create `payment-deadline-banner.tsx` so confirm/payment shows the separate 7-minute `paymentDeadlineAt` countdown and turns destructive only at `02:00` or less. Update `toss-payment-widget.tsx` and confirm-page CTA enablement so overseas card, Alipay+, and TrueMoney require the dedicated D-15 disclaimer checkbox and use the correct provider request shape from Plan 24-09. In `complete/page.tsx`, handle pending/failed/expired payment recovery inline instead of assuming immediate success.
  </action>
  <acceptance_criteria>
    - `payment-deadline-banner.tsx` exists and contains `02:00`.
    - `confirm/page.tsx` contains `해외 결제 및 환불 유의사항에 동의합니다`.
    - `toss-payment-widget.tsx` contains `FOREIGN_EASY_PAY` and `pendingUrl`.
    - `toss-payment-phase24.spec.ts` covers deadline, disclaimer, and pending/failure return behavior.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/web test -- hooks/__tests__/use-booking.test.tsx && pnpm --filter @grabit/web test:e2e --grep "toss-payment" && pnpm --filter @grabit/web typecheck</automated>
  </verify>
  <done>
    Frontend payment flow reflects the backend branch model and separate payment deadline.
  </done>
</task>

<task type="auto">
  <name>Task 2: Localize payment deadline, disclaimer, and recovery copy</name>
  <files>
    apps/web/messages/ko.json
    apps/web/messages/en.json
    apps/web/messages/th.json
    apps/web/messages/zh-CN.json
    apps/web/messages/zh-TW.json
  </files>
  <read_first>
    apps/web/messages/ko.json
    apps/web/messages/en.json
    apps/web/messages/th.json
    apps/web/messages/zh-CN.json
    apps/web/messages/zh-TW.json
    .planning/phases/24-traffic-booking-payment-core/24-UI-SPEC.md
  </read_first>
  <action>
    Add visible-copy keys for the separate payment deadline, overseas disclaimer, pending completion, payment failure recovery, and expired payment guidance across all five locales. Keep KRW as the primary amount and any FX note secondary per the UI contract.
  </action>
  <acceptance_criteria>
    - All five locale files contain payment deadline, disclaimer, pending, failed, and expired copy.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "paymentDeadline|overseas|pending|expired|refund" apps/web/messages/ko.json apps/web/messages/en.json apps/web/messages/th.json apps/web/messages/zh-CN.json apps/web/messages/zh-TW.json</automated>
  </verify>
  <done>
    Payment UX copy is localized consistently for all Phase 23 launch locales.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| confirm UI -> Toss widget/provider | User consent and method selection leave the app here. |
| payment completion state -> user recovery UI | Backend payment status is rendered into user-visible next steps here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-19 | T | `confirm/page.tsx`, `toss-payment-widget.tsx` | mitigate | Require explicit D-15 disclaimer consent and correct provider-specific request branches before enabling payment submission (Task 1). |
| T-24-20 | D | `payment-deadline-banner.tsx`, `complete/page.tsx` | mitigate | Separate payment-deadline UI from lock expiry and render inline recovery for pending/failed/expired states so users are not silently stranded (Task 1). |
</threat_model>

<verification>
- `pnpm --filter @grabit/web test -- hooks/__tests__/use-booking.test.tsx`
- `pnpm --filter @grabit/web test:e2e --grep "toss-payment"`
- `pnpm --filter @grabit/web typecheck`
- `rg -n "paymentDeadline|overseas|pending|expired|refund" apps/web/messages/ko.json apps/web/messages/en.json apps/web/messages/th.json apps/web/messages/zh-CN.json apps/web/messages/zh-TW.json`
</verification>

<success_criteria>
- Confirm/complete pages show the correct payment deadline, overseas consent gate, and recovery UI.
- Payment-specific copy is localized across all five launch locales.
</success_criteria>

<output>
After completion, create `.planning/phases/24-traffic-booking-payment-core/24-10-SUMMARY.md`
</output>
