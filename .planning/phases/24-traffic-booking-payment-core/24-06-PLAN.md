---
phase: 24-traffic-booking-payment-core
plan: "06"
type: execute
wave: 4
depends_on:
  - "24-03"
files_modified:
  - packages/shared/src/types/performance.types.ts
  - packages/shared/src/schemas/performance.schema.ts
  - apps/api/src/modules/performance/performance.service.ts
  - apps/api/src/modules/performance/performance.service.spec.ts
  - apps/api/src/modules/admin/admin-performance.controller.ts
  - apps/api/src/modules/admin/admin.service.ts
  - apps/api/src/modules/admin/admin.service.spec.ts
  - apps/web/hooks/use-admin.ts
  - apps/web/components/admin/performance-form.tsx
  - apps/web/components/admin/svg-preview.tsx
  - apps/web/components/admin/floor-seat-map-editor.tsx
autonomous: true
requirements:
  - BOOK-01
  - BOOK-03
must_haves:
  truths:
    - "Performance detail and admin save/load contracts expose floor arrays and booking policy instead of one seat-map row."
    - "Operators can input real floor and booking-policy data before user booking UI consumes it."
  artifacts:
    - path: "packages/shared/src/types/performance.types.ts"
      provides: "floor-aware performance read model"
      contains: "seatMaps"
    - path: "apps/api/src/modules/admin/admin.service.ts"
      provides: "multi-floor seat-map persistence"
      contains: "floorKey"
    - path: "apps/web/components/admin/floor-seat-map-editor.tsx"
      provides: "admin floor editor"
      contains: "floorLabel"
  key_links:
    - from: "performance.service.ts"
      to: "performance.types.ts"
      via: "seatMaps + bookingPolicy response"
      pattern: "seatMaps|bookingPolicy"
    - from: "performance-form.tsx"
      to: "admin.service.ts"
      via: "floor editor + policy payload"
      pattern: "floorKey|allowedPaymentMethods"
---

<objective>
Create the floor-aware performance/admin contracts and the minimal admin editing surface.

Purpose: D-10, D-12, D-13, and D-18 require real persisted floor and booking-policy data before the booking runtime can enforce or render it.
Output: floor-aware performance/admin contracts plus a minimal admin floor/policy editor.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-traffic-booking-payment-core/24-CONTEXT.md
@.planning/phases/24-traffic-booking-payment-core/24-RESEARCH.md
@.planning/phases/24-traffic-booking-payment-core/24-UI-SPEC.md
@.planning/milestones/v1.0-phases/04-booking-payment/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend performance/admin contracts to return floor arrays and booking policy</name>
  <files>
    packages/shared/src/types/performance.types.ts
    packages/shared/src/schemas/performance.schema.ts
    apps/api/src/modules/performance/performance.service.ts
    apps/api/src/modules/performance/performance.service.spec.ts
    apps/api/src/modules/admin/admin-performance.controller.ts
    apps/api/src/modules/admin/admin.service.ts
    apps/api/src/modules/admin/admin.service.spec.ts
    apps/web/hooks/use-admin.ts
  </files>
  <read_first>
    packages/shared/src/types/performance.types.ts
    packages/shared/src/schemas/performance.schema.ts
    apps/api/src/modules/performance/performance.service.ts
    apps/api/src/modules/admin/admin.service.ts
    apps/web/hooks/use-admin.ts
  </read_first>
  <action>
    Replace the single `seatMap` assumption with `seatMaps: SeatMapFloor[]` plus `bookingPolicy` across shared types, zod schemas, performance detail reads, admin controller/service payloads, and the admin hook. Preserve backward compatibility by synthesizing default `1F` for legacy rows per D-10, and make `maxTicketsPerUser`, `allowedPaymentMethods`, and change/cancel policy fields explicit per D-12 and D-13.
  </action>
  <acceptance_criteria>
    - `performance.types.ts` contains `seatMaps` and `bookingPolicy`.
    - `performance.schema.ts` validates floor arrays and booking-policy fields.
    - `performance.service.ts` no longer assumes `seatMapRows[0]` as the only seat-map source.
    - `use-admin.ts` carries floor/policy payloads.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/api test -- src/modules/performance/performance.service.spec.ts src/modules/admin/admin.service.spec.ts && pnpm --filter @grabit/api typecheck</automated>
  </verify>
  <done>
    API/shared/admin contracts can carry floor arrays and booking policy end to end.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the minimal multi-floor admin editor and policy controls</name>
  <files>
    apps/web/components/admin/performance-form.tsx
    apps/web/components/admin/svg-preview.tsx
    apps/web/components/admin/floor-seat-map-editor.tsx
  </files>
  <read_first>
    .planning/phases/24-traffic-booking-payment-core/24-UI-SPEC.md
    apps/web/components/admin/performance-form.tsx
    apps/web/components/admin/svg-preview.tsx
    packages/shared/src/types/performance.types.ts
  </read_first>
  <action>
    Build `floor-seat-map-editor.tsx` so operators can manage 2-7 floors with `floorKey`, `floorLabel`, `sortOrder`, `svgUrl`, and `seatConfig`, and wire it into `performance-form.tsx` and `svg-preview.tsx`. Add visible controls for `maxTicketsPerUser`, `allowedPaymentMethods`, `changePolicyEnabled`, and cancelled-seat hold range. Reuse the existing SVG safety behavior rather than replacing it.
  </action>
  <acceptance_criteria>
    - `floor-seat-map-editor.tsx` exists and contains `floorKey`, `floorLabel`, and `sortOrder`.
    - `performance-form.tsx` renders booking-policy controls.
    - `svg-preview.tsx` still performs existing SVG preview/safety behavior while supporting multiple floors.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/web test -- components/admin && pnpm --filter @grabit/web typecheck</automated>
  </verify>
  <done>
    Operators can save real multi-floor and policy data without waiting for Phase 25.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin floor/policy input -> API persistence | Operator input becomes authoritative booking behavior here. |
| performance detail API -> booking runtime | User-facing booking UI reads these floor/policy contracts here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-11 | T | `admin.service.ts`, `performance.service.ts` | mitigate | Persist concrete `floorKey`, `floorLabel`, `sortOrder`, and policy fields so downstream booking code does not infer them from ambiguous UI state (Task 1). |
| T-24-12 | R | `floor-seat-map-editor.tsx` | mitigate | Reuse the existing SVG safety workflow and persist real payloads rather than future-only placeholders (Task 2). |
</threat_model>

<verification>
- `pnpm --filter @grabit/api test -- src/modules/performance/performance.service.spec.ts src/modules/admin/admin.service.spec.ts`
- `pnpm --filter @grabit/api typecheck`
- `pnpm --filter @grabit/web test -- components/admin`
- `pnpm --filter @grabit/web typecheck`
</verification>

<success_criteria>
- Performance/admin contracts expose real floor arrays and booking policy.
- Admin UI can save multi-floor and policy data without waiting for later console work.
</success_criteria>

<output>
After completion, create `.planning/phases/24-traffic-booking-payment-core/24-06-SUMMARY.md`
</output>
