---
phase: 24-traffic-booking-payment-core
plan: "06"
type: execute
wave: 4
depends_on:
  - "24-03"
files_modified:
  - packages/shared/src/types/performance.types.ts
  - packages/shared/src/schemas/performance.schema.ts
  - apps/api/src/modules/performance/performance.service.ts
  - apps/api/src/modules/performance/performance.service.spec.ts
  - apps/api/src/modules/admin/admin-performance.controller.ts
  - apps/api/src/modules/admin/admin.service.ts
  - apps/api/src/modules/admin/admin.service.spec.ts
autonomous: true
requirements:
  - BOOK-01
  - BOOK-03
must_haves:
  truths:
    - "Performance detail and admin save/load contracts expose floor arrays and booking policy instead of one seat-map row."
    - "Legacy single-floor seat-map rows still resolve as default `1F` so downstream booking code can migrate without breaking existing records."
  artifacts:
    - path: "packages/shared/src/types/performance.types.ts"
      provides: "floor-aware performance read model"
      contains: "seatMaps"
    - path: "apps/api/src/modules/admin/admin.service.ts"
      provides: "multi-floor seat-map persistence"
      contains: "floorKey"
    - path: "apps/api/src/modules/performance/performance.service.ts"
      provides: "floor-aware performance detail response"
      contains: "bookingPolicy"
  key_links:
    - from: "performance.service.ts"
      to: "performance.types.ts"
      via: "seatMaps + bookingPolicy response"
      pattern: "seatMaps|bookingPolicy"
    - from: "admin-performance.controller.ts"
      to: "admin.service.ts"
      via: "floor/policy payload validation and persistence"
      pattern: "floorKey|maxTicketsPerUser|allowedPaymentMethods"
---

<objective>
Create the floor-aware performance/admin contracts and persistence layer.

Purpose: D-10, D-12, D-13, and D-18 require real persisted floor and booking-policy data before booking runtime, payment, refund, and admin UI plans can enforce or render it.
Output: floor-aware shared contracts, performance detail reads, and admin persistence/controller support.
Decision coverage: D-10: performance/admin contracts expose floor arrays with default `1F` compatibility for legacy rows. D-12: booking policy becomes event-configured contract data instead of an implicit UI rule. D-13: max-ticket and payment-method policy leaves hardcoded web/API assumptions behind. D-18: persistence exposes the manual-open-supporting policy state required before richer Phase 25 admin tooling.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-traffic-booking-payment-core/24-CONTEXT.md
@.planning/phases/24-traffic-booking-payment-core/24-RESEARCH.md
@.planning/phases/24-traffic-booking-payment-core/24-UI-SPEC.md
@.planning/milestones/v1.0-phases/04-booking-payment/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend shared contracts and performance detail reads for floor arrays and booking policy</name>
  <files>
    packages/shared/src/types/performance.types.ts
    packages/shared/src/schemas/performance.schema.ts
    apps/api/src/modules/performance/performance.service.ts
    apps/api/src/modules/performance/performance.service.spec.ts
  </files>
  <read_first>
    packages/shared/src/types/performance.types.ts
    packages/shared/src/schemas/performance.schema.ts
    apps/api/src/modules/performance/performance.service.ts
    apps/api/src/database/schema/seat-maps.ts
  </read_first>
  <action>
    Replace the single `seatMap` assumption with `seatMaps: SeatMapFloor[]` plus `bookingPolicy` across shared types, zod schemas, and performance detail reads. Preserve backward compatibility by synthesizing default `1F` for legacy rows per D-10, and expose explicit `maxTicketsPerUser`, `allowedPaymentMethods`, `changePolicyEnabled`, and cancelled-seat hold policy fields per D-12 and D-13 so downstream booking/payment plans do not infer policy from UI-only state.
  </action>
  <acceptance_criteria>
    - `performance.types.ts` contains `seatMaps` and `bookingPolicy`.
    - `performance.schema.ts` validates floor arrays and booking-policy fields.
    - `performance.service.ts` no longer assumes `seatMapRows[0]` as the only seat-map source.
    - `performance.service.spec.ts` covers the default `1F` compatibility path.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/api test -- src/modules/performance/performance.service.spec.ts && pnpm --filter @grabit/api typecheck</automated>
  </verify>
  <done>
    Shared contracts and performance detail reads carry floor arrays and booking policy end to end.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist floor-aware admin payloads without the old one-seat-map assumption</name>
  <files>
    apps/api/src/modules/admin/admin-performance.controller.ts
    apps/api/src/modules/admin/admin.service.ts
    apps/api/src/modules/admin/admin.service.spec.ts
  </files>
  <read_first>
    apps/api/src/modules/admin/admin.service.ts
    apps/api/src/modules/admin/admin-performance.controller.ts
    packages/shared/src/types/performance.types.ts
  </read_first>
  <action>
    Update admin controller and service payload handling so save/load flows persist `seatMaps` and `bookingPolicy` as authoritative server-side data instead of a single seat-map row. Keep D-10 compatibility for existing rows, make D-18's manual-open-supporting policy fields explicit in persistence, and ensure the controller/service boundary validates floor/policy structure before later admin UI plans submit it.
  </action>
  <acceptance_criteria>
    - `admin-performance.controller.ts` accepts floor/policy payloads.
    - `admin.service.ts` persists floor rows and booking policy without assuming a single seat map.
    - `admin.service.spec.ts` covers multi-floor save/load behavior.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/api test -- src/modules/admin/admin.service.spec.ts && pnpm --filter @grabit/api typecheck</automated>
  </verify>
  <done>
    Server-side admin persistence is ready for the separate admin editor/upload plan without hidden single-floor assumptions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin floor/policy payload -> API persistence | Operator input becomes authoritative booking behavior here. |
| performance detail API -> booking/payment runtime | User-facing booking code reads these floor/policy contracts here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-11 | T | `performance.service.ts`, `admin.service.ts` | mitigate | Persist concrete `floorKey`, `floorLabel`, `sortOrder`, and booking policy fields per D-10/D-12/D-13 so downstream booking code does not infer them from ambiguous state (Tasks 1-2). |
| T-24-12 | E | `admin-performance.controller.ts` | mitigate | Validate floor/policy payload shape before persistence so later admin editors cannot smuggle unsupported booking behavior into runtime state (Task 2). |
</threat_model>

<verification>
- `pnpm --filter @grabit/api test -- src/modules/performance/performance.service.spec.ts`
- `pnpm --filter @grabit/api test -- src/modules/admin/admin.service.spec.ts`
- `pnpm --filter @grabit/api typecheck`
</verification>

<success_criteria>
- Performance/admin contracts expose real floor arrays and booking policy.
- Legacy single-floor data still resolves as default `1F`.
- Admin persistence/controller paths are ready for the separate admin editor/upload plan.
</success_criteria>

<output>
After completion, create `.planning/phases/24-traffic-booking-payment-core/24-06-SUMMARY.md`
</output>
