---
phase: 24-traffic-booking-payment-core
plan: "21"
type: execute
wave: 12
depends_on:
  - "24-05"
files_modified:
  - .github/workflows/deploy.yml
  - docs/runbooks/phase24-queue-waf-prewarm.md
  - .planning/phases/24-traffic-booking-payment-core/24-HUMAN-UAT.md
autonomous: true
gap_closure: true
requirements:
  - TRAF-03
must_haves:
  truths:
    - "The deployed `grabit-api` service exposes the protected prewarm routes and no longer returns `404` for the documented endpoints."
    - "Cloud Scheduler `scale-up` and `step-down` jobs exist in `asia-northeast3` and target the same URI/audience contract that the API validates."
    - "Running the live Scheduler jobs leaves auditable evidence that OIDC plus `x-prewarm-control-token` reached the deployed prewarm endpoint successfully."
  artifacts:
    - path: ".github/workflows/deploy.yml"
      provides: "durable Cloud Run deploy contract for all five `PREWARM_*` settings"
      contains: "PREWARM_ALLOWED_AUDIENCE"
    - path: "docs/runbooks/phase24-queue-waf-prewarm.md"
      provides: "actual production rollout and verification steps for prewarm scale-up and step-down"
      contains: "grabit-prewarm-scale-up"
    - path: ".planning/phases/24-traffic-booking-payment-core/24-HUMAN-UAT.md"
      provides: "resolved Human UAT evidence for test 2"
      contains: "test: 2"
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "docs/runbooks/phase24-queue-waf-prewarm.md"
      via: "shared `PREWARM_*` secret names and target audience contract"
      pattern: "PREWARM_ALLOWED_AUDIENCE"
    - from: "docs/runbooks/phase24-queue-waf-prewarm.md"
      to: "apps/api/src/modules/ops/prewarm.controller.ts"
      via: "documented live URI for `/api/v1/internal/prewarm/services/grabit-api` and `/step-down`"
      pattern: "internal/prewarm/services/grabit-api"
    - from: ".planning/phases/24-traffic-booking-payment-core/24-HUMAN-UAT.md"
      to: "docs/runbooks/phase24-queue-waf-prewarm.md"
      via: "recorded job names, audience, and verification commands"
      pattern: "grabit-prewarm-scale-up"
---

<objective>
Close the Phase 24 HUMAN UAT blocker where Cloud Scheduler prewarm is specified locally but not actually deployed or reachable in GCP.

Purpose: TRAF-03 is not complete until the live `grabit-api` service exposes the protected prewarm routes, retains the `PREWARM_*` contract on deploy, and the real Scheduler jobs can call those routes with OIDC plus the control token.
Output: durable deploy wiring for prewarm secrets, a production-valid runbook, live GCP Scheduler/resources, and a resolved Human UAT record for test 2.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-traffic-booking-payment-core/24-CONTEXT.md
@.planning/phases/24-traffic-booking-payment-core/24-HUMAN-UAT.md
@.planning/phases/24-traffic-booking-payment-core/24-05-SUMMARY.md
@.github/workflows/deploy.yml
@docs/runbooks/phase24-queue-waf-prewarm.md
@apps/api/src/modules/ops/prewarm.controller.ts
@apps/api/src/modules/ops/prewarm.service.ts
@apps/api/src/app.module.ts
@apps/api/Dockerfile

<interfaces>
From `docs/runbooks/phase24-queue-waf-prewarm.md`:
```text
POST /api/v1/internal/prewarm/services/:serviceName
POST /api/v1/internal/prewarm/services/:serviceName/step-down
```

From `apps/api/src/modules/ops/prewarm.service.ts`:
```ts
export const PREWARM_CONTROL_TOKEN = 'PREWARM_CONTROL_TOKEN';
export const PREWARM_PROJECT_ID = 'PREWARM_PROJECT_ID';
export const PREWARM_REGION = 'PREWARM_REGION';
export const PREWARM_ALLOWED_SCHEDULER_EMAIL = 'PREWARM_ALLOWED_SCHEDULER_EMAIL';
export const PREWARM_ALLOWED_AUDIENCE = 'PREWARM_ALLOWED_AUDIENCE';
```

From `.github/workflows/deploy.yml`:
```yaml
env_vars:
  NODE_ENV=production
  VALKEY_MODE=cluster
  FRONTEND_URL=${{ vars.CLOUD_RUN_WEB_URL }}
secrets:
  DATABASE_URL=database-url:latest
  JWT_SECRET=jwt-secret:latest
  ...
  TOSS_SECRET_KEY=toss-secret-key:latest
```
</interfaces>

<gap_diagnosis>
Human UAT test 2 proved the code contract from Plan 24-05 exists only locally today: `cloudscheduler.googleapis.com` is disabled, no Scheduler jobs can be listed, the deployed `grabit-api` returns `404` on the documented prewarm routes, and the live Cloud Run env does not include any `PREWARM_*` settings. The fix is operational, but it must also be durable across future deploys, otherwise the next release will drop the prewarm contract again.
</gap_diagnosis>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the prewarm deploy contract durable in CI and the runbook</name>
  <files>
    .github/workflows/deploy.yml
    docs/runbooks/phase24-queue-waf-prewarm.md
  </files>
  <read_first>
    .github/workflows/deploy.yml
    docs/runbooks/phase24-queue-waf-prewarm.md
    .planning/phases/24-traffic-booking-payment-core/24-05-SUMMARY.md
  </read_first>
  <action>
    Update the API deploy contract so the five prewarm settings introduced in Plan 24-05 survive future deploys instead of existing only as local assumptions. Add the following Secret Manager mappings to `.github/workflows/deploy.yml` for the `deploy-api` job: `PREWARM_CONTROL_TOKEN=prewarm-control-token:latest`, `PREWARM_PROJECT_ID=prewarm-project-id:latest`, `PREWARM_REGION=prewarm-region:latest`, `PREWARM_ALLOWED_SCHEDULER_EMAIL=prewarm-allowed-scheduler-email:latest`, and `PREWARM_ALLOWED_AUDIENCE=prewarm-allowed-audience:latest`.

    In `docs/runbooks/phase24-queue-waf-prewarm.md`, keep the D-06 progressive-defense language intact and tighten the TRAF-03 rollout contract:
    - explicitly require `cloudscheduler.googleapis.com` enablement for `grapit-491806`;
    - pin the live job names to `grabit-prewarm-scale-up` and `grabit-prewarm-step-down`;
    - pin the Scheduler service account to `scheduler-prewarm@grapit-491806.iam.gserviceaccount.com` unless the project already has an equivalent dedicated principal;
    - require `PREWARM_ALLOWED_AUDIENCE` to equal the exact URL the job calls, not a stale hardcoded domain, so `run.app` and custom-domain deployments cannot drift;
    - document that this gap must not change the app-layer route contract or the OIDC + `x-prewarm-control-token` dual-factor guard from Plan 24-05.

    Reference D-06 by preserving the layered defense posture and TRAF-03 by turning the documented prewarm contract into a deploy-time invariant.
  </action>
  <verify>
    <automated>rg -n "PREWARM_CONTROL_TOKEN=prewarm-control-token:latest|PREWARM_PROJECT_ID=prewarm-project-id:latest|PREWARM_REGION=prewarm-region:latest|PREWARM_ALLOWED_SCHEDULER_EMAIL=prewarm-allowed-scheduler-email:latest|PREWARM_ALLOWED_AUDIENCE=prewarm-allowed-audience:latest" .github/workflows/deploy.yml && rg -n "cloudscheduler.googleapis.com|grabit-prewarm-scale-up|grabit-prewarm-step-down|scheduler-prewarm@grapit-491806|PREWARM_ALLOWED_AUDIENCE" docs/runbooks/phase24-queue-waf-prewarm.md</automated>
  </verify>
  <done>The deploy workflow and runbook both encode the same live `PREWARM_*` contract, so future deploys cannot silently drop the prewarm env surface again.</done>
</task>

<task type="auto">
  <name>Task 2: Roll out and verify the live GCP prewarm path for `grabit-api`</name>
  <files>
    docs/runbooks/phase24-queue-waf-prewarm.md
    .planning/phases/24-traffic-booking-payment-core/24-HUMAN-UAT.md
  </files>
  <read_first>
    docs/runbooks/phase24-queue-waf-prewarm.md
    .planning/phases/24-traffic-booking-payment-core/24-HUMAN-UAT.md
    .github/workflows/deploy.yml
    apps/api/src/modules/ops/prewarm.service.ts
  </read_first>
  <action>
    Operationalize TRAF-03 in project `grapit-491806` without changing application source code.

    Required rollout sequence:
    - Enable `cloudscheduler.googleapis.com` in `asia-northeast3`.
    - Create or reconcile the service account `scheduler-prewarm@grapit-491806.iam.gserviceaccount.com`.
    - Discover the runtime service account used by the deployed `grabit-api` service and grant the minimal permission needed to update `grabit-api` scaling through the Cloud Run Admin API. If a narrower binding is not already available, use the pragmatic fallback of `roles/run.admin` and record the exact binding in the runbook/UAT evidence.
    - Create or update the five Secret Manager secrets named in Task 1. Generate a fresh random value for `prewarm-control-token`; store `grapit-491806`, `asia-northeast3`, the dedicated Scheduler service-account email, and the exact live prewarm target URL in the other four secrets.
    - Deploy the latest API revision from the current repo, using `apps/api/Dockerfile` and the same env/secrets contract as `.github/workflows/deploy.yml`, so the live service actually exposes `/api/v1/internal/prewarm/services/grabit-api` and `/api/v1/internal/prewarm/services/grabit-api/step-down`.
    - Create or update the `grabit-prewarm-scale-up` and `grabit-prewarm-step-down` HTTP jobs in `asia-northeast3` with `POST`, OIDC, `x-prewarm-control-token`, and the same audience/URI stored in `PREWARM_ALLOWED_AUDIENCE`.
    - Smoke the live surface end to end:
      1. anonymous `curl -X POST` to both endpoints must return `401` or `403`, never `404`;
      2. `gcloud scheduler jobs run` must succeed for both jobs;
      3. capture fresh Cloud Scheduler or Cloud Run log evidence showing each job reached the endpoint;
      4. capture the resulting scale-up/step-down evidence or Cloud Run operation name in the UAT notes.

    Do not leave the runbook with `api.heygrabit.com` if the real target URL is still the `run.app` hostname. Use whichever URL is truly deployed, and make the runbook, secret value, and job audience all match.
  </action>
  <verify>
    <automated>TARGET_URL="$(gcloud run services describe grabit-api --region=asia-northeast3 --format='value(status.url)')" && for path in "" "/step-down"; do code="$(curl -sS -o /tmp/grapit-prewarm-response.txt -w '%{http_code}' -X POST "${TARGET_URL}/api/v1/internal/prewarm/services/grabit-api${path}")"; [[ "$code" = "401" || "$code" = "403" ]] || { echo "unexpected status for ${path}: ${code}"; exit 1; }; done && gcloud services list --enabled --filter='name:cloudscheduler.googleapis.com' && gcloud scheduler jobs describe grabit-prewarm-scale-up --location=asia-northeast3 && gcloud scheduler jobs describe grabit-prewarm-step-down --location=asia-northeast3 && gcloud scheduler jobs run grabit-prewarm-scale-up --location=asia-northeast3 && gcloud scheduler jobs run grabit-prewarm-step-down --location=asia-northeast3 && gcloud logging read 'resource.type="cloud_run_revision" AND resource.labels.service_name="grabit-api" AND ("internal/prewarm/services/grabit-api" OR "internal/prewarm/services/grabit-api/step-down")' --freshness=15m --limit=20 --format='value(timestamp,textPayload,jsonPayload.message,httpRequest.requestUrl)' | rg 'internal/prewarm/services/grabit-api' && gcloud run services describe grabit-api --region=asia-northeast3 --format='export' | rg 'PREWARM_(CONTROL_TOKEN|PROJECT_ID|REGION|ALLOWED_SCHEDULER_EMAIL|ALLOWED_AUDIENCE)'</automated>
  </verify>
  <done>The live `grabit-api` service exposes the protected prewarm routes, the Scheduler jobs exist and can be run, and the recorded evidence proves the jobs no longer fail at the missing-route or missing-env stage.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve Human UAT test 2 and summarize the operational rollout</name>
  <files>
    .planning/phases/24-traffic-booking-payment-core/24-HUMAN-UAT.md
    .planning/phases/24-traffic-booking-payment-core/24-21-SUMMARY.md
  </files>
  <action>
    Update `24-HUMAN-UAT.md` so the prewarm gap for test 2 is no longer recorded as failed once Task 2 evidence exists. Record the final live audience URL, the runtime service account used for Cloud Run scaling, the Scheduler service account, the exact job names, the commands run, and the log/evidence paths. Then create `24-21-SUMMARY.md` with the rollout steps, any IAM deviations, and the final verification results.
  </action>
  <verify>
    <automated>rg -n "test: 2|grabit-prewarm-scale-up|PREWARM_ALLOWED_AUDIENCE|status: resolved|status: closed" .planning/phases/24-traffic-booking-payment-core/24-HUMAN-UAT.md .planning/phases/24-traffic-booking-payment-core/24-21-SUMMARY.md</automated>
  </verify>
  <done>Phase 24 Human UAT no longer treats Cloud Scheduler prewarm as an unresolved operational blocker, and the execution record is resumable from repo artifacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cloud Scheduler OIDC caller -> internal prewarm endpoint | An external scheduled caller crosses into a privileged scaling control path. |
| CI deploy contract -> Cloud Run runtime env | Missing secret mappings can silently remove the prewarm protection or target metadata on the next deploy. |
| Cloud Run runtime service account -> Cloud Run Admin API | The app must have just enough permission to patch service-level min instances without widening the control plane unnecessarily. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-21-01 | S/E | `apps/api/src/modules/ops/prewarm.controller.ts` live surface | mitigate | Preserve the existing dual-factor contract from Plan 24-05 and verify the real job uses matching OIDC audience/email plus `x-prewarm-control-token` (Tasks 1-2). |
| T-24-21-02 | T | `.github/workflows/deploy.yml` | mitigate | Add durable `PREWARM_*` Secret Manager mappings so future deploys cannot strip the live prewarm contract (Task 1). |
| T-24-21-03 | D | GCP Scheduler / Cloud Run operations | mitigate | Enable Scheduler API, create/update the exact jobs, and prove end-to-end job execution with current logs and service evidence instead of trusting static docs (Task 2). |
| T-24-21-04 | R | `.planning/phases/24-traffic-booking-payment-core/24-HUMAN-UAT.md` | mitigate | Record the exact audience URL, service accounts, commands, and log evidence in the UAT artifact and summary (Task 3). |
</threat_model>

<verification>
- `rg -n "PREWARM_CONTROL_TOKEN=prewarm-control-token:latest|PREWARM_ALLOWED_AUDIENCE=prewarm-allowed-audience:latest" .github/workflows/deploy.yml`
- `gcloud services list --enabled --filter='name:cloudscheduler.googleapis.com'`
- `gcloud scheduler jobs describe grabit-prewarm-scale-up --location=asia-northeast3`
- `gcloud scheduler jobs describe grabit-prewarm-step-down --location=asia-northeast3`
- `gcloud run services describe grabit-api --region=asia-northeast3 --format='export' | rg 'PREWARM_'`
- capture the fresh Scheduler/Cloud Run evidence referenced from `24-HUMAN-UAT.md`
</verification>

<success_criteria>
- `grabit-api` no longer returns `404` for the documented prewarm routes.
- The deploy workflow permanently carries the five `PREWARM_*` settings through future API deploys.
- `grabit-prewarm-scale-up` and `grabit-prewarm-step-down` exist in `asia-northeast3`, use OIDC plus `x-prewarm-control-token`, and leave auditable success evidence.
- `24-HUMAN-UAT.md` records Human UAT test 2 as resolved with reproducible commands and identifiers.
</success_criteria>

<output>
After completion, create `.planning/phases/24-traffic-booking-payment-core/24-21-SUMMARY.md`
</output>
