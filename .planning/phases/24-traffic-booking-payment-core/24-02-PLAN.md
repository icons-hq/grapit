---
phase: 24-traffic-booking-payment-core
plan: "02"
type: execute
wave: 2
depends_on:
  - "24-01"
files_modified:
  - apps/api/src/modules/queue/queue.module.ts
  - apps/api/src/modules/queue/queue.controller.ts
  - apps/api/src/modules/queue/queue.service.ts
  - apps/api/src/modules/queue/queue.gateway.ts
  - apps/api/src/modules/queue/guards/admission.guard.ts
  - apps/api/src/modules/queue/queue.service.spec.ts
  - apps/api/src/modules/queue/queue.guard.spec.ts
  - apps/api/src/modules/traffic/traffic.module.ts
  - apps/api/src/modules/traffic/traffic-defense.service.ts
  - apps/api/src/modules/traffic/traffic-defense.service.spec.ts
  - apps/api/src/modules/ops/prewarm.module.ts
  - apps/api/src/modules/ops/prewarm.controller.ts
  - apps/api/src/modules/ops/prewarm.service.ts
  - apps/api/src/modules/ops/prewarm.service.spec.ts
  - apps/api/src/modules/booking/booking.controller.ts
  - apps/api/src/modules/reservation/reservation.controller.ts
  - apps/api/src/app.module.ts
  - docs/runbooks/phase24-queue-waf-prewarm.md
autonomous: true
requirements:
  - TRAF-01
  - TRAF-02
  - TRAF-03
user_setup:
  - service: cloudflare
    why: "TRAF-02 edge WAF/rate-limit/challenge rules must exist in the real zone even after API-side traffic logic lands."
    dashboard_config:
      - task: "Create queue-entry, lock-seat, prepare-reservation, confirm-payment, signup, login, and SMS rules with Managed Challenge / Block actions from the runbook."
        location: "Cloudflare Dashboard -> Security -> WAF / Rate Limiting Rules"
      - task: "Confirm whether Enterprise-only `cf.bot_management.*` fields are available; if not, apply the documented non-Enterprise fallback rule set."
        location: "Cloudflare Dashboard -> Security -> WAF"
  - service: google-cloud-scheduler
    why: "TRAF-03 requires prewarm and step-down jobs that call the protected internal control path."
    env_vars:
      - name: PREWARM_CONTROL_TOKEN
        source: "Secret Manager / Cloud Run env for the API service"
      - name: PREWARM_PROJECT_ID
        source: "GCP project id for the target Cloud Run services"
      - name: PREWARM_REGION
        source: "Cloud Run region, expected asia-northeast3"
    dashboard_config:
      - task: "Create POST Scheduler jobs for scale-up and scale-down against the documented internal endpoint."
        location: "Cloud Scheduler -> Jobs"
must_haves:
  truths:
    - "Only admitted users can reach booking mutations, and the admission token is bound to user/session/device context."
    - "Queue waiting state exposes position, ETA, remaining seats, and auto-entry data instead of a blind redirect loop."
    - "Traffic defense differentiates queue redirect, rate-limit retry, security challenge, and hard block states."
    - "Prewarm uses a protected intermediary control path instead of assuming Cloud Scheduler can PATCH Cloud Run directly."
  artifacts:
    - path: "apps/api/src/modules/queue/queue.service.ts"
      provides: "Valkey admission queue with active/grace window handling"
      contains: "queueSessionId"
    - path: "apps/api/src/modules/queue/guards/admission.guard.ts"
      provides: "API-side booking mutation admission enforcement"
      contains: "grabit_queue_admission"
    - path: "apps/api/src/modules/traffic/traffic-defense.service.ts"
      provides: "Endpoint-specific rate and macro decision engine"
      contains: "SECURITY_CHALLENGE_REQUIRED"
    - path: "apps/api/src/modules/ops/prewarm.service.ts"
      provides: "Protected prewarm control path"
      contains: "PREWARM_CONTROL_TOKEN"
    - path: "docs/runbooks/phase24-queue-waf-prewarm.md"
      provides: "Cloudflare and Scheduler operator runbook"
      contains: "Managed Challenge"
  key_links:
    - from: "apps/api/src/modules/queue/guards/admission.guard.ts"
      to: "apps/api/src/modules/booking/booking.controller.ts"
      via: "guarded lock-seat endpoint"
      pattern: "AdmissionGuard"
    - from: "apps/api/src/modules/queue/queue.service.ts"
      to: "apps/api/src/modules/reservation/reservation.controller.ts"
      via: "guarded prepare/confirm endpoints"
      pattern: "confirmPayment|prepareReservation"
    - from: "docs/runbooks/phase24-queue-waf-prewarm.md"
      to: "apps/api/src/modules/ops/prewarm.controller.ts"
      via: "documented POST control path"
      pattern: "POST /api/v1/internal/prewarm"
---

<objective>
Implement admission queue, traffic defense, and the prewarm control-path/runbook for Phase 24.

Purpose: TRAF-01 through TRAF-03 must exist before user-facing multi-floor booking or payment flows can be trusted under traffic.
Output: queue APIs/guard/gateway, traffic-defense service, protected prewarm endpoint, and operator runbook artifacts.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/24-traffic-booking-payment-core/24-CONTEXT.md
@.planning/phases/24-traffic-booking-payment-core/24-RESEARCH.md
@.planning/phases/24-traffic-booking-payment-core/24-VALIDATION.md
@.planning/phases/24-traffic-booking-payment-core/24-PATTERNS.md
@.planning/phases/23-launch-foundation/23-03-SUMMARY.md
@.planning/phases/23-launch-foundation/23-08-SUMMARY.md

<interfaces>
From `apps/api/src/modules/booking/booking.service.ts`:
```ts
async lockSeat(userId: string, showtimeId: string, seatId: string): Promise<LockSeatResponse>;
export const PAYMENT_CONFIRM_LOCK_TTL = 60;
```

From `packages/shared/src/types/booking.types.ts`:
```ts
export interface SeatStatusResponse {
  showtimeId: string;
  seats: Record<string, SeatState>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the admission queue service, guard, and mutation enforcement</name>
  <files>
    apps/api/src/modules/queue/queue.module.ts
    apps/api/src/modules/queue/queue.controller.ts
    apps/api/src/modules/queue/queue.service.ts
    apps/api/src/modules/queue/queue.gateway.ts
    apps/api/src/modules/queue/guards/admission.guard.ts
    apps/api/src/modules/queue/queue.service.spec.ts
    apps/api/src/modules/queue/queue.guard.spec.ts
    apps/api/src/modules/booking/booking.controller.ts
    apps/api/src/modules/reservation/reservation.controller.ts
  </files>
  <read_first>
    .planning/phases/24-traffic-booking-payment-core/24-CONTEXT.md
    .planning/phases/24-traffic-booking-payment-core/24-RESEARCH.md
    .planning/phases/24-traffic-booking-payment-core/24-VALIDATION.md
    .planning/phases/24-traffic-booking-payment-core/24-PATTERNS.md
    apps/api/src/modules/booking/booking.controller.ts
    apps/api/src/modules/reservation/reservation.controller.ts
    apps/api/src/modules/booking/booking.gateway.ts
    apps/api/src/modules/booking/booking.service.ts
  </read_first>
  <behavior>
    - Queue entry starts at `/booking` and not on detail/signup surfaces, and queue session state includes `position`, `waitingCount`, `etaSeconds`, `remainingSeats`, and `autoEnter` (D-01, D-05).
    - `lockSeat`, `prepareReservation`, and `confirmPayment` reject missing or mismatched admission tokens at the API boundary (D-02).
    - Admission is bound to `userId + refresh token family/device slot + queueSessionId`, with a 10-minute active window and 180-second grace/re-entry semantics (D-03, D-04).
  </behavior>
  <action>
    Create a new queue module with `queue.service.ts`, `queue.controller.ts`, `queue.gateway.ts`, and `guards/admission.guard.ts`. Use Valkey sorted sets plus hash-tagged keys for waiting and active sessions, matching the existing Lua/Redis style from `BookingService`. Implement `POST /api/v1/queue/performances/:performanceId/enter` and `GET /api/v1/queue/sessions/:queueSessionId` so the API can create/reuse a queue session, compute rank/ETA/remaining seats, and set an httpOnly `grabit_queue_admission` cookie when the session becomes admitted. The queue session identity must include authenticated `userId`, refresh-family/device-slot metadata, and `queueSessionId` per D-03. Apply `AdmissionGuard` to `POST /api/v1/booking/seats/lock`, `POST /api/v1/reservations/prepare`, and `POST /api/v1/payments/confirm` by modifying the existing booking/reservation controllers, not only the web router. Return explicit machine-readable states such as `WAITING`, `ADMITTED`, and `EXPIRED`; do not leave queue admission as an implicit redirect.
  </action>
  <acceptance_criteria>
    - `apps/api/src/modules/queue/queue.controller.ts` contains the exact route string `queue/performances/:performanceId/enter`.
    - `apps/api/src/modules/queue/guards/admission.guard.ts` contains `grabit_queue_admission` and validates `userId`, `queueSessionId`, and device-family/session binding.
    - `apps/api/src/modules/booking/booking.controller.ts` and `apps/api/src/modules/reservation/reservation.controller.ts` both reference `AdmissionGuard`.
    - `apps/api/src/modules/queue/queue.service.ts` contains `WAITING`, `ADMITTED`, `EXPIRED`, `etaSeconds`, and `remainingSeats`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/api test -- src/modules/queue/queue.service.spec.ts src/modules/queue/queue.guard.spec.ts</automated>
  </verify>
  <done>
    Queue state exists as a first-class API contract, and booking mutations are rejected when admission is missing or invalid.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add traffic-defense decisions for rate limits, macro scoring, and failure categorization</name>
  <files>
    apps/api/src/modules/traffic/traffic.module.ts
    apps/api/src/modules/traffic/traffic-defense.service.ts
    apps/api/src/modules/traffic/traffic-defense.service.spec.ts
    apps/api/src/app.module.ts
  </files>
  <read_first>
    .planning/phases/24-traffic-booking-payment-core/24-CONTEXT.md
    .planning/phases/24-traffic-booking-payment-core/24-RESEARCH.md
    .planning/phases/24-traffic-booking-payment-core/24-VALIDATION.md
    apps/api/src/app.module.ts
    apps/api/src/modules/booking/booking.service.ts
  </read_first>
  <behavior>
    - Per-endpoint traffic rules use the richest available identity context (`userId`, session cookie, admission token, IP) rather than IP-only throttling (D-07).
    - Traffic failures are categorized into queue redirect, rate-limit retry, security challenge, and hard block so the web layer can localize them distinctly (D-08).
    - Booking-critical macro scoring counts repeated `lock/prepare/confirm` attempts across account, phone, email, payment method, device-ish hash, and admission token without inventing a full fraud graph (D-06, D-09).
  </behavior>
  <action>
    Add a `traffic` module and service that centralize Phase 24 traffic decisions. Extend the existing throttler setup in `app.module.ts` with named booking-critical policies (`queue-entry`, `lock-seat`, `prepare-reservation`, `confirm-payment`, `signup`, `sms`). Compose identity keys in the exact order required by D-07: `userId`, session cookie, admission token, IP. Implement macro counters in Valkey for repeated booking attempts across account/phone/email/paymentMethod/device hash/admission token combinations; when thresholds are exceeded, return explicit decision codes `TRAFFIC_RATE_LIMITED`, `SECURITY_CHALLENGE_REQUIRED`, or `SECURITY_BLOCKED`. Do not reference Enterprise-only `cf.bot_management.*` fields in runtime code; instead, keep Cloudflare-specific rule expressions in the runbook and implement the fallback app-layer macro logic in code per the research caveat.
  </action>
  <acceptance_criteria>
    - `apps/api/src/modules/traffic/traffic-defense.service.ts` contains the strings `queue-entry`, `lock-seat`, `prepare-reservation`, `confirm-payment`, `TRAFFIC_RATE_LIMITED`, `SECURITY_CHALLENGE_REQUIRED`, and `SECURITY_BLOCKED`.
    - `apps/api/src/app.module.ts` references the new traffic module or provider wiring.
    - `apps/api/src/modules/traffic/traffic-defense.service.spec.ts` asserts decisions using at least `userId`, session cookie, admission token, and IP inputs.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/api test -- src/modules/traffic/traffic-defense.service.spec.ts && pnpm --filter @grabit/api typecheck</automated>
  </verify>
  <done>
    The API can make deterministic queue/rate/challenge/block decisions for booking-critical traffic without relying on raw Cloudflare plan assumptions inside runtime code.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement the protected prewarm control path and operator runbook</name>
  <files>
    apps/api/src/modules/ops/prewarm.module.ts
    apps/api/src/modules/ops/prewarm.controller.ts
    apps/api/src/modules/ops/prewarm.service.ts
    apps/api/src/modules/ops/prewarm.service.spec.ts
    docs/runbooks/phase24-queue-waf-prewarm.md
  </files>
  <read_first>
    AGENTS.md
    .planning/phases/24-traffic-booking-payment-core/24-CONTEXT.md
    .planning/phases/24-traffic-booking-payment-core/24-RESEARCH.md
    .planning/phases/24-traffic-booking-payment-core/24-VALIDATION.md
    apps/api/src/app.module.ts
  </read_first>
  <behavior>
    - Prewarm uses an authenticated intermediary POST control path and does not assume Cloud Scheduler can directly PATCH Cloud Run scaling (research pitfall 4).
    - The runbook names exact Cloudflare rule groups, prewarm scale-up/scale-down jobs, and the non-Enterprise fallback when bot-score fields are unavailable.
    - The internal prewarm path is not anonymously callable and is verified by tests (TRAF-03).
  </behavior>
  <action>
    Add an `ops` module with `prewarm.service.ts` and `prewarm.controller.ts` that expose `POST /api/v1/internal/prewarm/services/:serviceName` and `POST /api/v1/internal/prewarm/services/:serviceName/step-down`. Protect these routes with a dedicated `PREWARM_CONTROL_TOKEN` header or bearer-token check so only Cloud Scheduler or a trusted operator can call them. In the service, implement the Cloud Run service-level min-instance update helper using `PREWARM_PROJECT_ID`, `PREWARM_REGION`, and the target service name; do not plan a direct Scheduler-to-Cloud-Run `PATCH` shortcut. Write `docs/runbooks/phase24-queue-waf-prewarm.md` with: exact Cloudflare queue/booking/signup/login/SMS rule groups, challenge/block actions, the bot-plan caveat, Scheduler job creation examples, and scale-up/scale-down call sequences.
  </action>
  <acceptance_criteria>
    - `apps/api/src/modules/ops/prewarm.controller.ts` contains both `/internal/prewarm/services/:serviceName` and `/internal/prewarm/services/:serviceName/step-down`.
    - `apps/api/src/modules/ops/prewarm.service.ts` contains `PREWARM_CONTROL_TOKEN`, `PREWARM_PROJECT_ID`, and `PREWARM_REGION`.
    - `docs/runbooks/phase24-queue-waf-prewarm.md` contains the strings `Managed Challenge`, `queue-entry`, `lock-seat`, `confirm-payment`, and `step-down`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/api test -- src/modules/ops/prewarm.service.spec.ts && rg -n "Managed Challenge|PREWARM_CONTROL_TOKEN|queue-entry|lock-seat|confirm-payment|step-down" docs/runbooks/phase24-queue-waf-prewarm.md</automated>
  </verify>
  <done>
    Protected prewarm code and a concrete Cloudflare/Scheduler operator runbook exist for TRAF-02 and TRAF-03.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> queue admission API | A user tries to obtain or reuse admission state here. |
| admission cookie -> booking mutation endpoints | Admission state crosses into scarce-resource mutation paths here. |
| scheduler/operator -> prewarm control path | Infrastructure scaling is exposed through this internal endpoint. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05 | S/E | `queue.service`, `admission.guard` | mitigate | Bind admission to `userId + refresh family/device slot + queueSessionId`, set short-lived active/grace windows, and enforce the guard on every booking mutation (Task 1). |
| T-24-06 | D | `traffic-defense.service` | mitigate | Apply endpoint-specific throttles and macro counters keyed by richer identity than IP-only, returning explicit challenge/block decisions before lock/payment abuse escalates (Task 2). |
| T-24-07 | E | `prewarm.controller.ts` | mitigate | Require `PREWARM_CONTROL_TOKEN` on internal scale-up/step-down routes and test anonymous access rejection (Task 3). |
| T-24-08 | R | `docs/runbooks/phase24-queue-waf-prewarm.md` | mitigate | Document the Cloudflare plan caveat and exact rule/export evidence so operators do not claim unavailable bot-score coverage (Task 3). |
</threat_model>

<verification>
- `pnpm --filter @grabit/api test -- src/modules/queue/queue.service.spec.ts src/modules/queue/queue.guard.spec.ts`
- `pnpm --filter @grabit/api test -- src/modules/traffic/traffic-defense.service.spec.ts`
- `pnpm --filter @grabit/api test -- src/modules/ops/prewarm.service.spec.ts`
- `pnpm --filter @grabit/api typecheck`
- `rg -n "Managed Challenge|PREWARM_CONTROL_TOKEN|queue-entry|lock-seat|confirm-payment|step-down" docs/runbooks/phase24-queue-waf-prewarm.md`
</verification>

<success_criteria>
- Queue admission is an API-side authorization boundary, not only a client redirect.
- The platform has concrete rate/macro decision code plus a runbook for edge Cloudflare rules.
- Prewarm uses a protected intermediary control path and documented Scheduler steps.
</success_criteria>

<output>
After completion, create `.planning/phases/24-traffic-booking-payment-core/24-02-SUMMARY.md`
</output>
