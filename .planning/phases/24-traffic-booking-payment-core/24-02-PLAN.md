---
phase: 24-traffic-booking-payment-core
plan: "02"
type: execute
wave: 2
depends_on:
  - "24-01"
files_modified:
  - apps/api/src/database/schema/seat-maps.ts
  - apps/api/src/database/schema/seat-inventories.ts
  - apps/api/src/database/schema/reservations.ts
  - apps/api/src/database/schema/payments.ts
  - apps/api/src/database/schema/booking-policies.ts
  - apps/api/src/database/schema/refunds.ts
  - apps/api/src/database/schema/tickets.ts
  - apps/api/src/database/schema/payment-webhook-events.ts
  - apps/api/src/database/schema/booking-operation-audit-logs.ts
  - apps/api/src/database/schema/index.ts
autonomous: true
requirements:
  - TRAF-01
  - BOOK-01
  - BOOK-02
  - BOOK-03
  - PAY-02
  - REFUND-01
  - REFUND-02
  - QR-01
must_haves:
  truths:
    - "Floor-aware seat, policy, payment, refund, and QR state persist durably without destructive changes to existing production rows."
    - "Payment deadline and queue/admission traces are stored separately from lock TTL so BOOK-02 and TRAF-01 are enforceable."
    - "D-10: `seat_maps` persists floor-specific rows with `floorKey`, `floorLabel`, `sortOrder`, `svgUrl`, and `seatConfig`, while legacy single-map data remains migratable to default `1F`."
  artifacts:
    - path: "apps/api/src/database/schema/seat-maps.ts"
      provides: "multi-floor seat map table contract"
      contains: "floorKey"
    - path: "apps/api/src/database/schema/booking-policies.ts"
      provides: "event booking policy table"
      contains: "maxTicketsPerUser"
    - path: "apps/api/src/database/schema/payment-webhook-events.ts"
      provides: "webhook idempotency ledger"
      contains: "receivedAt"
    - path: "apps/api/src/database/schema/booking-operation-audit-logs.ts"
      provides: "manual-open exception audit ledger"
      contains: "manual_open"
  key_links:
    - from: "seat-maps.ts"
      to: "seat-inventories.ts"
      via: "floorKey + seatKey"
      pattern: "floorKey|seatKey"
    - from: "reservations.ts"
      to: "payments.ts"
      via: "paymentDeadlineAt + asyncStatus"
      pattern: "paymentDeadlineAt|asyncStatus"
---

<objective>
Implement the expand-only Phase 24 Drizzle schema changes on top of the shared contracts.

Purpose: D-10 through D-20 require durable storage before queue, booking, payment, refund, or QR runtime logic can be built safely.
Output: updated Drizzle schema files for floors, booking policy, payment deadline, refund state, QR tickets, and webhook idempotency.
Decision coverage: D-10: `seat_maps` expands to floor-specific rows and legacy data migrates to default `1F`. D-15: payment storage records overseas disclaimer/FX-related consent evidence. D-16: refund storage persists the visible refund state machine. D-17: cancelled-seat hold metadata persists the randomized delayed reopen contract. D-19: ticket storage supports immediate QR issuance after confirmed payment. D-20: ticket storage supports D-1 QR email scheduling. D-21: payment persistence stays aligned with Toss widget-backed method branching and webhook idempotency.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/24-traffic-booking-payment-core/24-CONTEXT.md
@.planning/phases/24-traffic-booking-payment-core/24-RESEARCH.md
@.planning/phases/24-traffic-booking-payment-core/24-PATTERNS.md
@.planning/phases/23-launch-foundation/23-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add floor-aware seat, reservation, payment, and policy schema fields</name>
  <files>
    apps/api/src/database/schema/seat-maps.ts
    apps/api/src/database/schema/seat-inventories.ts
    apps/api/src/database/schema/reservations.ts
    apps/api/src/database/schema/payments.ts
    apps/api/src/database/schema/booking-policies.ts
  </files>
  <read_first>
    .planning/phases/24-traffic-booking-payment-core/24-CONTEXT.md
    apps/api/src/database/schema/seat-maps.ts
    apps/api/src/database/schema/seat-inventories.ts
    apps/api/src/database/schema/reservations.ts
    apps/api/src/database/schema/payments.ts
    apps/api/src/database/schema/phase24-booking-core.schema.spec.ts
  </read_first>
  <action>
    Update `seat_maps` to support `(performanceId, floorKey)` rows with `floorLabel` and `sortOrder` per D-10. Update `seat_inventories` to carry `floorKey`, `seatKey`, and delayed-reopen metadata per D-17. Extend `reservations` with admission trace data and `paymentDeadlineAt` per D-03 and D-04. Extend `payments` with provider, currency, disclaimer, and async-state fields per D-15 and D-21. Add `booking-policies.ts` with `maxTicketsPerUser`, `changePolicyEnabled`, `allowedPaymentMethods`, and cancelled-seat hold bounds per D-12, D-13, D-17, and D-18. Keep all changes expand-only.
  </action>
  <acceptance_criteria>
    - `seat-maps.ts` contains `floorKey`, `floorLabel`, and `sortOrder`.
    - `seat-inventories.ts` contains `seatKey`, `reopenHoldUntil`, and `held_cancelled`.
    - `reservations.ts` contains `paymentDeadlineAt`.
    - `payments.ts` contains `provider`, `currency`, `asyncStatus`, and `disclaimerAcceptedAt`.
    - `booking-policies.ts` exists and contains `maxTicketsPerUser`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/api exec vitest run src/database/schema/phase24-booking-core.schema.spec.ts && rg -n "floorKey|floorLabel|sortOrder|seatKey|reopenHoldUntil|held_cancelled|paymentDeadlineAt|provider|currency|asyncStatus|disclaimerAcceptedAt|maxTicketsPerUser|allowedPaymentMethods" apps/api/src/database/schema/seat-maps.ts apps/api/src/database/schema/seat-inventories.ts apps/api/src/database/schema/reservations.ts apps/api/src/database/schema/payments.ts apps/api/src/database/schema/booking-policies.ts</automated>
  </verify>
  <done>
    Core booking/payment/refund policy state is represented in Drizzle schema files.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add refund and QR ticket tables</name>
  <files>
    apps/api/src/database/schema/refunds.ts
    apps/api/src/database/schema/tickets.ts
  </files>
  <read_first>
    apps/api/src/database/schema/phase24-booking-core.schema.spec.ts
    apps/api/src/database/schema/payments.ts
    apps/api/src/database/schema/reservations.ts
    packages/shared/src/schemas/booking.schema.ts
  </read_first>
  <action>
    Create `refunds.ts` with the visible state machine required by D-16 and the persisted provider/result metadata needed for retryable refund orchestration, and create `tickets.ts` with `qrTokenJti`, `secretVersion`, and D-1 email timestamps for D-19 and D-20. Keep this task limited to refund/QR storage so the later webhook/audit task can verify async ledgers separately instead of bundling every new table into one acceptance pass.
  </action>
  <acceptance_criteria>
    - `refunds.ts` contains `requested`, `sent_to_pg`, `processing_at_pg`, `completed`, and `failed`.
    - `tickets.ts` contains `qrTokenJti`, `issuedAt`, `emailScheduledAt`, and `emailSentAt`.
    - `refunds.ts` persists provider/result metadata needed for retryable refund state transitions instead of a terminal-only outcome.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/api exec vitest run src/database/schema/phase24-booking-core.schema.spec.ts && rg -n "requested|sent_to_pg|processing_at_pg|completed|failed|provider|resultCode|failureReason" apps/api/src/database/schema/refunds.ts && rg -n "qrTokenJti|secretVersion|issuedAt|emailScheduledAt|emailSentAt" apps/api/src/database/schema/tickets.ts</automated>
  </verify>
  <done>
    Refund and QR tables are available to downstream refund, payment, and QR runtime plans.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add webhook-ledger, manual-open audit tables, and schema exports</name>
  <files>
    apps/api/src/database/schema/payment-webhook-events.ts
    apps/api/src/database/schema/booking-operation-audit-logs.ts
    apps/api/src/database/schema/index.ts
  </files>
  <read_first>
    apps/api/src/database/schema/index.ts
    apps/api/src/database/schema/payments.ts
    apps/api/src/database/schema/refunds.ts
    .planning/phases/24-traffic-booking-payment-core/24-REVIEWS.md
  </read_first>
  <action>
    Create `payment-webhook-events.ts` with the standard ledger columns called out by the review feedback: `eventId`, `eventType`, `payload`, `receivedAt`, `processedAt`, and `processingResultCode` for D-21 async idempotency and replay-safe reconciliation. Create `booking-operation-audit-logs.ts` with the minimal immutable manual-open audit row required for D-18 follow-through (`operatorUserId`, `action`, `seatKey`, `reservationId`, `createdAt`), then export both tables plus the refund/ticket tables from `schema/index.ts`. Keep this task focused on async/audit durability so webhook and manual-open consumers have a discrete verification target.
  </action>
  <acceptance_criteria>
    - `payment-webhook-events.ts` contains `eventId`, `eventType`, `payload`, `receivedAt`, `processedAt`, and `processingResultCode`.
    - `booking-operation-audit-logs.ts` contains `operatorUserId`, `action`, `seatKey`, `reservationId`, and `createdAt`.
    - `schema/index.ts` exports `paymentWebhookEvents`, `bookingOperationAuditLogs`, `refunds`, and `tickets`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "eventId|eventType|payload|receivedAt|processedAt|processingResultCode" apps/api/src/database/schema/payment-webhook-events.ts && rg -n "operatorUserId|action|seatKey|reservationId|createdAt" apps/api/src/database/schema/booking-operation-audit-logs.ts && rg -n "paymentWebhookEvents|bookingOperationAuditLogs|refunds|tickets" apps/api/src/database/schema/index.ts && pnpm --filter @grabit/api typecheck</automated>
  </verify>
  <done>
    Webhook-idempotency and manual-open audit storage are isolated, exported, and ready for the later runtime plans that depend on them.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime booking/payment actions -> database | Scarce-seat and financial state becomes durable here. |
| provider webhook -> webhook event storage | External async events cross into core payment state here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03 | T | `seat_maps`, `seat_inventories` | mitigate | Use `floorKey + seatKey`-aware schema so same visible seat labels on different floors cannot collide (Task 1). |
| T-24-04 | T/R | `payments`, `refunds`, `payment-webhook-events` | mitigate | Persist explicit async/refund states, refund/result metadata, and an event-id ledger so retries cannot double-apply payment or refund transitions (Task 1, Task 2, Task 3). |
</threat_model>

<verification>
- `pnpm --filter @grabit/api exec vitest run src/database/schema/phase24-booking-core.schema.spec.ts`
- `rg -n "floorKey|floorLabel|sortOrder|seatKey|reopenHoldUntil|held_cancelled|paymentDeadlineAt|provider|currency|asyncStatus|disclaimerAcceptedAt|maxTicketsPerUser|allowedPaymentMethods" apps/api/src/database/schema/seat-maps.ts apps/api/src/database/schema/seat-inventories.ts apps/api/src/database/schema/reservations.ts apps/api/src/database/schema/payments.ts apps/api/src/database/schema/booking-policies.ts`
- `rg -n "requested|sent_to_pg|processing_at_pg|completed|failed|provider|resultCode|failureReason" apps/api/src/database/schema/refunds.ts`
- `rg -n "qrTokenJti|secretVersion|issuedAt|emailScheduledAt|emailSentAt" apps/api/src/database/schema/tickets.ts`
- `rg -n "eventId|eventType|payload|receivedAt|processedAt|processingResultCode" apps/api/src/database/schema/payment-webhook-events.ts`
- `rg -n "operatorUserId|action|seatKey|reservationId|createdAt" apps/api/src/database/schema/booking-operation-audit-logs.ts`
- `rg -n "paymentWebhookEvents|bookingOperationAuditLogs|refunds|tickets" apps/api/src/database/schema/index.ts`
- `pnpm --filter @grabit/api typecheck`
</verification>

<success_criteria>
- Expand-only Drizzle schema files exist for floors, policy, refund, QR, and webhook-idempotency state.
- Downstream runtime plans can rely on durable columns/tables instead of inferred JSON or TTL-only state.
</success_criteria>

<output>
After completion, create `.planning/phases/24-traffic-booking-payment-core/24-02-SUMMARY.md`
</output>
