---
phase: 24-traffic-booking-payment-core
plan: "07"
type: execute
wave: 5
depends_on:
  - "24-06"
files_modified:
  - apps/api/src/modules/booking/booking.service.ts
  - apps/api/src/modules/booking/__tests__/booking.service.spec.ts
  - apps/api/src/modules/reservation/reservation.service.ts
  - apps/api/src/modules/reservation/reservation.service.spec.ts
  - apps/web/components/booking/floor-selector.tsx
  - apps/web/components/booking/booking-page.tsx
  - apps/web/components/booking/seat-map-viewer.tsx
  - apps/web/stores/use-booking-store.ts
  - apps/web/hooks/use-booking.ts
  - apps/web/components/booking/__tests__/floor-selector.test.tsx
autonomous: true
requirements:
  - BOOK-01
  - BOOK-03
must_haves:
  truths:
    - "Seat locks, prepared reservations, and client selection state use floor-aware identity across all floors."
    - "UI ticket-limit guidance and enforcement follow event policy instead of hardcoded `MAX_SEATS=4`."
    - "After payment confirmation, seat-change self-service remains unavailable and the flow points to cancellation/refund instead."
  artifacts:
    - path: "apps/api/src/modules/booking/booking.service.ts"
      provides: "floor-aware lock enforcement"
      contains: "seatKey"
    - path: "apps/web/stores/use-booking-store.ts"
      provides: "floor-aware client booking state"
      contains: "floorLabel"
    - path: "apps/web/components/booking/floor-selector.tsx"
      provides: "floor navigation UI"
      contains: "ToggleGroup"
  key_links:
    - from: "booking.service.ts"
      to: "reservation.service.ts"
      via: "policy-aware prepare/confirm"
      pattern: "maxTicketsPerUser|seatKey"
    - from: "use-booking-store.ts"
      to: "booking-page.tsx"
      via: "grouped selections by floor"
      pattern: "floorKey|floorLabel"
---

<objective>
Make booking runtime and UI state floor-aware and policy-driven.

Purpose: D-11 through D-14 are only satisfied when backend enforcement and frontend state both stop assuming single-floor identity or hardcoded ticket limits.
Output: floor-aware booking/reservation enforcement plus a floor selector and grouped booking state.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-traffic-booking-payment-core/24-CONTEXT.md
@.planning/phases/24-traffic-booking-payment-core/24-UI-SPEC.md
@.planning/phases/24-traffic-booking-payment-core/24-PATTERNS.md
@.planning/phases/24-traffic-booking-payment-core/24-06-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce floor-aware seat identity and event ticket policy in booking services</name>
  <files>
    apps/api/src/modules/booking/booking.service.ts
    apps/api/src/modules/booking/__tests__/booking.service.spec.ts
    apps/api/src/modules/reservation/reservation.service.ts
    apps/api/src/modules/reservation/reservation.service.spec.ts
  </files>
  <read_first>
    apps/api/src/modules/booking/booking.service.ts
    apps/api/src/modules/reservation/reservation.service.ts
    packages/shared/src/types/performance.types.ts
  </read_first>
  <action>
    Refactor booking and reservation services so locks, canonical amount checks, reservation-seat persistence, and policy lookup use `seatKey`/`floorKey` instead of floor-blind `seatId`. Replace hardcoded `MAX_SEATS=4` with event-configured `maxTicketsPerUser` per D-12 and D-13, and keep D-14 explicit by refusing to invent any post-payment seat-change mutation path.
  </action>
  <acceptance_criteria>
    - `booking.service.ts` contains `seatKey` or `floorKey` in runtime lock/status logic.
    - `booking.service.ts` no longer uses hardcoded `MAX_SEATS = 4` for enforcement.
    - `reservation.service.spec.ts` covers cross-floor collision and ticket-limit cases.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/api test -- src/modules/booking/__tests__/booking.service.spec.ts src/modules/reservation/reservation.service.spec.ts</automated>
  </verify>
  <done>
    Backend booking and reservation logic is floor-aware and policy-driven.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add floor selector and grouped client booking state</name>
  <files>
    apps/web/components/booking/floor-selector.tsx
    apps/web/components/booking/booking-page.tsx
    apps/web/components/booking/seat-map-viewer.tsx
    apps/web/stores/use-booking-store.ts
    apps/web/hooks/use-booking.ts
    apps/web/components/booking/__tests__/floor-selector.test.tsx
  </files>
  <read_first>
    .planning/phases/24-traffic-booking-payment-core/24-UI-SPEC.md
    apps/web/components/booking/booking-page.tsx
    apps/web/components/booking/seat-map-viewer.tsx
    apps/web/stores/use-booking-store.ts
    apps/web/hooks/use-booking.ts
  </read_first>
  <action>
    Add a sticky `floor-selector.tsx` above the existing seat-map viewer, preserve zoom/pan/minimap, and refactor store/hook state so selected seats carry `floorKey`, `floorLabel`, and `seatKey`. Update the summary to group seats by floor label and replace the old `최대 4석` helper with event-policy copy. Keep sold/locked seats visibly disabled and route post-payment seat changes to cancellation/refund messaging per D-14.
  </action>
  <acceptance_criteria>
    - `floor-selector.tsx` exists and contains `ToggleGroup`.
    - `use-booking-store.ts` contains `floorKey`, `floorLabel`, and `seatKey`.
    - `booking-page.tsx` no longer presents hardcoded `최대 4석` copy.
    - `floor-selector.test.tsx` proves floor switching preserves selections.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/web test -- components/booking/__tests__/floor-selector.test.tsx hooks/__tests__/use-booking.test.tsx && pnpm --filter @grabit/web typecheck</automated>
  </verify>
  <done>
    Users can switch floors without losing selections and see policy-aligned grouped summaries.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| performance/admin contract -> booking runtime | Floor and policy data becomes enforceable booking behavior here. |
| client selection state -> lock/prepare requests | Selected seat identity crosses from UI into scarce-seat mutations here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-13 | T/E | `booking.service.ts`, `reservation.service.ts` | mitigate | Enforce floor-aware identity and event policy server-side so client-only state cannot spoof seat or ticket-limit validity (Task 1). |
| T-24-14 | T | `use-booking-store.ts`, `booking-page.tsx` | mitigate | Carry `floorKey`, `floorLabel`, and `seatKey` through client state so same-label seats on different floors never collapse into one selection (Task 2). |
</threat_model>

<verification>
- `pnpm --filter @grabit/api test -- src/modules/booking/__tests__/booking.service.spec.ts src/modules/reservation/reservation.service.spec.ts`
- `pnpm --filter @grabit/web test -- components/booking/__tests__/floor-selector.test.tsx hooks/__tests__/use-booking.test.tsx`
- `pnpm --filter @grabit/web typecheck`
</verification>

<success_criteria>
- Floor-aware seat identity is enforced in backend runtime and preserved in frontend booking state.
- Ticket-limit and seat-change rules follow event policy instead of old hardcoded behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/24-traffic-booking-payment-core/24-07-SUMMARY.md`
</output>
