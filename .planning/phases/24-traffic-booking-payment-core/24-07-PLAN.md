---
phase: 24-traffic-booking-payment-core
plan: "07"
type: execute
wave: 5
depends_on:
  - "24-06"
files_modified:
  - apps/api/src/modules/booking/booking.service.ts
  - apps/api/src/modules/booking/__tests__/booking.service.spec.ts
  - apps/api/src/modules/reservation/reservation.service.ts
  - apps/api/src/modules/reservation/reservation.service.spec.ts
autonomous: true
requirements:
  - BOOK-01
  - BOOK-03
must_haves:
  truths:
    - "Seat locks and prepared reservations use floor-aware identity across all floors."
    - "Server-side ticket-limit enforcement follows event policy instead of hardcoded `MAX_SEATS=4`."
    - "After payment confirmation, seat-change self-service remains unavailable and the flow points to cancellation/refund instead."
  artifacts:
    - path: "apps/api/src/modules/booking/booking.service.ts"
      provides: "floor-aware lock enforcement"
      contains: "seatKey"
    - path: "apps/api/src/modules/reservation/reservation.service.ts"
      provides: "policy-aware prepare/confirm flow"
      contains: "maxTicketsPerUser"
  key_links:
    - from: "booking.service.ts"
      to: "reservation.service.ts"
      via: "policy-aware prepare/confirm"
      pattern: "maxTicketsPerUser|seatKey"
---

<objective>
Make booking runtime floor-aware and policy-driven on the backend.

Purpose: D-12, D-13, and D-14 fail if the server still trusts floor-blind seat IDs or hardcoded limits, so backend enforcement must be isolated from the separate frontend floor-selector plan.
Output: floor-aware booking/reservation enforcement and tests, without the frontend selector/state work.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-traffic-booking-payment-core/24-CONTEXT.md
@.planning/phases/24-traffic-booking-payment-core/24-UI-SPEC.md
@.planning/phases/24-traffic-booking-payment-core/24-PATTERNS.md
@.planning/phases/24-traffic-booking-payment-core/24-06-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce floor-aware seat identity and event ticket policy in booking/reservation services</name>
  <files>
    apps/api/src/modules/booking/booking.service.ts
    apps/api/src/modules/booking/__tests__/booking.service.spec.ts
    apps/api/src/modules/reservation/reservation.service.ts
    apps/api/src/modules/reservation/reservation.service.spec.ts
  </files>
  <read_first>
    apps/api/src/modules/booking/booking.service.ts
    apps/api/src/modules/reservation/reservation.service.ts
    packages/shared/src/types/performance.types.ts
  </read_first>
  <action>
    Refactor booking and reservation services so locks, canonical amount checks, reservation-seat persistence, and policy lookup use `seatKey`/`floorKey` instead of floor-blind `seatId`. Replace hardcoded `MAX_SEATS=4` with event-configured `maxTicketsPerUser` per D-12 and D-13, keep D-14 explicit by refusing to invent any post-payment seat-change mutation path, and ensure cross-floor seat labels cannot collide into a false "same seat" on the server.
  </action>
  <acceptance_criteria>
    - `booking.service.ts` contains `seatKey` or `floorKey` in runtime lock/status logic.
    - `booking.service.ts` no longer uses hardcoded `MAX_SEATS = 4` for enforcement.
    - `reservation.service.spec.ts` covers cross-floor collision and ticket-limit cases.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/api test -- src/modules/booking/__tests__/booking.service.spec.ts src/modules/reservation/reservation.service.spec.ts</automated>
  </verify>
  <done>
    Backend booking and reservation logic is floor-aware and policy-driven.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| performance/admin contract -> booking runtime | Floor and policy data becomes enforceable booking behavior here. |
| reservation input -> lock/prepare requests | Selected seat identity crosses into scarce-seat mutations here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-13 | T/E | `booking.service.ts`, `reservation.service.ts` | mitigate | Enforce floor-aware identity and event policy server-side per D-12/D-13 so client-only state cannot spoof seat or ticket-limit validity (Task 1). |
| T-24-14 | T | `booking.service.ts` | mitigate | Use `seatKey`/`floorKey` rather than bare `seatId` so same-label seats on different floors never collapse into one server-side lock identity (Task 1). |
</threat_model>

<verification>
- `pnpm --filter @grabit/api test -- src/modules/booking/__tests__/booking.service.spec.ts src/modules/reservation/reservation.service.spec.ts`
</verification>

<success_criteria>
- Floor-aware seat identity is enforced in backend runtime.
- Ticket-limit and seat-change rules follow event policy instead of old hardcoded behavior.
- The separate frontend floor-selector/state plan can build on backend semantics without re-deciding server policy.
</success_criteria>

<output>
After completion, create `.planning/phases/24-traffic-booking-payment-core/24-07-SUMMARY.md`
</output>
