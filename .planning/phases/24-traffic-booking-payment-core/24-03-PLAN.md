---
phase: 24-traffic-booking-payment-core
plan: "03"
type: execute
wave: 3
depends_on:
  - "24-02"
files_modified:
  - apps/api/src/database/migrations/00xx_phase24_booking_core.sql
  - apps/api/src/database/migrations/meta/00xx_snapshot.json
  - apps/api/src/database/migrations/meta/_journal.json
autonomous: true
requirements:
  - BOOK-01
  - BOOK-02
  - BOOK-03
  - PAY-02
  - REFUND-01
  - REFUND-02
  - QR-01
must_haves:
  truths:
    - "Phase 24 schema changes are generated and reviewed as an expand-only migration before runtime plans depend on them."
    - "The migration uses the repository’s exact root `.env` convention and does not rely on implicit cwd behavior."
  artifacts:
    - path: "apps/api/src/database/migrations/00xx_phase24_booking_core.sql"
      provides: "Phase 24 migration SQL"
      contains: "statement-breakpoint"
    - path: "apps/api/src/database/migrations/meta/_journal.json"
      provides: "Drizzle migration journal entry"
      contains: "phase24_booking_core"
  key_links:
    - from: "schema/*.ts"
      to: "00xx_phase24_booking_core.sql"
      via: "drizzle-kit generate"
      pattern: "phase24_booking_core"
---

<objective>
Apply the blocking Phase 24 Drizzle migration gate.

Purpose: every downstream runtime plan assumes the database shape is real, reviewed, and locally applicable.
Output: generated migration SQL, Drizzle metadata, and a locally attempted migrate step using the exact project command.
Decision coverage: D-10: the migration must backfill existing single-floor rows to default `1F` without destructive schema rewrites.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-traffic-booking-payment-core/24-CONTEXT.md
@.planning/phases/23-launch-foundation/23-02-SUMMARY.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Generate, review, and apply the Phase 24 migration</name>
  <files>
    apps/api/src/database/migrations/00xx_phase24_booking_core.sql
    apps/api/src/database/migrations/meta/00xx_snapshot.json
    apps/api/src/database/migrations/meta/_journal.json
  </files>
  <read_first>
    AGENTS.md
    apps/api/src/database/schema/seat-maps.ts
    apps/api/src/database/schema/seat-inventories.ts
    apps/api/src/database/schema/reservations.ts
    apps/api/src/database/schema/payments.ts
    apps/api/src/database/schema/booking-policies.ts
    apps/api/src/database/schema/refunds.ts
    apps/api/src/database/schema/tickets.ts
    apps/api/src/database/schema/payment-webhook-events.ts
  </read_first>
  <action>
    Run the exact project commands from the repo root: `DOTENV_CONFIG_PATH=../../.env pnpm --filter @grabit/api exec drizzle-kit generate --name=phase24_booking_core` and then `DOTENV_CONFIG_PATH=../../.env pnpm --filter @grabit/api exec drizzle-kit migrate`. Review the generated SQL before applying it: it must backfill legacy single-floor rows to default `1F`, add new tables/columns only, and avoid destructive statements per D-10 and Phase 23 expand-only policy. If local Postgres or credentials are unavailable, stop the verification claim and record the redacted failure in the summary instead of pretending migrate passed.
  </action>
  <acceptance_criteria>
    - A `*phase24*booking*core*.sql` migration file exists.
    - `_journal.json` contains a new Phase 24 entry.
    - `rg -n "DROP TABLE|DROP COLUMN|TRUNCATE|DELETE FROM|ALTER COLUMN .* TYPE|RENAME TO" apps/api/src/database/migrations/*phase24*booking*core*.sql` returns no matches.
    - Both commands use the exact `DOTENV_CONFIG_PATH=../../.env` prefix.
  </acceptance_criteria>
  <verify>
    <automated>DOTENV_CONFIG_PATH=../../.env pnpm --filter @grabit/api exec drizzle-kit generate --name=phase24_booking_core && ! rg -n "DROP TABLE|DROP COLUMN|TRUNCATE|DELETE FROM|ALTER COLUMN .* TYPE|RENAME TO" apps/api/src/database/migrations/*phase24*booking*core*.sql && DOTENV_CONFIG_PATH=../../.env pnpm --filter @grabit/api exec drizzle-kit migrate</automated>
  </verify>
  <done>
    The Phase 24 migration exists, is expand-only, and has been applied locally or explicitly blocked by a recorded local prerequisite failure.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema source -> generated SQL | Drizzle generation turns application contracts into real migration SQL here. |
| migration SQL -> local database | Expand-only policy is enforced at this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05 | D | migration SQL | mitigate | Review generated SQL for destructive statements before migrate, using the exact grep gate in Task 1. |
| T-24-06 | T | env / migration command | mitigate | Use the project’s exact `DOTENV_CONFIG_PATH=../../.env` invocation so Drizzle reads the canonical root `.env` instead of an accidental cwd-local configuration (Task 1). |
</threat_model>

<verification>
- `DOTENV_CONFIG_PATH=../../.env pnpm --filter @grabit/api exec drizzle-kit generate --name=phase24_booking_core`
- `! rg -n "DROP TABLE|DROP COLUMN|TRUNCATE|DELETE FROM|ALTER COLUMN .* TYPE|RENAME TO" apps/api/src/database/migrations/*phase24*booking*core*.sql`
- `DOTENV_CONFIG_PATH=../../.env pnpm --filter @grabit/api exec drizzle-kit migrate`
</verification>

<success_criteria>
- The migration gate is explicit, blocking, and uses the repository’s required Drizzle command shape.
- Downstream plans depend on a generated/applied Phase 24 schema, not on speculative table definitions.
</success_criteria>

<output>
After completion, create `.planning/phases/24-traffic-booking-payment-core/24-03-SUMMARY.md`
</output>
