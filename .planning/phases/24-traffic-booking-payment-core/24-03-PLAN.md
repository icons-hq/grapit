---
phase: 24-traffic-booking-payment-core
plan: "03"
type: execute
wave: 2
depends_on:
  - "24-01"
files_modified:
  - packages/shared/src/types/performance.types.ts
  - packages/shared/src/schemas/performance.schema.ts
  - apps/api/src/modules/performance/performance.service.ts
  - apps/api/src/modules/performance/performance.service.spec.ts
  - apps/api/src/modules/admin/admin-performance.controller.ts
  - apps/api/src/modules/admin/admin.service.ts
  - apps/api/src/modules/admin/admin.service.spec.ts
  - apps/api/src/modules/booking/booking.service.ts
  - apps/api/src/modules/booking/__tests__/booking.service.spec.ts
  - apps/api/src/modules/reservation/reservation.service.ts
  - apps/api/src/modules/reservation/reservation.service.spec.ts
  - apps/web/hooks/use-admin.ts
  - apps/web/components/admin/performance-form.tsx
  - apps/web/components/admin/svg-preview.tsx
  - apps/web/components/admin/floor-seat-map-editor.tsx
autonomous: true
requirements:
  - BOOK-01
  - BOOK-03
must_haves:
  truths:
    - "Performance detail and admin surfaces can represent multiple seat-map floors plus event booking policy without breaking the existing performance detail route."
    - "Ticket-limit and floor-aware seat identity are enforced server-side across all floors, not only in client copy."
    - "Post-payment self-service seat changes remain unsupported; users are routed to cancellation/refund instead."
  artifacts:
    - path: "packages/shared/src/types/performance.types.ts"
      provides: "Multi-floor performance detail contract"
      contains: "seatMaps"
    - path: "apps/api/src/modules/performance/performance.service.ts"
      provides: "Floor-aware performance detail read model"
      contains: "bookingPolicy"
    - path: "apps/api/src/modules/booking/booking.service.ts"
      provides: "Server-side max-ticket and floor-aware lock enforcement"
      contains: "maxTicketsPerUser"
    - path: "apps/web/components/admin/floor-seat-map-editor.tsx"
      provides: "Admin floor upload/editor surface"
      contains: "floorLabel"
  key_links:
    - from: "apps/api/src/modules/admin/admin.service.ts"
      to: "apps/api/src/database/schema/seat-maps.ts"
      via: "multi-floor save/upsert contract"
      pattern: "floorKey|sortOrder"
    - from: "apps/api/src/modules/performance/performance.service.ts"
      to: "packages/shared/src/types/performance.types.ts"
      via: "seatMaps + bookingPolicy response shape"
      pattern: "seatMaps|bookingPolicy"
    - from: "apps/api/src/modules/reservation/reservation.service.ts"
      to: "apps/api/src/modules/booking/booking.service.ts"
      via: "policy-aware prepare enforcement"
      pattern: "assertOwnedSeatLocks|maxTicketsPerUser"
---

<objective>
Implement multi-floor seat-map data flow and event booking-policy enforcement across API and minimal admin surfaces.

Purpose: BOOK-01 and BOOK-03 require floor-aware persistence plus operator-configurable ticket/payment/cancellation policy before the web booking flow can safely render it.
Output: performance/admin contracts for floor arrays and policy, server-side floor/policy enforcement, and a minimal admin floor editor.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-traffic-booking-payment-core/24-CONTEXT.md
@.planning/phases/24-traffic-booking-payment-core/24-RESEARCH.md
@.planning/phases/24-traffic-booking-payment-core/24-PATTERNS.md
@.planning/phases/24-traffic-booking-payment-core/24-UI-SPEC.md
@.planning/milestones/v1.0-phases/04-booking-payment/04-01-SUMMARY.md

<interfaces>
From `packages/shared/src/types/performance.types.ts`:
```ts
export interface PerformanceWithDetails extends Performance {
  venue: Venue | null;
  priceTiers: PriceTier[];
  showtimes: Showtime[];
  castings: CastMember[];
  seatMap: SeatMap | null;
}
```

From `apps/api/src/modules/performance/performance.service.ts`:
```ts
const [priceTierRows, showtimeRows, castingRows, seatMapRows] = await Promise.all([...]);
seatMap: seatMapRows[0] ? { id, performanceId, svgUrl, seatConfig, totalSeats } : null
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend performance/admin contracts to return floor arrays and booking policy</name>
  <files>
    packages/shared/src/types/performance.types.ts
    packages/shared/src/schemas/performance.schema.ts
    apps/api/src/modules/performance/performance.service.ts
    apps/api/src/modules/performance/performance.service.spec.ts
    apps/api/src/modules/admin/admin-performance.controller.ts
    apps/api/src/modules/admin/admin.service.ts
    apps/api/src/modules/admin/admin.service.spec.ts
    apps/web/hooks/use-admin.ts
  </files>
  <read_first>
    .planning/phases/24-traffic-booking-payment-core/24-CONTEXT.md
    .planning/phases/24-traffic-booking-payment-core/24-RESEARCH.md
    packages/shared/src/types/performance.types.ts
    packages/shared/src/schemas/performance.schema.ts
    apps/api/src/modules/performance/performance.service.ts
    apps/api/src/modules/admin/admin-performance.controller.ts
    apps/api/src/modules/admin/admin.service.ts
  </read_first>
  <behavior>
    - Performance detail exposes a floor array and event booking policy instead of a single `seatMap` row (D-10, D-12, D-13).
    - Admin save/load paths persist `floorKey`, `floorLabel`, `sortOrder`, and the Phase 24 booking policy fields without requiring the full Phase 25 console (BOOK-03).
    - Existing performance detail and admin edit routes stay backward-compatible for single-floor legacy rows through the `1F` default floor contract.
  </behavior>
  <action>
    Update `packages/shared/src/types/performance.types.ts` so `PerformanceWithDetails` exposes `seatMaps: SeatMapFloor[]` and `bookingPolicy`. Extend `packages/shared/src/schemas/performance.schema.ts` with a floor-aware seat-map payload and a booking-policy schema that matches `booking_policies.ts`. Modify `apps/api/src/modules/performance/performance.service.ts` to fetch all `seat_maps` rows ordered by `sortOrder`, map them into a floor array, and include the policy record in performance detail responses. Modify `apps/api/src/modules/admin/admin-performance.controller.ts`, `apps/api/src/modules/admin/admin.service.ts`, and `apps/web/hooks/use-admin.ts` so admin save/read APIs can persist multi-floor rows and policy fields using the exact Phase 24 contract. Preserve legacy single-row data by returning a synthesized/default `1F` floor when older rows are encountered.
  </action>
  <acceptance_criteria>
    - `packages/shared/src/types/performance.types.ts` contains `seatMaps` and `bookingPolicy`.
    - `packages/shared/src/schemas/performance.schema.ts` contains floor-aware seat-map input and policy validation.
    - `apps/api/src/modules/performance/performance.service.ts` no longer maps only `seatMapRows[0]` and instead orders or iterates all floor rows.
    - `apps/api/src/modules/admin/admin-performance.controller.ts` and `apps/web/hooks/use-admin.ts` contain save/read handlers for floor arrays or booking policy payloads.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/api test -- src/modules/performance/performance.service.spec.ts src/modules/admin/admin.service.spec.ts && pnpm --filter @grabit/api typecheck</automated>
  </verify>
  <done>
    Performance detail and admin API contracts can carry multi-floor seat maps and booking policy data end to end.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce floor-aware seat identity and event ticket policy in booking/reservation services</name>
  <files>
    apps/api/src/modules/booking/booking.service.ts
    apps/api/src/modules/booking/__tests__/booking.service.spec.ts
    apps/api/src/modules/reservation/reservation.service.ts
    apps/api/src/modules/reservation/reservation.service.spec.ts
  </files>
  <read_first>
    .planning/phases/24-traffic-booking-payment-core/24-CONTEXT.md
    .planning/phases/24-traffic-booking-payment-core/24-RESEARCH.md
    apps/api/src/modules/booking/booking.service.ts
    apps/api/src/modules/reservation/reservation.service.ts
    apps/api/src/modules/performance/performance.service.ts
  </read_first>
  <behavior>
    - Lock and reservation flows use `seatKey`/`floorKey` so duplicate visible labels on different floors never collide (D-10, D-11).
    - Event-configured `maxTicketsPerUser` replaces hardcoded `MAX_SEATS=4` across all floors, with server-side enforcement during lock/prepare (D-12, D-13).
    - Self-service post-payment seat change remains unsupported, and booking policy/read models expose cancellation/change expectations instead of inventing a seat-swap endpoint (D-14).
  </behavior>
  <action>
    Refactor `apps/api/src/modules/booking/booking.service.ts` so lock keys, sold-seat checks, and lock ownership assertions use floor-aware identity from the new schema/contracts instead of assuming `seatId` is globally unique. Replace hardcoded `MAX_SEATS=4` behavior with a booking-policy lookup keyed by performance, and enforce the configured limit across all selected floors combined. Update `apps/api/src/modules/reservation/reservation.service.ts` so canonical seat validation, amount calculation, reservation seat persistence, and payment preparation all use `seatKey` plus floor metadata. Expose or preserve policy flags that make D-14 explicit: after payment confirmation there is no self-service seat-change mutation path, and cancellation/refund is the only user-facing route. Remove or isolate any remaining server-side assumption that visible `seatId` is unique without a floor context.
  </action>
  <acceptance_criteria>
    - `apps/api/src/modules/booking/booking.service.ts` contains `seatKey` or `floorKey` in lock/status code paths.
    - `apps/api/src/modules/booking/booking.service.ts` no longer uses a hardcoded `MAX_SEATS = 4` rule for runtime enforcement.
    - `apps/api/src/modules/reservation/reservation.service.ts` uses floor-aware seat selection data when validating and persisting reservation seats.
    - `apps/api/src/modules/reservation/reservation.service.spec.ts` covers ticket-limit and cross-floor collision cases.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/api test -- src/modules/booking/__tests__/booking.service.spec.ts src/modules/reservation/reservation.service.spec.ts</automated>
  </verify>
  <done>
    Backend booking and reservation logic is floor-aware and policy-driven, and single-floor/hardcoded ticket-limit assumptions are removed from server enforcement.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add the minimal admin floor editor and policy controls</name>
  <files>
    apps/web/components/admin/performance-form.tsx
    apps/web/components/admin/svg-preview.tsx
    apps/web/components/admin/floor-seat-map-editor.tsx
  </files>
  <read_first>
    .planning/phases/24-traffic-booking-payment-core/24-UI-SPEC.md
    apps/web/components/admin/performance-form.tsx
    apps/web/components/admin/svg-preview.tsx
    apps/api/src/modules/admin/admin.service.ts
    packages/shared/src/types/performance.types.ts
    packages/shared/src/schemas/performance.schema.ts
  </read_first>
  <action>
    Build a dedicated `floor-seat-map-editor.tsx` that lets the operator manage 2-7 floor rows with exact fields `floorKey`, `floorLabel`, `sortOrder`, `svgUrl`, and `seatConfig`. Integrate it into `performance-form.tsx` and `svg-preview.tsx` so existing upload/safety behavior is reused per floor instead of overwritten. Add visible policy controls for `maxTicketsPerUser`, `allowedPaymentMethods`, `changePolicyEnabled`, and cancelled-seat hold range while keeping the established admin visual language intact. The admin surface must save real floor/policy data that Plan 24 runtime code consumes; do not ship a static or future-only control.
  </action>
  <acceptance_criteria>
    - `apps/web/components/admin/floor-seat-map-editor.tsx` exists and contains `floorKey`, `floorLabel`, and `sortOrder`.
    - `apps/web/components/admin/performance-form.tsx` renders booking-policy controls for max tickets and payment methods.
    - `apps/web/components/admin/svg-preview.tsx` still uses existing SVG safety checks while supporting multiple floor uploads.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/web test -- components/admin && pnpm --filter @grabit/web typecheck</automated>
  </verify>
  <done>
    Operators can upload multiple floor SVGs and set event booking policy through the existing admin workflow without waiting for Phase 25 console work.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin floor/policy input -> API persistence | Operator-provided floor/policy data becomes authoritative booking behavior here. |
| performance detail API -> booking UI | Floor arrays and policy are rendered directly to users here. |
| booking/reservation logic -> ticket-limit enforcement | Scarce seat and ticket-policy decisions are enforced here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-09 | T | `admin.service.ts`, `performance.service.ts` | mitigate | Persist exact `floorKey`, `floorLabel`, `sortOrder`, and policy fields so later booking code cannot infer or mutate them from ambiguous UI data (Task 1, Task 3). |
| T-24-10 | T/E | `booking.service.ts`, `reservation.service.ts` | mitigate | Enforce `seatKey` and `maxTicketsPerUser` server-side, eliminating single-floor collisions and client-only ticket-limit checks (Task 2). |
| T-24-11 | R | `performance-form.tsx`, `floor-seat-map-editor.tsx` | mitigate | Reuse the existing SVG safety pipeline and persist concrete policy values rather than undocumented local state (Task 3). |
</threat_model>

<verification>
- `pnpm --filter @grabit/api test -- src/modules/performance/performance.service.spec.ts src/modules/admin/admin.service.spec.ts`
- `pnpm --filter @grabit/api test -- src/modules/booking/__tests__/booking.service.spec.ts src/modules/reservation/reservation.service.spec.ts`
- `pnpm --filter @grabit/web test -- components/admin`
- `pnpm --filter @grabit/api typecheck`
- `pnpm --filter @grabit/web typecheck`
</verification>

<success_criteria>
- Performance detail and admin APIs expose floor arrays and booking policy.
- Booking/reservation services enforce floor-aware identity and per-event ticket limits.
- Admin UI can save real multi-floor and policy data without waiting for later phases.
</success_criteria>

<output>
After completion, create `.planning/phases/24-traffic-booking-payment-core/24-03-SUMMARY.md`
</output>
