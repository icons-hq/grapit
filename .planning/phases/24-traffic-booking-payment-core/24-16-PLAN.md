---
phase: 24-traffic-booking-payment-core
plan: "16"
type: execute
wave: 6
depends_on:
  - "24-06"
  - "24-07"
files_modified:
  - apps/web/components/booking/floor-selector.tsx
  - apps/web/components/booking/booking-page.tsx
  - apps/web/components/booking/seat-map-viewer.tsx
  - apps/web/stores/use-booking-store.ts
  - apps/web/hooks/use-booking.ts
  - apps/web/components/booking/__tests__/floor-selector.test.tsx
autonomous: true
requirements:
  - BOOK-01
  - BOOK-03
must_haves:
  truths:
    - "Users can switch floors without losing selections, timer state, or grouped seat summary."
    - "Visible ticket-limit guidance follows event policy instead of hardcoded `MAX_SEATS=4` copy."
    - "Post-payment seat changes remain unsupported in UI and route users to cancellation/refund guidance."
  artifacts:
    - path: "apps/web/components/booking/floor-selector.tsx"
      provides: "sticky floor navigation UI"
      contains: "ToggleGroup"
    - path: "apps/web/stores/use-booking-store.ts"
      provides: "floor-aware client booking state"
      contains: "floorLabel"
    - path: "apps/web/components/booking/booking-page.tsx"
      provides: "grouped floor-aware booking summary"
      contains: "maxTicketsPerUser"
  key_links:
    - from: "use-booking-store.ts"
      to: "booking-page.tsx"
      via: "grouped selections by floor"
      pattern: "floorKey|floorLabel|seatKey"
    - from: "floor-selector.tsx"
      to: "seat-map-viewer.tsx"
      via: "selected floor drives rendered map without clearing selections"
      pattern: "selectedFloor|svgUrl"
---

<objective>
Add the frontend floor selector, grouped booking state, and policy-aligned seat-selection UI.

Purpose: D-11 through D-14 are only visible to users when the booking UI preserves selections across floors, renders policy-aware copy, and keeps post-payment changes routed to refund/cancellation rather than a fake seat-swap path.
Output: floor selector component, floor-aware booking store/hook, grouped summary UI, and selector tests.
Decision coverage: D-11: floor switching preserves selections and grouped summaries instead of clearing them. D-12: ticket-limit guidance uses the event-configured max policy across all floors. D-13: UI removes hardcoded `MAX_SEATS=4` copy and sends policy-aligned requests. D-14: post-payment seat changes remain unsupported and point users to cancellation/refund guidance.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-traffic-booking-payment-core/24-CONTEXT.md
@.planning/phases/24-traffic-booking-payment-core/24-UI-SPEC.md
@.planning/phases/24-traffic-booking-payment-core/24-PATTERNS.md
@.planning/phases/24-traffic-booking-payment-core/24-07-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add floor-aware booking state and request wiring in the web store/hook</name>
  <files>
    apps/web/stores/use-booking-store.ts
    apps/web/hooks/use-booking.ts
  </files>
  <read_first>
    apps/web/stores/use-booking-store.ts
    apps/web/hooks/use-booking.ts
    packages/shared/src/types/performance.types.ts
  </read_first>
  <action>
    Refactor client booking state so selected seats carry `floorKey`, `floorLabel`, and `seatKey`, preserving selections across floor switches per D-11 while aligning outgoing lock/prepare requests with the backend semantics from Plan 24-07. Replace hardcoded seat-limit assumptions with event-policy fields from the new performance contract per D-12 and D-13.
  </action>
  <acceptance_criteria>
    - `use-booking-store.ts` contains `floorKey`, `floorLabel`, and `seatKey`.
    - `use-booking.ts` sends/consumes floor-aware seat identity and event policy fields.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/web test -- hooks/__tests__/use-booking.test.tsx && pnpm --filter @grabit/web typecheck</automated>
  </verify>
  <done>
    Web booking state matches the floor-aware backend contract instead of the old floor-blind seat model.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the floor selector and grouped booking summary without breaking existing seat-map UX</name>
  <files>
    apps/web/components/booking/floor-selector.tsx
    apps/web/components/booking/booking-page.tsx
    apps/web/components/booking/seat-map-viewer.tsx
    apps/web/components/booking/__tests__/floor-selector.test.tsx
  </files>
  <read_first>
    .planning/phases/24-traffic-booking-payment-core/24-UI-SPEC.md
    apps/web/components/booking/booking-page.tsx
    apps/web/components/booking/seat-map-viewer.tsx
    apps/web/stores/use-booking-store.ts
  </read_first>
  <action>
    Add a sticky `floor-selector.tsx` above the existing `SeatMapViewer`, preserve zoom/pan/minimap behavior, and update `booking-page.tsx` so the summary groups selections by floor label and shows policy-driven ticket-limit copy instead of `최대 4석`. Keep sold/locked seats visibly disabled and route post-payment seat changes to cancellation/refund messaging per D-14.
  </action>
  <acceptance_criteria>
    - `floor-selector.tsx` exists and contains `ToggleGroup`.
    - `booking-page.tsx` groups selected seats by floor label and no longer presents hardcoded `최대 4석` copy.
    - `floor-selector.test.tsx` proves floor switching preserves selections.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/web test -- components/booking/__tests__/floor-selector.test.tsx hooks/__tests__/use-booking.test.tsx && pnpm --filter @grabit/web typecheck</automated>
  </verify>
  <done>
    Users can compare floors freely without losing selections or policy context.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| floor-aware client state -> booking mutations | Selected seat identity crosses from UI state into scarce-seat requests here. |
| floor selector -> seat-map renderer | User-selected floor changes which SVG is rendered here without clearing selections. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-25 | T | `use-booking-store.ts`, `use-booking.ts` | mitigate | Carry explicit `floorKey`, `floorLabel`, and `seatKey` through client state so same-label seats on different floors do not collapse before requests are sent (Task 1). |
| T-24-26 | T | `booking-page.tsx`, `floor-selector.tsx` | mitigate | Preserve selections and policy-driven limits across floor switches per D-11/D-12/D-13 so UI does not imply illegal or lost booking state (Task 2). |
</threat_model>

<verification>
- `pnpm --filter @grabit/web test -- hooks/__tests__/use-booking.test.tsx`
- `pnpm --filter @grabit/web test -- components/booking/__tests__/floor-selector.test.tsx hooks/__tests__/use-booking.test.tsx`
- `pnpm --filter @grabit/web typecheck`
</verification>

<success_criteria>
- Booking UI preserves selections across floors and shows grouped summaries by floor label.
- Client booking state and visible policy guidance align with the backend floor-aware contract and event ticket policy.
</success_criteria>

<output>
After completion, create `.planning/phases/24-traffic-booking-payment-core/24-16-SUMMARY.md`
</output>
