---
phase: 24-traffic-booking-payment-core
plan: "05"
type: execute
wave: 4
depends_on:
  - "24-03"
files_modified:
  - apps/api/src/modules/traffic/traffic.module.ts
  - apps/api/src/modules/traffic/traffic-defense.service.ts
  - apps/api/src/modules/traffic/traffic-defense.service.spec.ts
  - apps/api/src/app.module.ts
  - apps/api/src/modules/ops/prewarm.module.ts
  - apps/api/src/modules/ops/prewarm.controller.ts
  - apps/api/src/modules/ops/prewarm.service.ts
  - apps/api/src/modules/ops/prewarm.service.spec.ts
  - docs/runbooks/phase24-queue-waf-prewarm.md
autonomous: true
requirements:
  - TRAF-02
  - TRAF-03
user_setup:
  - service: cloudflare
    why: "TRAF-02 requires real WAF/rate-limit/challenge rules in the active zone."
    dashboard_config:
      - task: "Create queue-entry, lock-seat, prepare-reservation, confirm-payment, login, signup, and SMS rules from the runbook."
        location: "Cloudflare Dashboard -> Security -> WAF / Rate Limiting Rules"
      - task: "Verify whether Enterprise-only bot-score fields exist; if not, keep the non-Enterprise fallback documented in the runbook."
        location: "Cloudflare Dashboard -> Security -> WAF"
  - service: google-cloud-scheduler
    why: "TRAF-03 needs authenticated prewarm and step-down jobs."
    env_vars:
      - name: PREWARM_CONTROL_TOKEN
        source: "Secret Manager / Cloud Run env"
      - name: PREWARM_PROJECT_ID
        source: "GCP project id"
      - name: PREWARM_REGION
        source: "Cloud Run region, expected asia-northeast3"
      - name: PREWARM_ALLOWED_SCHEDULER_EMAIL
        source: "Cloud Scheduler OIDC service account email"
      - name: PREWARM_ALLOWED_AUDIENCE
        source: "Expected OIDC audience for the internal prewarm endpoint"
    dashboard_config:
      - task: "Create POST Scheduler jobs for scale-up and step-down against the internal prewarm endpoint."
        location: "Cloud Scheduler -> Jobs"
      - task: "Configure each Scheduler job to send an OIDC token from the dedicated service account and record the audience value used by the app validator."
        location: "Cloud Scheduler -> Jobs -> Security / Authentication"
must_haves:
  truths:
    - "Traffic failures are categorized into retry, challenge, or block instead of a generic failure."
    - "Prewarm uses a protected intermediary control path rather than an impossible direct Scheduler PATCH design."
    - "Prewarm requires both the app token and a valid Scheduler OIDC identity claim set before scale-up or step-down is allowed."
    - "D-06: Progressive defense escalates from endpoint-specific rate limits to `Managed Challenge` and then `Block` for clear macro/bot behavior."
    - "D-07: Rate limits are keyed by endpoint plus the richest available identity context (`userId`, session cookie, admission token, and IP)."
    - "D-09: Phase 24 limits macro scoring to repeated booking-critical `lock/prepare/payment` attempts rather than full anti-fraud graphing."
  artifacts:
    - path: "apps/api/src/modules/traffic/traffic-defense.service.ts"
      provides: "rate/challenge/block decision engine"
      contains: "SECURITY_CHALLENGE_REQUIRED"
    - path: "apps/api/src/modules/ops/prewarm.service.ts"
      provides: "protected prewarm controller logic"
      contains: "PREWARM_ALLOWED_SCHEDULER_EMAIL"
    - path: "docs/runbooks/phase24-queue-waf-prewarm.md"
      provides: "operator runbook"
      contains: "Managed Challenge"
  key_links:
    - from: "traffic-defense.service.ts"
      to: "app.module.ts"
      via: "named throttler wiring"
      pattern: "queue-entry|lock-seat|confirm-payment"
    - from: "prewarm.controller.ts"
      to: "phase24-queue-waf-prewarm.md"
      via: "documented POST control path"
      pattern: "internal/prewarm"
---

<objective>
Implement the Phase 24 traffic-defense and prewarm control path.

Purpose: D-06 through D-09 and TRAF-03 require app-layer rate/macro decisions plus a realistic prewarm design that matches Cloudflare/GCP constraints.
Output: traffic defense service, protected prewarm endpoint, tests, and operator runbook.
Decision coverage: D-06: progressive defense escalates from endpoint rate limits to `Managed Challenge` to `Block`. D-07: throttling keys use the richest available identity context (`userId`, session cookie, admission token, IP) per endpoint. D-09: Phase 24 limits macro scoring to booking-critical lock/prepare/payment behavior instead of full fraud graphing.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-traffic-booking-payment-core/24-CONTEXT.md
@.planning/phases/24-traffic-booking-payment-core/24-RESEARCH.md
@.planning/phases/24-traffic-booking-payment-core/24-VALIDATION.md
@.planning/phases/23-launch-foundation/23-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add app-layer traffic defense for booking-critical endpoints</name>
  <files>
    apps/api/src/modules/traffic/traffic.module.ts
    apps/api/src/modules/traffic/traffic-defense.service.ts
    apps/api/src/modules/traffic/traffic-defense.service.spec.ts
    apps/api/src/app.module.ts
  </files>
  <read_first>
    .planning/phases/24-traffic-booking-payment-core/24-CONTEXT.md
    .planning/phases/24-traffic-booking-payment-core/24-RESEARCH.md
    apps/api/src/app.module.ts
  </read_first>
  <action>
    Add a `traffic` module that centralizes rate-limit, challenge, and block decisions for D-06 through D-09. Extend the existing throttler configuration with named booking-critical policies (`queue-entry`, `lock-seat`, `prepare-reservation`, `confirm-payment`, `signup`, `sms`). Use the richest available identity context in order: `userId`, session cookie, admission token, and IP per D-07; for `queue-entry` before an admission token exists, key on authenticated `userId` first and otherwise combine session cookie + IP instead of IP-only fallback. Return explicit codes `TRAFFIC_RATE_LIMITED`, `SECURITY_CHALLENGE_REQUIRED`, and `SECURITY_BLOCKED`. Do not hardcode Enterprise-only `cf.bot_management.*` field assumptions into runtime code. Addresses review concern: queue-entry fallback identity quality.
  </action>
  <acceptance_criteria>
    - `traffic-defense.service.ts` contains `queue-entry`, `lock-seat`, `prepare-reservation`, `confirm-payment`, `TRAFFIC_RATE_LIMITED`, `SECURITY_CHALLENGE_REQUIRED`, and `SECURITY_BLOCKED`.
    - `traffic-defense.service.ts` contains an identity path for session cookie + IP fallback on `queue-entry`.
    - `app.module.ts` wires the traffic module or provider.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/api test -- src/modules/traffic/traffic-defense.service.spec.ts && pnpm --filter @grabit/api typecheck</automated>
  </verify>
  <done>
    Booking-critical traffic gets deterministic retry/challenge/block decisions in code.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the protected prewarm control path and runbook</name>
  <files>
    apps/api/src/modules/ops/prewarm.module.ts
    apps/api/src/modules/ops/prewarm.controller.ts
    apps/api/src/modules/ops/prewarm.service.ts
    apps/api/src/modules/ops/prewarm.service.spec.ts
    docs/runbooks/phase24-queue-waf-prewarm.md
  </files>
  <read_first>
    .planning/phases/24-traffic-booking-payment-core/24-RESEARCH.md
    .planning/phases/24-traffic-booking-payment-core/24-VALIDATION.md
    AGENTS.md
  </read_first>
  <action>
    Add `POST /api/v1/internal/prewarm/services/:serviceName` and `POST /api/v1/internal/prewarm/services/:serviceName/step-down` protected by `PREWARM_CONTROL_TOKEN`, using `PREWARM_PROJECT_ID` and `PREWARM_REGION` in the service. Validate the Scheduler-supplied OIDC identity token claims before honoring either route: require the expected issuer, `aud = PREWARM_ALLOWED_AUDIENCE`, and `email = PREWARM_ALLOWED_SCHEDULER_EMAIL`, then require the shared app token as a second factor. In the runbook, document the Cloudflare rule groups, the non-Enterprise fallback for bot-score uncertainty, and the exact Scheduler POST jobs plus OIDC service-account configuration needed for scale-up and step-down. This resolves the Cloud Scheduler PATCH limitation as a plan constraint rather than an unresolved assumption. Addresses review concern: layered prewarm defense with OIDC plus app token.
  </action>
  <acceptance_criteria>
    - `prewarm.controller.ts` contains both prewarm and step-down routes.
    - `prewarm.service.ts` contains `PREWARM_CONTROL_TOKEN`, `PREWARM_PROJECT_ID`, `PREWARM_REGION`, `PREWARM_ALLOWED_SCHEDULER_EMAIL`, and `PREWARM_ALLOWED_AUDIENCE`.
    - `prewarm.service.ts` validates `aud` and `email` claims before performing scale changes.
    - `phase24-queue-waf-prewarm.md` contains `Managed Challenge`, `OIDC`, `queue-entry`, `lock-seat`, `confirm-payment`, and `step-down`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/api test -- src/modules/ops/prewarm.service.spec.ts && rg -n "Managed Challenge|OIDC|queue-entry|lock-seat|confirm-payment|step-down" docs/runbooks/phase24-queue-waf-prewarm.md</automated>
  </verify>
  <done>
    TRAF-03 has a protected, testable prewarm control path and an operator runbook that does not depend on impossible direct Scheduler behavior.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| edge/app traffic -> booking API | Suspicious traffic is classified here. |
| scheduler/operator -> internal prewarm endpoint | Infrastructure-scaling authority crosses this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-09 | D | `traffic-defense.service.ts` | mitigate | Apply endpoint-specific rate/macro decisions keyed by richer identity than IP-only and return explicit challenge/block states (Task 1). |
| T-24-10 | E | `prewarm.controller.ts` | mitigate | Require both `PREWARM_CONTROL_TOKEN` and validated Scheduler OIDC audience/email claims on scale-up and step-down routes and test anonymous or wrong-claim rejection (Task 2). |
</threat_model>

<verification>
- `pnpm --filter @grabit/api test -- src/modules/traffic/traffic-defense.service.spec.ts`
- `pnpm --filter @grabit/api test -- src/modules/ops/prewarm.service.spec.ts`
- `pnpm --filter @grabit/api typecheck`
- `rg -n "Managed Challenge|OIDC|queue-entry|lock-seat|confirm-payment|step-down" docs/runbooks/phase24-queue-waf-prewarm.md`
</verification>

<success_criteria>
- TRAF-02 is implemented as explicit app-layer decision code plus a Cloudflare runbook.
- TRAF-03 uses a protected POST control path and documented Scheduler setup.
</success_criteria>

<output>
After completion, create `.planning/phases/24-traffic-booking-payment-core/24-05-SUMMARY.md`
</output>
