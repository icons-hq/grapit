---
phase: 24-traffic-booking-payment-core
plan: "20"
type: execute
wave: 11
depends_on:
  - "24-08"
  - "24-UAT"
files_modified:
  - apps/web/components/booking/queue-waiting.tsx
  - apps/web/e2e/booking-queue.spec.ts
autonomous: true
gap_closure: true
requirements:
  - TRAF-01
  - TRAF-02
must_haves:
  truths:
    - "The queue waiting Playwright test targets the metric tiles, not duplicated helper prose."
    - "Queue waiting still proves localized position, ETA, and remaining-seat copy before booking entry."
    - "The Phase 24 queue browser slice passes without strict-locator ambiguity."
  artifacts:
    - path: "apps/web/components/booking/queue-waiting.tsx"
      provides: "stable selector or accessibility contract for the three queue metrics"
      contains: "queue-metric-position"
    - path: "apps/web/e2e/booking-queue.spec.ts"
      provides: "scoped waiting-state assertions for position/ETA/remaining seats"
      contains: "queue-metric-position"
  key_links:
    - from: "apps/web/e2e/booking-queue.spec.ts"
      to: "apps/web/components/booking/queue-waiting.tsx"
      via: "stable metric locator contract"
      pattern: "queue-metric-position"
    - from: "apps/web/e2e/booking-queue.spec.ts"
      to: "apps/web/messages/ko.json"
      via: "localized queue metric labels"
      pattern: "booking.queue.metrics.position"
---

<objective>
Close the minor Phase 24 queue Playwright failure caused by an unscoped `현재 순번` locator.

Purpose: the waiting UI is rendering correctly, but the test is asserting against page-level text that appears in both the metric label and helper sentence. The fix should make the selector contract precise without rewriting user-visible copy.
Output: stable queue-metric selectors/semantics plus scoped Playwright assertions.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-traffic-booking-payment-core/24-CONTEXT.md
@.planning/phases/24-traffic-booking-payment-core/24-UAT.md
@.planning/phases/24-traffic-booking-payment-core/24-08-SUMMARY.md
@apps/web/components/booking/queue-waiting.tsx
@apps/web/e2e/booking-queue.spec.ts
@apps/web/messages/ko.json

<interfaces>
From `apps/web/components/booking/queue-waiting.tsx`:
```tsx
<div className="grid gap-3 sm:grid-cols-3">
  <div>
    <p>{queueCopy.metrics.position}</p>
    <p>{metricValue(status, position, '-')}</p>
  </div>
  <div>
    <p>{queueCopy.metrics.eta}</p>
    <p>{formatEta(etaSeconds, queueCopy)}</p>
  </div>
  <div>
    <p>{queueCopy.metrics.remainingSeats}</p>
    <p>{metricValue(status, remainingSeats, '-')}</p>
  </div>
</div>
```

From `apps/web/e2e/booking-queue.spec.ts`:
```ts
await expect(page.getByText(koMessages.booking.queue.metrics.position)).toBeVisible();
await expect(page.getByText(koMessages.booking.queue.metrics.eta)).toBeVisible();
await expect(page.getByText(koMessages.booking.queue.metrics.remainingSeats)).toBeVisible();
```
</interfaces>

<gap_diagnosis>
The failure is not a queue-state bug. The helper copy intentionally repeats the same localized substrings required by D-05/D-08, so page-level `getByText()` strict mode matches both the metric tile and the helper paragraph. The fix must tighten the selector scope instead of mutating copy or weakening assertions with `.first()` / `.nth()`.
</gap_diagnosis>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add stable queue metric semantics and scope the waiting-state assertions to them</name>
  <files>
    apps/web/components/booking/queue-waiting.tsx
    apps/web/e2e/booking-queue.spec.ts
  </files>
  <read_first>
    apps/web/components/booking/queue-waiting.tsx
    apps/web/e2e/booking-queue.spec.ts
    apps/web/messages/ko.json
  </read_first>
  <action>
    Add a stable selector contract for the waiting metrics in `queue-waiting.tsx` (preferred: explicit `data-testid` values such as `queue-metric-position`, `queue-metric-eta`, and `queue-metric-remaining-seats`, or an equally stable labeled description-list structure), then rewrite `booking-queue.spec.ts` to assert inside those metric tiles instead of using page-level `getByText(...)`. Keep the existing Korean helper copy and the queue waiting title unchanged so D-05 and D-08 remain intact. Do not “fix” this by adding `.first()` or `.nth()` to ambiguous locators; the execution must leave a stable contract that survives copy repetition.
  </action>
  <verify>
    <automated>pnpm --filter @grabit/web exec playwright test e2e/booking-queue.spec.ts --project=chromium --reporter=line</automated>
  </verify>
  <done>
    The queue waiting browser test passes while still checking localized title, position, ETA, and remaining-seat content.
  </done>
</task>

</tasks>

<risks>
- A selector-only change that depends on DOM order will become flaky again as soon as copy or layout shifts; the contract must be semantic or explicitly named.
- Copy edits to avoid the collision would silently change user-facing localization instead of fixing the actual test harness issue; that is out of scope for this gap.
</risks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright assertion -> localized queue UI | Browser verification must identify the intended metric surface even when copy is repeated elsewhere on the page. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-20-01 | D/R | `apps/web/e2e/booking-queue.spec.ts` | mitigate | Replace ambiguous page-level text assertions with stable metric-scoped selectors so the queue verification stops failing on correct UI (Task 1). |
| T-24-20-02 | T | `apps/web/components/booking/queue-waiting.tsx` | mitigate | Add an explicit selector/accessibility contract for queue metrics without mutating localized copy semantics (Task 1). |
</threat_model>

<verification>
- `pnpm --filter @grabit/web exec playwright test e2e/booking-queue.spec.ts --project=chromium --reporter=line`
</verification>

<success_criteria>
- The queue waiting Playwright spec has no strict-locator violation.
- Metric assertions remain localized and still verify position, ETA, and remaining-seat content.
- User-visible queue copy is not rewritten just to satisfy the test.
</success_criteria>

<output>
After completion, create `.planning/phases/24-traffic-booking-payment-core/24-20-SUMMARY.md`
</output>
