---
phase: 24-traffic-booking-payment-core
plan: "19"
type: execute
wave: 11
depends_on:
  - "24-03"
  - "24-07"
  - "24-UAT"
files_modified:
  - apps/api/src/modules/booking/__tests__/booking.service.integration.spec.ts
  - apps/api/test/booking-cluster-lua.integration.spec.ts
  - apps/api/src/database/migrations/0012_phase24_booking_core.sql
autonomous: true
gap_closure: true
requirements:
  - BOOK-01
  - BOOK-03
must_haves:
  truths:
    - "Booking integration specs exercise the current floor-aware seatKey and policy query contract instead of pre-Phase-24 fixtures."
    - "A fresh Postgres container can apply the full Drizzle migration chain without duplicate-index failure."
    - "The real-container API integration suite passes with Docker running."
  artifacts:
    - path: "apps/api/src/modules/booking/__tests__/booking.service.integration.spec.ts"
      provides: "real Valkey integration coverage updated for `leftJoin()` policy lookup and canonical runtime seat keys"
      contains: "leftJoin"
    - path: "apps/api/test/booking-cluster-lua.integration.spec.ts"
      provides: "cluster-mode booking Lua coverage aligned to Phase 24 runtime seat identity"
      contains: "encodeURIComponent"
    - path: "apps/api/src/database/migrations/0012_phase24_booking_core.sql"
      provides: "Phase 24 migration chain that no longer recreates the Phase 23 published-translation index"
      contains: "idx_seat_inv_showtime_floor_seat_key"
  key_links:
    - from: "apps/api/src/modules/booking/__tests__/booking.service.integration.spec.ts"
      to: "apps/api/src/modules/booking/booking.service.ts"
      via: "`getMaxTicketsPerUser()` query shape"
      pattern: "leftJoin"
    - from: "apps/api/test/booking-cluster-lua.integration.spec.ts"
      to: "apps/api/src/modules/booking/booking.service.ts"
      via: "encoded runtime seat keys"
      pattern: "encodeURIComponent"
    - from: "apps/api/src/database/migrations/0012_phase24_booking_core.sql"
      to: "apps/api/src/database/migrations/0007_phase23_launch_foundation.sql"
      via: "single source of truth for `idx_translation_drafts_one_published_per_source_locale`"
      pattern: "idx_translation_drafts_one_published_per_source_locale"
---

<objective>
Close the Phase 24 API integration blockers: stale booking fixtures and a duplicate migration index.

Purpose: automated UAT showed the production runtime is ahead of the real-container specs. Booking integration tests still assume raw seat ids and a pre-policy query shape, and fresh migration apply fails because Phase 24 recreates a translation-drafts index already created in Phase 23.
Output: updated booking/cluster integration fixtures plus a fresh-DB-safe `0012_phase24_booking_core.sql`.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-traffic-booking-payment-core/24-CONTEXT.md
@.planning/phases/24-traffic-booking-payment-core/24-UAT.md
@.planning/phases/24-traffic-booking-payment-core/24-03-SUMMARY.md
@.planning/phases/24-traffic-booking-payment-core/24-07-SUMMARY.md
@apps/api/src/modules/booking/booking.service.ts
@apps/api/src/modules/booking/__tests__/booking.service.integration.spec.ts
@apps/api/test/booking-cluster-lua.integration.spec.ts
@apps/api/test/admin-dashboard.integration.spec.ts
@apps/api/src/database/migrations/0007_phase23_launch_foundation.sql
@apps/api/src/database/migrations/0012_phase24_booking_core.sql

<interfaces>
From `apps/api/src/modules/booking/booking.service.ts`:
```ts
private async getMaxTicketsPerUser(showtimeId: string): Promise<number> {
  const [row] = await this.db
    .select({ maxTicketsPerUser: bookingPolicies.maxTicketsPerUser })
    .from(showtimes)
    .leftJoin(bookingPolicies, eq(bookingPolicies.performanceId, showtimes.performanceId))
    .where(eq(showtimes.id, showtimeId));
  return row?.maxTicketsPerUser ?? DEFAULT_PERFORMANCE_BOOKING_POLICY.maxTicketsPerUser;
}
```

From `apps/api/src/modules/booking/booking.service.ts`:
```ts
type RuntimeSeatIdentity = {
  seatId: string;
  floorKey: string;
  seatKey: string;
  runtimeSeatId: string;
};
// runtimeSeatId = encodeURIComponent(seatKey)
```

From `apps/api/test/admin-dashboard.integration.spec.ts`:
```ts
await migrate(db, { migrationsFolder: 'src/database/migrations' });
```
</interfaces>

<gap_diagnosis>
Automated UAT isolated two independent regressions inside one verification gate:
- both booking integration specs still provide `mockDb.select().from().where()` only, but `BookingService.lockSeat()` now calls `getMaxTicketsPerUser()` with `leftJoin()` per D-12/D-13;
- both specs still seed raw lock ids like `A-1`, while Phase 24 canonical runtime keys are floor-aware and URL-encoded (for example `1F%3AA-1`);
- `0012_phase24_booking_core.sql` recreates `idx_translation_drafts_one_published_per_source_locale`, which `0007_phase23_launch_foundation.sql` already created, so fresh migration apply explodes before admin-dashboard integration can even seed data.
</gap_diagnosis>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh booking integration stubs and seat-key fixtures to the Phase 24 runtime contract</name>
  <files>
    apps/api/src/modules/booking/__tests__/booking.service.integration.spec.ts
    apps/api/test/booking-cluster-lua.integration.spec.ts
  </files>
  <read_first>
    apps/api/src/modules/booking/booking.service.ts
    .planning/phases/24-traffic-booking-payment-core/24-07-SUMMARY.md
    apps/api/src/modules/booking/__tests__/booking.service.integration.spec.ts
    apps/api/test/booking-cluster-lua.integration.spec.ts
  </read_first>
  <action>
    Replace the stale `mockDb.select().from().where()` helper shape in both specs with a stub that supports `.leftJoin().where()` for `getMaxTicketsPerUser()` and returns deterministic `maxTicketsPerUser` rows per D-12/D-13. Update seeded Redis keys, `user-seats` members, `locked-seats` members, and `lockId` expectations to the canonical runtime seat ids derived from `encodeURIComponent(\`\${floorKey}:\${seatId}\`)`, while keeping user-facing assertions on decoded seat ids where `BookingService` intentionally returns them. Preserve the Phase 19 stale-index assertions and cluster-slot invariants; do not weaken the test by replacing specific ownership/conflict expectations with generic pass/fail checks.
  </action>
  <verify>
    <automated>pnpm --filter @grabit/api test:integration -- src/modules/booking/__tests__/booking.service.integration.spec.ts test/booking-cluster-lua.integration.spec.ts</automated>
  </verify>
  <done>
    The booking and cluster integration specs prove the current floor-aware lock identity and booking-policy query contract against real Valkey containers.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make the migration chain fresh-DB safe by removing the duplicate translation index from 0012</name>
  <files>
    apps/api/src/database/migrations/0012_phase24_booking_core.sql
  </files>
  <read_first>
    apps/api/src/database/migrations/0007_phase23_launch_foundation.sql
    apps/api/src/database/migrations/0012_phase24_booking_core.sql
    apps/api/test/admin-dashboard.integration.spec.ts
  </read_first>
  <action>
    Delete only the duplicate `CREATE UNIQUE INDEX "idx_translation_drafts_one_published_per_source_locale"` statement from `0012_phase24_booking_core.sql`, leaving `0007_phase23_launch_foundation.sql` as the single source of truth for that partial unique index. Do not renumber migrations, rename the index, or broaden the migration edit to unrelated Phase 24 DDL. Prove the fix through `admin-dashboard.integration.spec.ts`, which reapplies the entire migration chain onto a blank Postgres container before exercising service queries, then rerun the full API integration suite so the booking-spec fixes and fresh-migration fix are validated together.
  </action>
  <verify>
    <automated>pnpm --filter @grabit/api test:integration -- admin-dashboard.integration &amp;&amp; pnpm --filter @grabit/api test:integration</automated>
  </verify>
  <done>
    A blank integration database migrates cleanly, and the full API integration suite no longer fails on stale booking fixtures or duplicate-index startup.
  </done>
</task>

</tasks>

<risks>
- If the two booking specs keep separate ad-hoc mock shapes, they can drift again on the next booking-policy or seat-key change; keep them explicitly aligned during execution.
- Removing the wrong index statement from `0012` would mutate production schema intent instead of only fixing fresh-bootstrap duplication; the edit must target the one index already created in `0007`.
</risks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| integration test harness -> production booking contract | Tests must reflect the current runtime query and seat-key semantics rather than obsolete fixtures. |
| migration chain -> empty Postgres database | Fresh environments depend on additive migrations that can run exactly once without duplicate objects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-19-01 | T/R | booking integration specs | mitigate | Update the DB stub and lock-key fixtures to the real Phase 24 `leftJoin()` and encoded seatKey contract so tests stop giving false negatives/positives (Task 1). |
| T-24-19-02 | D/T | `0012_phase24_booking_core.sql` | mitigate | Remove only the duplicate published-translation index so fresh DB migration succeeds without altering the established Phase 23 schema contract (Task 2). |
</threat_model>

<verification>
- `pnpm --filter @grabit/api test:integration -- src/modules/booking/__tests__/booking.service.integration.spec.ts test/booking-cluster-lua.integration.spec.ts`
- `pnpm --filter @grabit/api test:integration -- admin-dashboard.integration`
- `pnpm --filter @grabit/api test:integration`
</verification>

<success_criteria>
- Booking integration specs use the current floor-aware seat identity and booking-policy query shape.
- Fresh migration apply succeeds on an empty Postgres container.
- `pnpm --filter @grabit/api test:integration` is green with Docker running.
</success_criteria>

<output>
After completion, create `.planning/phases/24-traffic-booking-payment-core/24-19-SUMMARY.md`
</output>
