---
phase: 24-traffic-booking-payment-core
plan: "12"
type: execute
wave: 8
depends_on:
  - "24-06"
  - "24-11"
files_modified:
  - apps/api/src/modules/admin/admin-booking.controller.ts
  - apps/api/src/modules/admin/admin-booking.service.ts
  - apps/api/src/modules/admin/admin-booking.service.spec.ts
autonomous: true
requirements:
  - BOOK-03
  - REFUND-02
must_haves:
  truths:
    - "Operator manual-open is a real backend exception path that can bypass delayed reopen when policy allows."
    - "Manual-open is separated from user cancellation so normal refunds still keep randomized hold behavior."
    - "Every manual-open action writes a minimal immutable audit row before the delayed-job bypass occurs."
    - "D-18: Operator manual open is the explicit exception to random holding and may reopen cancelled seats immediately."
  artifacts:
    - path: "apps/api/src/modules/admin/admin-booking.service.ts"
      provides: "manual-open exception path"
      contains: "manualOpen"
  key_links:
    - from: "admin-booking.controller.ts"
      to: "admin-booking.service.ts"
      via: "manual-open admin endpoint"
      pattern: "manualOpen"
---

<objective>
Implement the admin manual-open exception path separately from the normal refund track.

Purpose: D-18 makes manual open the explicit exception to randomized cancelled-seat holding, so it cannot remain an undocumented future note.
Output: admin controller/service/spec support for immediate operator reopen.
Decision coverage: D-18: manual-open is the privileged backend exception that can bypass randomized cancelled-seat holding immediately.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-traffic-booking-payment-core/24-CONTEXT.md
@.planning/phases/24-traffic-booking-payment-core/24-RESEARCH.md
@.planning/milestones/v1.0-phases/04-booking-payment/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the backend-only manual-open exception path</name>
  <files>
    apps/api/src/modules/admin/admin-booking.controller.ts
    apps/api/src/modules/admin/admin-booking.service.ts
    apps/api/src/modules/admin/admin-booking.service.spec.ts
  </files>
  <read_first>
    apps/api/src/modules/admin/admin-booking.controller.ts
    apps/api/src/modules/admin/admin-booking.service.ts
    apps/api/src/modules/jobs/cancelled-seat-release.worker.ts
    apps/api/src/database/schema/booking-operation-audit-logs.ts
  </read_first>
  <action>
    Extend the admin booking controller/service so an operator can reopen cancelled seats immediately via a dedicated backend path. The service must bypass or clear any pending delayed reopen job from Plan 24-11, write one minimal immutable audit row to `booking_operation_audit_logs` with `operatorUserId`, `action='manual_open'`, `seatKey`, `reservationId`, and `createdAt`, and must not change normal user-cancel behavior. Keep the exception explicit and server-side only; the richer operator UI belongs to Phase 25. Addresses review concern: manual-open audit evidence.
  </action>
  <acceptance_criteria>
    - `admin-booking.service.ts` contains `manualOpen`, `manual_open`, and `bookingOperationAuditLogs`.
    - `admin-booking.controller.ts` exposes a dedicated manual-open action.
    - `admin-booking.service.spec.ts` covers immediate reopen bypassing the delayed job path and writing the audit row.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/api test -- src/modules/admin/admin-booking.service.spec.ts && pnpm --filter @grabit/api typecheck</automated>
  </verify>
  <done>
    Admin manual-open exists as a real exception flow and is isolated from the standard refund path.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin action -> seat availability | Immediate reopen authority crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-23 | E | `admin-booking.service.ts` | mitigate | Keep manual-open as a separate privileged backend path that explicitly bypasses delayed reopen only when invoked by admin logic (Task 1). |
</threat_model>

<verification>
- `pnpm --filter @grabit/api test -- src/modules/admin/admin-booking.service.spec.ts`
- `pnpm --filter @grabit/api typecheck`
</verification>

<success_criteria>
- D-18 manual-open is implementable without weakening the normal randomized cancelled-seat hold path.
- Manual-open leaves a minimal backend audit trail for later Phase 25 operations review.
</success_criteria>

<output>
After completion, create `.planning/phases/24-traffic-booking-payment-core/24-12-SUMMARY.md`
</output>
