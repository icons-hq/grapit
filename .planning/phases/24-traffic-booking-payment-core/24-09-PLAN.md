---
phase: 24-traffic-booking-payment-core
plan: "09"
type: execute
wave: 6
depends_on:
  - "24-04"
  - "24-07"
files_modified:
  - apps/api/src/modules/payment/payment.module.ts
  - apps/api/src/modules/payment/payment.controller.ts
  - apps/api/src/modules/payment/payment-webhook.controller.ts
  - apps/api/src/modules/payment/payment.service.ts
  - apps/api/src/modules/payment/payment.service.spec.ts
  - apps/api/src/modules/payment/toss-webhook.controller.spec.ts
  - apps/api/src/modules/reservation/reservation.service.ts
  - apps/api/src/modules/reservation/reservation.service.spec.ts
autonomous: true
requirements:
  - BOOK-02
  - PAY-02
must_haves:
  truths:
    - "Domestic and overseas payment branches are split correctly between sync and async Toss flows."
    - "Async payment and cancel webhooks are idempotent, replay-safe, and out-of-order safe."
    - "Payment deadline is a 7-minute invariant distinct from the 10-minute seat-lock TTL and 10-minute admission active window."
    - "Redirect-return amount tampering is rejected before confirm succeeds."
    - "Valid in-flight payment confirmation can extend recovery state deliberately instead of expiring silently."
    - "D-21: Payment expansion stays with the Toss Payments SDK/widget-compatible path, using provider branches and webhooks instead of a custom direct payment UI."
  artifacts:
    - path: "apps/api/src/modules/payment/payment-webhook.controller.ts"
      provides: "async Toss webhook handling"
      contains: "PAYMENT_STATUS_CHANGED"
    - path: "apps/api/src/modules/payment/payment.service.ts"
      provides: "payment branch orchestration"
      contains: "FOREIGN_EASY_PAY"
    - path: "apps/api/src/modules/reservation/reservation.service.ts"
      provides: "deadline/tampering/cascade orchestration"
      contains: "PAYMENT_DEADLINE_MINUTES"
  key_links:
    - from: "payment-webhook.controller.ts"
      to: "payment-webhook-events.ts"
      via: "provider event idempotency"
      pattern: "eventId"
    - from: "reservation.service.ts"
      to: "payment.service.ts"
      via: "pendingUrl + asyncStatus"
      pattern: "pendingUrl|asyncStatus"
---

<objective>
Implement the backend synchronous/asynchronous Toss payment branching.

Purpose: D-15 and D-21 are backend state-branch problems first; the server must know which methods are sync, which are async, and how webhook retries are made safe.
Output: payment controller/webhook/controller wiring, idempotent async handling, and reservation-side payment progress updates.
Decision coverage: D-04: legitimate in-flight payment progress extends the active admission/payment window instead of expiring on the old lock timer. D-15: backend payment state preserves overseas provider/disclaimer branches for card, Alipay+, and TrueMoney paths. D-21: payment expansion stays on Toss synchronous/widget-compatible branches plus async webhooks, not a custom direct payment UI.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-traffic-booking-payment-core/24-CONTEXT.md
@.planning/phases/24-traffic-booking-payment-core/24-RESEARCH.md
@.planning/phases/24-traffic-booking-payment-core/24-PATTERNS.md
@.planning/milestones/v1.0-phases/04-booking-payment/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add provider-specific Toss request branching for sync and async methods</name>
  <files>
    apps/api/src/modules/payment/payment.module.ts
    apps/api/src/modules/payment/payment.controller.ts
    apps/api/src/modules/payment/payment.service.ts
    apps/api/src/modules/payment/payment.service.spec.ts
  </files>
  <read_first>
    apps/api/src/modules/payment/toss-payments.client.ts
    apps/api/src/modules/payment/payment.service.ts
    .planning/phases/24-traffic-booking-payment-core/24-RESEARCH.md
  </read_first>
  <action>
    Keep domestic methods on the existing synchronous confirm path, use `CARD` with `useInternationalCardOnly=true` for overseas card, and use `FOREIGN_EASY_PAY` plus `pendingUrl` for Alipay+ and TrueMoney per D-21. Keep the branch matrix explicit in `payment.service.ts`/`payment.controller.ts` so the backend never treats every method as a generic redirect-confirm flow. Add `payment.service.spec.ts` to lock the domestic card, overseas card, and foreign easy-pay branch selection contract before webhook work lands. Addresses review concern: split the oversized payment task into concrete branch orchestration first.
  </action>
  <acceptance_criteria>
    - `payment.service.ts` contains `CARD`, `useInternationalCardOnly`, `FOREIGN_EASY_PAY`, and `pendingUrl`.
    - `payment.controller.ts` exposes the provider-branch input needed by the Toss widget path without introducing a custom direct-payment UI.
    - `payment.service.spec.ts` covers domestic card, overseas card, and foreign easy-pay branch selection.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/api test -- src/modules/payment/payment.service.spec.ts</automated>
  </verify>
  <done>
    Backend payment method branching is explicit before async webhook handling begins.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement webhook idempotency for replay and out-of-order async events</name>
  <files>
    apps/api/src/modules/payment/payment-webhook.controller.ts
    apps/api/src/modules/payment/toss-webhook.controller.spec.ts
  </files>
  <read_first>
    apps/api/src/modules/payment/payment.service.ts
    apps/api/src/database/schema/payment-webhook-events.ts
    .planning/phases/24-traffic-booking-payment-core/24-RESEARCH.md
  </read_first>
  <action>
    Add `payment-webhook.controller.ts` for `PAYMENT_STATUS_CHANGED` and `CANCEL_STATUS_CHANGED`, using `payment_webhook_events` as the durable ledger with `eventId`, `eventType`, `payload`, `receivedAt`, `processedAt`, and `processingResultCode`. Make handlers idempotent for duplicate replay and safe for out-of-order async delivery, and only acknowledge success after the durable ledger write succeeds. Addresses review concerns: webhook replay safety, out-of-order handling, and ledger completeness.
  </action>
  <acceptance_criteria>
    - `payment-webhook.controller.ts` contains `PAYMENT_STATUS_CHANGED` and `CANCEL_STATUS_CHANGED`.
    - `payment-webhook.controller.ts` or its persistence path uses `eventId`, `eventType`, `receivedAt`, `processedAt`, and `processingResultCode`.
    - `toss-webhook.controller.spec.ts` covers duplicate webhook replay and out-of-order event handling.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/api test -- src/modules/payment/toss-webhook.controller.spec.ts</automated>
  </verify>
  <done>
    Async payment and cancel webhooks are durably replay-safe before reservation orchestration consumes them.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce the 7-minute payment deadline, amount integrity, and expiry cascade</name>
  <files>
    apps/api/src/modules/reservation/reservation.service.ts
    apps/api/src/modules/reservation/reservation.service.spec.ts
  </files>
  <read_first>
    apps/api/src/modules/reservation/reservation.service.ts
    apps/api/src/modules/booking/booking.service.ts
    apps/api/src/modules/payment/payment.service.ts
    apps/api/src/modules/queue/queue.service.ts
  </read_first>
  <action>
    Extend reservation orchestration so payment preparation/confirmation stores `paymentDeadlineAt = preparedAt + 7 minutes`, `pendingUrl`, and async status from the new backend branch. Preserve the existing 10-minute seat-lock TTL and the 10-minute admission active window as separate invariants: after 7 minutes, block new payment attempts and mark the pending reservation expired if provider handoff never began; if the provider handoff started before the 7-minute deadline, allow only completion/recovery until the 10-minute lock/admission window ends, then compensate/cancel instead of silently succeeding later. Compare redirect-return `amount` against the prepared canonical reservation amount before confirm succeeds, so client-side tampering cannot produce a false success. Addresses review concerns: timer invariant, amount tampering, and reservation-side orchestration size.
  </action>
  <acceptance_criteria>
    - `reservation.service.ts` contains `PAYMENT_DEADLINE_MINUTES = 7`, `paymentDeadlineAt`, and redirect amount equality enforcement against the prepared amount.
    - `reservation.service.spec.ts` covers deadline expiry at 7 minutes, redirect amount tampering rejection, and replay-safe completion for provider handoff started before deadline but before the 10-minute lock/admission expiry.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/api test -- src/modules/reservation/reservation.service.spec.ts && pnpm --filter @grabit/api typecheck</automated>
  </verify>
  <done>
    Reservation state enforces the payment/lock/admission timer invariant and amount integrity for both synchronous and asynchronous payment completion.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| confirm path -> Toss provider | User-selected payment method leaves the app here. |
| provider webhook -> payment persistence | External async payment state enters durable storage here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-17 | T/R | `payment-webhook.controller.ts` | mitigate | Persist provider event ids plus processing metadata and make webhook handlers replay-safe and out-of-order safe before acknowledging success (Task 2). |
| T-24-18 | D/T | `reservation.service.ts` | mitigate | Store a 7-minute payment deadline distinct from the 10-minute lock/admission windows and reject redirect amount tampering before confirm succeeds (Task 3). |
</threat_model>

<verification>
- `pnpm --filter @grabit/api test -- src/modules/payment/payment.service.spec.ts`
- `pnpm --filter @grabit/api test -- src/modules/payment/toss-webhook.controller.spec.ts`
- `pnpm --filter @grabit/api test -- src/modules/reservation/reservation.service.spec.ts`
- `pnpm --filter @grabit/api typecheck`
</verification>

<success_criteria>
- Backend payment orchestration distinguishes sync and async Toss branches correctly.
- Webhook retries and out-of-order async events cannot double-apply payment or cancel state.
- Payment deadline, seat lock TTL, and admission active window remain separate, testable invariants.
</success_criteria>

<output>
After completion, create `.planning/phases/24-traffic-booking-payment-core/24-09-SUMMARY.md`
</output>
