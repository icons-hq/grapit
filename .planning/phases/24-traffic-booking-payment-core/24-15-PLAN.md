---
phase: 24-traffic-booking-payment-core
plan: "15"
type: execute
wave: 5
depends_on:
  - "24-06"
files_modified:
  - apps/web/hooks/use-admin.ts
  - apps/web/components/admin/performance-form.tsx
  - apps/web/components/admin/svg-preview.tsx
  - apps/web/components/admin/floor-seat-map-editor.tsx
autonomous: true
requirements:
  - BOOK-01
  - BOOK-03
must_haves:
  truths:
    - "Operators can manage 2-7 floor rows with visible order, label, SVG preview, and booking-policy controls before public booking UI consumes the data."
    - "Admin floor editing preserves existing SVG safety/preview behavior instead of replacing it with a looser upload path."
  artifacts:
    - path: "apps/web/components/admin/floor-seat-map-editor.tsx"
      provides: "multi-floor admin editor"
      contains: "floorKey"
    - path: "apps/web/components/admin/performance-form.tsx"
      provides: "booking-policy controls"
      contains: "allowedPaymentMethods"
    - path: "apps/web/hooks/use-admin.ts"
      provides: "floor/policy payload transport"
      contains: "seatMaps"
  key_links:
    - from: "performance-form.tsx"
      to: "use-admin.ts"
      via: "floor editor + policy payload submit"
      pattern: "seatMaps|bookingPolicy"
    - from: "svg-preview.tsx"
      to: "floor-seat-map-editor.tsx"
      via: "per-floor preview and validation"
      pattern: "svgUrl|floorLabel"
---

<objective>
Create the admin floor editor and upload/policy UI that consumes the new floor-aware contracts.

Purpose: D-10, D-12, D-13, and the Phase 24 side of D-18 require a concrete operator surface for multi-floor SVG and booking policy data, separate from the contract/API expansion in Plan 24-06.
Output: updated admin hook, floor-seat-map editor, multi-floor SVG preview wiring, and booking-policy form controls.
Decision coverage: D-10: the admin editor manages floor rows as first-class seat-map inputs. D-12: operators edit event-configured max-ticket policy. D-13: admin controls supply the values that replace hardcoded `MAX_SEATS` and payment-method assumptions. D-18: Phase 24 captures the manual-open-supporting policy state before Phase 25 expands the operator console.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-traffic-booking-payment-core/24-CONTEXT.md
@.planning/phases/24-traffic-booking-payment-core/24-UI-SPEC.md
@.planning/phases/24-traffic-booking-payment-core/24-06-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add floor-aware admin hook payloads and the reusable floor editor component</name>
  <files>
    apps/web/hooks/use-admin.ts
    apps/web/components/admin/floor-seat-map-editor.tsx
  </files>
  <read_first>
    apps/web/hooks/use-admin.ts
    packages/shared/src/types/performance.types.ts
    apps/web/components/admin/performance-form.tsx
  </read_first>
  <action>
    Update `use-admin.ts` to send and receive the `seatMaps` and `bookingPolicy` contract defined in Plan 24-06, then create `floor-seat-map-editor.tsx` for managing 2-7 floors with `floorKey`, `floorLabel`, `sortOrder`, `svgUrl`, and `seatConfig`. Keep the data shape explicit per D-10, D-12, and D-13 so later booking/payment plans do not reverse-engineer policy from ad-hoc UI state.
  </action>
  <acceptance_criteria>
    - `use-admin.ts` carries `seatMaps` and `bookingPolicy`.
    - `floor-seat-map-editor.tsx` exists and contains `floorKey`, `floorLabel`, and `sortOrder`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/web test -- components/admin && pnpm --filter @grabit/web typecheck</automated>
  </verify>
  <done>
    Admin UI code can submit floor-aware contracts without falling back to the old single-seat-map payload shape.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire multi-floor SVG preview and booking-policy controls into the performance form</name>
  <files>
    apps/web/components/admin/performance-form.tsx
    apps/web/components/admin/svg-preview.tsx
  </files>
  <read_first>
    .planning/phases/24-traffic-booking-payment-core/24-UI-SPEC.md
    apps/web/components/admin/performance-form.tsx
    apps/web/components/admin/svg-preview.tsx
    apps/web/components/admin/floor-seat-map-editor.tsx
  </read_first>
  <action>
    Embed `floor-seat-map-editor.tsx` into `performance-form.tsx`, expose `maxTicketsPerUser`, `allowedPaymentMethods`, `changePolicyEnabled`, and cancelled-seat hold controls per D-12/D-13/D-18, and adapt `svg-preview.tsx` so each floor preview preserves the existing SVG validation/safety behavior instead of introducing a separate upload path.
  </action>
  <acceptance_criteria>
    - `performance-form.tsx` renders multi-floor and booking-policy controls.
    - `svg-preview.tsx` still performs existing SVG safety/preview behavior while supporting multiple floors.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/web test -- components/admin && pnpm --filter @grabit/web typecheck</automated>
  </verify>
  <done>
    Operators can edit real multi-floor and booking-policy data before later Phase 25 console work.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin floor editor -> admin hook payload | Operator-entered floor/policy data becomes the API request here. |
| SVG preview input -> admin UI state | Uploaded SVG metadata must remain validated before persistence here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-23 | T | `use-admin.ts`, `performance-form.tsx` | mitigate | Carry explicit `seatMaps` and `bookingPolicy` payloads per D-10/D-12/D-13 so UI state cannot silently drop floor or policy fields before submission (Tasks 1-2). |
| T-24-24 | T | `svg-preview.tsx`, `floor-seat-map-editor.tsx` | mitigate | Reuse the existing SVG preview/safety workflow instead of adding a second unchecked upload path for multi-floor assets (Task 2). |
</threat_model>

<verification>
- `pnpm --filter @grabit/web test -- components/admin`
- `pnpm --filter @grabit/web typecheck`
</verification>

<success_criteria>
- Admin hook and form submit the floor-aware `seatMaps` and `bookingPolicy` contract intact.
- Operators can manage multi-floor SVG metadata and booking policy with the existing SVG safety behavior preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/24-traffic-booking-payment-core/24-15-SUMMARY.md`
</output>
