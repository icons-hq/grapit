---
phase: 24-traffic-booking-payment-core
plan: "08"
type: execute
wave: 5
depends_on:
  - "24-04"
  - "24-05"
files_modified:
  - apps/web/app/booking/[performanceId]/page.tsx
  - apps/web/hooks/use-queue.ts
  - apps/web/hooks/__tests__/use-queue.test.tsx
  - apps/web/components/booking/queue-waiting.tsx
  - apps/web/e2e/booking-queue.spec.ts
  - apps/web/messages/ko.json
  - apps/web/messages/en.json
  - apps/web/messages/th.json
  - apps/web/messages/zh-CN.json
  - apps/web/messages/zh-TW.json
autonomous: true
requirements:
  - TRAF-01
  - TRAF-02
must_haves:
  truths:
    - "A user entering `/booking` sees queue position, ETA, remaining seats, and auto-entry instead of a blind redirect."
    - "Queue, rate-limit retry, challenge, and block states each have distinct localized UI."
    - "D-05: Queue UI shows position, estimated wait, remaining seats, and automatically enters the booking screen when admitted."
    - "D-08: User-facing failure states distinguish queue redirect, 429/rate-limit retry, security challenge, and blocked/macro behavior with localized copy."
  artifacts:
    - path: "apps/web/hooks/use-queue.ts"
      provides: "queue status/admission hook"
      contains: "queue:position"
    - path: "apps/web/components/booking/queue-waiting.tsx"
      provides: "full-page queue waiting surface"
      contains: "예매 대기열에서 입장 순서를 기다리고 있습니다"
    - path: "apps/web/e2e/booking-queue.spec.ts"
      provides: "queue flow E2E"
      contains: "queue"
  key_links:
    - from: "use-queue.ts"
      to: "page.tsx"
      via: "queue gate before booking UI"
      pattern: "autoEnter|remainingSeats"
    - from: "queue-waiting.tsx"
      to: "messages/*.json"
      via: "five-locale copy"
      pattern: "queue"
---

<objective>
Wire the queue waiting experience and localized failure states into the booking route.

Purpose: D-01, D-05, D-06, and D-08 require the queue to feel transparent to users while preserving the backend admission and traffic-defense states built earlier.
Output: queue hook, route wrapper, waiting surface, localized copy, and booking queue E2E.
Decision coverage: D-05: the waiting UI shows queue position, ETA, remaining seats, and automatic entry. D-08: queue redirect, 429/retry, security challenge, and blocked/macro states remain distinct in localized user-facing copy.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-traffic-booking-payment-core/24-CONTEXT.md
@.planning/phases/24-traffic-booking-payment-core/24-UI-SPEC.md
@.planning/phases/24-traffic-booking-payment-core/24-VALIDATION.md
@.planning/phases/23-launch-foundation/23-08-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the queue hook, route wrapper, and waiting surface</name>
  <files>
    apps/web/app/booking/[performanceId]/page.tsx
    apps/web/hooks/use-queue.ts
    apps/web/hooks/__tests__/use-queue.test.tsx
    apps/web/components/booking/queue-waiting.tsx
  </files>
  <read_first>
    apps/web/app/booking/[performanceId]/page.tsx
    apps/api/src/modules/queue/queue.gateway.ts
    .planning/phases/24-traffic-booking-payment-core/24-UI-SPEC.md
    .planning/phases/24-traffic-booking-payment-core/24-CONTEXT.md
  </read_first>
  <action>
    Use `page.tsx` as the route-level gate for D-01 so the booking page renders only after queue admission is ready. Create `use-queue.ts` to read the initial snapshot from `GET /api/v1/queue/sessions/:queueSessionId`, then subscribe to the `queue` Socket.IO namespace events `queue:position`, `queue:admitted`, and `queue:expired` from Plan 24-04. Model waiting, admitted, expired, retry, challenge, and blocked states from Plans 24-04 and 24-05, and add `use-queue.test.tsx` for the hook contract instead of extending `use-booking.test.tsx`. Create `queue-waiting.tsx` as the full-page surface that shows position, ETA, remaining seats, and auto-entry behavior per D-05, with a fallback enter action only when automatic navigation fails. Addresses review concerns: queue transport lock and correct hook verification target.
  </action>
  <acceptance_criteria>
    - `queue-waiting.tsx` contains `예매 대기열에서 입장 순서를 기다리고 있습니다`.
    - `use-queue.ts` contains `etaSeconds`, `remainingSeats`, `autoEnter`, `queue:position`, `queue:admitted`, and `queue:expired`.
    - `use-queue.test.tsx` exists and covers at least one auto-enter or expiry transition.
    - `page.tsx` references the queue hook or waiting surface before rendering the booking screen.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/web test -- hooks/__tests__/use-queue.test.tsx && pnpm --filter @grabit/web typecheck</automated>
  </verify>
  <done>
    Booking route entry is queue-aware and renders a transparent waiting surface.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Localize queue failure states and add queue E2E coverage</name>
  <files>
    apps/web/e2e/booking-queue.spec.ts
    apps/web/messages/ko.json
    apps/web/messages/en.json
    apps/web/messages/th.json
    apps/web/messages/zh-CN.json
    apps/web/messages/zh-TW.json
  </files>
  <read_first>
    apps/web/messages/ko.json
    apps/web/messages/en.json
    apps/web/messages/th.json
    apps/web/messages/zh-CN.json
    apps/web/messages/zh-TW.json
    .planning/phases/24-traffic-booking-payment-core/24-VALIDATION.md
  </read_first>
  <action>
    Add exact visible-copy keys for queue waiting, admission success, expired admission, rate-limit retry, security challenge, and hard block across all five locales. Create `booking-queue.spec.ts` to verify the booking route shows queue status before booking and distinguishes retry/challenge/block states instead of collapsing them into one generic error. Reuse the hook contract from `use-queue.test.tsx` so UI and browser coverage share the same queue-state vocabulary.
  </action>
  <acceptance_criteria>
    - All five locale files contain queue waiting, expired, retry, challenge, and blocked copy.
    - `booking-queue.spec.ts` exists and asserts queue position or ETA plus at least one failure-state distinction.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/web test -- hooks/__tests__/use-queue.test.tsx && rg -n "queue|expired|retry|challenge|blocked" apps/web/messages/ko.json apps/web/messages/en.json apps/web/messages/th.json apps/web/messages/zh-CN.json apps/web/messages/zh-TW.json apps/web/e2e/booking-queue.spec.ts && pnpm --filter @grabit/web typecheck</automated>
  </verify>
  <done>
    Queue UX is localized and covered by browser-level verification.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| queue status API -> booking route UI | Admission and failure-state data is rendered to users here. |
| localized copy -> security-sensitive states | Retry/challenge/block semantics are communicated here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-15 | I | `queue-waiting.tsx` | mitigate | Render only user-facing queue metadata and distinct failure-state copy, not raw internal tokens or provider details (Task 1, Task 2). |
| T-24-16 | R | `booking-queue.spec.ts` | mitigate | Add E2E coverage for queue and traffic-defense states so UI regressions cannot silently collapse challenge/block behavior into one state (Task 2). |
</threat_model>

<verification>
- `pnpm --filter @grabit/web test -- hooks/__tests__/use-queue.test.tsx`
- `pnpm --filter @grabit/web test:e2e --grep "queue"`
- `pnpm --filter @grabit/web typecheck`
</verification>

<success_criteria>
- `/booking` shows queue progress and localized retry/challenge/block states.
- Queue waiting UX is tested as a real route-level behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/24-traffic-booking-payment-core/24-08-SUMMARY.md`
</output>
