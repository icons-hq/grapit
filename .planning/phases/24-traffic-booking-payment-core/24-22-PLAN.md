---
phase: 24-traffic-booking-payment-core
plan: "22"
type: execute
wave: 12
depends_on:
  - "24-16"
  - "24-HUMAN-UAT"
files_modified:
  - apps/web/components/booking/seat-map-viewer.tsx
  - apps/web/components/booking/__tests__/seat-map-viewer.test.tsx
  - apps/web/e2e/booking-floor-selection.spec.ts
  - .planning/phases/24-traffic-booking-payment-core/24-HUMAN-UAT.md
autonomous: true
gap_closure: true
requirements:
  - BOOK-01
  - BOOK-02
must_haves:
  truths:
    - "Clicking or tapping the visible center of a numbered seat selects the seat on desktop and mobile; users do not need to hit the rect edge."
    - "After a center click on a numbered seat, the grouped summary, lock timer, and next CTA update through the existing booking flow."
    - "Switching between 1F and 2F after the hit-target fix still preserves the selected seat state required by D-11."
  artifacts:
    - path: "apps/web/components/booking/seat-map-viewer.tsx"
      provides: "overlay-safe SVG seat hit-target behavior"
      contains: "pointer-events"
    - path: "apps/web/components/booking/__tests__/seat-map-viewer.test.tsx"
      provides: "unit regression for centered seat-number text overlays"
      contains: "A-1"
    - path: "apps/web/e2e/booking-floor-selection.spec.ts"
      provides: "browser regression for desktop/mobile center-click selection across floors"
      contains: "floor-browser"
    - path: ".planning/phases/24-traffic-booking-payment-core/24-HUMAN-UAT.md"
      provides: "resolved Human UAT evidence for test 4"
      contains: "test: 4"
  key_links:
    - from: "apps/web/components/booking/seat-map-viewer.tsx"
      to: "apps/web/components/booking/__tests__/seat-map-viewer.test.tsx"
      via: "centered text-overlay regression"
      pattern: "onSeatClick('A-1')"
    - from: "apps/web/e2e/booking-floor-selection.spec.ts"
      to: "apps/web/components/booking/booking-page.tsx"
      via: "route-stubbed `/booking/floor-browser` flow"
      pattern: "floor-browser"
    - from: "apps/web/e2e/booking-floor-selection.spec.ts"
      to: ".planning/phases/24-traffic-booking-payment-core/24-HUMAN-UAT.md"
      via: "desktop/mobile evidence for test 4 closure"
      pattern: "desktop/mobile"
---

<objective>
Close the Phase 24 HUMAN UAT major gap where visible seat-number overlays intercept clicks and break multi-floor booking selection.

Purpose: BOOK-01 is not actually complete until users can reliably click the seat they see, across floors and device sizes, and the existing selection summary/timer/CTA path still responds from that center click.
Output: a seat-map hit-target fix in `SeatMapViewer`, focused unit/browser regressions, and a resolved Human UAT record for test 4.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-traffic-booking-payment-core/24-CONTEXT.md
@.planning/phases/24-traffic-booking-payment-core/24-HUMAN-UAT.md
@.planning/phases/24-traffic-booking-payment-core/24-16-SUMMARY.md
@apps/web/components/booking/seat-map-viewer.tsx
@apps/web/components/booking/booking-page.tsx
@apps/web/components/booking/__tests__/seat-map-viewer.test.tsx
@apps/web/playwright.config.ts

<interfaces>
From `apps/web/components/booking/seat-map-viewer.tsx`:
```tsx
const handleClick = useCallback(
  (e: React.MouseEvent) => {
    const target = (e.target as HTMLElement).closest<SVGElement>('[data-seat-id]');
    if (!target) return;
    const seatId = target.getAttribute('data-seat-id');
    if (!seatId) return;
    ...
    onSeatClick(seatId);
  },
  [seatStates, selectedSeatIds, onSeatClick, maxSelect],
);
```

From `apps/web/playwright.config.ts`:
```ts
projects: [
  {
    name: 'chromium',
    use: { ...devices['Desktop Chrome'] },
  },
]
```

From `apps/web/components/booking/booking-page.tsx`:
```tsx
<SeatMapViewer
  ...
  onSeatClick={handleSeatClick}
/>
```
</interfaces>

<gap_diagnosis>
Human UAT test 4 proved the booking state path already works once the seat rect receives the event: clicking the rect edge updated the summary, timer, and CTA. The actual gap is the rendered hit target. Center clicks on visible seat labels currently land on an SVG `<text>` node that has no `[data-seat-id]` ancestry, so `SeatMapViewer` exits early and the booking UI never hears about the seat.
</gap_diagnosis>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden `SeatMapViewer` against seat-number text overlay interception</name>
  <files>
    apps/web/components/booking/seat-map-viewer.tsx
    apps/web/components/booking/__tests__/seat-map-viewer.test.tsx
  </files>
  <behavior>
    - Clicking the visible centered seat-number text over `A-1` triggers `onSeatClick('A-1')`.
    - The existing locked-seat delegation invariant remains intact, while sold seats still do not trigger selection.
    - The viewer's own injected checkmark text remains non-interactive after the change.
  </behavior>
  <read_first>
    apps/web/components/booking/seat-map-viewer.tsx
    apps/web/components/booking/__tests__/seat-map-viewer.test.tsx
    .planning/phases/24-traffic-booking-payment-core/24-HUMAN-UAT.md
  </read_first>
  <action>
    Fix the hit-target at the processed SVG layer, not by teaching tests to click different pixels. Extend `SeatMapViewer` so vendor seat-number and similar non-seat overlays inside the interactive seat canvas no longer intercept pointer events before the underlying `[data-seat-id]` shape can receive them. Preserve the D-10/D-11 floor-aware renderer contract from Plan 24-16, keep existing checkmark/stage visuals visible, and keep `data-seat-checkmark` overlays non-interactive.

    Add a focused unit regression with an inline SVG fixture where centered `<text>` sits directly over `<rect data-seat-id="A-1">`. The test must click the visible text node itself, not the rect edge, and prove `onSeatClick('A-1')` fires while sold-seat and max-select behavior remain unchanged.
  </action>
  <verify>
    <automated>pnpm --filter @grabit/web test -- components/booking/__tests__/seat-map-viewer.test.tsx</automated>
  </verify>
  <done>Centered numbered-seat overlays no longer block selection inside `SeatMapViewer`, and the unit regression proves the exact human UAT failure is closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add a browser regression for desktop/mobile center-click selection across floors</name>
  <files>apps/web/e2e/booking-floor-selection.spec.ts</files>
  <behavior>
    - On `/booking/floor-browser`, a desktop center click on visible seat `A-1` populates the grouped summary, starts the timer, and enables the next CTA.
    - The same centered interaction passes in a mobile-sized viewport using the current single `chromium` Playwright project.
    - After selecting on 1F, switching to 2F and back preserves the 1F selection required by D-11.
  </behavior>
  <read_first>
    apps/web/playwright.config.ts
    apps/web/components/booking/booking-page.tsx
    .planning/phases/24-traffic-booking-payment-core/24-HUMAN-UAT.md
  </read_first>
  <action>
    Add a focused Playwright regression at `apps/web/e2e/booking-floor-selection.spec.ts` that reproduces the Human UAT flow on `/booking/floor-browser`. Stub auth, runtime flags, performance detail, showtime, seat-status, lock, and prepare-reservation responses so the booking page renders two floors and a visible seat-number overlay over `A-1`.

    Important contract details:
    - Because `apps/web/playwright.config.ts` currently defines only the `chromium` project, cover mobile by creating a second mobile-sized viewport or browser context inside the same spec. Do not reference a non-existent `Mobile Chrome` project.
    - The spec must click or tap the visible center of the seat label, not the rect edge or a DOM node selected by `[data-seat-id]`.
    - Assert the post-click summary, timer, and next CTA state that the control-check in Human UAT already proved when the rect receives the event.
    - Switch floors and back after the hit-target fix so the browser regression still covers D-11 floor persistence, not only a single-floor click.
  </action>
  <verify>
    <automated>pnpm --filter @grabit/web exec playwright test e2e/booking-floor-selection.spec.ts --project=chromium --reporter=line</automated>
  </verify>
  <done>The browser suite now exercises the exact center-click/tap interaction that failed in Human UAT, on both desktop and mobile-sized flows, without relying on edge-click workarounds.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve Human UAT test 4 and summarize the hit-target fix</name>
  <files>
    .planning/phases/24-traffic-booking-payment-core/24-HUMAN-UAT.md
    .planning/phases/24-traffic-booking-payment-core/24-22-SUMMARY.md
  </files>
  <action>
    Update `24-HUMAN-UAT.md` so test 4 is marked resolved once Tasks 1-2 pass. Record the unit test command, the new Playwright spec command, and the exact assertions proving the summary, timer, and CTA now update from center clicks on visible seat numbers. Then create `24-22-SUMMARY.md` with the changed files, the SVG-hit-target approach chosen, and verification output.
  </action>
  <verify>
    <automated>rg -n "test: 4|seat-map-viewer.test.tsx|booking-floor-selection.spec.ts|status: resolved|status: closed" .planning/phases/24-traffic-booking-payment-core/24-HUMAN-UAT.md .planning/phases/24-traffic-booking-payment-core/24-22-SUMMARY.md</automated>
  </verify>
  <done>Phase 24 Human UAT no longer records the seat hit-target issue as unresolved, and the repo contains a focused execution record for the regression coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rendered SVG overlay -> seat selection handler | Non-seat SVG nodes can intercept browser pointer events before booking state receives the selection. |
| Browser regression -> real booking UI state | Test coverage must exercise the visible seat center, not a synthetic rect-edge shortcut that hides the real UX failure. |
| Floor switch UI -> selected-seat state | A hit-target fix must not regress the D-11 floor-persistence contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-22-01 | D | `apps/web/components/booking/seat-map-viewer.tsx` | mitigate | Neutralize or remap non-seat SVG overlay pointer events so visible seat labels cannot deny selection before the seat element receives the event (Task 1). |
| T-24-22-02 | R/D | `apps/web/e2e/booking-floor-selection.spec.ts` | mitigate | Add a browser regression that clicks the visible seat center on desktop and mobile-sized flows instead of using rect-edge or DOM-shortcut interactions (Task 2). |
| T-24-22-03 | T | `apps/web/components/booking/booking-page.tsx` floor state | mitigate | Include 1F -> 2F -> 1F persistence assertions after the hit-target fix so the viewer change cannot silently corrupt the D-11 floor-aware selection contract (Task 2). |
</threat_model>

<verification>
- `pnpm --filter @grabit/web test -- components/booking/__tests__/seat-map-viewer.test.tsx`
- `pnpm --filter @grabit/web exec playwright test e2e/booking-floor-selection.spec.ts --project=chromium --reporter=line`
- `rg -n "status: resolved|booking-floor-selection.spec.ts" .planning/phases/24-traffic-booking-payment-core/24-HUMAN-UAT.md`
</verification>

<success_criteria>
- Center clicks or taps on visible numbered seats select correctly on both desktop and mobile-sized flows.
- The summary, timer, and next CTA update from that centered interaction without manual edge clicking.
- Floor switching after selection still preserves state across 1F and 2F.
- `24-HUMAN-UAT.md` records test 4 as resolved with reproducible automated evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/24-traffic-booking-payment-core/24-22-SUMMARY.md`
</output>
