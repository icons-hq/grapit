---
phase: 24-traffic-booking-payment-core
plan: "11"
type: execute
wave: 7
depends_on:
  - "24-03"
  - "24-09"
files_modified:
  - apps/api/src/modules/refund/refund.module.ts
  - apps/api/src/modules/refund/refund.controller.ts
  - apps/api/src/modules/refund/refund.service.ts
  - apps/api/src/modules/refund/refund.service.spec.ts
  - apps/api/src/modules/jobs/jobs.module.ts
  - apps/api/src/modules/jobs/pgboss.provider.ts
  - apps/api/src/modules/jobs/cancelled-seat-release.worker.ts
  - apps/api/src/modules/jobs/cancelled-seat-release.worker.spec.ts
  - apps/api/src/modules/jobs/refund-cancel-retry.worker.ts
  - apps/api/src/modules/jobs/refund-cancel-retry.worker.spec.ts
autonomous: true
requirements:
  - REFUND-01
  - REFUND-02
must_haves:
  truths:
    - "Refund state changes are visible as a durable state machine rather than an opaque cancel side effect."
    - "User cancellations place seats into a held-cancelled state and reopen them only through a delayed random job."
    - "Transient Toss cancel timeouts/5xx do not silently fail; they enter a retryable reconciliation path."
    - "Cancelled seats never reopen into an already-imminent showtime window."
    - "D-16: Refund UX uses a visible state machine covering `requested`, `sent_to_pg`, `processing_at_pg`, `completed`, and `failed`."
    - "D-17: User cancellations do not reopen seats immediately and instead use a uniform random 1-10 minute delayed reopen job."
  artifacts:
    - path: "apps/api/src/modules/refund/refund.service.ts"
      provides: "refund preview/state machine orchestration"
      contains: "processing_at_pg"
    - path: "apps/api/src/modules/jobs/cancelled-seat-release.worker.ts"
      provides: "delayed cancelled-seat reopen worker"
      contains: "release-cancelled-seat"
  key_links:
    - from: "refund.service.ts"
      to: "cancelled-seat-release.worker.ts"
      via: "delayed reopen enqueue"
      pattern: "release-cancelled-seat"
---

<objective>
Implement refund state handling and delayed cancelled-seat release jobs.

Purpose: D-16 and D-17 require refunds and seat reopen behavior to be durable, explicit, and not tied to the old immediate-reopen cancel path.
Output: refund module, refund tests, pg-boss provider, delayed reopen worker, and refund/job orchestration.
Decision coverage: D-16: refund orchestration persists the visible `requested -> sent_to_pg -> processing_at_pg -> completed/failed` state machine. D-17: user cancellations reopen seats only through randomized delayed jobs, never immediate release.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-traffic-booking-payment-core/24-CONTEXT.md
@.planning/phases/24-traffic-booking-payment-core/24-RESEARCH.md
@.planning/phases/24-traffic-booking-payment-core/24-VALIDATION.md
@.planning/milestones/v1.0-phases/04-booking-payment/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build refund preview and refund state-machine orchestration</name>
  <files>
    apps/api/src/modules/refund/refund.module.ts
    apps/api/src/modules/refund/refund.controller.ts
    apps/api/src/modules/refund/refund.service.ts
    apps/api/src/modules/refund/refund.service.spec.ts
  </files>
  <read_first>
    apps/api/src/modules/reservation/reservation.service.ts
    apps/api/src/modules/payment/toss-payments.client.ts
    .planning/phases/24-traffic-booking-payment-core/24-CONTEXT.md
  </read_first>
  <action>
    Add a dedicated refund module/controller/service that exposes refund preview and tracks the visible D-16 states `requested`, `sent_to_pg`, `processing_at_pg`, `completed`, and `failed`. The service must call Toss cancel safely, be idempotent against duplicate requests, and emit the data the web refund timeline will later render. When Toss cancel returns a network timeout or 5xx/transient failure, keep the refund in a retryable non-terminal state (`sent_to_pg` with persisted result code/error metadata) and enqueue a retry job instead of collapsing straight to `failed`; only terminal provider rejection or exhausted retries become `failed`. Addresses review concern: transient Toss cancel failure/retry policy.
  </action>
  <acceptance_criteria>
    - `refund.service.ts` contains the exact states `requested`, `sent_to_pg`, `processing_at_pg`, `completed`, and `failed`.
    - `refund.service.ts` contains or enqueues `refund-cancel-retry` for transient cancel failure handling.
    - `refund.service.spec.ts` covers duplicate or replay-safe refund behavior and transient Toss cancel failure enqueuing a retry path.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/api test -- src/modules/refund/refund.service.spec.ts</automated>
  </verify>
  <done>
    Refund state is durable and visible before any UI work.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add pg-boss delayed reopen infrastructure for held-cancelled seats</name>
  <files>
    apps/api/src/modules/jobs/jobs.module.ts
    apps/api/src/modules/jobs/pgboss.provider.ts
    apps/api/src/modules/jobs/cancelled-seat-release.worker.ts
    apps/api/src/modules/jobs/cancelled-seat-release.worker.spec.ts
    apps/api/src/modules/jobs/refund-cancel-retry.worker.ts
    apps/api/src/modules/jobs/refund-cancel-retry.worker.spec.ts
  </files>
  <read_first>
    .planning/phases/24-traffic-booking-payment-core/24-RESEARCH.md
    apps/api/src/database/schema/seat-inventories.ts
    apps/api/src/database/schema/refunds.ts
  </read_first>
  <action>
    Add `pg-boss`-backed job infrastructure for D-17. Create `release-cancelled-seat` worker logic that waits a uniform random 60-600 seconds before reopening user-cancelled seats, using the `held_cancelled` schema state created earlier, and skip reopening when the scheduled reopen time lands at or after `showtimeAt - 5 minutes` by preserving the held state with an explicit imminent-showtime reason. Add `refund-cancel-retry.worker.ts` so transient Toss cancel failures can retry with durable backoff instead of disappearing after the original HTTP request ends. Do not let user cancellation restore seats immediately. Addresses review concerns: showtime-adjacent delayed reopen guard and durable cancel retry.
  </action>
  <acceptance_criteria>
    - `cancelled-seat-release.worker.ts` contains `release-cancelled-seat`.
    - `cancelled-seat-release.worker.ts` contains `showtimeAt` or `SHOWTIME_IMMINENT` guard logic.
    - `refund-cancel-retry.worker.ts` exists and is wired as a durable retry worker.
    - The worker or spec demonstrates random delay between 60 and 600 seconds plus the imminent-showtime skip path.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @grabit/api test -- src/modules/jobs/cancelled-seat-release.worker.spec.ts src/modules/jobs/refund-cancel-retry.worker.spec.ts && pnpm --filter @grabit/api typecheck</automated>
  </verify>
  <done>
    Delayed reopen and retryable refund reconciliation are infrastructure-backed and testable.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user cancel/refund request -> financial state | Irreversible refund state starts here. |
| background worker -> seat availability | Seat reopen occurs outside the original HTTP request here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-21 | T/R | `refund.service.ts` | mitigate | Use explicit refund states and idempotent orchestration so duplicate user/provider events cannot silently double-refund (Task 1). |
| T-24-22 | T/D | `cancelled-seat-release.worker.ts`, `refund-cancel-retry.worker.ts` | mitigate | Reopen seats only through delayed jobs acting on held-cancelled state, skip showtime-imminent reopen, and retry transient PG cancel failures through durable background work (Task 2). |
</threat_model>

<verification>
- `pnpm --filter @grabit/api test -- src/modules/refund/refund.service.spec.ts`
- `pnpm --filter @grabit/api test -- src/modules/jobs/cancelled-seat-release.worker.spec.ts src/modules/jobs/refund-cancel-retry.worker.spec.ts`
- `pnpm --filter @grabit/api typecheck`
</verification>

<success_criteria>
- Refund state is durable and queryable.
- Cancelled seats reopen only through the randomized delayed worker path, never into an imminent showtime window.
- Transient Toss cancel failures enter a durable retry/reconciliation path instead of disappearing.
</success_criteria>

<output>
After completion, create `.planning/phases/24-traffic-booking-payment-core/24-11-SUMMARY.md`
</output>
